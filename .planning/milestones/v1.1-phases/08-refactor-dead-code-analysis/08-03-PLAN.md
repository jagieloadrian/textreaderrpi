---
phase: 08-refactor-dead-code-analysis
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/main/kotlin/com/anjo/service/DisplaySelectionService.kt
  - src/test/kotlin/com/anjo/service/DisplaySelectionServiceTest.kt
  - src/test/kotlin/com/anjo/driver/DriverIntegrationTest.kt
autonomous: true
requirements: [REF-01, REF-04]
must_haves:
  truths:
    - "DisplaySelectionService has no logCurrentData(), pendingSwitches queue, getPendingSwitches(), or clearPendingSwitches()"
    - "selectDisplay() and selectDisplayAtStartup() retain their existing behavior without the removed calls"
    - "Tests no longer assert on pending switches and the full suite passes"
  artifacts:
    - path: "src/main/kotlin/com/anjo/service/DisplaySelectionService.kt"
      provides: "DisplaySelectionService without dead debug/queue code"
      contains: "class DisplaySelectionService"
    - path: "src/test/kotlin/com/anjo/service/DisplaySelectionServiceTest.kt"
      provides: "DisplaySelectionService tests without pending-switch assertions"
      contains: "class DisplaySelectionServiceTest"
  key_links:
    - from: "src/test/kotlin/com/anjo/service/DisplaySelectionServiceTest.kt"
      to: "src/main/kotlin/com/anjo/service/DisplaySelectionService.kt"
      via: "selectDisplay() return-value assertions (selectDisplay shouldBe true)"
      pattern: "selectDisplay\\("
---

<objective>
Remove dead code from `DisplaySelectionService`: the `logCurrentData()` Pi4J debug method and its two call sites, and the `pendingSwitches` queue together with `getPendingSwitches()`, `clearPendingSwitches()`, the `offer()` call, and the `ConcurrentLinkedQueue` import. Update the two test files that asserted on pending switches so the suite stays green.

Purpose: Remove a startup/driver-switch debug workaround and a switch-history queue that has no production consumer (REF-01 / D-08, D-09), keeping coverage intact (REF-04).
Output: Slimmed `DisplaySelectionService.kt`; updated `DisplaySelectionServiceTest.kt` and `DriverIntegrationTest.kt`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-refactor-dead-code-analysis/08-CONTEXT.md
@.planning/phases/08-refactor-dead-code-analysis/08-RESEARCH.md
@.planning/phases/08-refactor-dead-code-analysis/08-PATTERNS.md
@src/main/kotlin/com/anjo/service/DisplaySelectionService.kt
</context>

<artifacts_this_phase_produces>
Symbols created by THIS plan (exclude from drift verification):
- None created. This plan only deletes symbols: `DisplaySelectionService.logCurrentData()`, `pendingSwitches` field, `getPendingSwitches()`, `clearPendingSwitches()`, the `pendingSwitches.offer(...)` call, the `ConcurrentLinkedQueue` import, and the test `should empty queue after clearing pending switches` plus three pending-switch assertions across two test files.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Remove logCurrentData(), pendingSwitches, and accessors from DisplaySelectionService</name>
  <files>src/main/kotlin/com/anjo/service/DisplaySelectionService.kt</files>
  <read_first>
    - src/main/kotlin/com/anjo/service/DisplaySelectionService.kt (current file — `logCurrentData()` body, `pendingSwitches` field, `selectDisplay()`/`selectDisplayAtStartup()` call sites, accessors)
    - .planning/phases/08-refactor-dead-code-analysis/08-PATTERNS.md (DisplaySelectionService section — exact items to delete and the resulting selectDisplay() success branch)
    - .planning/phases/08-refactor-dead-code-analysis/08-RESEARCH.md (§Common Pitfalls §2 — delete only the single offer line and field; do not touch surrounding control flow)
  </read_first>
  <action>
    In `DisplaySelectionService.kt` delete: the import `java.util.concurrent.ConcurrentLinkedQueue`; the field `private val pendingSwitches = ConcurrentLinkedQueue<String>()`; the entire `private fun logCurrentData() { ... }` method (iterates `ctx.providers()/platforms()/properties()/registry()`); the `logCurrentData()` call inside `selectDisplayAtStartup()`; the `logCurrentData()` call inside `selectDisplay()`; and the single `pendingSwitches.offer(normalizedType)` line inside the success branch of `selectDisplay()`; the `getPendingSwitches(): List<String> = pendingSwitches.toList()` method and the `clearPendingSwitches() { pendingSwitches.clear() }` method. After removal, the `selectDisplay()` success branch is exactly: `currentDriver?.stop()`, `currentDriver = newDriver`, `currentType = normalizedType`, `log.info("Driver switched: $normalizedType")`, `true`. Do NOT alter `currentDriver()`, `createDriver()`, `driverCache`, `getCurrentDisplayType()`, `normalizeDisplayType()`, the `displayLock`, or the companion `defaultDriverFactory` — these remain. Per D-16 remove the now-unused `ConcurrentLinkedQueue` import; per the no-comments rule add no comments.
  </action>
  <verify>
    <automated>./gradlew compileKotlin</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "logCurrentData\|pendingSwitches\|getPendingSwitches\|clearPendingSwitches\|ConcurrentLinkedQueue" src/main/kotlin/com/anjo/service/DisplaySelectionService.kt` returns no matches
    - `DisplaySelectionService.kt` still contains `fun selectDisplay`, `fun currentDriver`, `fun getCurrentDisplayType`, and `private fun createDriver`
    - `selectDisplay()` success branch ends with `log.info("Driver switched: $normalizedType")` then `true`
    - `./gradlew compileKotlin` exits 0
  </acceptance_criteria>
  <done>DisplaySelectionService no longer references the debug method or switch queue, all remaining methods are intact, and production code compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Remove pending-switch assertions from DisplaySelectionServiceTest and DriverIntegrationTest</name>
  <files>src/test/kotlin/com/anjo/service/DisplaySelectionServiceTest.kt, src/test/kotlin/com/anjo/driver/DriverIntegrationTest.kt</files>
  <read_first>
    - src/test/kotlin/com/anjo/service/DisplaySelectionServiceTest.kt (current — five tests; three assert on pending switches, one is entirely about the queue)
    - src/test/kotlin/com/anjo/driver/DriverIntegrationTest.kt (current — line asserting `getPendingSwitches() shouldContainExactly listOf("LCD", "OLED")`)
    - .planning/phases/08-refactor-dead-code-analysis/08-PATTERNS.md (DisplaySelectionServiceTest / DriverIntegrationTest sections — exact assertions and the full test to delete)
  </read_first>
  <action>
    In `DisplaySelectionServiceTest.kt`: delete the assertion `service.getPendingSwitches().isEmpty().shouldBeTrue()` from `should load startup driver from display config`; delete the assertion `service.getPendingSwitches() shouldContainExactly listOf("LCD")` from `should stop old driver and update type when switching display`; delete the assertion `service.getPendingSwitches().isEmpty().shouldBeTrue()` from `should keep current driver unchanged on failed switch`; delete the entire `should empty queue after clearing pending switches` test. Keep the non-queue assertions in those three tests (e.g. `selectDisplay("lcd") shouldBe true`, current-type checks) intact so each test still verifies real behavior. In `DriverIntegrationTest.kt`: delete the line `selection.getPendingSwitches() shouldContainExactly listOf("LCD", "OLED")`, keeping the rest of that test's assertions. In both files remove the `import io.kotest.matchers.collections.shouldContainExactly` only if no other usage of it remains in that file (grep the file first). Per the no-comments rule add no comments.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.anjo.service.DisplaySelectionServiceTest" --tests "com.anjo.driver.DriverIntegrationTest"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "getPendingSwitches\|clearPendingSwitches" src/test` returns no matches
    - `grep -n "should empty queue after clearing pending switches" src/test/kotlin/com/anjo/service/DisplaySelectionServiceTest.kt` returns no match
    - `DisplaySelectionServiceTest.kt` still contains the three retained tests (startup driver, switching display, failed switch) with their non-queue assertions
    - `./gradlew test --tests "com.anjo.service.DisplaySelectionServiceTest" --tests "com.anjo.driver.DriverIntegrationTest"` exits 0
  </acceptance_criteria>
  <done>Both test files compile and pass with no references to the removed pending-switch API, retaining their real-behavior assertions.</done>
</task>

</tasks>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| Deleting the `offer()` line accidentally changes selectDisplay() control flow (Pitfall 2) | Task 1 deletes only the single `offer` line and field; acceptance criterion pins the exact success-branch ending; `compileKotlin` + test gate |
| Coverage degradation after removing tested accessors (REF-04) | `getPendingSwitches`/`clearPendingSwitches` are dead; remaining `selectDisplay`/`currentDriver` paths stay covered by retained assertions; final coverage gate in 08-05 enforces ≥70% |
| Accidental API breakage — a production caller of `getPendingSwitches()` exists | grep `src/main` confirms zero production callers (research VERIFIED); only test callers removed |
| Over-deletion strips a retained test's real assertions, weakening regression coverage | Task 2 explicitly keeps non-queue assertions; acceptance criterion verifies the three tests still exist |
</threat_model>

<verification>
- `./gradlew test --tests "com.anjo.service.DisplaySelectionServiceTest" --tests "com.anjo.driver.DriverIntegrationTest"` exits 0
- `grep -rn "getPendingSwitches\|clearPendingSwitches\|logCurrentData\|pendingSwitches" src/main src/test` returns nothing
</verification>

<success_criteria>
- DisplaySelectionService free of logCurrentData, pendingSwitches, and accessors (REF-01 / D-08, D-09)
- Both affected test files green with real-behavior assertions retained (REF-04)
</success_criteria>

<output>
Create `.planning/phases/08-refactor-dead-code-analysis/08-03-SUMMARY.md` when done
</output>
