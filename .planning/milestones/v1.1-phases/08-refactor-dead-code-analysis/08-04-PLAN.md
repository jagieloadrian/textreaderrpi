---
phase: 08-refactor-dead-code-analysis
plan: 04
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/main/kotlin/com/anjo/service/ScreenDriver.kt
  - src/main/kotlin/com/anjo/service/ScreenDriverService.kt
  - src/main/kotlin/com/anjo/utils/Font.kt
  - src/main/kotlin/com/anjo/driver/Max7219Matrix.kt
  - src/test/kotlin/com/anjo/service/ScreenDriverRecoveryTest.kt
  - src/test/kotlin/com/anjo/service/ScreenDriverResourceTest.kt
  - src/test/kotlin/com/anjo/utils/FontTest.kt
autonomous: true
requirements: [REF-01, REF-02, REF-04]
must_haves:
  truths:
    - "ScreenDriver.kt is renamed to ScreenDriverService.kt with the class name unchanged"
    - "readInput() is removed and all test call sites use displayImmediate() directly"
    - "Font exposes a safe getChar() that returns a blank 5-byte row for unmapped characters"
    - "Max7219Matrix renders unmapped characters without throwing (no force-unwrap)"
  artifacts:
    - path: "src/main/kotlin/com/anjo/service/ScreenDriverService.kt"
      provides: "Renamed service file without readInput()"
      contains: "class ScreenDriverService"
    - path: "src/main/kotlin/com/anjo/utils/Font.kt"
      provides: "Safe character lookup with bounds fallback"
      contains: "fun getChar"
    - path: "src/test/kotlin/com/anjo/utils/FontTest.kt"
      provides: "Coverage for Font.getChar mapped and unmapped paths"
      contains: "class FontTest"
  key_links:
    - from: "src/main/kotlin/com/anjo/driver/Max7219Matrix.kt"
      to: "src/main/kotlin/com/anjo/utils/Font.kt"
      via: "Font.getChar(c) replacing asciiFont[c] ?: asciiFont[' ']!!"
      pattern: "Font\\.getChar"
---

<objective>
Three light-cleanup changes that share no files with the other Wave 2 plans: (1) rename `ScreenDriver.kt` → `ScreenDriverService.kt` (file rename only; class is already `ScreenDriverService`) and delete the trivial `readInput()` wrapper, migrating its test call sites to `displayImmediate()`; (2) add a safe `Font.getChar()` bounds fallback and wire it into `Max7219Matrix` to remove the `!!` force-unwrap; (3) add `FontTest.kt` covering the mapped and unmapped lookup paths.

Purpose: Uniform `*Service` naming, remove a one-line indirection, and harden character rendering against unmapped input (REF-01 / D-10, D-14; REF-02 / D-15; REF-04).
Output: Renamed service file; hardened `Font.kt` + `Max7219Matrix.kt`; migrated two test files; new `FontTest.kt`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-refactor-dead-code-analysis/08-CONTEXT.md
@.planning/phases/08-refactor-dead-code-analysis/08-RESEARCH.md
@.planning/phases/08-refactor-dead-code-analysis/08-PATTERNS.md
@src/main/kotlin/com/anjo/utils/Font.kt
@src/main/kotlin/com/anjo/driver/Max7219Matrix.kt
</context>

<artifacts_this_phase_produces>
Symbols created by THIS plan (exclude from drift verification):
- New file path `src/main/kotlin/com/anjo/service/ScreenDriverService.kt` (renamed from `ScreenDriver.kt`)
- New function `Font.getChar(char: Char): ByteArray` in `src/main/kotlin/com/anjo/utils/Font.kt`
- New test file `src/test/kotlin/com/anjo/utils/FontTest.kt` with class `FontTest` and tests `should return mapped glyph for known character`, `should return blank ByteArray for unmapped character`
Deleted symbols: `ScreenDriverService.readInput(input: String)`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Rename ScreenDriver.kt to ScreenDriverService.kt and remove readInput()</name>
  <files>src/main/kotlin/com/anjo/service/ScreenDriver.kt, src/main/kotlin/com/anjo/service/ScreenDriverService.kt, src/test/kotlin/com/anjo/service/ScreenDriverRecoveryTest.kt, src/test/kotlin/com/anjo/service/ScreenDriverResourceTest.kt</files>
  <read_first>
    - src/main/kotlin/com/anjo/service/ScreenDriver.kt (current — class is already `ScreenDriverService`; `readInput()` wrapper to delete; `displayImmediate()` signature with `effect: Effect = Effect.SCROLL` default)
    - src/test/kotlin/com/anjo/service/ScreenDriverRecoveryTest.kt (current — `readInput(...)` call sites to migrate)
    - src/test/kotlin/com/anjo/service/ScreenDriverResourceTest.kt (current — `readInput(...)` call sites; metric key strings `textreaderrpi.screenDriver.readInput.*` must NOT change)
    - .planning/phases/08-refactor-dead-code-analysis/08-PATTERNS.md (ScreenDriver rename + readInput migration sections)
    - .planning/phases/08-refactor-dead-code-analysis/08-RESEARCH.md (§Common Pitfalls §4 — run `./gradlew clean test` after rename)
  </read_first>
  <action>
    Rename the file with `git mv src/main/kotlin/com/anjo/service/ScreenDriver.kt src/main/kotlin/com/anjo/service/ScreenDriverService.kt`. Do NOT change the class name (already `ScreenDriverService`), the `package com.anjo.service` declaration, or any import elsewhere — all callers already import `com.anjo.service.ScreenDriverService`. In the renamed file delete the method `suspend fun readInput(input: String) { displayImmediate(input) }`; leave `displayImmediate`, `displayScheduled`, `executeWithRecovery`, and all other members unchanged. In `ScreenDriverRecoveryTest.kt` and `ScreenDriverResourceTest.kt` replace every `readInput("...")` call with `displayImmediate("...")` (same argument; `displayImmediate` has `effect: Effect = Effect.SCROLL` default so no extra args). Do NOT modify the hard-coded metric key strings `"textreaderrpi.screenDriver.readInput.*"` in `ScreenDriverResourceTest.kt` — they live in `ScreenDriverMetrics` and are independent of the method name. After all edits run `./gradlew clean test` (full recompile clears the incremental cache per Pitfall 4). Per the no-comments rule add no comments.
  </action>
  <verify>
    <automated>./gradlew clean test --tests "com.anjo.service.ScreenDriverRecoveryTest" --tests "com.anjo.service.ScreenDriverResourceTest"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/main/kotlin/com/anjo/service/ScreenDriverService.kt` exists and `src/main/kotlin/com/anjo/service/ScreenDriver.kt` does not
    - `grep -rn "readInput" src/main src/test` returns no matches in production/test call code (metric key strings `textreaderrpi.screenDriver.readInput` are allowed to remain as string literals)
    - `ScreenDriverService.kt` contains `class ScreenDriverService` and `suspend fun displayImmediate`
    - `./gradlew clean test --tests "com.anjo.service.ScreenDriverRecoveryTest" --tests "com.anjo.service.ScreenDriverResourceTest"` exits 0
  </acceptance_criteria>
  <done>The service file is renamed, readInput() is gone, both test files call displayImmediate() directly, metric strings are untouched, and the targeted tests pass after a clean build.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Font.getChar() bounds fallback, wire it into Max7219Matrix, and add FontTest</name>
  <files>src/main/kotlin/com/anjo/utils/Font.kt, src/main/kotlin/com/anjo/driver/Max7219Matrix.kt, src/test/kotlin/com/anjo/utils/FontTest.kt</files>
  <read_first>
    - src/main/kotlin/com/anjo/utils/Font.kt (current — `object Font` with `asciiFont: Map<Char, ByteArray>`; all entries are 5-byte arrays)
    - src/main/kotlin/com/anjo/driver/Max7219Matrix.kt (line ~160: `val glyph = Font.asciiFont[c] ?: Font.asciiFont[' ']!!` — the force-unwrap to remove)
    - .planning/phases/08-refactor-dead-code-analysis/08-PATTERNS.md (Font.kt + FontTest.kt sections — getChar pattern, FontTest body, 5-byte width)
    - .planning/phases/08-refactor-dead-code-analysis/08-RESEARCH.md (§Common Pitfalls §3 — width must be 5 bytes to match existing glyphs)
  </read_first>
  <behavior>
    - `Font.getChar('A')` returns `byteArrayOf(126,17,17,17,126)` (the mapped glyph)
    - `Font.getChar('中')` returns `ByteArray(5) { 0 }` (unmapped → blank 5-byte row)
    - `Font.getChar(' ')` returns the space glyph `byteArrayOf(0,0,0,0,0)` (mapped, equals 5-byte zeros)
    - Max7219Matrix renders a string containing an unmapped character without throwing
  </behavior>
  <action>
    In `Font.kt` add, before the closing `}` of the `object Font`, the function `fun getChar(char: Char): ByteArray = asciiFont[char] ?: ByteArray(5) { 0 }`. Use width 5 (matching every existing glyph entry, e.g. `'A' to byteArrayOf(126,17,17,17,126)`) — NOT 8; the "8 rows" in CONTEXT.md refers to display height, not glyph column width (per Pitfall 3 / A2). In `Max7219Matrix.kt` replace the expression `Font.asciiFont[c] ?: Font.asciiFont[' ']!!` (around line 160) with `Font.getChar(c)`, removing the `!!` force-unwrap; do not change any other rendering logic. Create `src/test/kotlin/com/anjo/utils/FontTest.kt` as a Kotest `FunSpec` (package `com.anjo.utils`, imports `io.kotest.core.spec.style.FunSpec` and `io.kotest.matchers.shouldBe`) with three tests: a mapped known character returns its exact glyph (`Font.getChar('A') shouldBe byteArrayOf(126,17,17,17,126)`), an unmapped high-unicode character returns `ByteArray(5) { 0 }`, and the space character returns `byteArrayOf(0,0,0,0,0)`. Note Kotest `shouldBe` on `ByteArray` compares by content. Per the no-comments rule add no comments.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.anjo.utils.FontTest" --tests "com.anjo.driver.Max7219MatrixTest"</automated>
  </verify>
  <acceptance_criteria>
    - `Font.kt` contains `fun getChar(char: Char): ByteArray = asciiFont[char] ?: ByteArray(5) { 0 }`
    - `grep -n "asciiFont\[' '\]!!" src/main/kotlin/com/anjo/driver/Max7219Matrix.kt` returns no match
    - `Max7219Matrix.kt` contains `Font.getChar(`
    - `FontTest.kt` exists with class `FontTest` and tests for mapped, unmapped, and space characters
    - `./gradlew test --tests "com.anjo.utils.FontTest" --tests "com.anjo.driver.Max7219MatrixTest"` exits 0
  </acceptance_criteria>
  <done>Font.getChar() returns a 5-byte glyph or blank fallback, Max7219Matrix no longer force-unwraps, FontTest covers both paths, and the matrix tests still pass.</done>
</task>

</tasks>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| File rename breaks Kotlin incremental compilation cache → "unresolved reference" (Pitfall 4) | Task 1 verify uses `./gradlew clean test` to force full recompile |
| readInput() migration changes metric key strings → ScreenDriverResourceTest metric assertions fail | Action explicitly forbids changing `textreaderrpi.screenDriver.readInput.*` literals; they are independent of the method name |
| Font fallback width mismatch (8 vs 5) corrupts rendering (Pitfall 3 / A2) | Width pinned to 5 to match existing glyphs; Max7219MatrixTest gates the wiring change |
| Removing `!!` is a no-op if `asciiFont[' ']` always exists, leaving the unmapped path untested (coverage degradation, REF-04) | FontTest directly exercises the unmapped path; Max7219Matrix now routes through `getChar`, eliminating the unsafe `!!` entirely |
| Accidental API breakage — a non-test caller of readInput() exists | grep `src/main` confirms zero production callers before deletion (research VERIFIED) |
</threat_model>

<verification>
- `./gradlew clean test` exits 0 (full suite after rename)
- `grep -rn "asciiFont\[' '\]!!\|readInput(" src/main` returns nothing (string literals in tests permitted)
</verification>

<success_criteria>
- ScreenDriver.kt renamed to ScreenDriverService.kt; readInput() removed; call sites migrated (REF-01, REF-02 / D-10, D-14)
- Font.getChar() bounds fallback added and wired into Max7219Matrix, removing the force-unwrap (REF-02 / D-15)
- FontTest covers mapped and unmapped paths (REF-04)
</success_criteria>

<output>
Create `.planning/phases/08-refactor-dead-code-analysis/08-04-SUMMARY.md` when done
</output>
