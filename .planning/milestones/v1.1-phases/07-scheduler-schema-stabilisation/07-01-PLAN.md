---
phase: 07-scheduler-schema-stabilisation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gradle/ktor-libs.versions.toml
  - build.gradle.kts
  - src/main/kotlin/com/anjo/model/ConflictPolicy.kt
  - src/main/kotlin/com/anjo/model/Schedule.kt
  - src/main/kotlin/com/anjo/model/TextRequest.kt
  - src/main/kotlin/com/anjo/db/SchedulesTable.kt
  - src/main/kotlin/com/anjo/db/ScheduleRepository.kt
  - src/main/kotlin/com/anjo/db/DatabaseFactory.kt
  - src/main/resources/db/migration/V1__initial_schema.sql
  - src/main/resources/db/migration/V2__add_scheduler_columns.sql
autonomous: true
requirements: [SCHED-01, SCHED-02, SCHED-03, SCHED-04]
must_haves:
  truths:
    - "Application starts and Flyway applies V2 migration adding 4 columns to the schedules table"
    - "A schedule row can persist and read back conflictPolicy, firedAt, webhookUrl, and zoneId values"
    - "ScheduleStatus enum has an ERROR value usable on Schedule rows"
    - "findAllActive() excludes ONESHOT rows whose firedAt is non-null"
    - "updateFiredAtAndDone() sets firedAt and status=DONE in a single transaction"
  artifacts:
    - path: "src/main/kotlin/com/anjo/model/ConflictPolicy.kt"
      provides: "ConflictPolicy enum (INTERRUPT, SKIP_NEW), @Serializable"
      contains: "enum class ConflictPolicy"
    - path: "src/main/resources/db/migration/V2__add_scheduler_columns.sql"
      provides: "ALTER TABLE adding conflict_policy, fired_at, webhook_url, zone_id"
      contains: "ALTER TABLE schedules ADD COLUMN"
    - path: "src/main/kotlin/com/anjo/db/SchedulesTable.kt"
      provides: "4 new Exposed column definitions"
      contains: "conflict_policy"
    - path: "src/main/kotlin/com/anjo/db/ScheduleRepository.kt"
      provides: "updateFiredAtAndDone() + findAllActive() ONESHOT firedAt filter + new column read/write"
      contains: "updateFiredAtAndDone"
    - path: "src/main/kotlin/com/anjo/db/DatabaseFactory.kt"
      provides: "Flyway.migrate() call before SchemaUtils.create()"
      contains: "Flyway.configure()"
  key_links:
    - from: "src/main/kotlin/com/anjo/db/DatabaseFactory.kt"
      to: "Flyway"
      via: "Flyway.configure().dataSource(dataSource).baselineOnMigrate(true).baselineVersion(\"1\").load().migrate()"
      pattern: "Flyway\\.configure\\(\\)"
    - from: "src/main/kotlin/com/anjo/db/ScheduleRepository.kt"
      to: "SchedulesTable.firedAt"
      via: "findAllActive() not(ONESHOT and firedAt.isNotNull())"
      pattern: "firedAt"
---

<objective>
Lay the schema and data-layer foundation for Phase 7: add the `ConflictPolicy` enum, extend the `Schedule` model and `SchedulesTable` with four new columns (`conflictPolicy`, `firedAt`, `webhookUrl`, `zoneId`), add `ScheduleStatus.ERROR`, add the `conflictPolicy` field to `TextRequest`, introduce Flyway as the migration layer, and update `ScheduleRepository` with the new column read/write, the atomic `updateFiredAtAndDone()` method, and the ONESHOT `firedAt` filter in `findAllActive()`.

Purpose: Every behavioral fix in plan 02 (SKIP_NEW, ONESHOT restart guard, CRON ERROR persist) and every test in plan 03 consumes these model fields, enum values, repository methods, and columns. They are the contracts the rest of the phase builds against.
Output: Compilable Kotlin with the new model + DB layer, two SQL migration files, and Flyway wired into startup.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-scheduler-schema-stabilisation/07-CONTEXT.md
@.planning/phases/07-scheduler-schema-stabilisation/07-RESEARCH.md
@.planning/phases/07-scheduler-schema-stabilisation/07-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols created by Phase 7 (exclude from drift verification — these are new, not pre-existing):

**This plan (07-01) creates:**
- `com.anjo.model.ConflictPolicy` — new `@Serializable` enum with values `INTERRUPT`, `SKIP_NEW`
- `ScheduleStatus.ERROR` — new enum value added to existing `ScheduleStatus`
- `Schedule.conflictPolicy: ConflictPolicy? = null` — new field
- `Schedule.firedAt: String? = null` — new field
- `Schedule.webhookUrl: String? = null` — new field
- `Schedule.zoneId: String? = null` — new field
- `TextRequest.conflictPolicy: ConflictPolicy = ConflictPolicy.INTERRUPT` — new field
- `SchedulesTable.conflictPolicy` — `varchar("conflict_policy", 16).nullable().default("INTERRUPT")`
- `SchedulesTable.firedAt` — `varchar("fired_at", 32).nullable()`
- `SchedulesTable.webhookUrl` — `varchar("webhook_url", 512).nullable()`
- `SchedulesTable.zoneId` — `varchar("zone_id", 64).nullable()`
- `ScheduleRepository.updateFiredAtAndDone(id: String, firedAt: String)` — new suspend method
- `src/main/resources/db/migration/V1__initial_schema.sql` — new file (baseline doc)
- `src/main/resources/db/migration/V2__add_scheduler_columns.sql` — new file (ALTER)
- `flyway` version + `flyway-core` library entries in `ktor-libs.versions.toml`
- `org.flywaydb:flyway-core` dependency in `build.gradle.kts`

**Created by later plans in this phase (do not flag as drift):**
- `ScreenDriverService.displayImmediate(...)` / `displayScheduled(...)` `Boolean` return + `conflictPolicy` param (07-02)
- `SchedulerService.fire(...)` `Boolean` return (07-02)
- `cronParser` val in `ScheduleRoutes.kt` (07-02)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add ConflictPolicy enum, model fields, and ScheduleStatus.ERROR</name>
  <files>
    src/main/kotlin/com/anjo/model/ConflictPolicy.kt,
    src/main/kotlin/com/anjo/model/Schedule.kt,
    src/main/kotlin/com/anjo/model/TextRequest.kt
  </files>
  <read_first>
    - src/main/kotlin/com/anjo/model/Schedule.kt (current enums + Schedule data class)
    - src/main/kotlin/com/anjo/model/TextRequest.kt (current fields + @Serializable pattern)
    - src/main/kotlin/com/anjo/model/TextResponse.kt (referenced as enum @Serializable analog in PATTERNS.md)
    - .planning/phases/07-scheduler-schema-stabilisation/07-PATTERNS.md (ConflictPolicy.kt, Schedule.kt, TextRequest.kt sections — exact field text)
  </read_first>
  <action>
    Create new file `src/main/kotlin/com/anjo/model/ConflictPolicy.kt` in package `com.anjo.model` containing a `@Serializable enum class ConflictPolicy { INTERRUPT, SKIP_NEW }` (per D-01). The `@Serializable` annotation (import `kotlinx.serialization.Serializable`) is mandatory because the enum is embedded in `TextRequest` and `Schedule`, both of which are deserialized via `call.receive<>()` — omitting it causes `SerializationException` (RESEARCH.md Pitfall 4).

    In `Schedule.kt`: add `ERROR` as the last value of the existing `ScheduleStatus` enum so it reads `enum class ScheduleStatus { ACTIVE, PAUSED, EXPIRED, DONE, ERROR }` (per D-17). Append four nullable fields to the `Schedule` data class after `status`, each following the existing `val x: Type? = null` style: `conflictPolicy: ConflictPolicy? = null` (D-02), `firedAt: String? = null` (D-13), `webhookUrl: String? = null` (D-10), `zoneId: String? = null` (D-11). `ConflictPolicy` is in the same package `com.anjo.model`, so no import is needed.

    In `TextRequest.kt`: add `val conflictPolicy: ConflictPolicy = ConflictPolicy.INTERRUPT` after `effect` (per D-01). The default is mandatory so existing callers that omit the field still deserialize.
  </action>
  <verify>
    <automated>./gradlew compileKotlin --no-daemon</automated>
  </verify>
  <acceptance_criteria>
    - File `src/main/kotlin/com/anjo/model/ConflictPolicy.kt` exists and contains `enum class ConflictPolicy`
    - `ConflictPolicy.kt` contains both `INTERRUPT` and `SKIP_NEW`
    - `ConflictPolicy.kt` contains `@Serializable` and imports `kotlinx.serialization.Serializable`
    - `Schedule.kt` `ScheduleStatus` enum contains `ERROR`
    - `Schedule.kt` `Schedule` data class contains `conflictPolicy: ConflictPolicy? = null`, `firedAt: String? = null`, `webhookUrl: String? = null`, `zoneId: String? = null`
    - `TextRequest.kt` contains `conflictPolicy: ConflictPolicy = ConflictPolicy.INTERRUPT`
    - `./gradlew compileKotlin --no-daemon` exits 0
  </acceptance_criteria>
  <done>ConflictPolicy enum exists with both values and @Serializable; Schedule has ERROR status and 4 new nullable fields; TextRequest has conflictPolicy with INTERRUPT default; main source compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Add Flyway dependency, migration SQL files, and wire Flyway into DatabaseFactory</name>
  <files>
    gradle/ktor-libs.versions.toml,
    build.gradle.kts,
    src/main/resources/db/migration/V1__initial_schema.sql,
    src/main/resources/db/migration/V2__add_scheduler_columns.sql,
    src/main/kotlin/com/anjo/db/DatabaseFactory.kt
  </files>
  <read_first>
    - gradle/ktor-libs.versions.toml ([versions] + [libraries] structure)
    - build.gradle.kts (dependencies block — Database group near exposed/hikaricp)
    - src/main/kotlin/com/anjo/db/DatabaseFactory.kt (current init body, import block)
    - src/main/kotlin/com/anjo/db/SchedulesTable.kt (source of truth for V1 column list)
    - .planning/phases/07-scheduler-schema-stabilisation/07-RESEARCH.md (Gradle Dependency Entry, V1/V2 SQL code examples, Pattern 1, Pitfall 1, Pitfall 2)
    - .planning/phases/07-scheduler-schema-stabilisation/07-PATTERNS.md (DatabaseFactory.kt, V1__initial_schema.sql, V2__add_scheduler_columns.sql sections)
  </read_first>
  <action>
    Add Flyway to the version catalog (per D-06). In `ktor-libs.versions.toml` `[versions]` add `flyway = "9.22.3"`; in `[libraries]` add `flyway-core = { module = "org.flywaydb:flyway-core", version.ref = "flyway" }`. In `build.gradle.kts` dependencies block, add `implementation(ktorLibs.flyway.core)` in the Database group (near `exposed`/`hikaricp`). H2 support is bundled in `flyway-core` — no separate module.

    Create `src/main/resources/db/migration/V1__initial_schema.sql` documenting the CURRENT 10-column `schedules` table exactly as `SchedulesTable.kt` defines it (id varchar(36) PK, text TEXT, trigger_type varchar(16), trigger_value varchar(256), effect varchar(16) default 'SCROLL', priority INT default 0, max_runs INT null, expires_at varchar(32) null, created_at varchar(32), status varchar(16) default 'ACTIVE'). Use `CREATE TABLE IF NOT EXISTS schedules (...)`. Do NOT add the new columns to V1 (RESEARCH.md Pitfall 2). Include a comment noting V1 is skipped on existing installs (baselineOnMigrate) and runs only on fresh installs.

    Create `src/main/resources/db/migration/V2__add_scheduler_columns.sql` with four `ALTER TABLE schedules ADD COLUMN` statements (per D-09 and CONTEXT.md §Specifics): `conflict_policy VARCHAR(16) DEFAULT 'INTERRUPT'`, `fired_at VARCHAR(32)`, `webhook_url VARCHAR(512)`, `zone_id VARCHAR(64)`. No explicit NULL keyword (H2/PostgreSQL default added columns to nullable).

    In `DatabaseFactory.kt`: add import `org.flywaydb.core.Flyway`. Insert the Flyway call AFTER `Database.connect(dataSource)` and BEFORE the `transaction { SchemaUtils.create(SchedulesTable) }` block (order is critical per RESEARCH.md Pitfall 1). Use exactly the chain from D-07 and D-08: `Flyway.configure().dataSource(dataSource).baselineOnMigrate(true).baselineVersion("1").load().migrate()`. The `baselineOnMigrate(true)` with `baselineVersion("1")` (D-08) ensures existing Pi installs that already have the `schedules` table but no Flyway history are baselined at V1 on first run, then V2 is applied — never re-running V1 on existing data. Keep `SchemaUtils.create(SchedulesTable)` as the safety net after Flyway.
  </action>
  <verify>
    <automated>./gradlew compileKotlin --no-daemon</automated>
  </verify>
  <acceptance_criteria>
    - `gradle/ktor-libs.versions.toml` contains `flyway = "9.22.3"` and `flyway-core = { module = "org.flywaydb:flyway-core"`
    - `build.gradle.kts` contains `implementation(ktorLibs.flyway.core)`
    - `src/main/resources/db/migration/V1__initial_schema.sql` exists and contains `CREATE TABLE IF NOT EXISTS schedules` with all 10 existing columns and NOT `conflict_policy`
    - `src/main/resources/db/migration/V2__add_scheduler_columns.sql` exists and contains 4 lines matching `ALTER TABLE schedules ADD COLUMN`, covering `conflict_policy`, `fired_at`, `webhook_url`, `zone_id`
    - `DatabaseFactory.kt` contains `Flyway.configure()` and `baselineOnMigrate(true)` and `baselineVersion("1")` and the call appears before `SchemaUtils.create`
    - `DatabaseFactory.kt` imports `org.flywaydb.core.Flyway`
    - `./gradlew compileKotlin --no-daemon` exits 0
  </acceptance_criteria>
  <done>Flyway 9.22.3 is on the classpath; V1 documents the existing schema; V2 adds the 4 columns; DatabaseFactory runs Flyway.migrate() before SchemaUtils.create(); main source compiles.</done>
</task>

<task type="auto">
  <name>Task 3: Extend SchedulesTable columns and ScheduleRepository (new columns, atomic firedAt update, ONESHOT filter)</name>
  <files>
    src/main/kotlin/com/anjo/db/SchedulesTable.kt,
    src/main/kotlin/com/anjo/db/ScheduleRepository.kt
  </files>
  <read_first>
    - src/main/kotlin/com/anjo/db/SchedulesTable.kt (current column definitions + primaryKey position)
    - src/main/kotlin/com/anjo/db/ScheduleRepository.kt (findAllActive, insert, updateStatus, toSchedule)
    - src/main/kotlin/com/anjo/model/Schedule.kt (updated model fields from Task 1)
    - .planning/phases/07-scheduler-schema-stabilisation/07-PATTERNS.md (SchedulesTable.kt, ScheduleRepository.kt sections — exact column + method text)
    - .planning/phases/07-scheduler-schema-stabilisation/07-RESEARCH.md (Pattern 2, Pitfall 5 firedAt filter scope, Common Pitfalls Pitfall 5 Exposed code)
  </read_first>
  <action>
    In `SchedulesTable.kt`: append four columns before `override val primaryKey`, matching the existing column style: `val conflictPolicy = varchar("conflict_policy", 16).nullable().default("INTERRUPT")` (D-12), `val firedAt = varchar("fired_at", 32).nullable()` (D-13), `val webhookUrl = varchar("webhook_url", 512).nullable()` (D-10), `val zoneId = varchar("zone_id", 64).nullable()` (D-11).

    In `ScheduleRepository.kt`:
    - Add import `com.anjo.model.ConflictPolicy`.
    - Modify `findAllActive()` to scope the firedAt exclusion to ONESHOT only (safer than bare `AND firedAt IS NULL` per RESEARCH.md Pitfall 5 / Assumption A3, satisfies D-14 for the ONESHOT crash case): WHERE `(status eq "ACTIVE") and not((triggerType eq "ONESHOT") and (firedAt.isNotNull()))`. This excludes a ONESHOT that fired (set firedAt) but crashed before writing status, without affecting RECURRING/CRON rows.
    - Extend `insert {}` to write the four new columns: `it[conflictPolicy] = schedule.conflictPolicy?.name`, `it[firedAt] = schedule.firedAt`, `it[webhookUrl] = schedule.webhookUrl`, `it[zoneId] = schedule.zoneId`.
    - Extend `update {}` similarly to write `conflictPolicy`, `webhookUrl`, `zoneId` (firedAt is set only by the atomic method, not by PATCH).
    - Add a new suspend method `updateFiredAtAndDone(id: String, firedAt: String)` that sets `firedAt` and `status="DONE"` inside a SINGLE `suspendTransaction {}` block (D-15, atomic — never two transactions per RESEARCH.md anti-pattern).
    - Extend `toSchedule()` to read the four new columns: `conflictPolicy = this[SchedulesTable.conflictPolicy]?.let { ConflictPolicy.valueOf(it) }`, `firedAt = this[SchedulesTable.firedAt]`, `webhookUrl = this[SchedulesTable.webhookUrl]`, `zoneId = this[SchedulesTable.zoneId]`.
  </action>
  <verify>
    <automated>./gradlew compileKotlin --no-daemon</automated>
  </verify>
  <acceptance_criteria>
    - `SchedulesTable.kt` contains `varchar("conflict_policy", 16).nullable().default("INTERRUPT")`, `varchar("fired_at", 32).nullable()`, `varchar("webhook_url", 512).nullable()`, `varchar("zone_id", 64).nullable()`
    - `ScheduleRepository.kt` `findAllActive()` contains `not(` and references `triggerType eq "ONESHOT"` and `firedAt.isNotNull()`
    - `ScheduleRepository.kt` contains a suspend `fun updateFiredAtAndDone(id: String, firedAt: String)` with exactly one `suspendTransaction {` that sets both `firedAt` and `status` to `"DONE"`
    - `ScheduleRepository.kt` `insert` writes `conflictPolicy`, `firedAt`, `webhookUrl`, `zoneId`
    - `ScheduleRepository.kt` `toSchedule()` reads all four new columns
    - `./gradlew compileKotlin --no-daemon` exits 0
  </acceptance_criteria>
  <done>SchedulesTable has the 4 new columns; ScheduleRepository writes/reads them, exposes atomic updateFiredAtAndDone(), and findAllActive() excludes fired ONESHOT rows without touching RECURRING/CRON; main source compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /api/v1/schedule | Untrusted JSON (incl. new webhookUrl, zoneId, conflictPolicy) crosses into persistence layer |
| application → H2/PostgreSQL | DDL (Flyway migration) and parameterized DML cross into the database |
| classpath → Flyway scanner | Migration SQL resources loaded from the compiled jar |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | webhookUrl / zoneId persisted via ScheduleRepository.insert | mitigate | Exposed parameterizes all column writes (`it[col] = value`) — no string-concatenated SQL; values bound as parameters. Format validation lands in 07-02. |
| T-07-02 | Tampering | Flyway migration SQL files | accept | Migration files are classpath resources compiled into the jar; not writable at runtime (RESEARCH.md Security Domain). |
| T-07-03 | Denial of Service | conflictPolicy column default + nullable | accept | New columns nullable with defaults; existing rows unaffected; no unbounded growth introduced this plan. |
| T-07-SC | Tampering | org.flywaydb:flyway-core install | mitigate | Package legitimacy: flyway-core VERIFIED on Maven Central (15+ yrs, 259 versions, github.com/flyway/flyway) per RESEARCH.md Package Legitimacy Audit — verdict OK/Approved. No [ASSUMED]/[SUS]/[SLOP] package → no blocking checkpoint required. |
</threat_model>

<verification>
- `./gradlew compileKotlin --no-daemon` exits 0 after each task
- `./gradlew compileTestKotlin --no-daemon` exits 0 (existing tests still compile against new fields with defaults)
- `./gradlew test --no-daemon` passes (no behavioral regression; existing tests do not exercise new columns yet)
- Grep confirms: `grep -rl "conflict_policy" src/main/resources/db/migration/V2__add_scheduler_columns.sql` non-empty
</verification>

<success_criteria>
- ConflictPolicy enum, ScheduleStatus.ERROR, 4 Schedule fields, TextRequest.conflictPolicy all present and compiling
- Flyway 9.22.3 on classpath; V1 + V2 migration files present; DatabaseFactory runs Flyway before SchemaUtils
- SchedulesTable has 4 new columns; ScheduleRepository reads/writes them, exposes updateFiredAtAndDone(), filters fired ONESHOT rows
- Full existing test suite green (`./gradlew test --no-daemon`)
</success_criteria>

<output>
Create `.planning/phases/07-scheduler-schema-stabilisation/07-01-SUMMARY.md` when done
</output>
