---
phase: 07-scheduler-schema-stabilisation
plan: 03
type: tdd
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - src/test/kotlin/com/anjo/db/ScheduleRepositoryTest.kt
  - src/test/kotlin/com/anjo/service/SchedulerServiceTest.kt
  - src/test/kotlin/com/anjo/service/ConflictPolicyTest.kt
  - src/test/kotlin/com/anjo/routing/ScheduleRoutesTest.kt
  - src/test/kotlin/com/anjo/routing/TextApiRouteTest.kt
autonomous: true
requirements: [SCHED-01, SCHED-02, SCHED-03, SCHED-04]
must_haves:
  truths:
    - "A test proves webhookUrl + zoneId + conflictPolicy persist and read back through ScheduleRepository"
    - "A test proves findAllActive() excludes a ONESHOT row whose firedAt is set"
    - "A test proves updateFiredAtAndDone sets both firedAt and status=DONE"
    - "A test proves SKIP_NEW drops the request when the display mutex is locked"
    - "A test proves a SKIP_NEW skip does not decrement maxRuns"
    - "A test proves an invalid CRON POST persists a row with status=ERROR and returns 422"
    - "A test proves an invalid webhookUrl returns 422"
    - "Full suite green and JaCoCo LINE coverage >= 70%"
  artifacts:
    - path: "src/test/kotlin/com/anjo/db/ScheduleRepositoryTest.kt"
      provides: "new column round-trip + ONESHOT firedAt filter + updateFiredAtAndDone tests"
      contains: "webhookUrl"
    - path: "src/test/kotlin/com/anjo/service/SchedulerServiceTest.kt"
      provides: "ONESHOT firedAt atomic + ERROR-not-loaded + SKIP_NEW maxRuns tests"
      contains: "updateFiredAtAndDone"
    - path: "src/test/kotlin/com/anjo/service/ConflictPolicyTest.kt"
      provides: "SKIP_NEW drops when busy test"
      contains: "SKIP_NEW"
    - path: "src/test/kotlin/com/anjo/routing/ScheduleRoutesTest.kt"
      provides: "invalid CRON persists ERROR + invalid webhookUrl 422"
      contains: "ERROR"
    - path: "src/test/kotlin/com/anjo/routing/TextApiRouteTest.kt"
      provides: "SKIP_NEW response shape test"
      contains: "SKIP_NEW"
  key_links:
    - from: "src/test/kotlin/com/anjo/db/ScheduleRepositoryTest.kt"
      to: "ScheduleRepository.updateFiredAtAndDone / findAllActive"
      via: "insert ONESHOT with firedAt -> findAllActive excludes it"
      pattern: "findAllActive"
    - from: "src/test/kotlin/com/anjo/routing/ScheduleRoutesTest.kt"
      to: "POST /api/v1/schedule CRON path"
      via: "invalid cron -> GET schedule shows status ERROR"
      pattern: "ERROR"
---

<objective>
Add automated tests for every behavior introduced in plans 01 and 02, extending the five existing test files (no new files needed — all infrastructure exists per RESEARCH.md Wave 0 Gaps: none). Cover the data-layer round-trip and filters (SCHED-04, SCHED-02), the SKIP_NEW policy and maxRuns rule (SCHED-01), the ONESHOT atomic update (SCHED-02), and the CRON-ERROR persistence + webhookUrl rejection (SCHED-03).

Purpose: Plans 01/02 changed model, DB, service, validation, and route layers. These tests are the regression guard and the proof that all four requirements are satisfied. JaCoCo LINE >= 70% must hold (new code in db/service/routing counts toward coverage; model/db excluded per build.gradle.kts).
Output: Five extended test files; `./gradlew test --no-daemon` green; coverage gate passing.

TDD note: write each new test to assert the target behavior (it should already pass against plans 01/02), then run. If a test fails, it has surfaced a gap in 01/02 — fix the source per the cited decision rather than weakening the test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-scheduler-schema-stabilisation/07-RESEARCH.md
@.planning/phases/07-scheduler-schema-stabilisation/07-PATTERNS.md
@.planning/phases/07-scheduler-schema-stabilisation/07-VALIDATION.md
@.planning/phases/07-scheduler-schema-stabilisation/07-01-SUMMARY.md
@.planning/phases/07-scheduler-schema-stabilisation/07-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan (07-03) creates only test cases (new `test("...")` blocks) inside existing FunSpec test classes. No new production symbols. Consumed (created in 07-01/07-02, not drift): `ConflictPolicy`, `ScheduleStatus.ERROR`, `Schedule.{conflictPolicy,firedAt,webhookUrl,zoneId}`, `ScheduleRepository.updateFiredAtAndDone`, `displayImmediate/displayScheduled` Boolean+conflictPolicy, `fire(): Boolean`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repository + scheduler unit tests (SCHED-02, SCHED-04, SKIP_NEW maxRuns)</name>
  <files>
    src/test/kotlin/com/anjo/db/ScheduleRepositoryTest.kt,
    src/test/kotlin/com/anjo/service/SchedulerServiceTest.kt
  </files>
  <behavior>
    - ScheduleRepositoryTest: inserting a Schedule with conflictPolicy=SKIP_NEW, webhookUrl="https://example.com/hook", zoneId="zone-a" then findById round-trips all three values (SCHED-04, SCHED-01 column).
    - ScheduleRepositoryTest: a ONESHOT row with firedAt set (via updateFiredAtAndDone) is NOT returned by findAllActive(); a ONESHOT row with firedAt null and status ACTIVE IS returned (SCHED-02).
    - ScheduleRepositoryTest: updateFiredAtAndDone(id, ts) makes findById(id) show firedAt == ts and status == DONE (SCHED-02 atomic).
    - ScheduleRepositoryTest: a RECURRING row with status ACTIVE is still returned by findAllActive() even if firedAt were set (filter scoped to ONESHOT — Pitfall 5).
    - SchedulerServiceTest: an ERROR-status schedule is excluded from findAllActive() so the scheduler never loads it (SCHED-03 — verify via mock returning only ACTIVE rows, or assert ERROR row absent from the loaded set).
    - SchedulerServiceTest: a recurring SKIP_NEW schedule whose displayScheduled returns false does not advance runs toward maxRuns (SCHED-01 / D-05) — mock screenService.displayScheduled returns false and assert fire is retried up to maxRuns successful displays.
  </behavior>
  <read_first>
    - src/test/kotlin/com/anjo/db/ScheduleRepositoryTest.kt (beforeSpec/beforeEach H2 setup, testSchedule factory, assertion style)
    - src/test/kotlin/com/anjo/service/SchedulerServiceTest.kt (TestScope/StandardTestDispatcher pattern, mockk usage)
    - src/test/kotlin/com/anjo/service/ConflictPolicyTest.kt (priority/createdAt ordering tests as mock analog)
    - src/main/kotlin/com/anjo/db/ScheduleRepository.kt (updateFiredAtAndDone, findAllActive — final from 07-01)
    - src/main/kotlin/com/anjo/service/SchedulerService.kt (fire Boolean, launchRecurring conditional runs — final from 07-02)
    - .planning/phases/07-scheduler-schema-stabilisation/07-PATTERNS.md (Kotest FunSpec, H2 in-memory test DB setup shared patterns)
  </read_first>
  <action>
    Extend `ScheduleRepositoryTest.kt` with the four behaviors above. Reuse the existing `beforeSpec` H2 connect + `SchemaUtils.create(SchedulesTable)` and the `beforeEach` truncate; construct `Schedule(...)` with the new fields directly. For the ONESHOT firedAt filter test, insert a ONESHOT (`triggerType=TriggerType.ONESHOT`, an ISO `triggerValue`), call `repository.updateFiredAtAndDone(id, Instant.now().toString())`, then assert `findAllActive()` does not contain that id. Use the existing `shouldBe`/`shouldNotBeNull`/collection matchers.

    Extend `SchedulerServiceTest.kt` with the ERROR-not-loaded and SKIP_NEW-maxRuns behaviors using the existing `TestScope(StandardTestDispatcher(testScheduler) + Job())` + `mockk(relaxed=true)` pattern. For the maxRuns test, set `coEvery { mockScreen.displayScheduled(any(), any(), any(), any()) } returns false` (note the new 4th `conflictPolicy` param) for the SKIP path and assert the recurring loop does not terminate early by exhausting maxRuns on skips. Always call `service.stop()` and cancel the test scope job in each test (existing teardown convention).
  </action>
  <verify>
    <automated>./gradlew test --tests "com.anjo.db.ScheduleRepositoryTest" --tests "com.anjo.service.SchedulerServiceTest" --no-daemon</automated>
  </verify>
  <acceptance_criteria>
    - `ScheduleRepositoryTest.kt` contains `webhookUrl` and `zoneId` and `updateFiredAtAndDone` in test bodies
    - `ScheduleRepositoryTest.kt` contains a test asserting a fired ONESHOT is absent from `findAllActive()`
    - `SchedulerServiceTest.kt` contains a test referencing `ScheduleStatus.ERROR` (or status="ERROR") exclusion and a SKIP_NEW/maxRuns test
    - `./gradlew test --tests "com.anjo.db.ScheduleRepositoryTest" --tests "com.anjo.service.SchedulerServiceTest" --no-daemon` exits 0
  </acceptance_criteria>
  <done>Repository round-trip, ONESHOT firedAt filter, atomic update, RECURRING-not-excluded, ERROR-not-loaded, and SKIP_NEW-maxRuns behaviors are all proven by passing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SKIP_NEW conflict + CRON ERROR + webhookUrl route/integration tests</name>
  <files>
    src/test/kotlin/com/anjo/service/ConflictPolicyTest.kt,
    src/test/kotlin/com/anjo/routing/ScheduleRoutesTest.kt,
    src/test/kotlin/com/anjo/routing/TextApiRouteTest.kt
  </files>
  <behavior>
    - ConflictPolicyTest: when the display mutex is already locked (a long-running scheduled display in flight) and displayImmediate is called with conflictPolicy=SKIP_NEW, it returns false and the driver render for the new text is NOT invoked (SCHED-01 / D-04).
    - ConflictPolicyTest: displayImmediate with conflictPolicy=INTERRUPT (default) when busy still cancels and renders the new text (regression — default behavior unchanged).
    - ScheduleRoutesTest: POST /api/v1/schedule with triggerType=CRON and an invalid triggerValue returns 422 AND a subsequent GET /api/v1/schedule contains a row with status ERROR for that text (SCHED-03 persistence — extends the existing 422-only test per Pitfall 3).
    - ScheduleRoutesTest: POST with a valid schedule and webhookUrl="not-a-url" returns 422 (SCHED-04 / D-10 validation).
    - ScheduleRoutesTest: POST with a valid schedule and webhookUrl="https://example.com/hook" returns 201 (valid URL accepted).
    - TextApiRouteTest: POST /api/v1/text with conflictPolicy=SKIP_NEW deserializes and returns 202 with an `accepted` field (SKIP_NEW response shape — Claude's Discretion 202+accepted).
  </behavior>
  <read_first>
    - src/test/kotlin/com/anjo/service/ConflictPolicyTest.kt (makeService helper, runTest/advanceUntilIdle, coVerify pattern)
    - src/test/kotlin/com/anjo/routing/ScheduleRoutesTest.kt (testApplication { module() } pattern, existing invalid-cron 422 test)
    - src/test/kotlin/com/anjo/routing/TextApiRouteTest.kt (POST /api/v1/text body + status assertions)
    - src/main/kotlin/com/anjo/service/ScreenDriver.kt (displayImmediate/displayScheduled Boolean + SKIP_NEW guard — final from 07-02)
    - src/main/kotlin/com/anjo/routing/ScheduleRoutes.kt (CRON ERROR persistence — final from 07-02)
    - src/main/kotlin/com/anjo/routing/TextRoutes.kt (conflictPolicy wiring — final from 07-02)
    - .planning/phases/07-scheduler-schema-stabilisation/07-RESEARCH.md (Phase Requirements -> Test Map, Pitfall 3)
  </read_first>
  <action>
    Extend `ConflictPolicyTest.kt` using the existing `makeService(driver)` helper and `runTest`/`advanceUntilIdle`. To force a busy mutex, launch a `displayScheduled(...)` coroutine that holds the lock (advance until it is in-flight), then call `displayImmediate("new", Effect.SCROLL, ConflictPolicy.SKIP_NEW)` and assert the return is `false` and `coVerify(exactly = 0) { driver.scrollText(any(), "new", any()) }`. Add the INTERRUPT regression case using `coVerify` that the new text IS rendered. Clean up launched jobs as the existing tests do (`cancel()` / `join()`).

    Extend `ScheduleRoutesTest.kt` using `testApplication { application { module() } }`. For the CRON ERROR test, POST the invalid-cron body (reuse the existing payload), assert 422, then GET `/api/v1/schedule` and assert the body contains `"ERROR"` (and the schedule text). Add the two webhookUrl tests (invalid -> 422, valid -> 201) by adding `webhookUrl` to a valid RECURRING body.

    Extend `TextApiRouteTest.kt`: POST `/api/v1/text` with body including `"conflictPolicy":"SKIP_NEW"`, assert 202 and the body contains `accepted` — proves the new field deserializes and the response shape is intact.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.anjo.service.ConflictPolicyTest" --tests "com.anjo.routing.ScheduleRoutesTest" --tests "com.anjo.routing.TextApiRouteTest" --no-daemon</automated>
  </verify>
  <acceptance_criteria>
    - `ConflictPolicyTest.kt` contains `ConflictPolicy.SKIP_NEW` and a `coVerify(exactly = 0)` (or equivalent) asserting the dropped render
    - `ScheduleRoutesTest.kt` contains a test asserting an invalid-CRON POST yields a persisted row with `ERROR` visible via GET, plus invalid (`422`) and valid (`Created`) webhookUrl tests
    - `TextApiRouteTest.kt` contains a `SKIP_NEW` POST test asserting `HttpStatusCode.Accepted` and `accepted` in body
    - `./gradlew test --tests "com.anjo.service.ConflictPolicyTest" --tests "com.anjo.routing.ScheduleRoutesTest" --tests "com.anjo.routing.TextApiRouteTest" --no-daemon` exits 0
  </acceptance_criteria>
  <done>SKIP_NEW drop + INTERRUPT regression, CRON-ERROR persistence, webhookUrl accept/reject, and SKIP_NEW request deserialization are all proven by passing tests.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite green + coverage gate</name>
  <files>
    src/test/kotlin/com/anjo/db/ScheduleRepositoryTest.kt,
    src/test/kotlin/com/anjo/service/SchedulerServiceTest.kt,
    src/test/kotlin/com/anjo/service/ConflictPolicyTest.kt,
    src/test/kotlin/com/anjo/routing/ScheduleRoutesTest.kt,
    src/test/kotlin/com/anjo/routing/TextApiRouteTest.kt
  </files>
  <read_first>
    - build.gradle.kts (jacocoTestCoverageVerification rule: LINE 0.70; excludes driver/utils/config.model/model)
    - .planning/phases/07-scheduler-schema-stabilisation/07-VALIDATION.md (Sampling Rate, Phase gate)
  </read_first>
  <action>
    Run the full test suite with the coverage gate (`finalizedBy jacocoTestReport, jacocoTestCoverageVerification` per build.gradle.kts). If `jacocoTestCoverageVerification` fails the 70% LINE rule, identify the under-covered new lines in `ScheduleRepository`, `SchedulerService`, `ScreenDriver`, or `ScheduleRoutes` (model/db are NOT excluded from the report but the verification excludes `com/anjo/model/**` only; routing/service ARE counted) and add focused test cases in the appropriate existing test file from Task 1/Task 2 to cover the new branches (e.g. the CRON catch path, the SKIP_NEW false branch, updateFiredAtAndDone). Do not modify production code to chase coverage. Do not lower the gate.
  </action>
  <verify>
    <automated>./gradlew test --no-daemon</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --no-daemon` exits 0 (suite green AND jacocoTestCoverageVerification passes, since test is finalizedBy the verification task)
    - Build output shows `BUILD SUCCESSFUL` (the run includes jacocoTestCoverageVerification via finalizedBy)
    - No production source file under `src/main` modified by this task (coverage achieved via tests only)
  </acceptance_criteria>
  <done>Full suite green and JaCoCo LINE coverage >= 70% (the 70% verification rule passes as part of `./gradlew test`).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test client → routes/services | Tests exercise the same untrusted-input boundaries (CRON, webhookUrl, conflictPolicy) as production callers |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Repudiation | regression coverage of SCHED-01..04 | mitigate | Automated tests assert each security-relevant validation path (invalid CRON -> ERROR+422, invalid webhookUrl -> 422, unknown conflictPolicy enum -> deserialization failure) so regressions are caught by `./gradlew test`. |
| T-07-09 | Tampering | coverage gate integrity | accept | Coverage achieved by adding tests only; production code is not weakened and the 0.70 LINE rule is not lowered (build.gradle.kts unchanged). |
</threat_model>

<verification>
- Per task: the scoped `--tests` commands exit 0
- Phase gate: `./gradlew test --no-daemon` exits 0 with `BUILD SUCCESSFUL` (includes jacocoTestCoverageVerification)
- Coverage: jacocoTestCoverageVerification LINE >= 0.70 passes (failure aborts the build, so a green build proves the gate)
</verification>

<success_criteria>
- All four requirements (SCHED-01..04) have at least one passing automated test mapped per 07-RESEARCH.md Phase Requirements -> Test Map
- Full suite green; JaCoCo LINE >= 70%
- No production code changed by the test plan
</success_criteria>

<output>
Create `.planning/phases/07-scheduler-schema-stabilisation/07-03-SUMMARY.md` when done
</output>
