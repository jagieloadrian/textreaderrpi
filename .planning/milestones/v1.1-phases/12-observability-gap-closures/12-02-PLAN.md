---
phase: 12-observability-gap-closures
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/main/kotlin/com/anjo/model/ZoneStatus.kt
  - src/main/kotlin/com/anjo/zone/LocalZoneDriver.kt
  - src/main/kotlin/com/anjo/zone/NetworkZoneDriver.kt
  - src/main/kotlin/com/anjo/model/HealthDetailResponse.kt
  - src/main/kotlin/com/anjo/routing/HealthRoutes.kt
  - src/main/kotlin/com/anjo/di/Monitoring.kt
  - src/main/kotlin/com/anjo/routing/Routing.kt
  - src/main/kotlin/com/anjo/di/DependencyInjection.kt
  - src/test/kotlin/com/anjo/routing/HealthRoutesTest.kt
autonomous: true
requirements: [OBS-01]
must_haves:
  truths:
    - "GET /health/detail returns 200 with a JSON payload containing uptime, memoryUsed, memoryMax, displayStatus, totalFailures, and zoneErrors"
    - "GET /health includes a 'displayAvailable' check that reflects whether any zone is ONLINE"
    - "/health/detail is rate-limited at the same rate as /metrics (120 req/min)"
    - "zoneErrors maps each zoneId to its error string or null when no error"
  artifacts:
    - path: "src/main/kotlin/com/anjo/model/HealthDetailResponse.kt"
      provides: "Serializable flat DTO for /health/detail"
      contains: "data class HealthDetailResponse"
    - path: "src/main/kotlin/com/anjo/routing/HealthRoutes.kt"
      provides: "fun Route.healthRoutes(...) registering GET /health/detail"
      contains: "fun Route.healthRoutes"
    - path: "src/main/kotlin/com/anjo/model/ZoneStatus.kt"
      provides: "error: String? field added"
      contains: "val error: String?"
  key_links:
    - from: "src/main/kotlin/com/anjo/routing/Routing.kt"
      to: "src/main/kotlin/com/anjo/routing/HealthRoutes.kt"
      via: "healthRoutes(...) call inside routing block"
      pattern: "healthRoutes\\("
    - from: "src/main/kotlin/com/anjo/routing/HealthRoutes.kt"
      to: "src/main/kotlin/com/anjo/service/ZoneRegistry.kt"
      via: "zoneRegistry.listAll() for displayStatus and zoneErrors"
      pattern: "zoneRegistry\\.listAll\\(\\)"
    - from: "src/main/kotlin/com/anjo/di/Monitoring.kt"
      to: "src/main/kotlin/com/anjo/service/ZoneRegistry.kt"
      via: "healthCheck('displayAvailable') reads zoneRegistry.listAll()"
      pattern: "check\\(\"displayAvailable\"\\)"
---

<objective>
Add the standalone `GET /health/detail` endpoint (OBS-01) and a `displayAvailable` liveness check. Create the `HealthDetailResponse` DTO, a `healthRoutes()` extension, propagate per-zone `error` through `ZoneStatus`, register the new route in `Routing.kt`, and add the KHealth check in `Monitoring.kt`.

Purpose: Close the v1.0 audit gap where there was no consolidated health detail payload (uptime, memory, display status, error counts) and `/health` did not reflect display state. Depends on 12-01 because both plans edit `DependencyInjection.kt`; this plan also needs the metrics infrastructure to already be wired.
Output: New `HealthDetailResponse.kt` + `HealthRoutes.kt`, `ZoneStatus.error` field with driver propagation, updated `Monitoring.kt`/`Routing.kt`/`DependencyInjection.kt`, updated `HealthRoutesTest`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-observability-gap-closures/12-CONTEXT.md
@.planning/phases/12-observability-gap-closures/12-RESEARCH.md
@.planning/phases/12-observability-gap-closures/12-PATTERNS.md
@.planning/phases/12-observability-gap-closures/12-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols created/modified by THIS plan (exclude from drift verification):

New files:
- `src/main/kotlin/com/anjo/model/HealthDetailResponse.kt`
- `src/main/kotlin/com/anjo/routing/HealthRoutes.kt`

New symbols:
- `HealthDetailResponse` (@Serializable data class) with fields `uptime: Long`, `memoryUsed: Long`, `memoryMax: Long`, `displayStatus: String`, `totalFailures: Long`, `zoneErrors: Map<String, String?>`
- `fun Route.healthRoutes(zoneRegistry: ZoneRegistry, screenDriverMetrics: ScreenDriverMetrics, metricsRateLimitPerMinute: Int)`
- New route path `GET /health/detail`
- New KHealth check name `"displayAvailable"`
- New optional field `error: String? = null` on `ZoneStatus`
- New `by dependencies` declaration `screenDriverMetrics: ScreenDriverMetrics` in `Routing.kt`
- New `provide { screenDriverMetrics }` binding in `DependencyInjection.kt`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add ZoneStatus.error + HealthDetailResponse DTO + healthRoutes()</name>
  <files>
    src/main/kotlin/com/anjo/model/ZoneStatus.kt,
    src/main/kotlin/com/anjo/zone/LocalZoneDriver.kt,
    src/main/kotlin/com/anjo/zone/NetworkZoneDriver.kt,
    src/main/kotlin/com/anjo/model/HealthDetailResponse.kt,
    src/main/kotlin/com/anjo/routing/HealthRoutes.kt
  </files>
  <read_first>
    - src/main/kotlin/com/anjo/model/ZoneStatus.kt (current fields; add error like the existing nullable ip/lastSeenAt)
    - src/main/kotlin/com/anjo/zone/LocalZoneDriver.kt (status() converts DisplayStatus → ZoneStatus; driverStatus.error available)
    - src/main/kotlin/com/anjo/zone/NetworkZoneDriver.kt (status() builds ZoneStatus from online flag)
    - src/main/kotlin/com/anjo/driver/DisplayStatus.kt (error: String? field source for LocalZoneDriver propagation)
    - src/main/kotlin/com/anjo/model/MetricsModels.kt (@Serializable model convention to mirror)
    - src/main/kotlin/com/anjo/routing/MetricsRoutes.kt (exact route-extension shape: route(path){ installMetricsRateLimiting(...); get { ... } })
    - src/main/kotlin/com/anjo/model/ScreenDriverMetrics.kt (failedMeter?.count source for totalFailures)
    - src/main/kotlin/com/anjo/service/ZoneRegistry.kt (listAll(): List<ZoneStatus>)
    - .planning/phases/12-observability-gap-closures/12-PATTERNS.md (HealthDetailResponse, HealthRoutes, ZoneStatus sections)
  </read_first>
  <action>
    `ZoneStatus.kt`: add `val error: String? = null` as the last constructor field (same nullable-with-default form as `ip` and `lastSeenAt`). This is additive — existing constructors stay valid (Critical Implementation Detail Option 1 in 12-RESEARCH.md, A2).

    `LocalZoneDriver.kt` `status()`: pass `error = driverStatus.error` into the `ZoneStatus(...)` it returns, so a local zone's `lastError` surfaces. Read `DisplayStatus.error` from the existing `driverStatus` local.

    `NetworkZoneDriver.kt` `status()`: set `error = if (online) null else "OFFLINE"` in the `ZoneStatus(...)` it returns.

    Create `src/main/kotlin/com/anjo/model/HealthDetailResponse.kt` in package `com.anjo.model`: a `@Serializable data class HealthDetailResponse(val uptime: Long, val memoryUsed: Long, val memoryMax: Long, val displayStatus: String, val totalFailures: Long, val zoneErrors: Map<String, String?>)`. Import `kotlinx.serialization.Serializable`. No companion object, no factory — assembled in the route handler. Per Pitfall 5 (12-RESEARCH.md): `@Serializable` annotation is mandatory; `Map<String, String?>` is serializable by default.

    Create `src/main/kotlin/com/anjo/routing/HealthRoutes.kt` in package `com.anjo.routing`: `fun Route.healthRoutes(zoneRegistry: ZoneRegistry, screenDriverMetrics: ScreenDriverMetrics, metricsRateLimitPerMinute: Int)` mirroring `metricsRoutes()`. Body: `route("/health/detail") { installMetricsRateLimiting(metricsRateLimitPerMinute); get { ... } }`. Inside `get`, build the response: `val runtime = Runtime.getRuntime()`; `val zones = zoneRegistry.listAll()`; then `call.respond(HealthDetailResponse(uptime = ManagementFactory.getRuntimeMXBean().uptime, memoryUsed = runtime.totalMemory() - runtime.freeMemory(), memoryMax = runtime.maxMemory(), displayStatus = if (zones.any { it.status == "ONLINE" }) "ONLINE" else "OFFLINE", totalFailures = screenDriverMetrics.failedMeter?.count ?: 0L, zoneErrors = zones.associate { it.id to it.error }))`. Imports: `com.anjo.di.installMetricsRateLimiting`, `com.anjo.model.HealthDetailResponse`, `com.anjo.model.ScreenDriverMetrics`, `com.anjo.service.ZoneRegistry`, `io.ktor.server.response.respond`, `io.ktor.server.routing.Route/get/route`, `java.lang.management.ManagementFactory`. Do NOT add validation logic to the handler (project rule). No comments in any file.
  </action>
  <verify>
    <automated>cd /home/diether18/IdeaProjects/TextReaderRpi && ./gradlew compileKotlin compileTestKotlin</automated>
  </verify>
  <acceptance_criteria>
    - ZoneStatus.kt contains `val error: String? = null`
    - LocalZoneDriver.kt status() passes `error = driverStatus.error`
    - NetworkZoneDriver.kt status() sets `error = if (online) null else "OFFLINE"`
    - HealthDetailResponse.kt contains `@Serializable` and `data class HealthDetailResponse` with all six fields uptime, memoryUsed, memoryMax, displayStatus, totalFailures, zoneErrors
    - HealthRoutes.kt contains `fun Route.healthRoutes(` and `route("/health/detail")` and `installMetricsRateLimiting(metricsRateLimitPerMinute)`
    - HealthRoutes.kt contains `zones.associate { it.id to it.error }`
    - `./gradlew compileKotlin compileTestKotlin` exits 0
    - No `//`, `/*`, or `/**` comments in any file modified by this task
  </acceptance_criteria>
  <done>ZoneStatus carries an error field propagated by both drivers; HealthDetailResponse DTO and healthRoutes() exist and compile; the route is not yet registered (Task 2 wires it).</done>
</task>

<task type="auto">
  <name>Task 2: Register healthRoutes + displayAvailable check + DI binding; update HealthRoutesTest</name>
  <files>
    src/main/kotlin/com/anjo/routing/Routing.kt,
    src/main/kotlin/com/anjo/di/Monitoring.kt,
    src/main/kotlin/com/anjo/di/DependencyInjection.kt,
    src/test/kotlin/com/anjo/routing/HealthRoutesTest.kt
  </files>
  <read_first>
    - src/main/kotlin/com/anjo/routing/Routing.kt (by dependencies block lines 32-40; routing block; metricsRoutes call line 48; swaggerUI last)
    - src/main/kotlin/com/anjo/di/Monitoring.kt (configureMonitoring; by dependencies; healthChecks block lines 32-34)
    - src/main/kotlin/com/anjo/di/DependencyInjection.kt (screenDriverMetrics local from 12-01; dependencies provide block lines 73-90)
    - src/main/kotlin/com/anjo/service/ZoneRegistry.kt (listAll() used by displayAvailable check)
    - src/test/kotlin/com/anjo/routing/HealthRoutesTest.kt (current 404 assertion for /health/detail lines 32-38 to flip; appAlive /health test pattern)
    - .planning/phases/12-observability-gap-closures/12-PATTERNS.md (Routing.kt, Monitoring.kt, HealthRoutesTest sections)
  </read_first>
  <action>
    `Routing.kt`: add `val screenDriverMetrics: ScreenDriverMetrics by dependencies` alongside the other `by dependencies` declarations, and `import com.anjo.model.ScreenDriverMetrics`. Inside `routing { }`, add `healthRoutes(zoneRegistry, screenDriverMetrics, apiConfig.metricsRateLimitPerMinute)` immediately after the `metricsRoutes(...)` line and before the `route("/api/v1")` block. Do not move `swaggerUI(path = "openapi")` — it is already last (OBS-03 is handled in plan 03).

    `Monitoring.kt`: add `val zoneRegistry: ZoneRegistry by dependencies` alongside the existing `by dependencies` declarations in `configureMonitoring()`, and `import com.anjo.service.ZoneRegistry`. In the `healthChecks { }` block add `check("displayAvailable") { zoneRegistry.listAll().any { it.status == "ONLINE" } }` after `check("appAlive") { true }`. Per Pitfall 6 (12-RESEARCH.md): the new check goes in `healthChecks {}` (liveness), NOT `readyChecks {}`.

    `DependencyInjection.kt`: add `provide { screenDriverMetrics }` in the `dependencies { }` block (the `screenDriverMetrics` local already exists from line 34). This makes it resolvable for `Routing.kt`'s `by dependencies` declaration.

    `HealthRoutesTest.kt`: replace the existing "should return 404 for GET /health/detail" test with one asserting 200 and the response shape — `response.status shouldBe HttpStatusCode.OK`, parse JSON via `Json.parseToJsonElement(response.bodyAsText()).jsonObject`, assert `json["uptime"]` non-null, `json["memoryUsed"]` non-null, `json["memoryMax"]` non-null, `json["displayStatus"]!!.jsonPrimitive.content` is one of "ONLINE"/"OFFLINE", `json["totalFailures"]` non-null, and `json["zoneErrors"]` present. Add a new test asserting `GET /health` body `shouldContain "displayAvailable"`. Add the needed imports (`kotlinx.serialization.json.Json`, `jsonObject`, `jsonPrimitive`, `io.kotest.matchers.collections.shouldBeIn` or `shouldBe` for the status set check). Keep the existing `/health` appAlive and `/health/ready` displayReady tests unchanged.
  </action>
  <verify>
    <automated>cd /home/diether18/IdeaProjects/TextReaderRpi && ./gradlew test --tests "com.anjo.routing.HealthRoutesTest" --tests "com.anjo.ApplicationTest"</automated>
  </verify>
  <acceptance_criteria>
    - Routing.kt contains `val screenDriverMetrics: ScreenDriverMetrics by dependencies` and `healthRoutes(zoneRegistry, screenDriverMetrics, apiConfig.metricsRateLimitPerMinute)`
    - In Routing.kt the `healthRoutes(` call appears before `route("/api/v1")` and `swaggerUI(` remains the last call inside `routing {`
    - Monitoring.kt contains `check("displayAvailable")` inside the healthChecks block and `zoneRegistry.listAll().any { it.status == "ONLINE" }`
    - DependencyInjection.kt contains `provide { screenDriverMetrics }`
    - HealthRoutesTest.kt no longer asserts `HttpStatusCode.NotFound` for `/health/detail`; it asserts `HttpStatusCode.OK` and checks uptime/memoryUsed/displayStatus fields
    - HealthRoutesTest.kt contains a test asserting `/health` body `shouldContain "displayAvailable"`
    - `./gradlew test --tests "com.anjo.routing.HealthRoutesTest" --tests "com.anjo.ApplicationTest"` exits 0
    - No `//`, `/*`, or `/**` comments added to any modified Kotlin file
  </acceptance_criteria>
  <done>GET /health/detail returns 200 with the full payload, rate-limited at 120/min; GET /health reports the displayAvailable check; screenDriverMetrics resolves from DI; HealthRoutesTest reflects the new behavior and passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/client → GET /health/detail | Untrusted reader can scrape uptime/memory/zone error strings; rate-limited at 120 req/min |
| KHealth healthChecks → ZoneRegistry | In-process read of zone status; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Information Disclosure | GET /health/detail zoneErrors / memory | accept | Error strings are driver `lastError` messages and zone IDs; no secrets/PII; trusted home network per v1.1 out-of-scope auth decision |
| T-12-05 | Denial of Service | GET /health/detail scraping | mitigate | `installMetricsRateLimiting(metricsRateLimitPerMinute)` applied to the route at 120 req/min, identical to /metrics (D-01) |
| T-12-06 | Information Disclosure | zoneErrors leaking stack traces | mitigate | zoneErrors carries only `DisplayStatus.error` short strings / "OFFLINE", never stack traces; no raw exception text exposed |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced (12-RESEARCH Package Legitimacy Audit: none); no install task in this plan |
</threat_model>

<verification>
- `./gradlew test --tests "com.anjo.routing.HealthRoutesTest" --tests "com.anjo.ApplicationTest"` exits 0
- `GET /health/detail` returns HTTP 200 with JSON containing keys uptime, memoryUsed, memoryMax, displayStatus, totalFailures, zoneErrors
- `GET /health` body contains "displayAvailable"
- `displayStatus` is "ONLINE" or "OFFLINE"
- No Kotlin file modified in this plan contains `//`, `/*`, or `/**` comments (project no-comments rule)
</verification>

<success_criteria>
- OBS-01 satisfied: `GET /health/detail` returns a JSON payload with uptime, memory, display status, and error counts; `/health` reflects display state via the displayAvailable check
- /health/detail is rate-limited consistently with /metrics
- zoneErrors maps each zoneId to its error string or null; HealthRoutesTest green
</success_criteria>

<output>
Create `.planning/phases/12-observability-gap-closures/12-02-SUMMARY.md` when done
</output>
