---
phase: 12-observability-gap-closures
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/com/anjo/model/HardwareMetrics.kt
  - src/main/kotlin/com/anjo/service/RetryPolicy.kt
  - src/main/kotlin/com/anjo/service/MetricsCollector.kt
  - src/main/kotlin/com/anjo/service/ScreenDriverService.kt
  - src/main/kotlin/com/anjo/di/DependencyInjection.kt
  - src/test/kotlin/com/anjo/service/HardwareMetricsTest.kt
  - src/test/kotlin/com/anjo/service/RetryPolicyTest.kt
  - src/test/kotlin/com/anjo/routing/MetricsRoutesTest.kt
  - src/test/kotlin/com/anjo/ApplicationTest.kt
autonomous: true
requirements: [OBS-02]
must_haves:
  truths:
    - "GET /metrics response includes a group named 'hardware' alongside 'runtime' and 'api'"
    - "The hardware group reports display failure counts, recovery retry counts, in-flight slot count, and skipped-request count"
    - "retryWithBackoff increments recoveryRetryCounter on each retry and displayFailureCounter only on final failure"
    - "HardwareMetrics resolves from the DI container"
  artifacts:
    - path: "src/main/kotlin/com/anjo/model/HardwareMetrics.kt"
      provides: "HardwareMetrics data class with DISABLED sentinel and from(registry, config) factory"
      contains: "data class HardwareMetrics"
    - path: "src/main/kotlin/com/anjo/service/MetricsCollector.kt"
      provides: "hardwareGroup() added to collect()"
      contains: "hardwareGroup"
    - path: "src/test/kotlin/com/anjo/service/HardwareMetricsTest.kt"
      provides: "Factory + DISABLED sentinel coverage for HardwareMetrics"
      contains: "HardwareMetricsTest"
  key_links:
    - from: "src/main/kotlin/com/anjo/service/MetricsCollector.kt"
      to: "src/main/kotlin/com/anjo/model/HardwareMetrics.kt"
      via: "constructor injection of HardwareMetrics; hardwareGroup() reads counters"
      pattern: "hardwareMetrics\\.(displayFailureCounter|recoveryRetryCounter|inFlightCounter|skippedCounter)"
    - from: "src/main/kotlin/com/anjo/service/RetryPolicy.kt"
      to: "src/main/kotlin/com/anjo/model/HardwareMetrics.kt"
      via: "optional hardwareMetrics param; ?.inc() in catch and rethrow"
      pattern: "hardwareMetrics\\?\\.(recoveryRetryCounter|displayFailureCounter)\\?\\.inc"
    - from: "src/main/kotlin/com/anjo/di/DependencyInjection.kt"
      to: "src/main/kotlin/com/anjo/model/HardwareMetrics.kt"
      via: "HardwareMetrics.from(...) registration + provide block"
      pattern: "HardwareMetrics\\.from"
---

<objective>
Add the `/metrics` hardware group (OBS-02). Create a `HardwareMetrics` Dropwizard-counter class, instrument `retryWithBackoff` and `ScreenDriverService` with hardware counters, expose them as a new `hardwareGroup()` in `MetricsCollector`, and wire `HardwareMetrics` through DI.

Purpose: Close the v1.0 audit gap where `/metrics` reported only runtime and API groups, leaving hardware failure/recovery/saturation invisible. This is the foundation plan — it claims `DependencyInjection.kt` first so the later health-detail plan can layer additional DI changes on top without conflict.
Output: New `HardwareMetrics.kt`, instrumented retry/service code, third metrics group, new `HardwareMetricsTest.kt`, updated `RetryPolicyTest`/`MetricsRoutesTest`/`ApplicationTest`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-observability-gap-closures/12-CONTEXT.md
@.planning/phases/12-observability-gap-closures/12-RESEARCH.md
@.planning/phases/12-observability-gap-closures/12-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols created/modified by THIS plan (exclude from drift verification):

New files:
- `src/main/kotlin/com/anjo/model/HardwareMetrics.kt`
- `src/test/kotlin/com/anjo/service/HardwareMetricsTest.kt`

New symbols:
- `HardwareMetrics` (data class) with fields `displayFailureCounter: Counter?`, `recoveryRetryCounter: Counter?`, `inFlightCounter: Counter?`, `skippedCounter: Counter?`
- `HardwareMetrics.Companion.DISABLED` (sentinel)
- `HardwareMetrics.Companion.from(registry: MetricRegistry, config: MetricsConfig): HardwareMetrics`
- New metric registry keys: `textreaderrpi.hardware.display.failures`, `textreaderrpi.hardware.recovery.retries`, `textreaderrpi.hardware.display.inFlight`, `textreaderrpi.hardware.display.skipped`
- `MetricsCollector.hardwareGroup()` (private fun)
- New constructor parameter `hardwareMetrics: HardwareMetrics` on `MetricsCollector`
- New constructor parameter `hardwareMetrics: HardwareMetrics = HardwareMetrics.DISABLED` on `ScreenDriverService`
- New parameter `hardwareMetrics: HardwareMetrics? = null` on `retryWithBackoff`
- New metrics group name string `"hardware"` in the `/metrics` response
- `HardwareMetricsTest` (Kotest FunSpec class)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create HardwareMetrics class + instrument retryWithBackoff</name>
  <files>
    src/main/kotlin/com/anjo/model/HardwareMetrics.kt,
    src/main/kotlin/com/anjo/service/RetryPolicy.kt,
    src/test/kotlin/com/anjo/service/HardwareMetricsTest.kt,
    src/test/kotlin/com/anjo/service/RetryPolicyTest.kt
  </files>
  <read_first>
    - src/main/kotlin/com/anjo/model/ScreenDriverMetrics.kt (exact factory/DISABLED pattern to copy — fields, companion object, from(registry, config) signature)
    - src/main/kotlin/com/anjo/config/model/MetricsConfig.kt (config.enabled + config.prefix fields used by from())
    - src/main/kotlin/com/anjo/service/RetryPolicy.kt (current retryWithBackoff signature and catch/rethrow control flow)
    - src/test/kotlin/com/anjo/service/RetryPolicyTest.kt (existing FunSpec test structure to extend)
    - .planning/phases/12-observability-gap-closures/12-PATTERNS.md (HardwareMetrics + retryWithBackoff sections)
  </read_first>
  <behavior>
    - HardwareMetrics.DISABLED has all four counter fields null
    - HardwareMetrics.from(registry, config) returns DISABLED when config.enabled == false
    - HardwareMetrics.from(registry, config) with config.enabled == true returns non-null counters registered under keys textreaderrpi.hardware.display.failures, textreaderrpi.hardware.recovery.retries, textreaderrpi.hardware.display.inFlight, textreaderrpi.hardware.display.skipped
    - retryWithBackoff increments recoveryRetryCounter once per retry attempt (transient failure that recovers on attempt 2 of maxAttempts=3 → recoveryRetryCounter.count == 1, displayFailureCounter.count == 0)
    - retryWithBackoff increments displayFailureCounter exactly once on final exhaustion (maxAttempts=3, always-fail → displayFailureCounter.count == 1, recoveryRetryCounter.count == 2)
    - retryWithBackoff with hardwareMetrics == null (default) behaves identically to before — no NPE
  </behavior>
  <action>
    Create `src/main/kotlin/com/anjo/model/HardwareMetrics.kt` in package `com.anjo.model`. Model it line-for-line on `ScreenDriverMetrics`: a `data class HardwareMetrics` with four nullable `com.codahale.metrics.Counter?` fields defaulting to null — `displayFailureCounter`, `recoveryRetryCounter`, `inFlightCounter`, `skippedCounter`. Add a companion object with `val DISABLED = HardwareMetrics()` and `fun from(registry: MetricRegistry, config: MetricsConfig): HardwareMetrics` that returns `DISABLED` when `!config.enabled`, otherwise reads `val p = config.prefix` and registers the four counters via `registry.counter(...)` using keys `"$p.hardware.display.failures"`, `"$p.hardware.recovery.retries"`, `"$p.hardware.display.inFlight"`, `"$p.hardware.display.skipped"`. Import only `com.anjo.config.model.MetricsConfig`, `com.codahale.metrics.Counter`, `com.codahale.metrics.MetricRegistry` (no Meter/Timer). No comments anywhere.

    Modify `retryWithBackoff` in `RetryPolicy.kt`: change the signature to `suspend fun <T> retryWithBackoff(config: RetryConfig = RetryConfig(), hardwareMetrics: HardwareMetrics? = null, block: suspend () -> T): T`. Inside the existing catch block: on the final-failure path (the `if (attempt >= config.maxAttempts)` branch, before `throw e`) add `hardwareMetrics?.displayFailureCounter?.inc()`. On the retry path (after the warn log, before `delay(...)`) add `hardwareMetrics?.recoveryRetryCounter?.inc()`. Add `import com.anjo.model.HardwareMetrics`. Per Pitfall 2 (12-RESEARCH.md): displayFailureCounter increments ONLY on final rethrow, never per attempt.

    Create `src/test/kotlin/com/anjo/service/HardwareMetricsTest.kt` (Kotest `FunSpec`, package `com.anjo.service`) covering the DISABLED-sentinel, enabled-vs-disabled factory, and counter-key assertions from the behavior block. Use a real `MetricRegistry()` and a `MetricsConfig(enabled = true)` / `MetricsConfig(enabled = false)`. Assert registered keys exist via `registry.counters.keys`.

    Extend `RetryPolicyTest.kt`: add two new tests using a real `MetricRegistry()` + `HardwareMetrics.from(registry, MetricsConfig())` — one proving recovery increments recoveryRetryCounter (and not displayFailureCounter), one proving final exhaustion increments displayFailureCounter exactly once. Keep all existing tests unchanged.
  </action>
  <verify>
    <automated>cd /home/diether18/IdeaProjects/TextReaderRpi && ./gradlew test --tests "com.anjo.service.HardwareMetricsTest" --tests "com.anjo.service.RetryPolicyTest"</automated>
  </verify>
  <acceptance_criteria>
    - src/main/kotlin/com/anjo/model/HardwareMetrics.kt contains `data class HardwareMetrics`
    - HardwareMetrics.kt contains `val DISABLED = HardwareMetrics()` and `fun from(registry: MetricRegistry, config: MetricsConfig)`
    - `grep -v '^[[:space:]]*//' src/main/kotlin/com/anjo/model/HardwareMetrics.kt | grep -c 'registry.counter("\$p.hardware'` returns 4
    - RetryPolicy.kt signature contains `hardwareMetrics: HardwareMetrics? = null`
    - RetryPolicy.kt contains `hardwareMetrics?.displayFailureCounter?.inc()` inside the `attempt >= config.maxAttempts` branch and `hardwareMetrics?.recoveryRetryCounter?.inc()` on the retry path
    - `./gradlew test --tests "com.anjo.service.HardwareMetricsTest" --tests "com.anjo.service.RetryPolicyTest"` exits 0
    - No Kotlin file in this task contains `//`, `/*`, or `/**` comments
  </acceptance_criteria>
  <done>HardwareMetrics class exists with DISABLED + from() factory; retryWithBackoff is instrumented with correct retry-vs-failure counter placement; both unit test files pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire HardwareMetrics into MetricsCollector, ScreenDriverService, and DI</name>
  <files>
    src/main/kotlin/com/anjo/service/MetricsCollector.kt,
    src/main/kotlin/com/anjo/service/ScreenDriverService.kt,
    src/main/kotlin/com/anjo/di/DependencyInjection.kt,
    src/test/kotlin/com/anjo/routing/MetricsRoutesTest.kt,
    src/test/kotlin/com/anjo/ApplicationTest.kt
  </files>
  <read_first>
    - src/main/kotlin/com/anjo/service/MetricsCollector.kt (constructor + collect() + runtimeGroup() structure to mirror)
    - src/main/kotlin/com/anjo/model/MetricsModels.kt (MetricGroup + MetricEntry signatures — MetricEntry uses key, type, count)
    - src/main/kotlin/com/anjo/service/ScreenDriverService.kt (constructor; renderImmediate inFlight pattern lines 126-144; SKIP_NEW drop path lines 63-66; executeWithRecovery retryWithBackoff caller lines 191-199)
    - src/main/kotlin/com/anjo/di/DependencyInjection.kt (screenDriverMetrics registration line 34, screenDriverService construction lines 45-51, metricsCollector line 53, provide block lines 73-90)
    - src/test/kotlin/com/anjo/routing/MetricsRoutesTest.kt (group-count + group-name assertions to update — lines 31, 42)
    - src/test/kotlin/com/anjo/ApplicationTest.kt (DI smoke assertions lines 84-99 — getBlocking pattern)
    - .planning/phases/12-observability-gap-closures/12-PATTERNS.md (MetricsCollector + ScreenDriverService + DependencyInjection sections)
  </read_first>
  <action>
    `MetricsCollector.kt`: add constructor param `private val hardwareMetrics: HardwareMetrics` after `metricRegistry`. Add `import com.anjo.model.HardwareMetrics`. Add a private `hardwareGroup(): MetricGroup` returning `MetricGroup(name = "hardware", metrics = listOf(...))` with four `MetricEntry(key = ..., type = "counter", count = <counter>?.count ?: 0L)` entries using keys `"display.failures"`, `"recovery.retries"`, `"display.inFlight"`, `"display.skipped"` reading `hardwareMetrics.displayFailureCounter`, `.recoveryRetryCounter`, `.inFlightCounter`, `.skippedCounter` respectively. Add `hardwareGroup()` to the `collect()` groups list so it reads `listOf(runtimeGroup(), apiGroup(), hardwareGroup())`.

    `ScreenDriverService.kt`: add constructor param `private val hardwareMetrics: HardwareMetrics = HardwareMetrics.DISABLED` immediately after `metrics: ScreenDriverMetrics`. Add `import com.anjo.model.HardwareMetrics`. In the SKIP_NEW drop branch in `displayImmediate` (the `acquireMutex(...) ?: run { ... }` block, lines 63-66) add `hardwareMetrics.skippedCounter?.inc()` before `return DisplayResult.Accepted(false)`. In `renderImmediate`, alongside the existing `metrics.inFlightCounter?.inc()` (line 128) add `hardwareMetrics.inFlightCounter?.inc()`, and alongside `metrics.inFlightCounter?.dec()` in the finally block (line 140) add `hardwareMetrics.inFlightCounter?.dec()`. In `executeWithRecovery`, pass the hardware counters to the retry call: change `retryWithBackoff(retryConfig) { ... }` to `retryWithBackoff(retryConfig, hardwareMetrics) { ... }`. Do not add inFlight instrumentation to runScheduledRender (out of D-08 scope; D-08 names renderImmediate and runScheduledRender but runScheduledRender currently has no inFlight context — increment/decrement only in renderImmediate to match the existing ScreenDriverMetrics.inFlightCounter pattern, keeping symmetry; the scheduled path's recovery/failure counters are still captured via executeWithRecovery → retryWithBackoff).

    `DependencyInjection.kt`: add `val hardwareMetrics = HardwareMetrics.from(metricRegistry, appConfig.metrics)` immediately after the `screenDriverMetrics` line (line 34). Add `import com.anjo.model.HardwareMetrics`. Pass `hardwareMetrics = hardwareMetrics` into the `ScreenDriverService(...)` constructor call (after `metrics = screenDriverMetrics`). Change `MetricsCollector(metricRegistry)` to `MetricsCollector(metricRegistry, hardwareMetrics)`. Add `provide { hardwareMetrics }` in the `dependencies { }` block after `provide { metricRegistry }`. Per Pitfall 3 (12-RESEARCH.md): hardwareMetrics must be declared before screenDriverService and metricsCollector to avoid forward-reference errors.

    `MetricsRoutesTest.kt`: change `json["groups"]!!.jsonArray shouldHaveSize 2` to `shouldHaveSize 3`; change `groupNames shouldBe listOf("runtime", "api")` to `listOf("runtime", "api", "hardware")`. Also update the test description text "2 groups" if present for accuracy.

    `ApplicationTest.kt`: in the DI smoke test add `deps.getBlocking<HardwareMetrics>(DependencyKey<HardwareMetrics>()) shouldNotBeNull {}` alongside the other binding assertions, and add `import com.anjo.model.HardwareMetrics`.
  </action>
  <verify>
    <automated>cd /home/diether18/IdeaProjects/TextReaderRpi && ./gradlew test --tests "com.anjo.routing.MetricsRoutesTest" --tests "com.anjo.ApplicationTest"</automated>
  </verify>
  <acceptance_criteria>
    - MetricsCollector.kt constructor contains `hardwareMetrics: HardwareMetrics`
    - MetricsCollector.kt collect() reads `listOf(runtimeGroup(), apiGroup(), hardwareGroup())`
    - `grep -c 'name = "hardware"' src/main/kotlin/com/anjo/service/MetricsCollector.kt` returns 1
    - ScreenDriverService.kt constructor contains `hardwareMetrics: HardwareMetrics = HardwareMetrics.DISABLED`
    - ScreenDriverService.kt contains `hardwareMetrics.skippedCounter?.inc()` and `retryWithBackoff(retryConfig, hardwareMetrics)`
    - DependencyInjection.kt contains `HardwareMetrics.from(metricRegistry, appConfig.metrics)`, `MetricsCollector(metricRegistry, hardwareMetrics)`, and `provide { hardwareMetrics }`
    - MetricsRoutesTest.kt asserts `shouldHaveSize 3` and group names `listOf("runtime", "api", "hardware")`
    - ApplicationTest.kt asserts `getBlocking<HardwareMetrics>` resolves non-null
    - `./gradlew test --tests "com.anjo.routing.MetricsRoutesTest" --tests "com.anjo.ApplicationTest"` exits 0
    - No new `//`, `/*`, or `/**` comments added to any modified Kotlin file
  </acceptance_criteria>
  <done>The /metrics response carries a third "hardware" group with all four counters; ScreenDriverService increments skipped/inFlight and passes hardware counters into the retry path; HardwareMetrics resolves from DI and is proven by the smoke test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/client → GET /metrics | Untrusted reader can scrape metric counts; rate-limited at 120 req/min (existing `installMetricsRateLimiting`) |
| service layer → MetricRegistry | In-process counter mutation; thread-safety provided by Dropwizard Counter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | GET /metrics hardware group | accept | Counts (failures/retries/inFlight/skipped) carry no PII or secrets; trusted home network per v1.1 out-of-scope auth decision |
| T-12-02 | Denial of Service | GET /metrics scraping | mitigate | Existing `installMetricsRateLimiting(120)` already applied to `/metrics`; no change needed |
| T-12-03 | Tampering | Dropwizard Counter increment concurrency | accept | Dropwizard `Counter.inc()/dec()` is thread-safe by design; null-safe `?.inc()` prevents NPE when metrics disabled |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced (12-RESEARCH Package Legitimacy Audit: none); no install task in this plan |
</threat_model>

<verification>
- `./gradlew test --tests "com.anjo.service.HardwareMetricsTest" --tests "com.anjo.service.RetryPolicyTest" --tests "com.anjo.routing.MetricsRoutesTest" --tests "com.anjo.ApplicationTest"` exits 0
- `GET /metrics` returns a JSON body whose `groups` array has exactly 3 entries named `runtime`, `api`, `hardware`
- The `hardware` group contains metric keys `display.failures`, `recovery.retries`, `display.inFlight`, `display.skipped`
- No Kotlin file modified in this plan contains `//`, `/*`, or `/**` comments (project no-comments rule)
</verification>

<success_criteria>
- OBS-02 satisfied: `GET /metrics` includes a hardware group with display failure counts, recovery retry counts, and resource slot (inFlight/skipped) utilisation
- All four new tests/test-updates pass; full targeted suite green
- HardwareMetrics resolves from DI; zero existing callers of retryWithBackoff broken (null default)
</success_criteria>

<output>
Create `.planning/phases/12-observability-gap-closures/12-01-SUMMARY.md` when done
</output>
