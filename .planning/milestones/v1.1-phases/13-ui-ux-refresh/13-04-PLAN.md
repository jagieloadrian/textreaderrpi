---
phase: 13-ui-ux-refresh
plan: 04
type: execute
wave: 3
depends_on: ["13-01", "13-02", "13-03"]
files_modified:
  - src/test/kotlin/com/anjo/db/HistoryRepositoryTest.kt
  - src/test/kotlin/com/anjo/service/HistoryServiceTest.kt
  - src/test/kotlin/com/anjo/routing/WebAndDisplayRoutesTest.kt
  - src/test/kotlin/com/anjo/routing/WebRoutesTest.kt
autonomous: false
requirements: [UI-01, UI-02, UI-03, UI-04, UI-05, UI-06, UI-07, UI-08]
must_haves:
  truths:
    - "HistoryRepositoryTest proves zone filter returns only matching-zone records"
    - "HistoryServiceTest proves zone + effect params forward to the repository"
    - "WebAndDisplayRoutesTest no longer references GET /settings/display and GET /history?zone=X returns filtered results"
    - "WebRoutesTest proves GET / contains a zone <select>, GET /schedule contains Zone + Webhook URL columns, GET /status contains the Loading... skeleton spans, and the side nav has no Settings link"
    - "Full ./gradlew test is green and JaCoCo >= 70% gate passes"
    - "A human confirms dark mode, mobile hamburger nav, effect preview animation, and /status live refresh visually"
  artifacts:
    - path: "src/test/kotlin/com/anjo/service/HistoryServiceTest.kt"
      provides: "zone/effect param forwarding test"
      contains: "findPaginated"
    - path: "src/test/kotlin/com/anjo/routing/WebRoutesTest.kt"
      provides: "SSR assertions for zone selector, schedule columns, status skeleton, no settings link"
      contains: "zoneSelect"
  key_links:
    - from: "src/test/kotlin/com/anjo/routing/WebAndDisplayRoutesTest.kt"
      to: "GET /history?zone=X"
      via: "filtered history integration assertion"
      pattern: "history\\?zone"
    - from: "build.gradle.kts"
      to: "jacocoTestCoverageVerification"
      via: "0.70 minimum gate"
      pattern: "0.70"
---

<objective>
Lock the phase with automated coverage and a single human visual checkpoint. Add a zone-filter test to `HistoryRepositoryTest`; create `HistoryServiceTest` (does not exist yet) proving zone/effect forwarding; in `WebAndDisplayRoutesTest` delete the now-invalid `GET /settings/display` test and add a `GET /history?zone=X` filtered-results test; create `WebRoutesTest` (does not exist yet) asserting the new SSR structure (zone selector, schedule columns, status skeleton, no Settings link). Run the full suite green with the JaCoCo 70% gate, then a human verifies the four manual-only visual behaviors (dark mode palette, mobile hamburger nav, effect preview animation, /status live refresh) from VALIDATION.md.

Purpose: Wave 3 verification. Plans 01-03 are all merged; this plan proves UI-01..UI-08 hold and the regression suite is green. The human checkpoint covers behaviors CSS `prefers-color-scheme`, responsive layout, and animation timing that cannot be asserted in a headless test host (VALIDATION Manual-Only table).

Output: 2 modified test files + 2 new test files + green suite + human sign-off.
</objective>

<artifacts_this_phase_produces>
Symbols this plan creates (exclude from drift verification):
- `src/test/kotlin/com/anjo/service/HistoryServiceTest.kt` — new test file
- `src/test/kotlin/com/anjo/routing/WebRoutesTest.kt` — new test file
- New test cases added to `HistoryRepositoryTest` and `WebAndDisplayRoutesTest`
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-ui-ux-refresh/13-VALIDATION.md
@.planning/phases/13-ui-ux-refresh/13-UI-SPEC.md
@.planning/phases/13-ui-ux-refresh/13-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Unit + service zone-filter tests (HistoryRepositoryTest, new HistoryServiceTest)</name>
  <files>src/test/kotlin/com/anjo/db/HistoryRepositoryTest.kt, src/test/kotlin/com/anjo/service/HistoryServiceTest.kt</files>
  <read_first>
    - src/test/kotlin/com/anjo/db/HistoryRepositoryTest.kt (full file — Kotest FunSpec style, makeRecord helper, beforeSpec H2 setup, existing effect/source filter tests lines 94-116 — copy their shape for zone)
    - src/main/kotlin/com/anjo/db/HistoryRepository.kt (Plan 02 added the zone param to findPaginated — confirm the final signature)
    - src/main/kotlin/com/anjo/service/HistoryService.kt (Plan 02 added zone forwarding — confirm constructor takes HistoryRepository)
    - src/main/kotlin/com/anjo/model/HistoryRecord.kt (zoneId field for makeRecord with a zone)
  </read_first>
  <action>
    HistoryRepositoryTest.kt: add a Kotest test `"should filter by zone returning only matching-zone records"` modeled on the existing effect/source filter tests. Insert three records — two with `zoneId = "zone-a"` and one with `zoneId = "zone-b"` (extend the `makeRecord` helper or build `HistoryRecord(...)` directly with a `zoneId` arg). Call `repository.findPaginated(1, 20, zone = "zone-a")` and assert `total shouldBe 2L` and `items.all { it.zoneId == "zone-a" } shouldBe true`. Also add one assertion that combined `effect = "SCROLL", zone = "zone-a"` narrows correctly (proves the andWhere chaining from Plan 02 — both `where` and `andWhere` branches).

    HistoryServiceTest.kt: CREATE this file (it does not exist). Use Kotest `FunSpec`. Construct a `HistoryService` over a real `HistoryRepository` backed by an in-memory H2 schema (copy the `beforeSpec`/`beforeEach` H2 + `SchemaUtils.create(HistoryTable)` setup from HistoryRepositoryTest). Insert records across two zones and assert that `service.findPaginated(1, 20, zone = "zone-a")` returns only zone-a rows, and that `service.findPaginated(1, 20, effect = "SCROLL", zone = "zone-a")` forwards BOTH params (returns only zone-a SCROLL rows). This proves the service forwards `zone` and `effect` to the repository (RESEARCH validation: "zone + effect params forwarded to repository").
  </action>
  <verify>
    <automated>./gradlew test --tests "*.HistoryRepositoryTest" --tests "*.HistoryServiceTest" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "*.HistoryRepositoryTest" --tests "*.HistoryServiceTest"` exits 0
    - HistoryRepositoryTest contains a test asserting `findPaginated(..., zone = "zone-a")` returns only zone-a records (total 2L)
    - `src/test/kotlin/com/anjo/service/HistoryServiceTest.kt` exists and asserts `service.findPaginated(..., effect = "SCROLL", zone = "zone-a")` forwards both params
  </acceptance_criteria>
  <done>The zone filter is proven at both the repository (where/andWhere branches) and service (param forwarding) layers with passing Kotest cases.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Integration SSR tests (WebAndDisplayRoutesTest update, new WebRoutesTest), full suite + JaCoCo gate</name>
  <files>src/test/kotlin/com/anjo/routing/WebAndDisplayRoutesTest.kt, src/test/kotlin/com/anjo/routing/WebRoutesTest.kt</files>
  <read_first>
    - src/test/kotlin/com/anjo/routing/WebAndDisplayRoutesTest.kt (full file — the GET /settings/display test at lines 37-45 must be DELETED; module() + testApplication pattern to reuse)
    - src/test/kotlin/com/anjo/ApplicationTest.kt (lines 34-40 — confirms `testApplication { application { module() } }` and client.get patterns; module() is the app entry)
    - src/main/kotlin/com/anjo/web/templates/IndexPage.kt (final Plan-02 markup — what to assert: zoneSelect, effectPreview, "All zones")
    - src/main/kotlin/com/anjo/web/templates/SchedulePage.kt (final Plan-02 markup — "Zone" + "Webhook URL" headers)
    - src/main/kotlin/com/anjo/web/templates/StatusPage.kt (final Plan-02 markup — status-uptime span + "Loading...")
    - src/main/kotlin/com/anjo/web/templates/BaseLayout.kt (final Plan-01 markup — 5 nav links, no Settings link, custom.css link)
    - .planning/phases/13-ui-ux-refresh/13-VALIDATION.md (Wave 0 Requirements: which test files to add which assertions; Per-Task Verification Map)
    - build.gradle.kts (lines 119-142 — jacocoTestCoverageVerification 0.70 gate)
  </read_first>
  <action>
    WebAndDisplayRoutesTest.kt:
    - DELETE the `"should return display settings HTML for GET /settings/display"` test entirely (the route no longer exists — D-25 / RESEARCH pitfall 2).
    - ADD a test `"should filter history by zone for GET /history?zone=X"`: in `testApplication { application { module() } }`, GET `/history?zone=ALL` (or any zone value) and assert `response.status shouldBe HttpStatusCode.OK` and the body is HTML containing the zone filter select (`shouldContain "All zones"`). (A full data-filtered assertion requires seeded history; if seeding via the public API is impractical in the test host, assert the route accepts the `zone` param and renders the populated filter select — this still proves the param is wired end-to-end without 500.)

    WebRoutesTest.kt: CREATE this file (it does not exist). Kotest `FunSpec`, `testApplication { application { module() } }` pattern. Add tests:
    - `GET /` returns 200 and body `shouldContain "zoneSelect"` and `shouldContain "All zones"` and `shouldContain "effectPreview"` (UI-04).
    - `GET /schedule` returns 200 and body `shouldContain "Zone"` and `shouldContain "Webhook URL"` (UI-05 columns).
    - `GET /status` returns 200 and body `shouldContain "status-uptime"` and `shouldContain "Loading..."` (UI-08 skeleton).
    - `GET /` body `shouldContain "/static/custom.css"` and the nav does NOT contain `/settings/display` (`body.contains("/settings/display") shouldBe false`) and contains the 5 nav hrefs (UI-01/02/03 — assert presence of `href="/zones"` and `href="/history"` etc.).
    - `GET /settings/display` returns `HttpStatusCode.NotFound` (route deleted).

    Then run the FULL suite and confirm the JaCoCo gate. If coverage falls below 70% after the SettingsPage deletion shifts the ratio, add one or two focused tests on the new template render paths (e.g., assert IndexPage with a non-empty zone list renders an OFFLINE-suffixed option) to restore the gate. Do not lower the gate.
  </action>
  <verify>
    <automated>./gradlew test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test` exits 0 (full suite green, includes jacocoTestCoverageVerification per build.gradle.kts finalizedBy)
    - WebAndDisplayRoutesTest no longer contains `settings/display` as a 200-expecting test
    - WebAndDisplayRoutesTest contains a `GET /history?zone=` test expecting 200
    - `src/test/kotlin/com/anjo/routing/WebRoutesTest.kt` exists and asserts `zoneSelect`, `effectPreview`, schedule `Zone`/`Webhook URL`, `status-uptime`/`Loading...`, `custom.css`, and `GET /settings/display` → 404
    - JaCoCo coverage verification passes at >= 0.70 (no gate lowering)
  </acceptance_criteria>
  <done>The integration suite proves the new SSR structure for index/schedule/status/nav, the settings route is gone (404), the history zone param is wired, and the full suite is green with the 70% coverage gate intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual verification of dark mode, mobile nav, effect preview, status live refresh</name>
  <what-built>
    The full MD3 UI refresh: side-nav layout (BaseLayout + custom.css), dark/light palette via prefers-color-scheme, mobile hamburger overlay nav, the Send Text effect-preview animation, and the /status page live-polling (10s). These behaviors are CSS- and timing-driven and cannot be asserted in the headless Ktor test host (VALIDATION Manual-Only table).
  </what-built>
  <how-to-verify>
    1. Start the app: `./gradlew run` (or the existing run task). Open `http://localhost:8080/` (adjust port if configured).
    2. Desktop dark mode (UI-02): with OS in dark mode, confirm the page background is the dark MD3 surface (`#1c1b1f`) and the side nav (240px) shows on the left with the active "Send Text" link highlighted. Inspect `<html>` — confirm there is NO `data-theme` attribute. Toggle OS to light mode and reload — confirm the palette switches to the light surface (`#fffbfe`).
    3. Side nav links (UI-03): confirm exactly 5 links — Send Text, Schedules, History, Zones, Status — and NO Settings link.
    4. Mobile hamburger (UI-01/03): resize the browser to < 960px (or device toolbar). Confirm the side nav is hidden and a top bar with `☰` appears. Tap `☰` → nav slides in over a dark backdrop. Tap the backdrop → nav closes.
    5. Effect preview (UI-04): on `/`, type text and confirm it mirrors into the preview box; change the effect select among Scroll/Blink/Reverse/Fade and confirm the preview animation changes accordingly.
    6. Status live refresh (UI-08): open `/status`. Confirm the `Loading...` spans are replaced with real uptime/memory/display/failure values within ~1s, and that values refresh (no full page reload) roughly every 10s.
    7. Zones tint (UI-07): if any auto-discovered zone exists, confirm its card uses the secondary-container tint (`.zone-card--discovered`).
  </how-to-verify>
  <resume-signal>Type "approved" if all visual checks pass, or describe the specific issues observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test host → module() | tests exercise the real application module over the Ktor test host; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-10 | (test-only) | new test files | accept | Test code only; no production attack surface added. Existing H2 in-memory test DB pattern reused. |
| T-13-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs — test files use existing Kotest/Ktor-test/H2 deps already on the classpath. No legitimacy gate needed. |
</threat_model>

<verification>
- `./gradlew test` exits 0 (full suite + JaCoCo 70% gate via finalizedBy)
- New/updated test files present and asserting the documented SSR structure
- Human checkpoint approved for the four manual-only visual behaviors
</verification>

<success_criteria>
- Zone filter proven at repository + service layers (UI-06)
- SSR structure proven: zone selector, schedule columns, status skeleton, no Settings link (UI-04/05/08/01/03)
- /history?zone=X wired without 500 (UI-06)
- /settings/display returns 404 (D-25)
- Full suite green, JaCoCo >= 70%
- Human-verified dark mode, hamburger nav, effect preview, status live refresh (UI-01/02/03/04/07/08)
</success_criteria>

<output>
Create `.planning/phases/13-ui-ux-refresh/13-04-SUMMARY.md` when done
</output>
