---
phase: 11-multi-zone-displays
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/com/anjo/model/ZoneStatus.kt
  - src/main/kotlin/com/anjo/zone/ZoneDriver.kt
  - src/main/kotlin/com/anjo/zone/LocalZoneDriver.kt
  - src/test/kotlin/com/anjo/zone/LocalZoneDriverTest.kt
autonomous: true
requirements: [ZONE-01]
must_haves:
  truths:
    - "LocalZoneDriver.send() delegates to the wrapped DisplayDriver and returns true on success"
    - "LocalZoneDriver.send() returns false (and the zone reports OFFLINE) when the wrapped driver throws"
    - "A LocalZoneDriver wrapping OfflineDisplayDriver reports status OFFLINE"
  artifacts:
    - path: "src/main/kotlin/com/anjo/zone/ZoneDriver.kt"
      provides: "Uniform zone abstraction interface"
      contains: "interface ZoneDriver"
    - path: "src/main/kotlin/com/anjo/zone/LocalZoneDriver.kt"
      provides: "DisplayDriver-backed zone driver"
      contains: "class LocalZoneDriver"
    - path: "src/main/kotlin/com/anjo/model/ZoneStatus.kt"
      provides: "Serializable zone status value object"
      contains: "data class ZoneStatus"
  key_links:
    - from: "src/main/kotlin/com/anjo/zone/LocalZoneDriver.kt"
      to: "com.anjo.driver.DisplayDriver"
      via: "constructor delegation"
      pattern: "DisplayDriver"
---

<objective>
Define the `ZoneDriver` abstraction (D-16) that gives `ZoneRegistry` a uniform `send(text, effect): Boolean` / `status(): ZoneStatus` contract over both local hardware and remote WebSocket connections, and implement `LocalZoneDriver` which wraps an already-constructed `DisplayDriver`. Also define the serializable `ZoneStatus` value object used by `GET /api/v1/zones`.

Purpose: Interface-first ordering — these contracts are consumed by `ZoneRegistry` (Plan 03) and `NetworkZoneDriver` (Plan 04). Defining them now means downstream plans receive the contract in the plan, not via a scavenger hunt.
Output: `ZoneDriver` interface, `LocalZoneDriver`, `ZoneStatus` model, and `LocalZoneDriverTest`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-multi-zone-displays/11-CONTEXT.md
@.planning/phases/11-multi-zone-displays/11-PATTERNS.md
</context>

<artifacts_this_phase_produces>
New symbols created in THIS plan (exclude from drift verification):
- `com.anjo.zone.ZoneDriver` (interface; `fun send(text: String, effect: Effect): Boolean`, `fun status(): ZoneStatus`)
- `com.anjo.zone.LocalZoneDriver` (class wrapping `DisplayDriver`)
- `com.anjo.model.ZoneStatus` (data class; fields `id`, `type`, `status`, `ip?`, `lastSeenAt?`)
- Test file `src/test/kotlin/com/anjo/zone/LocalZoneDriverTest.kt`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ZoneStatus model + ZoneDriver interface</name>
  <files>src/main/kotlin/com/anjo/model/ZoneStatus.kt, src/main/kotlin/com/anjo/zone/ZoneDriver.kt</files>
  <behavior>
    - ZoneStatus serializes to JSON with fields id, type, status, ip (omittable), lastSeenAt (omittable)
    - ZoneDriver exposes exactly two members: send(text, effect): Boolean and status(): ZoneStatus
  </behavior>
  <read_first>
    - src/main/kotlin/com/anjo/driver/DisplayDriver.kt (DisplayStatus data class + interface shape to mirror)
    - src/main/kotlin/com/anjo/model/TextRequest.kt (existing @Serializable model annotation style)
    - src/main/kotlin/com/anjo/model/Effect.kt (the Effect enum passed to send())
  </read_first>
  <action>
Create `com.anjo.model.ZoneStatus` as `@Serializable data class ZoneStatus(val id: String, val type: String, val status: String, val ip: String? = null, val lastSeenAt: String? = null)`. `status` is a String (`"ONLINE"` / `"OFFLINE"` / `"DEGRADED"`) not an enum — matches the DB string convention and PicoCSS badge contract in 11-UI-SPEC.md. Use the same kotlinx.serialization import as existing models.

Create `com.anjo.zone.ZoneDriver` interface in a new `com.anjo.zone` package: `fun send(text: String, effect: Effect): Boolean` and `fun status(): ZoneStatus` (import `com.anjo.model.Effect`, `com.anjo.model.ZoneStatus`). No default implementations — both `LocalZoneDriver` and `NetworkZoneDriver` implement fully (D-16).
  </action>
  <verify>
    <automated>./gradlew compileKotlin -x test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `ZoneStatus.kt` contains `data class ZoneStatus` annotated `@Serializable`
    - `ZoneStatus` has fields `id`, `type`, `status`, `ip`, `lastSeenAt` with `ip`/`lastSeenAt` nullable defaulting null
    - `ZoneDriver.kt` contains `interface ZoneDriver` with `fun send(text: String, effect: Effect): Boolean` and `fun status(): ZoneStatus`
    - `./gradlew compileKotlin -x test` exits 0
  </acceptance_criteria>
  <done>ZoneStatus and ZoneDriver compile; interface has exactly the two contract members.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LocalZoneDriver wrapping DisplayDriver + test</name>
  <files>src/main/kotlin/com/anjo/zone/LocalZoneDriver.kt, src/test/kotlin/com/anjo/zone/LocalZoneDriverTest.kt</files>
  <behavior>
    - send(text, SCROLL) on a healthy driver returns true and invokes the driver's render path
    - send() returns false when the wrapped driver throws
    - status() returns ZoneStatus.status == "ONLINE" when DisplayStatus.hardwareAvailable is true; "OFFLINE" when wrapping OfflineDisplayDriver
  </behavior>
  <read_first>
    - src/main/kotlin/com/anjo/driver/DisplayDriver.kt (scrollText/write/displayStatic/status signatures, DisplayStatus.hardwareAvailable)
    - src/main/kotlin/com/anjo/driver/OfflineDisplayDriver.kt (offline status shape to map to "OFFLINE")
    - src/main/kotlin/com/anjo/service/WebhookService.kt (catch + log + false-return error handling pattern, lines 47-52)
    - src/main/kotlin/com/anjo/zone/ZoneDriver.kt (interface created in Task 1)
    - src/test/kotlin/com/anjo/driver (any existing driver test for Kotest setup + mockk usage)
  </read_first>
  <action>
Create `com.anjo.zone.LocalZoneDriver(private val id: String, private val type: String, private val driver: DisplayDriver) : ZoneDriver`. `send(text, effect)`: wrap the driver call in try/catch — for `Effect.SCROLL` delegate to a scroll render via the driver, otherwise call `driver.displayStatic(text)` (use the driver's existing methods; do not duplicate effect logic — Plan 03's ScreenDriverService pipeline owns full effect rendering, LocalZoneDriver only needs a direct write path for the registry's broadcast/route fallback). On any exception, log a warning (WebhookService pattern) and return `false`. On success return `true`. `status()`: read `driver.status()` (DisplayStatus); map `hardwareAvailable == true` → ZoneStatus.status `"ONLINE"`, else `"OFFLINE"`; `ip = null` (local zones have no IP). Per D-04 (failed-init zone is OFFLINE via OfflineDisplayDriver) and D-05 (status from last render). Since `send` may be `suspend` if scroll delegation requires it, mark `send` consistent with the `ZoneDriver` interface signature defined in Task 1 — if a suspend modifier is needed, add it to the interface in Task 1's file as well so both stay in sync (prefer non-suspend `send` returning Boolean, using `driver.write()`/`displayStatic` synchronously where possible).

Create `src/test/kotlin/com/anjo/zone/LocalZoneDriverTest.kt` (Kotest `should`). Use mockk to supply a `DisplayDriver`: (1) healthy driver, `status()` returns `DisplayStatus(isActive=false, hardwareAvailable=true)` → `send()` true and `status().status == "ONLINE"`; (2) driver whose write throws → `send()` returns false; (3) `LocalZoneDriver` wrapping `OfflineDisplayDriver` → `status().status == "OFFLINE"`.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.anjo.zone.LocalZoneDriverTest" -x jacocoTestCoverageVerification 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `LocalZoneDriver.kt` contains `class LocalZoneDriver` implementing `ZoneDriver`
    - `LocalZoneDriver.send` is wrapped in try/catch and returns `false` on exception (grep shows `catch` and `return false` / `false`)
    - `LocalZoneDriver.status()` maps `hardwareAvailable` to `"ONLINE"`/`"OFFLINE"`
    - `./gradlew test --tests "com.anjo.zone.LocalZoneDriverTest"` exits 0 with 3 passing cases
  </acceptance_criteria>
  <done>LocalZoneDriver delegates to DisplayDriver, returns false on failure, reports ONLINE/OFFLINE correctly; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ZoneRegistry → LocalZoneDriver | text/effect routed to hardware (no untrusted input added here) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Denial of Service | LocalZoneDriver.send() throwing on hardware fault | mitigate | All driver calls wrapped in try/catch returning false; a faulty local zone cannot crash the registry or block other zones (D-04). ASVS V5 (graceful failure). |

ASVS controls satisfied: V5 (no new external input; defensive error handling). No new attack surface — this plan adds no routes or sockets.
</threat_model>

<verification>
- `./gradlew compileKotlin -x test` exits 0
- `./gradlew test --tests "com.anjo.zone.LocalZoneDriverTest"` exits 0
</verification>

<success_criteria>
- ZoneDriver contract defined and consumable by Plans 03/04
- LocalZoneDriver wraps DisplayDriver with OFFLINE fallback semantics
- ZoneStatus serializable for the zones API
</success_criteria>

<output>
Create `.planning/phases/11-multi-zone-displays/11-02-SUMMARY.md` when done
</output>
