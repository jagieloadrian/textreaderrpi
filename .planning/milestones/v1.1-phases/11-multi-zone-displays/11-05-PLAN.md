---
phase: 11-multi-zone-displays
plan: 05
type: execute
wave: 4
depends_on: [11-04]
files_modified:
  - src/main/kotlin/com/anjo/web/templates/ZonesPage.kt
  - src/main/kotlin/com/anjo/routing/ui/ZonesUIRoutes.kt
  - src/main/kotlin/com/anjo/web/templates/BaseLayout.kt
  - src/main/kotlin/com/anjo/routing/Routing.kt
  - src/main/resources/static/app.js
  - src/test/kotlin/com/anjo/routing/ZonesUIRoutesTest.kt
autonomous: false
requirements: [ZONE-06, ZONE-08]
must_haves:
  truths:
    - "GET /zones renders an HTML page listing each registered zone as a card with a status badge (ONLINE/OFFLINE/DEGRADED)"
    - "The /zones page contains a 'Scan for Displays' button that POSTs to /api/v1/zones/discover and an 'Add by IP' form that POSTs to /api/v1/zones/{ip}"
    - "The top nav contains a 'Zones' link between 'History' and 'Settings'"
    - "An empty zone list renders the 'No zones registered' empty state"
  artifacts:
    - path: "src/main/kotlin/com/anjo/web/templates/ZonesPage.kt"
      provides: "PicoCSS zones page template"
      contains: "fun FlowContent.zonesPage"
    - path: "src/main/kotlin/com/anjo/routing/ui/ZonesUIRoutes.kt"
      provides: "GET /zones HTML route"
      contains: "fun Route.zonesUIRoutes"
  key_links:
    - from: "src/main/kotlin/com/anjo/routing/ui/ZonesUIRoutes.kt"
      to: "ZoneRegistry.listAll"
      via: "view-model mapping into zonesPage"
      pattern: "zoneRegistry\\.listAll"
    - from: "src/main/resources/static/app.js"
      to: "/api/v1/zones/discover and /api/v1/zones/{ip}"
      via: "fetch handlers"
      pattern: "fetch\\(\"/api/v1/zones"
---

<objective>
Deliver the `/zones` HTML management page (11-UI-SPEC.md): a server-rendered PicoCSS page listing every registered zone with a status badge, a "Scan for Displays" button wired to `POST /api/v1/zones/discover`, and an "Add by IP" form wired to `POST /api/v1/zones/{ip}` (ZONE-06 UI). Adds the "Zones" nav link to `BaseLayout` and the two `app.js` fetch handlers.

Purpose: Gives the user a browser surface for manual zone registration and discovery (SC-4) and a live view of zone status (ZONE-08 UI consumer). This is the only HTML surface in Phase 11; the Material 3 refresh is deferred to Phase 13.
Output: `ZonesPage.kt`, `ZonesUIRoutes.kt`, BaseLayout nav update, Routing wiring, `app.js` handlers, and `ZonesUIRoutesTest`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-multi-zone-displays/11-UI-SPEC.md
@.planning/phases/11-multi-zone-displays/11-04-SUMMARY.md
</context>

<artifacts_this_phase_produces>
New symbols created in THIS plan (exclude from drift verification):
- `com.anjo.web.templates.zonesPage` (`FlowContent.(List<ZoneInfo>) -> Unit` extension)
- `com.anjo.web.templates.ZoneInfo` (or routing-layer view model: `id`, `type`, `isLocal`, `ipAddress?`, `status`, `discoveryMethod?`, `lastSeenAt?`)
- `com.anjo.routing.ui.zonesUIRoutes` (`fun Route.zonesUIRoutes(...)`)
- "Zones" nav `<li>` in BaseLayout
- `scanBtn` / `addZoneForm` handlers in app.js
- Test file `src/test/kotlin/com/anjo/routing/ZonesUIRoutesTest.kt`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: ZonesPage template + ZonesUIRoutes + BaseLayout nav + Routing wiring</name>
  <files>src/main/kotlin/com/anjo/web/templates/ZonesPage.kt, src/main/kotlin/com/anjo/routing/ui/ZonesUIRoutes.kt, src/main/kotlin/com/anjo/web/templates/BaseLayout.kt, src/main/kotlin/com/anjo/routing/Routing.kt, src/test/kotlin/com/anjo/routing/ZonesUIRoutesTest.kt</files>
  <read_first>
    - .planning/phases/11-multi-zone-displays/11-UI-SPEC.md (Component Inventory, Page Layout, Copywriting Contract, status badge markup, empty state)
    - src/main/kotlin/com/anjo/web/templates/HistoryPage.kt (FlowContent extension + PicoCSS article/section pattern)
    - src/main/kotlin/com/anjo/web/templates/BaseLayout.kt (nav <ul> lines 36-42; add Zones link between History and Settings)
    - src/main/kotlin/com/anjo/routing/ui/HistoryUIRoutes.kt (UI route + BaseLayout.render(pageTitle, activePath) pattern)
    - src/main/kotlin/com/anjo/routing/Routing.kt (UI route registration line 39 historyUIRoutes; by dependencies block — ZoneRegistry already provided by Plan 03/04)
    - src/main/kotlin/com/anjo/service/ZoneRegistry.kt (listAll(): List<ZoneStatus>)
    - src/main/kotlin/com/anjo/db/ZoneRepository.kt (findAll for discoveryMethod/lastSeenAt fields the registry status lacks)
  </read_first>
  <action>
Create `com.anjo.web.templates.ZonesPage.kt` — top-level `fun FlowContent.zonesPage(zones: List<ZoneInfo>)` mirroring `HistoryPage.kt`. Define the `ZoneInfo` view model (in the templates or routing layer) with `id, type, isLocal, ipAddress: String?, status, discoveryMethod: String?, lastSeenAt: String?` (11-UI-SPEC Implementation Note 5). Render per 11-UI-SPEC Page Layout: `h2 "Zones"`; a "Registered Zones" `section` with one PicoCSS `article` per zone (header: zone id bold, type label `"Local ({type})"` / `"Network ({type})"`, status badge as `<mark>` with the exact inline `style` from the spec — ONLINE `var(--pico-primary)`, OFFLINE `var(--pico-color-red-500, #c0392b)`, DEGRADED `var(--pico-color-orange-500, #e67e22)`; body: IP for network zones, discovery method, last seen). Empty state uses the exact copy "No zones registered" / spec body text. Add the "Discover Network Displays" section with `button#scanBtn` "Scan for Displays" + `div#scanResult`, and the "Add Display by IP" section with `form#addZoneForm` containing `input#ipInput[name=ip][placeholder="192.168.x.x"][required]` + submit "Add Zone" + `div#addZoneResult`. Use exact copy strings from the Copywriting Contract. Inline `style` ONLY on status badges (spec rule).

Create `com.anjo.routing.ui.ZonesUIRoutes.kt` — `fun Route.zonesUIRoutes(zoneRegistry: ZoneRegistry, zoneRepository: ZoneRepository)` following `HistoryUIRoutes`: `get("/zones") { ... }` builds the `ZoneInfo` list by combining `zoneRegistry.listAll()` (live status) with `zoneRepository.findAll()` (discoveryMethod, ip, lastSeenAt for network zones; local zones are isLocal=true with no ip). Render via `BaseLayout.render(pageTitle = "Zones — TextReaderRpi", activePath = "/zones") { zonesPage(zones) }` and `respondText(html, ContentType.Text.Html)`.

BaseLayout: add `li { a(href = "/zones") { attributes["aria-current"] = if (activePath == "/zones") "page" else ""; +"Zones" } }` between the History `<li>` (line 39) and the Settings `<li>` (line 40).

Routing: add `val zoneRegistry: ZoneRegistry by dependencies` if not already in scope and a `val zoneRepository: ZoneRepository by dependencies` lookup; register `zonesUIRoutes(zoneRegistry, zoneRepository)` next to `historyUIRoutes(...)` in the top-level `routing { }` block (not inside `/api/v1`). Keep this edit limited to adding the one registration line + needed `by dependencies` lookups to avoid clobbering Plan 04's `/api/v1` zoneRoutes line.

Create `src/test/kotlin/com/anjo/routing/ZonesUIRoutesTest.kt` (Ktor testApplication): GET /zones returns 200 + Content-Type text/html; body contains "Zones", the "Scan for Displays" label, the "Add Zone" label, and a status badge for a seeded zone; with no zones the body contains "No zones registered".
  </action>
  <verify>
    <automated>./gradlew test --tests "com.anjo.routing.ZonesUIRoutesTest" -x jacocoTestCoverageVerification 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `ZonesPage.kt` contains `fun FlowContent.zonesPage`
    - `ZonesPage.kt` renders `id="scanBtn"`, `id="addZoneForm"`, `id="ipInput"` and the exact empty-state copy "No zones registered"
    - status badge markup uses `<mark>` with `var(--pico-primary)` / `var(--pico-color-red-500` / `var(--pico-color-orange-500` (no hardcoded-only hex)
    - `BaseLayout.kt` contains `a(href = "/zones")` positioned between the `/history` and `/settings/display` links
    - `ZonesUIRoutes.kt` contains `fun Route.zonesUIRoutes` and calls `zoneRegistry.listAll`
    - `Routing.kt` registers `zonesUIRoutes(`
    - `./gradlew test --tests "com.anjo.routing.ZonesUIRoutesTest"` exits 0
  </acceptance_criteria>
  <done>/zones renders zone cards + badges + scan/add UI; nav link present; empty state works; route test green.</done>
</task>

<task type="auto">
  <name>Task 2: app.js scan + add-by-IP fetch handlers</name>
  <files>src/main/resources/static/app.js</files>
  <read_first>
    - src/main/resources/static/app.js (showToast helper lines 5-27; DOMContentLoaded boot block lines 200-219; existing fetch+toast handler style e.g. submitForm/applyDriver)
    - .planning/phases/11-multi-zone-displays/11-UI-SPEC.md (Interaction States table, busy-state "Scanning..." rule, inline result copy)
  </read_first>
  <action>
Add two handlers to `app.js` following the existing `submitForm`/`applyDriver` fetch+`showToast` style (no framework). `scanForDisplays()`: on `#scanBtn` click — disable the button, set its label to "Scanning..." (busy state per UI-SPEC), `fetch("/api/v1/zones/discover", { method: "POST" })`; on success write the spec copy into `#scanResult` ("Found {N} new display(s). Page reloading..." or "No new displays found...") and reload `/zones`; on error show "Scan failed. Check server connection."; always re-enable the button and restore its label. `addZoneByIp()`: on `#addZoneForm` submit (preventDefault) — read `#ipInput` value, `fetch("/api/v1/zones/" + encodeURIComponent(ip), { method: "POST" })`; 201/200 → "Zone added. Page reloading..." + reload; 409 → "A zone with this IP is already registered."; 400 → "Enter a valid IP address (e.g. 192.168.1.50)."; other failure → "Could not connect to {ip}. Verify the device is online." Wire both into the `DOMContentLoaded` boot block guarded by element existence (`if (document.getElementById("scanBtn"))` etc.) so other pages are unaffected.
  </action>
  <verify>
    <automated>node --check src/main/resources/static/app.js && grep -c "zones/discover" src/main/resources/static/app.js</automated>
  </verify>
  <acceptance_criteria>
    - `app.js` contains `fetch("/api/v1/zones/discover"` and `fetch("/api/v1/zones/"` (add-by-ip)
    - `app.js` sets a "Scanning..." busy label on the scan button
    - `app.js` handles 409 with the duplicate-zone copy and 400 with the invalid-IP copy
    - boot handlers are guarded by `getElementById` existence checks
    - `node --check src/main/resources/static/app.js` exits 0 (valid JS)
  </acceptance_criteria>
  <done>Scan and add-by-IP fetch handlers wired with busy state and spec copy; app.js parses clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The `/zones` management page: server-rendered zone cards with ONLINE/OFFLINE/DEGRADED status badges, a "Scan for Displays" button (POST /api/v1/zones/discover), and an "Add Display by IP" form (POST /api/v1/zones/{ip}), plus the "Zones" nav link. Claude has already verified the route returns 200 HTML and app.js parses; this checkpoint confirms the visual/interaction layer renders correctly in a browser.</what-built>
  <how-to-verify>
    1. Run the app: `./gradlew run` (or your usual local run command).
    2. Open `http://localhost:8080/zones` in a browser.
    3. Confirm the top nav shows "Zones" between "History" and "Settings", and the page heading reads "Zones".
    4. Confirm each registered zone renders as a card with a colored status badge (the default `main` local zone should appear). With no network zones yet, the network section/empty-state copy should read sensibly.
    5. Click "Scan for Displays" — the button label should change to "Scanning..." and re-enable after ~3 seconds; an inline result message appears (likely "No new displays found..." with no devices on the LAN).
    6. In "Add Display by IP", enter a public IP like `8.8.8.8` and submit — expect the invalid/validation error message (RFC1918 rejection). Enter a private IP like `192.168.1.50` and submit — expect "Zone added. Page reloading..." and the new zone card on reload.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what rendered incorrectly</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /zones (GET) | server-rendered HTML; zone identity strings reflected into the page |
| browser app.js → /api/v1/zones/* | client triggers discovery + manual add (validated server-side in Plan 04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-11 | XSS (Tampering) | zone id/ip/name rendered into ZonesPage HTML | mitigate | Rendering uses kotlinx.html DSL which HTML-escapes text content by default (same as HistoryPage/SchedulePage). Do not build raw HTML strings for zone fields. ASVS V5. |
| T-11-12 | Tampering (SSRF) | add-by-IP form | mitigate | Authoritative validation is server-side in ZoneRoutes (Plan 04, T-11-06 RFC1918). app.js client-side messaging is UX only and not a security control. |

ASVS controls satisfied: V5 (output encoding via kotlinx.html auto-escaping; server-side validation owns the trust decision).
</threat_model>

<verification>
- `./gradlew test --tests "com.anjo.routing.ZonesUIRoutesTest"` exits 0
- `node --check src/main/resources/static/app.js` exits 0
- `./gradlew test` full suite green (phase gate)
- Human checkpoint approved (visual + interaction)
</verification>

<success_criteria>
- /zones page lists zones with status badges and the scan/add controls (ZONE-08 + ZONE-06 UI)
- "Zones" nav link present
- Discovery + add-by-IP triggerable from the browser with correct busy state and error copy
- Human-verified rendering
</success_criteria>

<output>
Create `.planning/phases/11-multi-zone-displays/11-05-SUMMARY.md` when done
</output>
