---
phase: 09-display-history-audit-log
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/com/anjo/model/HistoryRecord.kt
  - src/main/kotlin/com/anjo/db/HistoryTable.kt
  - src/main/kotlin/com/anjo/db/HistoryRepository.kt
  - src/main/kotlin/com/anjo/db/DatabaseFactory.kt
  - src/main/resources/db/migration/V3__add_history_table.sql
  - src/test/kotlin/com/anjo/db/HistoryRepositoryTest.kt
autonomous: true
requirements: [HIST-01, HIST-02]
must_haves:
  truths:
    - "A HistoryRecord can be inserted and round-trips all seven columns from the display_history table"
    - "Inserting a 1001st record leaves the table at exactly 1000 rows, deleting the oldest by displayedAt"
    - "findPaginated returns the correct page slice plus a total count, newest first"
    - "findPaginated narrows by effect and by source before paginating"
  artifacts:
    - path: "src/main/kotlin/com/anjo/db/HistoryTable.kt"
      provides: "Exposed table object for display_history (D-06)"
      contains: "object HistoryTable : Table(\"display_history\")"
    - path: "src/main/kotlin/com/anjo/db/HistoryRepository.kt"
      provides: "insert with 1000-row cap (D-05) and findPaginated with filters (D-10..D-13)"
      contains: "class HistoryRepository"
    - path: "src/main/kotlin/com/anjo/model/HistoryRecord.kt"
      provides: "@Serializable HistoryRecord + HistoryPageResponse (D-11)"
      contains: "data class HistoryRecord"
    - path: "src/main/resources/db/migration/V3__add_history_table.sql"
      provides: "Flyway V3 migration creating display_history (D-09)"
      contains: "CREATE TABLE IF NOT EXISTS display_history"
    - path: "src/test/kotlin/com/anjo/db/HistoryRepositoryTest.kt"
      provides: "H2 in-memory coverage: insert round-trip, pruning, pagination, filters (D-24)"
      contains: "class HistoryRepositoryTest"
  key_links:
    - from: "src/main/kotlin/com/anjo/db/DatabaseFactory.kt"
      to: "HistoryTable"
      via: "SchemaUtils.create(HistoryTable) in the existing transaction block"
      pattern: "SchemaUtils\\.create\\(HistoryTable\\)"
    - from: "src/main/kotlin/com/anjo/db/HistoryRepository.kt"
      to: "HistoryTable"
      via: "suspendTransaction insert/selectAll/deleteWhere"
      pattern: "HistoryTable\\.(insert|selectAll|deleteWhere)"
---

<objective>
Build the persistence foundation for display history: the `HistoryRecord` model, the `HistoryTable` Exposed object, the `HistoryRepository` (insert with 1000-row cap + paginated/filtered query), the Flyway `V3` migration, the `DatabaseFactory` schema wiring, and full H2 in-memory repository tests.

Purpose: This is the data layer every other Phase 9 plan consumes. It is the only Wave 1 plan; both downstream plans (recording integration and API/UI) depend on the symbols it produces.
Output: New `HistoryRecord.kt`, `HistoryTable.kt`, `HistoryRepository.kt`, `V3__add_history_table.sql`; modified `DatabaseFactory.kt`; new `HistoryRepositoryTest.kt`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-display-history-audit-log/09-CONTEXT.md
@.planning/phases/09-display-history-audit-log/09-RESEARCH.md
@.planning/phases/09-display-history-audit-log/09-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

New symbols created by this plan (exclude from drift verification — they do not exist yet):

- `com.anjo.model.HistoryRecord` — `@Serializable data class` (id, text, effect, source, scheduleId?, zoneId?, displayedAt)
- `com.anjo.model.HistoryPageResponse` — `@Serializable data class` (items, page, size, total)
- `com.anjo.db.HistoryTable` — `object HistoryTable : Table("display_history")`
- `com.anjo.db.HistoryRepository` — `class` with `insert(record): HistoryRecord`, `findPaginated(page, size, effect?, source?): Pair<List<HistoryRecord>, Long>`, private `ResultRow.toHistoryRecord()`, `companion object { MAX_ROWS = 1000 }`
- `src/main/resources/db/migration/V3__add_history_table.sql` — Flyway migration file (new)
- `com.anjo.db.HistoryRepositoryTest` — Kotest `FunSpec` test class (new)

Modified existing symbol:
- `com.anjo.db.DatabaseFactory.init()` — add `SchemaUtils.create(HistoryTable)` line
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HistoryRecord model + HistoryTable + V3 migration + DatabaseFactory wiring</name>
  <files>
    src/main/kotlin/com/anjo/model/HistoryRecord.kt,
    src/main/kotlin/com/anjo/db/HistoryTable.kt,
    src/main/resources/db/migration/V3__add_history_table.sql,
    src/main/kotlin/com/anjo/db/DatabaseFactory.kt
  </files>
  <read_first>
    - src/main/kotlin/com/anjo/model/Schedule.kt (model + Effect enum + @Serializable pattern; Effect = SCROLL, BLINK, REVERSE, FADE)
    - src/main/kotlin/com/anjo/db/SchedulesTable.kt (Exposed Table object pattern: one val per column, override primaryKey)
    - src/main/resources/db/migration/V2__add_scheduler_columns.sql (Flyway file style: leading comment block, snake_case columns)
    - src/main/kotlin/com/anjo/db/DatabaseFactory.kt (current SchemaUtils.create(SchedulesTable) inside transaction block)
  </read_first>
  <behavior>
    - HistoryRecord serializes to JSON with fields id, text, effect, source, scheduleId, zoneId, displayedAt
    - HistoryTable column count is 7; primary key is id
    - V3 migration creates display_history with all seven columns and PRIMARY KEY (id)
    - DatabaseFactory.init() creates both schedules and display_history tables on startup
  </behavior>
  <action>
    Create `com.anjo.model.HistoryRecord` as a file containing TWO `@Serializable data class` declarations (import `kotlinx.serialization.Serializable`):
    `HistoryRecord(id: String = "", text: String, effect: String, source: String, scheduleId: String? = null, zoneId: String? = null, displayedAt: String = "")` and
    `HistoryPageResponse(items: List<HistoryRecord>, page: Int, size: Int, total: Long)`.
    Note: `effect` is `String` (NOT the `Effect` enum) so it round-trips through JSON without a `valueOf()` call (per D-06, D-11, RESEARCH Pattern 3).

    Create `com.anjo.db.HistoryTable` as `object HistoryTable : Table("display_history")` (import `org.jetbrains.exposed.v1.core.Table`), with columns per D-06: `id = varchar("id", 36)`, `text = text("text")`, `effect = varchar("effect", 16)`, `source = varchar("source", 16)`, `scheduleId = varchar("schedule_id", 36).nullable()`, `zoneId = varchar("zone_id", 64).nullable()`, `displayedAt = varchar("displayed_at", 32)`, and `override val primaryKey = PrimaryKey(id)`. Do NOT add `.default()` on the nullable columns (D-08 — nullable only).

    Create `src/main/resources/db/migration/V3__add_history_table.sql` with a leading comment block (matching V2 style) then `CREATE TABLE IF NOT EXISTS display_history (...)` with columns: `id VARCHAR(36) NOT NULL`, `text TEXT NOT NULL`, `effect VARCHAR(16) NOT NULL`, `source VARCHAR(16) NOT NULL`, `schedule_id VARCHAR(36) NULL`, `zone_id VARCHAR(64) NULL`, `displayed_at VARCHAR(32) NOT NULL`, `PRIMARY KEY (id)`. Use `CREATE TABLE IF NOT EXISTS` (safe for re-runs) — NOT `ALTER TABLE` (D-09, RESEARCH Pitfall 6). The `displayed_at` column is VARCHAR(32) because the repository stores `Instant.now().toString()` (ISO-8601), matching the createdAt/firedAt convention in SchedulesTable (D-07).

    Modify `com.anjo.db.DatabaseFactory.init()`: add `SchemaUtils.create(HistoryTable)` immediately after the existing `SchemaUtils.create(SchedulesTable)` line inside the same `transaction {}` block. Add the `import com.anjo.db.HistoryTable` only if not implied by package membership (same package — no import needed). Do NOT change to `createMissingTablesAndColumns` (RESEARCH anti-pattern). No code comments (project rule).
  </action>
  <verify>
    <automated>cd /home/diether18/IdeaProjects/TextReaderRpi && ./gradlew compileKotlin -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - HistoryRecord.kt contains `data class HistoryRecord(` and `data class HistoryPageResponse(`
    - HistoryRecord.kt declares `val effect: String` (not `Effect`) — verify: `grep -n "val effect: String" src/main/kotlin/com/anjo/model/HistoryRecord.kt` returns a match
    - HistoryTable.kt contains `object HistoryTable : Table("display_history")` and `override val primaryKey = PrimaryKey(id)`
    - HistoryTable.kt contains exactly 7 column `val` declarations (id, text, effect, source, scheduleId, zoneId, displayedAt)
    - V3__add_history_table.sql contains `CREATE TABLE IF NOT EXISTS display_history` and `PRIMARY KEY (id)` and `displayed_at VARCHAR(32)`
    - DatabaseFactory.kt contains `SchemaUtils.create(HistoryTable)` — verify: `grep -c "SchemaUtils.create(HistoryTable)" src/main/kotlin/com/anjo/db/DatabaseFactory.kt` returns 1
    - `./gradlew compileKotlin` exits 0
  </acceptance_criteria>
  <done>Model, table, migration, and DatabaseFactory wiring exist and compile; display_history schema is defined in both Flyway V3 and SchemaUtils.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HistoryRepository (insert + 1000-row cap + findPaginated) with H2 tests</name>
  <files>
    src/main/kotlin/com/anjo/db/HistoryRepository.kt,
    src/test/kotlin/com/anjo/db/HistoryRepositoryTest.kt
  </files>
  <read_first>
    - src/main/kotlin/com/anjo/db/ScheduleRepository.kt (insert with UUID+Instant pattern, suspendTransaction, ResultRow mapper, deleteWhere; note where{} then andWhere{} chaining is NOT used here — findAllActive uses where{A and B})
    - src/main/kotlin/com/anjo/db/HistoryTable.kt (column references created in Task 1)
    - src/main/kotlin/com/anjo/model/HistoryRecord.kt (HistoryRecord shape created in Task 1)
    - src/test/kotlin/com/anjo/db/ScheduleRepositoryTest.kt (H2 in-memory FunSpec scaffold: Database.connect, beforeSpec SchemaUtils.create, beforeEach deleteWhere, runTest, shouldBe)
  </read_first>
  <behavior>
    - insert() persists all seven columns; returns the record with generated id and displayedAt populated (round-trip)
    - When row count reaches 1000, insert() deletes the single oldest row (ORDER BY displayed_at ASC) before inserting, leaving the table at 1000 after the 1001st insert
    - findPaginated(1, 20) returns up to 20 newest-first records and the total count
    - findPaginated page 2 returns a different slice than page 1
    - findPaginated(effect="SCROLL") returns only SCROLL records; findPaginated(source="IMMEDIATE") returns only IMMEDIATE records
  </behavior>
  <action>
    Create `com.anjo.db.HistoryRepository` as a `class` (NEVER `object` — repositories are injectable instances, RESEARCH anti-pattern). Imports use `org.jetbrains.exposed.v1.*` only (never `org.jetbrains.exposed.sql.*`).

    `suspend fun insert(record: HistoryRecord): HistoryRecord`: generate `newId = UUID.randomUUID().toString()` and `now = Instant.now().toString()` (ISO-8601 string, D-07 — same convention as SchedulesTable createdAt/firedAt). Inside a single `suspendTransaction {}` (per D-05, atomicity — count check + delete + insert MUST share one transaction to avoid TOCTOU): `val count = HistoryTable.selectAll().count()`; `if (count >= MAX_ROWS)` find the oldest row via `HistoryTable.selectAll().orderBy(HistoryTable.displayedAt to SortOrder.ASC).limit(1).single()[HistoryTable.id]` then `HistoryTable.deleteWhere { HistoryTable.id eq oldest }`; then `HistoryTable.insert { it[id] = newId; it[text] = record.text; it[effect] = record.effect; it[source] = record.source; it[scheduleId] = record.scheduleId; it[zoneId] = record.zoneId; it[displayedAt] = now }`. Return `record.copy(id = newId, displayedAt = now)`. Add `companion object { private const val MAX_ROWS = 1000 }`.

    `suspend fun findPaginated(page: Int, size: Int, effect: String? = null, source: String? = null): Pair<List<HistoryRecord>, Long>` wrapped in `suspendTransaction {}`: build `var query = HistoryTable.selectAll()`; if `effect != null` apply `query = query.where { HistoryTable.effect eq effect }`; if `source != null` apply `andWhere` when effect already filtered else `where` (per RESEARCH Code Examples filter-chaining). Compute `val total = query.count()`. Compute `val items = query.orderBy(HistoryTable.displayedAt to SortOrder.DESC).limit(size, offset = ((page - 1).toLong() * size)).map { it.toHistoryRecord() }` — offset MUST cast to Long via `.toLong()` to avoid Int overflow (T-09-04). Return `Pair(items, total)`.

    Add private `fun ResultRow.toHistoryRecord(): HistoryRecord` mapping each column to the record field.

    Create `com.anjo.db.HistoryRepositoryTest` as a Kotest `FunSpec`. Use `Database.connect("jdbc:h2:mem:test_history;DB_CLOSE_DELAY=-1", driver = "org.h2.Driver")` in `beforeSpec` + `transaction { SchemaUtils.create(HistoryTable) }`; `beforeEach { transaction { HistoryTable.deleteWhere { HistoryTable.id.isNotNull() } } }`. Wrap suspend calls in `runTest {}`; assertions use `shouldBe`. Tests (per D-24): (a) insert round-trips all columns including scheduleId/zoneId; (b) pruning — insert 1000 rows, total == 1000, insert 1001st, total still == 1000; (c) findPaginated page 1 vs page 2 return different first-item ids; (d) findPaginated(effect = "SCROLL") returns only SCROLL; (e) findPaginated(source = "IMMEDIATE") returns only IMMEDIATE. No code comments (project rule).
  </action>
  <verify>
    <automated>cd /home/diether18/IdeaProjects/TextReaderRpi && ./gradlew test --tests "com.anjo.db.HistoryRepositoryTest" -x jacocoTestReport 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - HistoryRepository.kt contains `class HistoryRepository` (not `object`) — verify: `grep -c "class HistoryRepository" src/main/kotlin/com/anjo/db/HistoryRepository.kt` returns 1
    - HistoryRepository.kt contains `private const val MAX_ROWS = 1000`
    - HistoryRepository.kt contains `.toLong()` on the offset calculation — verify: `grep -n "toLong()" src/main/kotlin/com/anjo/db/HistoryRepository.kt` returns a match
    - HistoryRepository.kt uses only `org.jetbrains.exposed.v1.` imports — verify: `grep -c "org.jetbrains.exposed.sql" src/main/kotlin/com/anjo/db/HistoryRepository.kt` (excluding comments) returns 0
    - HistoryRepositoryTest.kt uses `jdbc:h2:mem:test_history`
    - `./gradlew test --tests "com.anjo.db.HistoryRepositoryTest"` exits 0 with the pruning, pagination, and filter tests passing
  </acceptance_criteria>
  <done>HistoryRepository inserts with cap and queries with pagination/filters; all repository tests green against real H2 in-memory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application → H2/PostgreSQL | Repository writes user-supplied text and queries with filter values |
| disk (SD card on Pi) | Unbounded table growth would fill the SD card |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | HistoryRepository.findPaginated filter args (effect/source) | mitigate | Exposed DSL `where { column eq value }` uses parameterized queries — never string-interpolates values into SQL (RESEARCH Security Domain, [VERIFIED: ScheduleRepository same pattern]) |
| T-09-02 | Denial of Service | HistoryRepository.insert unbounded growth | mitigate | D-05 — 1000-row cap enforced inside the same suspendTransaction{} on every insert; oldest row deleted before insert |
| T-09-04 | Tampering | offset = (page-1)*size integer overflow | mitigate | `.toLong()` cast on offset before passing to `limit(size, offset)` — prevents Int overflow for large page/size |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced in Phase 9; all libraries (Exposed 1.3.0, Flyway 9.22.3, H2, Kotest) are existing project dependencies verified in gradle/ktor-libs.versions.toml (RESEARCH Package Legitimacy Audit — none added) |

Residual risk: T-09-01 relies on Exposed never being bypassed with raw SQL — no raw SQL is used in this plan (LOW).
</threat_model>

<verification>
- `./gradlew test --tests "com.anjo.db.HistoryRepositoryTest" -x jacocoTestReport` exits 0
- `./gradlew compileKotlin` exits 0
- `grep -c "SchemaUtils.create(HistoryTable)" src/main/kotlin/com/anjo/db/DatabaseFactory.kt` returns 1
- `ls src/main/resources/db/migration/V3__add_history_table.sql` exists
</verification>

<success_criteria>
- display_history table is defined in both Flyway V3 and DatabaseFactory SchemaUtils
- HistoryRepository.insert() persists records and enforces the 1000-row cap atomically (D-05)
- HistoryRepository.findPaginated() returns newest-first paginated slices with effect/source filters and a total count
- HistoryRepositoryTest covers insert round-trip, pruning at 1001, pagination, and both filters — all green
</success_criteria>

<output>
Create `.planning/phases/09-display-history-audit-log/09-01-SUMMARY.md` when done
</output>
