---
status: complete
phase: 11.2-code-quality-cleanup
source: [11.2-01-SUMMARY.md, 11.2-02-SUMMARY.md, 11.2-03-SUMMARY.md, 11.2-04-SUMMARY.md]
started: 2026-06-16T00:00:00Z
updated: 2026-06-17T11:02:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Full Test Suite Passes
expected: Run ./gradlew test from the project root. All tests pass with BUILD SUCCESSFUL. No compilation errors, no test failures. The JaCoCo 70% coverage gate is met.
result: issue
reported: "BUILD SUCCESSFUL but took 2m 33s — should be under a few seconds. runBlocking antipattern visible in tests."
severity: major

### 2. Invalid IP Returns 422
expected: POST /zone with an invalid (non-RFC1918) IP address in the request body returns HTTP 422 Unprocessable Entity. Previously this returned 400. Example body: {"ip":"1.2.3.4"} or {"ip":"not-an-ip"}.
result: pass

### 3. Invalid Display Type Returns 422
expected: POST /display/select with an invalid driver type in the request body returns HTTP 422 Unprocessable Entity. Previously this returned 400. Example body: {"type":"INVALID_TYPE"}.
result: skipped
reason: deferred by user

### 4. History Routes Still Work via HistoryService
expected: GET /history (or equivalent history endpoint) returns results correctly now that it routes through HistoryService instead of directly through HistoryRepository. No change in response format — the service layer is transparent to the API consumer.
result: skipped
reason: deferred by user

### 5. queueDisplaySwitch Behaviour Fixed
expected: queueDisplaySwitch("MAX7219") returns true (known type). queueDisplaySwitch("UNKNOWN_GARBAGE") returns false. Previously it always returned false. This is testable via the service unit test in ./gradlew test --tests "com.anjo.service.*" or through the display switch API flow.
result: skipped
reason: deferred by user

### 6. runBlocking removed from ZoneDriver hot path
expected: |
  grep -rn 'runBlocking' src/main/kotlin/com/anjo/zone/ → no output.
  grep -n 'runBlocking' src/main/kotlin/com/anjo/service/ZoneRegistry.kt → only line 48 (constructor).
result: pass

### 7. ZoneDriver.send() is suspend — tests use coEvery
expected: ./gradlew test --tests "com.anjo.zone.*" → BUILD SUCCESSFUL. NetworkZoneDriverTest and LocalZoneDriverTest pass with coEvery mocks.
result: pass

### 8. Full test suite green post-suspend propagation
expected: ./gradlew test → BUILD SUCCESSFUL. All 10 files changed by plan 04 compile and test correctly. JaCoCo 70% gate met.
result: pass

## Summary

total: 8
passed: 4
issues: 1
pending: 0
skipped: 3
blocked: 0

## Gaps

- truth: "No runBlocking antipattern in coroutine-context code paths"
  status: resolved
  reason: "User reported: runBlocking antipattern visible in tests."
  severity: major
  test: 1
  root_cause: |
    runBlocking was used in 3 production locations:
    1. NetworkZoneDriver.send() — wraps suspend WebSocketSession.send(), blocking IO thread.
    2. ZoneRegistry.broadcast() — wraps async dispatch in runBlocking.
    3. ZoneRegistry constructor (line 48) — acceptable startup-only usage; retained.
    Root: ZoneDriver.send() was non-suspend, forcing implementors to use runBlocking.
  fix_applied: "plan 04 — commits 6dadefa, 88e0a8f — ZoneDriver.send() made suspend, propagated through NetworkZoneDriver, LocalZoneDriver, ZoneRegistry.broadcast/route; constructor runBlocking retained as acceptable."
  verified_by: [test-6, test-7, test-8]
