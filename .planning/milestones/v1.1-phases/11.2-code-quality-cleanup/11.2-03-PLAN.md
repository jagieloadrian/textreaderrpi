---
phase: 11.2-code-quality-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/com/anjo/service/NetworkDiscoveryService.kt
  - src/main/kotlin/com/anjo/service/SchedulerService.kt
  - src/main/kotlin/com/anjo/service/ScreenDriverService.kt
  - src/main/kotlin/com/anjo/service/WebhookService.kt
autonomous: true
requirements: [REF-11.2]
must_haves:
  truths:
    - "No method in NetworkDiscoveryService, SchedulerService, ScreenDriverService, or WebhookService exceeds 30 lines"
    - "Nested launch/try/if blocks are flattened into named private functions"
    - "Service behaviour is unchanged — all existing service tests still pass"
  artifacts:
    - path: "src/main/kotlin/com/anjo/service/NetworkDiscoveryService.kt"
      provides: "startMdnsListener + buildNetworkZone private funs"
      contains: "private fun startMdnsListener"
    - path: "src/main/kotlin/com/anjo/service/SchedulerService.kt"
      provides: "checkExpiry + checkMaxRuns private funs"
      contains: "checkExpiry"
    - path: "src/main/kotlin/com/anjo/service/ScreenDriverService.kt"
      provides: "withMutex + acquireMutex private funs"
      contains: "withMutex"
    - path: "src/main/kotlin/com/anjo/service/WebhookService.kt"
      provides: "executePost private suspend fun"
      contains: "executePost"
  key_links:
    - from: "src/main/kotlin/com/anjo/service/WebhookService.kt"
      to: "executePost"
      via: "scope.launch { executePost(...) }"
      pattern: "scope\\.launch \\{ executePost"
---

<objective>
Reduce nesting in the four service classes by extracting named private functions, so no method exceeds 30 lines and the nested launch/try/if pyramids are flattened. Implements D-15, D-16, D-17, D-18 (and the discretionary D-10 in ScreenDriverService).

Purpose: Address the user's explicit requests "nie lubie mieć ifologii w kodzie" and "podziel na mniejsze funkcje" without changing any behaviour (REF-11.2).
Output: Private helper functions extracted in NetworkDiscoveryService, SchedulerService, ScreenDriverService, and WebhookService; existing service tests remain green.
</objective>

<artifacts_this_phase_produces>
Symbols and files created or changed by THIS plan (exclude from drift verification):
- `NetworkDiscoveryService.startMdnsListener()` — private fun extracted from `start()`
- `NetworkDiscoveryService.buildNetworkZone(ip, method, name, type)` — private fun shared by onDeviceDiscovered + parseDiscoveryReply
- `SchedulerService.checkExpiry(schedule): Boolean` — private fun
- `SchedulerService.checkMaxRuns(runs, schedule): Boolean` — private fun
- `ScreenDriverService.withMutex(mutex, alreadyLocked, block)` — private suspend fun
- `ScreenDriverService.acquireMutex(zoneId, policy): Mutex?` — private fun
- `WebhookService.executePost(url, payload)` — private suspend fun
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.2-code-quality-cleanup/11.2-CONTEXT.md
@.planning/phases/11.2-code-quality-cleanup/11.2-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flatten NetworkDiscoveryService and SchedulerService nesting</name>
  <files>src/main/kotlin/com/anjo/service/NetworkDiscoveryService.kt, src/main/kotlin/com/anjo/service/SchedulerService.kt</files>
  <read_first>
    - src/main/kotlin/com/anjo/service/NetworkDiscoveryService.kt (start() has nested scope.launch { try { JmDNS.create; addServiceListener { serviceResolved { scope.launch { onDeviceDiscovered } } } } }; onDeviceDiscovered builds NetworkZone; parseDiscoveryReply builds NetworkZone — duplicated NetworkZone construction)
    - src/main/kotlin/com/anjo/service/SchedulerService.kt (launchRecurring while-loop body has nested if(expiresAt!=null){try{Instant.parse}catch{updateStatus ERROR; break}} and if(maxRuns!=null && runs>=maxRuns) break)
    - src/test/kotlin/com/anjo/service/NetworkDiscoveryServiceTest.kt (exercises testOnDeviceDiscovered seam — keep that internal seam working)
    - src/test/kotlin/com/anjo/service/SchedulerServiceTest.kt (covers recurring/oneshot/cron fire behaviour — must stay green)
  </read_first>
  <action>
    NetworkDiscoveryService (D-15): extract `private fun startMdnsListener()` containing the body currently inside `scope.launch(Dispatchers.IO) { ... }` of `start()` (the JmDNS.create + addServiceListener registration + try/catch). `start()` becomes `scope.launch(Dispatchers.IO) { startMdnsListener() }` — collapsing the outer launch to a single flat call. Inside the ServiceListener.serviceResolved, keep the inner `scope.launch { onDeviceDiscovered(...) }` (it dispatches the suspend call). Extract `private fun buildNetworkZone(ip: String, method: String, name: String?, type: String = DisplayType.MAX7219.name): NetworkZone` that constructs the `NetworkZone(...)` value, and call it from BOTH `onDeviceDiscovered` (after computing sanitised id/name) and `parseDiscoveryReply`. Preserve the existing id/name sanitisation rules in onDeviceDiscovered (regex `[^a-zA-Z0-9._-]` → "-", take(64), fall back to ip) and parseDiscoveryReply's regex name/type extraction — only the final NetworkZone construction moves into buildNetworkZone. Keep `testOnDeviceDiscovered` internal seam unchanged.
    SchedulerService (D-16): extract `private fun checkExpiry(schedule: Schedule): Boolean` returning true when the schedule should stop (expiresAt present, parses, and Instant.now() is after it; on parse failure persist `repository.updateStatus(schedule.id, "ERROR")` and return true). Because updateStatus is suspend, declare it `private suspend fun checkExpiry(...)`. Extract `private fun checkMaxRuns(runs: Int, schedule: Schedule): Boolean` returning `schedule.maxRuns != null && runs >= schedule.maxRuns`. In `launchRecurring` the while body becomes: `delay(intervalMs.milliseconds)`; `if (checkExpiry(schedule)) break`; `if (checkMaxRuns(runs, schedule)) break`; `val displayed = fire(schedule)`; `if (displayed) runs++`. Keep the surrounding `repository.updateStatus(schedule.id, "DONE")` + `activeJobs.remove` after the loop. Do not change launchOneShot/launchCron/fire behaviour.
    Add NO comments (D-19).
  </action>
  <verify>
    <automated>cd /home/diether18/IdeaProjects/TextReaderRpi && ./gradlew test --tests "com.anjo.service.NetworkDiscoveryServiceTest" --tests "com.anjo.service.SchedulerServiceTest" -q</automated>
  </verify>
  <acceptance_criteria>
    - `NetworkDiscoveryService.kt` contains `private fun startMdnsListener` and `buildNetworkZone`
    - `SchedulerService.kt` contains `checkExpiry` and `checkMaxRuns`
    - `SchedulerService.launchRecurring` while-body contains `if (checkExpiry(schedule)) break` and `if (checkMaxRuns(runs, schedule)) break`
    - No method body exceeds 30 lines in either file (manual line-count check per Manual-Only Verification in VALIDATION.md)
    - `./gradlew test --tests "com.anjo.service.NetworkDiscoveryServiceTest" --tests "com.anjo.service.SchedulerServiceTest"` exits 0
  </acceptance_criteria>
  <done>NetworkDiscoveryService start() flattened with startMdnsListener + shared buildNetworkZone; SchedulerService launchRecurring uses checkExpiry/checkMaxRuns; both service tests green with unchanged behaviour.</done>
</task>

<task type="auto">
  <name>Task 2: Flatten ScreenDriverService mutex handling and WebhookService send</name>
  <files>src/main/kotlin/com/anjo/service/ScreenDriverService.kt, src/main/kotlin/com/anjo/service/WebhookService.kt</files>
  <read_first>
    - src/main/kotlin/com/anjo/service/ScreenDriverService.kt (renderImmediate + runScheduledRender both have if(alreadyLocked){block}else{mutex.withLock{block}}; displayImmediate + displayScheduled both compute zoneMutex via mutexes.getOrPut and do SKIP_NEW tryLock; queueDisplaySwitch(displayType: String): Boolean currently returns false)
    - src/main/kotlin/com/anjo/service/WebhookService.kt (send() launches scope.launch { try { withTimeout(5_000.ms) { buildPayload + httpClient.post } } catch ... })
    - src/test/kotlin/com/anjo/service/ScreenDriverRecoveryTest.kt (recovery/retry behaviour)
    - src/test/kotlin/com/anjo/service/ScreenDriverResourceTest.kt (mutex/concurrency behaviour)
    - src/test/kotlin/com/anjo/service/ConflictPolicyTest.kt (SKIP_NEW tryLock semantics — must not regress)
    - src/test/kotlin/com/anjo/service/WebhookServiceTest.kt (uses real delay(200ms) not virtual clock — withTimeout must stay inside scope.launch; Pitfall 7)
    - src/test/kotlin/com/anjo/service/HistoryRecordingTest.kt (tryInsertHistory still called inside the locked block)
  </read_first>
  <action>
    ScreenDriverService (D-17): extract `private suspend fun withMutex(mutex: Mutex, alreadyLocked: Boolean, block: suspend () -> Unit)` that runs `if (alreadyLocked) block() else mutex.withLock { block() }`. Refactor `renderImmediate` and `runScheduledRender` to call `withMutex(mutex, alreadyLocked) { executeWithRecovery(...); tryInsertHistory(...) }` instead of duplicating the branch. IMPORTANT: preserve the `finally { if (alreadyLocked) mutex.unlock() }` semantics — keep the manual unlock in the finally block of each caller exactly as it is now (withMutex only covers the non-finally locking path). In runScheduledRender keep `displaySucceeded = true` set inside the block after executeWithRecovery. Extract `private fun acquireMutex(zoneId: String?, policy: ConflictPolicy): Mutex?` that returns the mutex from `mutexes.getOrPut(zoneId ?: "broadcast") { Mutex() }`, and when policy == SKIP_NEW attempts `tryLock()` returning null if the lock is already held (caller treats null as "busy → drop"). Refactor `displayImmediate` and `displayScheduled` to call `acquireMutex` and branch on null (immediate → DisplayResult.Accepted(false); scheduled → return false). Preserve the existing log.info SKIP_NEW messages. Do NOT change the broadcast path or history-insertion behaviour. (D-10) implement `queueDisplaySwitch(displayType: String): Boolean` to compute `DisplayType.fromString(displayType) != DisplayType.UNKNOWN` and return that boolean (validation has already passed at the route layer; this keeps the method honest). Add `import com.anjo.model.DisplayType`.
    WebhookService (D-18, Pitfall 7): extract `private suspend fun executePost(url: String, payload: WebhookPayload)` containing the `withTimeout(5_000.milliseconds) { httpClient.post(url) { header ContentType.Application.Json; setBody(payload) }; if (!response.status.isSuccess()) log.warn(...) }` plus the existing TimeoutCancellationException / Exception catches. `send()` becomes: `val url = resolveUrl(schedule) ?: return`; `scope.launch { executePost(url, buildPayload(schedule, firedAt)) }`. The `withTimeout` MUST stay inside `executePost` which is called from within `scope.launch` — do not move it outside the launch. Keep willSend/resolveUrl/buildPayload/stop/companion create unchanged.
    Add NO comments (D-19).
  </action>
  <verify>
    <automated>cd /home/diether18/IdeaProjects/TextReaderRpi && ./gradlew test --tests "com.anjo.service.ScreenDriver*" --tests "com.anjo.service.ConflictPolicyTest" --tests "com.anjo.service.WebhookServiceTest" --tests "com.anjo.service.HistoryRecordingTest" -q</automated>
  </verify>
  <acceptance_criteria>
    - `ScreenDriverService.kt` contains `private suspend fun withMutex` and `acquireMutex`
    - `ScreenDriverService.kt` `queueDisplaySwitch` contains `DisplayType.fromString`
    - `WebhookService.kt` contains `private suspend fun executePost` and `scope.launch { executePost`
    - `WebhookService.kt` still contains `withTimeout` inside executePost (grep `withTimeout` returns ≥1)
    - No method body exceeds 30 lines in either file (manual line-count check per VALIDATION.md)
    - `./gradlew test --tests "com.anjo.service.ScreenDriver*" --tests "com.anjo.service.ConflictPolicyTest" --tests "com.anjo.service.WebhookServiceTest" --tests "com.anjo.service.HistoryRecordingTest"` exits 0
  </acceptance_criteria>
  <done>ScreenDriverService uses withMutex/acquireMutex helpers with preserved unlock/SKIP_NEW semantics and a real queueDisplaySwitch; WebhookService.send delegates to executePost with withTimeout inside the launch; all service tests green.</done>
</task>

</tasks>

<verification>
- `./gradlew test --tests "com.anjo.service.*"` passes (full service-layer suite).
- Manual line-count: open each of the four files and confirm no method body exceeds 30 lines (REF-11.2 success criterion 1; VALIDATION.md Manual-Only Verification).
- Behaviour preserved: SKIP_NEW drop semantics, webhook timeout, history insert inside lock, recurring expiry/maxRuns all unchanged.
</verification>

<success_criteria>
- D-15, D-16, D-17, D-18 implemented exactly; D-10 queueDisplaySwitch honoured.
- No method in the four services exceeds 30 lines.
- No behaviour change — all existing service tests green.
- No comments added (D-19).
</success_criteria>

<output>
Create `.planning/phases/11.2-code-quality-cleanup/11.2-03-SUMMARY.md` when done.
</output>
