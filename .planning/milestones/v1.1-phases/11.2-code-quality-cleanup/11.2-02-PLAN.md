---
phase: 11.2-code-quality-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/com/anjo/service/HistoryService.kt
  - src/main/kotlin/com/anjo/routing/HistoryRoutes.kt
  - src/main/kotlin/com/anjo/routing/ui/HistoryUIRoutes.kt
  - src/main/kotlin/com/anjo/routing/Routing.kt
  - src/main/kotlin/com/anjo/di/DependencyInjection.kt
  - src/test/kotlin/com/anjo/ApplicationTest.kt
autonomous: true
requirements: [REF-11.2]
must_haves:
  truths:
    - "historyRoutes and historyUIRoutes receive a HistoryService, not a HistoryRepository"
    - "No route file imports or calls HistoryRepository directly"
    - "HistoryService resolves from DI; HistoryRepository remains DI-registered for tests"
    - "GET /api/v1/history and GET /history continue to return correct paginated/filtered data"
  artifacts:
    - path: "src/main/kotlin/com/anjo/service/HistoryService.kt"
      provides: "Service wrapping HistoryRepository.findPaginated"
      contains: "class HistoryService"
  key_links:
    - from: "src/main/kotlin/com/anjo/routing/HistoryRoutes.kt"
      to: "HistoryService"
      via: "historyService.findPaginated"
      pattern: "historyService\\.findPaginated"
    - from: "src/main/kotlin/com/anjo/di/DependencyInjection.kt"
      to: "HistoryService"
      via: "provide { historyService }"
      pattern: "provide \\{ historyService"
---

<objective>
Introduce a `HistoryService` layer between the history route handlers and `HistoryRepository`, so routes no longer call the repository directly. Implements D-11 through D-14.

Purpose: Enforce the project rule "routes i controllery nie powinny wołać bezpośrednio do repository" for both the API and UI history routes (REF-11.2).
Output: New `HistoryService` class; `historyRoutes` and `historyUIRoutes` re-typed to receive `HistoryService`; `Routing.kt` and `DependencyInjection.kt` rewired; `HistoryRepository` binding retained for tests; `ApplicationTest` DI smoke test extended to resolve `HistoryService`.
</objective>

<artifacts_this_phase_produces>
Symbols and files created or changed by THIS plan (exclude from drift verification):
- `com.anjo.service.HistoryService` — new `class HistoryService(private val repository: HistoryRepository)` with `suspend fun findPaginated(page, size, effect, source): Pair<List<HistoryRecord>, Long>`
- `historyRoutes(historyService: HistoryService)` — signature changed from `HistoryRepository`
- `historyUIRoutes(historyService: HistoryService)` — signature changed from `HistoryRepository`
- New DI binding `provide { historyService }` in `DependencyInjection.kt`
- New `getBlocking<HistoryService>` assertion in `ApplicationTest.kt`
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.2-code-quality-cleanup/11.2-CONTEXT.md
@.planning/phases/11.2-code-quality-cleanup/11.2-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HistoryService and re-point history routes through it</name>
  <files>src/main/kotlin/com/anjo/service/HistoryService.kt, src/main/kotlin/com/anjo/routing/HistoryRoutes.kt, src/main/kotlin/com/anjo/routing/ui/HistoryUIRoutes.kt</files>
  <read_first>
    - src/main/kotlin/com/anjo/db/HistoryRepository.kt (findPaginated signature: page: Int, size: Int, effect: String? = null, source: String? = null → Pair<List<HistoryRecord>, Long>)
    - src/main/kotlin/com/anjo/routing/HistoryRoutes.kt (current historyRoutes(historyRepository) — calls historyRepository.findPaginated)
    - src/main/kotlin/com/anjo/routing/ui/HistoryUIRoutes.kt (current historyUIRoutes(historyRepository) — calls historyRepository.findPaginated, MAX_UI_SIZE handling)
  </read_first>
  <action>
    Create `src/main/kotlin/com/anjo/service/HistoryService.kt` (D-11): package `com.anjo.service`; `class HistoryService(private val repository: HistoryRepository)` with `suspend fun findPaginated(page: Int, size: Int, effect: String? = null, source: String? = null): Pair<List<HistoryRecord>, Long> = repository.findPaginated(page, size, effect, source)`. Import `com.anjo.db.HistoryRepository` and `com.anjo.model.HistoryRecord`. Class, not interface (Claude's discretion confirmed in CONTEXT).
    `HistoryRoutes.kt` (D-13): change `fun Route.historyRoutes(historyRepository: HistoryRepository)` to `fun Route.historyRoutes(historyService: HistoryService)`. Replace the `historyRepository.findPaginated(...)` call with `historyService.findPaginated(...)`. Remove `import com.anjo.db.HistoryRepository`; add `import com.anjo.service.HistoryService`. Keep all query-param parsing logic unchanged.
    `HistoryUIRoutes.kt` (D-12): change `fun Route.historyUIRoutes(historyRepository: HistoryRepository)` to `fun Route.historyUIRoutes(historyService: HistoryService)`. Replace `historyRepository.findPaginated(...)` with `historyService.findPaginated(...)`. Remove `import com.anjo.db.HistoryRepository`; add `import com.anjo.service.HistoryService`. Keep MAX_UI_SIZE, expand, and filter logic unchanged.
    Add NO comments to any file (D-19).
  </action>
  <verify>
    <automated>cd /home/diether18/IdeaProjects/TextReaderRpi && ./gradlew compileKotlin -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/kotlin/com/anjo/service/HistoryService.kt` contains `class HistoryService` and `repository.findPaginated`
    - `HistoryRoutes.kt` contains `historyService.findPaginated` and does NOT contain `import com.anjo.db.HistoryRepository`
    - `HistoryUIRoutes.kt` contains `historyService.findPaginated` and does NOT contain `import com.anjo.db.HistoryRepository`
    - `grep -rl "com.anjo.db.HistoryRepository" src/main/kotlin/com/anjo/routing/` returns no files
    - `./gradlew compileKotlin` exits 0
  </acceptance_criteria>
  <done>HistoryService created; both history route functions receive HistoryService and import no repository; main source compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Wire HistoryService in DI and Routing, retain HistoryRepository binding, extend DI smoke test</name>
  <files>src/main/kotlin/com/anjo/di/DependencyInjection.kt, src/main/kotlin/com/anjo/routing/Routing.kt, src/test/kotlin/com/anjo/ApplicationTest.kt</files>
  <read_first>
    - src/main/kotlin/com/anjo/di/DependencyInjection.kt (historyRepository instantiated line ~34; dependencies{} block provides historyRepository; forward-reference ordering — historyRepository before screenDriverService)
    - src/main/kotlin/com/anjo/routing/Routing.kt (resolves historyRepository by dependencies; calls historyUIRoutes(historyRepository) and historyRoutes(historyRepository))
    - src/test/kotlin/com/anjo/ApplicationTest.kt (the "should resolve all configureDI bindings" test uses getBlocking<HistoryRepository>; HistoryRepository must STAY resolvable — Pitfall 3)
    - src/test/kotlin/com/anjo/routing/HistoryRoutesTest.kt (uses getBlocking<HistoryRepository> to seed data — binding must remain)
    - src/test/kotlin/com/anjo/routing/HistoryUIRoutesTest.kt (uses getBlocking<HistoryRepository> to seed data — binding must remain)
  </read_first>
  <action>
    `DependencyInjection.kt` (D-14, Pitfall 3): after `val historyRepository = HistoryRepository()`, add `val historyService = HistoryService(historyRepository)`. In the `dependencies {}` block add `provide { historyService }` and KEEP the existing `provide { historyRepository }` (tests resolve HistoryRepository directly). Add `import com.anjo.service.HistoryService`.
    `Routing.kt`: add `val historyService: HistoryService by dependencies`. Change `historyUIRoutes(historyRepository)` to `historyUIRoutes(historyService)` and `historyRoutes(historyRepository)` to `historyRoutes(historyService)`. Remove the now-unused `val historyRepository: HistoryRepository by dependencies` declaration and its `import com.anjo.db.HistoryRepository` ONLY IF no other call site in Routing.kt uses historyRepository (it does not). Add `import com.anjo.service.HistoryService`.
    `ApplicationTest.kt` (Pitfall 3, A1): in the "should resolve all configureDI bindings" test, add `deps.getBlocking<HistoryService>(DependencyKey<HistoryService>()) shouldNotBeNull {}`. Keep the existing `getBlocking<HistoryRepository>` assertion. Add `import com.anjo.service.HistoryService`.
    Add NO comments to any file (D-19).
  </action>
  <verify>
    <automated>cd /home/diether18/IdeaProjects/TextReaderRpi && ./gradlew test --tests "com.anjo.ApplicationTest" --tests "com.anjo.routing.HistoryRoutesTest" --tests "com.anjo.routing.HistoryUIRoutesTest" -q</automated>
  </verify>
  <acceptance_criteria>
    - `DependencyInjection.kt` contains `val historyService = HistoryService(historyRepository)` and both `provide { historyService }` and `provide { historyRepository }`
    - `Routing.kt` contains `historyRoutes(historyService)` and `historyUIRoutes(historyService)`
    - `Routing.kt` does NOT contain `historyRoutes(historyRepository)` or `historyUIRoutes(historyRepository)`
    - `ApplicationTest.kt` contains `getBlocking<HistoryService>` and still contains `getBlocking<HistoryRepository>`
    - `./gradlew test --tests "com.anjo.ApplicationTest" --tests "com.anjo.routing.HistoryRoutesTest" --tests "com.anjo.routing.HistoryUIRoutesTest"` exits 0
  </acceptance_criteria>
  <done>HistoryService registered in DI and wired into both route registrations; HistoryRepository binding retained; DI smoke test resolves HistoryService; history route + UI tests green.</done>
</task>

</tasks>

<verification>
- `./gradlew test --tests "com.anjo.routing.History*" --tests "com.anjo.ApplicationTest"` passes.
- `grep -rn "HistoryRepository" src/main/kotlin/com/anjo/routing/` returns no results.
- HistoryRepository remains resolvable from DI (ApplicationTest + HistoryRoutesTest seeding via getBlocking still works).
</verification>

<success_criteria>
- D-11 through D-14 implemented exactly.
- No history route file references `HistoryRepository`.
- DI provides both `HistoryService` and `HistoryRepository`.
- No comments added (D-19).
</success_criteria>

<output>
Create `.planning/phases/11.2-code-quality-cleanup/11.2-02-SUMMARY.md` when done.
</output>
