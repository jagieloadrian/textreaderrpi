---
phase: 03-production-ready
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/com/anjo/service/ScreenDriver.kt
  - src/main/kotlin/com/anjo/service/RecoveryPolicy.kt
  - src/main/kotlin/com/anjo/di/DependencyInjection.kt
  - src/test/kotlin/com/anjo/service/RecoveryPolicyTest.kt
  - src/test/kotlin/com/anjo/service/ScreenDriverRecoveryTest.kt
autonomous: true
requirements:
  - R3-ERROR-RECOVERY
must_haves:
  truths:
    - "Transient driver failures recover automatically."
    - "Permanent failures fail fast with explicit error signaling."
  artifacts:
    - "src/main/kotlin/com/anjo/service/RecoveryPolicy.kt"
    - "src/test/kotlin/com/anjo/service/ScreenDriverRecoveryTest.kt"
  key_links:
    - "ScreenDriver.kt -> RecoveryPolicy.kt for retries/backoff"
---

<objective>
Add robust error recovery in the service layer for display-driving operations.

Purpose: Prevent transient faults from taking down core operations.
Output: Recovery policy + service integration + tests.
</objective>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/03-production-ready/03-RESEARCH.md
@src/main/kotlin/com/anjo/service/ScreenDriver.kt
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement reusable recovery policy</name>
  <read_first>
    src/main/kotlin/com/anjo/service/ScreenDriver.kt
    .planning/phases/03-production-ready/03-RESEARCH.md
  </read_first>
  <action>Create RecoveryPolicy in service layer with bounded retries, jitter/backoff, and typed failure classification. Add unit tests for retryable vs non-retryable errors and max-attempt behavior.</action>
  <verify>
    <automated>./gradlew test --tests "*RecoveryPolicyTest"</automated>
  </verify>
  <acceptance_criteria>Retry policy behavior is deterministic and fully covered by tests for success-after-retry and terminal failure.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate recovery into ScreenDriver operations</name>
  <read_first>
    src/main/kotlin/com/anjo/service/ScreenDriver.kt
    src/main/kotlin/com/anjo/di/DependencyInjection.kt
  </read_first>
  <action>Refactor ScreenDriver to route hardware operations through RecoveryPolicy while keeping business logic in service layer. Add integration-style tests covering transient fault recovery and non-retryable failure propagation.</action>
  <verify>
    <automated>./gradlew test --tests "*ScreenDriverRecoveryTest"</automated>
  </verify>
  <acceptance_criteria>Transient failures recover within configured attempts; terminal failures surface actionable errors without infinite retry loops.</acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| service -> hardware driver | External device behavior is unreliable/untrusted |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-03 | DoS | retry loops | mitigate | Bounded attempts and timeout ceilings |
| T-03-04 | Tampering | error mapping | mitigate | Typed exception classes and explicit handling branches |
| T-03-SC | Tampering | dependency installs | accept | No new packages in this plan |
</threat_model>

<verification>
Run `./gradlew test --tests "*Recovery*"` and full `./gradlew test`.
</verification>

<success_criteria>
Error recovery is in place with bounded retry behavior and test-proven failure handling.
</success_criteria>

<output>
Create `.planning/phases/03-production-ready/03-02-SUMMARY.md` when done.
</output>

