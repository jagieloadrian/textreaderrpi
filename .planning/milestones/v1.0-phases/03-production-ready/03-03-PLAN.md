---
phase: 03-production-ready
plan: 03
type: execute
wave: 2
depends_on:
  - 03-02
files_modified:
  - src/main/kotlin/com/anjo/service/ScreenDriver.kt
  - src/main/kotlin/com/anjo/service/ResourceTracker.kt
  - src/main/resources/application.yaml
  - src/test/kotlin/com/anjo/service/ResourceTrackerTest.kt
  - src/test/kotlin/com/anjo/service/ScreenDriverResourceTest.kt
autonomous: true
requirements:
  - R3-MEMORY-RESOURCE-OPTIMIZATION
must_haves:
  truths:
    - "Long-running process does not leak memory/resources during repeated operations."
    - "Resource usage has enforceable limits and cleanup semantics."
  artifacts:
    - "src/main/kotlin/com/anjo/service/ResourceTracker.kt"
    - "src/test/kotlin/com/anjo/service/ScreenDriverResourceTest.kt"
  key_links:
    - "ScreenDriver.kt -> ResourceTracker for acquire/release lifecycle"
---

<objective>
Optimize memory and resource lifecycle for stable long-running production operation.

Purpose: Prevent degradation and crashes from leaks or unbounded allocations.
Output: Resource tracker, bounded buffering, cleanup tests.
</objective>

<context>
@.planning/phases/03-production-ready/03-PATTERNS.md
@src/main/kotlin/com/anjo/service/ScreenDriver.kt
@src/main/resources/application.yaml
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Replace ResourceTracker runtime instrumentation with Ktor metrics</name>
  <read_first>
    src/main/kotlin/com/anjo/service/ScreenDriver.kt
    .planning/phases/03-production-ready/03-PATTERNS.md
  </read_first>
  <action>Replace `ScreenDriver` runtime instrumentation based on `ResourceTracker` with Ktor metrics (`ktorLibs.server.metrics` / Dropwizard). Add tests for in-flight counter release and failure metrics.</action>
  <verify>
    <automated>./gradlew test --tests "*ResourceTrackerTest"</automated>
  </verify>
  <acceptance_criteria>Runtime instrumentation is metrics-based, with deterministic counter/timer behavior in normal and exceptional paths.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply optimization in ScreenDriver runtime path</name>
  <read_first>
    src/main/kotlin/com/anjo/service/ScreenDriver.kt
    src/main/resources/application.yaml
  </read_first>
  <action>Refactor ScreenDriver to use bounded buffers/queues and tracker-backed cleanup. Add config keys in application.yaml for safe resource limits and tests validating repeated operation stability.</action>
  <verify>
    <automated>./gradlew test --tests "*ScreenDriverResourceTest"</automated>
  </verify>
  <acceptance_criteria>Repeated execution remains within configured bounds and no unreleased resources are detected by tests.</acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| service runtime -> JVM/process resources | Unbounded usage can degrade availability |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-05 | DoS | memory/handles | mitigate | Bounded allocations and mandatory release hooks |
| T-03-06 | Repudiation | cleanup behavior | mitigate | Structured logs on acquire/release/failure cleanup |
| T-03-SC | Tampering | dependency installs | accept | No new packages in this plan |
</threat_model>

<verification>
Run `./gradlew test --tests "*Resource*"` and memory-stability test suite.
</verification>

<success_criteria>
Service layer has bounded resource behavior and cleanup guarantees validated by tests.
</success_criteria>

<output>
Create `.planning/phases/03-production-ready/03-03-SUMMARY.md` when done.
</output>

