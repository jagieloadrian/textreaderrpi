---
phase: 09-display-history-audit-log
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/main/kotlin/com/anjo/service/ScreenDriverService.kt
  - src/main/kotlin/com/anjo/service/SchedulerService.kt
  - src/main/kotlin/com/anjo/di/DependencyInjection.kt
  - src/test/kotlin/com/anjo/ApplicationTest.kt
  - src/test/kotlin/com/anjo/service/HistoryRecordingTest.kt
autonomous: true
requirements: [HIST-01]
must_haves:
  truths:
    - "After displayImmediate() succeeds, a record with source=IMMEDIATE is written to display_history"
    - "After displayScheduled() succeeds, a record with source=SCHEDULED and the schedule id is written"
    - "A SKIP_NEW request that is dropped (display busy) writes no history record"
    - "If historyRepository.insert() throws, displayImmediate() still returns true (display path not broken)"
    - "HistoryRepository resolves from the DI container in the application smoke test"
  artifacts:
    - path: "src/main/kotlin/com/anjo/service/ScreenDriverService.kt"
      provides: "history recording after executeWithRecovery in both display paths (D-01..D-04)"
      contains: "historyRepository"
    - path: "src/main/kotlin/com/anjo/di/DependencyInjection.kt"
      provides: "HistoryRepository instantiation + provide{} + injection into ScreenDriverService (D-22)"
      contains: "provide { historyRepository }"
    - path: "src/test/kotlin/com/anjo/service/HistoryRecordingTest.kt"
      provides: "integration coverage: immediate records, scheduled records, SKIP_NEW does not, DB error non-fatal (D-24)"
      contains: "class HistoryRecordingTest"
  key_links:
    - from: "src/main/kotlin/com/anjo/service/ScreenDriverService.kt"
      to: "HistoryRepository.insert"
      via: "try/catch insert after executeWithRecovery inside displayMutex.withLock"
      pattern: "historyRepository\\?\\.insert"
    - from: "src/main/kotlin/com/anjo/di/DependencyInjection.kt"
      to: "ScreenDriverService"
      via: "historyRepository constructor argument"
      pattern: "historyRepository = historyRepository"
    - from: "src/main/kotlin/com/anjo/service/SchedulerService.kt"
      to: "ScreenDriverService.displayScheduled"
      via: "pass schedule.effect as the new effect parameter"
      pattern: "displayScheduled\\("
---

<objective>
Wire history recording into the display lifecycle: add a nullable `HistoryRepository` constructor param to `ScreenDriverService`, insert a record after `executeWithRecovery()` succeeds in both `displayImmediate()` and `displayScheduled()` (swallowing insert failures), thread the `Effect` through `displayScheduled()` so the scheduled record can store the effect name, register `HistoryRepository` in DI and inject it, extend the DI smoke test, and add integration tests.

Purpose: This is the HIST-01 "every display event is recorded" requirement. Recording is observability — D-03 mandates a DB hiccup must never break the display path.
Output: Modified `ScreenDriverService.kt`, `SchedulerService.kt`, `DependencyInjection.kt`, `ApplicationTest.kt`; new `HistoryRecordingTest.kt`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-display-history-audit-log/09-CONTEXT.md
@.planning/phases/09-display-history-audit-log/09-RESEARCH.md
@.planning/phases/09-display-history-audit-log/09-PATTERNS.md
@.planning/phases/09-display-history-audit-log/09-01-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

New symbols created by this plan:

- `com.anjo.service.HistoryRecordingTest` — Kotest `FunSpec` integration test class (new)

Consumes (created in Plan 01 — do NOT flag as drift):
- `com.anjo.db.HistoryRepository`, `com.anjo.model.HistoryRecord`

Modified existing symbols:
- `com.anjo.service.ScreenDriverService` — add `historyRepository: HistoryRepository? = null` constructor param; add `effect: Effect` param to `displayScheduled()`; insert calls in both display paths
- `com.anjo.service.SchedulerService.fire()` — pass `schedule.effect` to `displayScheduled()`
- `com.anjo.di.DependencyInjection.configureDI()` — `val historyRepository = HistoryRepository()`, inject into `ScreenDriverService(...)`, `provide { historyRepository }`
- `com.anjo.ApplicationTest` — add `HistoryRepository` DI resolution assertion + import
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ScreenDriverService recording + displayScheduled effect param + SchedulerService call site</name>
  <files>
    src/main/kotlin/com/anjo/service/ScreenDriverService.kt,
    src/main/kotlin/com/anjo/service/SchedulerService.kt
  </files>
  <read_first>
    - src/main/kotlin/com/anjo/service/ScreenDriverService.kt (current constructor params; displayImmediate INTERRUPT path lines 78-90 and SKIP_NEW path lines 50-70; displayScheduled INTERRUPT path lines 128-144 and SKIP_NEW path lines 100-124; executeWithRecovery sets success)
    - src/main/kotlin/com/anjo/service/SchedulerService.kt (fire() at line 170: effectFactory.create(schedule.effect) then displayScheduled(schedule.text, schedule.id, renderer, policy) — schedule.effect IS available here)
    - src/main/kotlin/com/anjo/service/effect/EffectRenderer.kt (CONFIRMED: EffectRenderer is an INTERFACE with only render(); it has NO effect property — so the effect name must be threaded as a parameter, not read from the renderer)
    - src/main/kotlin/com/anjo/model/HistoryRecord.kt (HistoryRecord shape — created in Plan 01)
    - src/main/kotlin/com/anjo/model/Schedule.kt (Effect enum: SCROLL, BLINK, REVERSE, FADE)
  </read_first>
  <behavior>
    - displayImmediate() (both INTERRUPT and SKIP_NEW paths) inserts a HistoryRecord with source="IMMEDIATE" and effect=effect.name after executeWithRecovery() succeeds, inside the lock
    - displayScheduled() (both paths) inserts a HistoryRecord with source="SCHEDULED", scheduleId=scheduleId, effect=effect.name after the display succeeds, inside the lock
    - A dropped SKIP_NEW request (tryLock fails, early return false) inserts NO record
    - An exception from historyRepository.insert() is caught and logged at WARN; the display method still returns its normal Boolean
  </behavior>
  <action>
    In `ScreenDriverService`: add `private val historyRepository: HistoryRepository? = null` as the LAST constructor parameter (nullable with default null per D-01 + RESEARCH Pitfall 5 — keeps the 4 existing direct-construction tests compiling). Add `import com.anjo.db.HistoryRepository`.

    Add an `effect: Effect` parameter to `displayScheduled(...)`. Because `EffectRenderer` is an interface with no `effect` property (CONFIRMED at implementation time), the effect name MUST come from a parameter — choose RESEARCH Pitfall 1 option (b): new signature `displayScheduled(text: String, scheduleId: String, renderer: EffectRenderer, effect: Effect, conflictPolicy: ConflictPolicy = ConflictPolicy.INTERRUPT)`. Place `effect` before `conflictPolicy` so the default-arg callers (if any) still resolve; update all call sites accordingly.

    In `displayImmediate()`: after `executeWithRecovery(text, effectFactory.create(effect))` succeeds, insert history. There are TWO success sites — the SKIP_NEW branch (currently around line 58, inside the `try` after `executeWithRecovery`) and the INTERRUPT branch (currently around line 80, inside `displayMutex.withLock {}`). In BOTH, immediately after the `executeWithRecovery(...)` call returns normally and still inside the held lock, run: `try { historyRepository?.insert(HistoryRecord(text = text, effect = effect.name, source = "IMMEDIATE")) } catch (e: Exception) { log.warn("History insert failed (non-fatal): ${e.message}", e) }`. The dropped SKIP_NEW case (early `return false` when `tryLock()` fails) records nothing — D-04.

    In `displayScheduled()`: after `executeWithRecovery(text, renderer)` and `displaySucceeded = true`, in BOTH the SKIP_NEW branch (around line 110-111) and the INTERRUPT branch (around line 130-132), still inside the held lock, run: `try { historyRepository?.insert(HistoryRecord(text = text, effect = effect.name, source = "SCHEDULED", scheduleId = scheduleId)) } catch (e: Exception) { log.warn("History insert failed (non-fatal): ${e.message}", e) }`. Do NOT insert on the CancellationException path or the dropped SKIP_NEW path (D-02, D-04).

    In `SchedulerService.fire()` (line ~175): change the call to `screenService.displayScheduled(schedule.text, schedule.id, renderer, schedule.effect, policy)` — passing `schedule.effect` as the new effect argument.

    No code comments anywhere (project rule). No validation logic in the service (project rule — validation stays in route handlers only).
  </action>
  <verify>
    <automated>cd /home/diether18/IdeaProjects/TextReaderRpi && ./gradlew compileKotlin compileTestKotlin -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - ScreenDriverService.kt constructor contains `historyRepository: HistoryRepository? = null`
    - ScreenDriverService.kt contains `historyRepository?.insert(` — verify: `grep -c "historyRepository?.insert(" src/main/kotlin/com/anjo/service/ScreenDriverService.kt` returns 4 (two immediate paths + two scheduled paths)
    - ScreenDriverService.kt contains `source = "IMMEDIATE"` and `source = "SCHEDULED"`
    - displayScheduled signature contains `effect: Effect` — verify: `grep -n "effect: Effect" src/main/kotlin/com/anjo/service/ScreenDriverService.kt` returns a match in the displayScheduled signature
    - SchedulerService.kt calls `displayScheduled(schedule.text, schedule.id, renderer, schedule.effect, policy)` — verify: `grep -c "schedule.effect, policy" src/main/kotlin/com/anjo/service/SchedulerService.kt` returns 1
    - `./gradlew compileKotlin compileTestKotlin` exits 0 (the 4 existing tests that construct ScreenDriverService directly still compile because historyRepository defaults to null)
  </acceptance_criteria>
  <done>Both display paths record history after success and swallow insert failures; displayScheduled threads the effect through; SchedulerService passes schedule.effect; everything compiles including existing service tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DI registration + DI smoke test + HistoryRecordingTest</name>
  <files>
    src/main/kotlin/com/anjo/di/DependencyInjection.kt,
    src/test/kotlin/com/anjo/ApplicationTest.kt,
    src/test/kotlin/com/anjo/service/HistoryRecordingTest.kt
  </files>
  <read_first>
    - src/main/kotlin/com/anjo/di/DependencyInjection.kt (current ScreenDriverService(...) construction; scheduleRepository = ScheduleRepository() position; DatabaseFactory.init() call; dependencies{ provide{} } block)
    - src/test/kotlin/com/anjo/ApplicationTest.kt (DI smoke test "should resolve all configureDI bindings"; getBlocking<T>(DependencyKey<T>()) shouldNotBeNull pattern; import list)
    - src/main/kotlin/com/anjo/service/ScreenDriverService.kt (updated constructor + display methods from Task 1)
    - src/main/kotlin/com/anjo/db/HistoryRepository.kt (created in Plan 01)
  </read_first>
  <behavior>
    - configureDI() constructs a HistoryRepository, injects it into ScreenDriverService, and provides it to the DI container
    - The DI smoke test resolves HistoryRepository without error
    - displayImmediate() through a real wired app writes a record retrievable via HistoryRepository.findPaginated
    - displayScheduled() writes a record with source=SCHEDULED
    - A dropped SKIP_NEW request writes no record
    - An insert failure does not make displayImmediate() return false or throw
  </behavior>
  <action>
    In `DependencyInjection.configureDI()`: add `val historyRepository = HistoryRepository()` following the SAME position as `val scheduleRepository = ScheduleRepository()` (RESEARCH Pitfall 2 — follow the existing established ordering even though it sits before `DatabaseFactory.init()`; the framework defers DB access until first request). Add `import com.anjo.db.HistoryRepository`. Add `historyRepository = historyRepository` as the LAST argument to the `ScreenDriverService(...)` constructor call. Add `provide { historyRepository }` inside the `dependencies {}` block alongside `provide { scheduleRepository }` (D-22).

    In `ApplicationTest.kt`: add `import com.anjo.db.HistoryRepository`, and inside the "should resolve all configureDI bindings without error" test add one assertion: `deps.getBlocking<HistoryRepository>(DependencyKey<HistoryRepository>()) shouldNotBeNull {}` (D-23).

    Create `com.anjo.service.HistoryRecordingTest` as a Kotest `FunSpec` using the `testApplication { application { module() } }` real-H2 pattern (D-24 — no mock repositories). After triggering `client.get("/health")` to start the module, resolve `screenDriverService` and `historyRepository` from `application.dependencies` via `getBlocking`. Tests: (a) call `displayImmediate("rec-test", Effect.SCROLL)`, then assert `findPaginated(1, 50)` total >= 1 and contains a record with text "rec-test" and source "IMMEDIATE"; (b) call `displayScheduled("sched-test", "sched-id-1", ScrollEffect(), Effect.SCROLL)`, then assert a record with source "SCHEDULED" and scheduleId "sched-id-1" exists; (c) issue two concurrent displays with `ConflictPolicy.SKIP_NEW` where one is dropped, and assert the dropped one produced no extra record (assert total count matches only the rendered displays); (d) construct a `ScreenDriverService` with a `historyRepository` stub whose `insert` throws (an anonymous subclass / fake that throws), call `displayImmediate(...)`, and assert it returns `true` (D-03 non-fatal). Use `runTest {}` for suspend calls; assertions use `shouldBe`/`shouldContain`. No code comments (project rule).
  </action>
  <verify>
    <automated>cd /home/diether18/IdeaProjects/TextReaderRpi && ./gradlew test --tests "com.anjo.service.HistoryRecordingTest" --tests "com.anjo.ApplicationTest" -x jacocoTestReport 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - DependencyInjection.kt contains `val historyRepository = HistoryRepository()` and `provide { historyRepository }` and `historyRepository = historyRepository`
    - ApplicationTest.kt contains `getBlocking<HistoryRepository>(DependencyKey<HistoryRepository>())`
    - HistoryRecordingTest.kt contains `class HistoryRecordingTest` and uses `testApplication`
    - HistoryRecordingTest.kt asserts a dropped SKIP_NEW request produces no record and that an insert exception does not break the return value
    - `./gradlew test --tests "com.anjo.service.HistoryRecordingTest" --tests "com.anjo.ApplicationTest"` exits 0
  </acceptance_criteria>
  <done>HistoryRepository is wired through DI and injected into ScreenDriverService; the DI smoke test resolves it; recording integration is proven against a real wired application including the SKIP_NEW-no-record and insert-failure-non-fatal cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP route → ScreenDriverService | User-supplied display text flows into the recording path |
| ScreenDriverService → HistoryRepository → DB | Service writes a record after each successful render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Denial of Service | historyRepository.insert() exception propagating into display path | mitigate | D-03 — insert wrapped in try/catch, logged at WARN; display method returns normally even when insert throws (proven by HistoryRecordingTest case d) |
| T-09-06 | Tampering | stored display text (user input persisted verbatim) | accept | Text is stored as a parameterized column value (Exposed DSL) and is HTML-escaped only at render time in Plan 03; storage layer holds raw text intentionally for audit fidelity. No PII, trusted home network (REQUIREMENTS out-of-scope: auth) |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced; all libraries are existing project dependencies (RESEARCH Package Legitimacy Audit — none added) |

Residual risk: a malformed effect name reaching `effect.name` is impossible — `effect` is a compile-time `Effect` enum, not free text (LOW).
</threat_model>

<verification>
- `./gradlew test --tests "com.anjo.service.HistoryRecordingTest" --tests "com.anjo.ApplicationTest" -x jacocoTestReport` exits 0
- `grep -c "historyRepository?.insert(" src/main/kotlin/com/anjo/service/ScreenDriverService.kt` returns 4
- `grep -c "provide { historyRepository }" src/main/kotlin/com/anjo/di/DependencyInjection.kt` returns 1
- `./gradlew compileKotlin compileTestKotlin` exits 0 (existing service tests unbroken)
</verification>

<success_criteria>
- Successful immediate and scheduled displays write history records with correct source/effect/scheduleId (HIST-01)
- Dropped SKIP_NEW requests write nothing (D-04)
- History insert failures never break the display path (D-03)
- HistoryRepository is resolvable from DI and covered by the smoke test (D-22, D-23)
</success_criteria>

<output>
Create `.planning/phases/09-display-history-audit-log/09-02-SUMMARY.md` when done
</output>
