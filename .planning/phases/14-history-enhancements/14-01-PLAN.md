---
phase: 14-history-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gradle/ktor-libs.versions.toml
  - build.gradle.kts
  - src/main/kotlin/com/anjo/model/HistoryRecord.kt
  - src/main/kotlin/com/anjo/validation/HistoryValidators.kt
  - src/main/kotlin/com/anjo/db/HistoryRepository.kt
  - src/main/kotlin/com/anjo/service/HistoryService.kt
  - src/test/kotlin/com/anjo/validation/HistoryValidatorsTest.kt
  - src/test/kotlin/com/anjo/db/HistoryRepositoryTest.kt
  - src/test/kotlin/com/anjo/service/HistoryServiceTest.kt
autonomous: true
requirements: [HIST-04, HIST-05]
must_haves:
  truths:
    - "HistoryRepository.findPaginated filters records whose lowercased text contains the lowercased search term (case-insensitive)"
    - "HistoryValidators.sanitizeSearchTerm strips % and _ characters from input"
    - "A search term that becomes blank after sanitization applies no filter (returns all rows)"
    - "HistoryService.exportCsv produces an RFC 4180 CSV string with the D-10 header row"
    - "CSV fields containing commas are quoted per RFC 4180"
  artifacts:
    - path: "src/main/kotlin/com/anjo/model/HistoryFilter.kt"
      provides: "HistoryFilter data class shared by repository and service"
      contains: "data class HistoryFilter"
    - path: "src/main/kotlin/com/anjo/validation/HistoryValidators.kt"
      provides: "sanitizeSearchTerm wildcard stripping"
      contains: "object HistoryValidators"
    - path: "src/main/kotlin/com/anjo/db/HistoryRepository.kt"
      provides: "findPaginated(filter, page, size) + findAll(filter) with LIKE search"
      contains: "fun findAll"
    - path: "src/main/kotlin/com/anjo/service/HistoryService.kt"
      provides: "findPaginated(filter, page, size) + exportCsv(filter)"
      contains: "fun exportCsv"
  key_links:
    - from: "src/main/kotlin/com/anjo/service/HistoryService.kt"
      to: "src/main/kotlin/com/anjo/db/HistoryRepository.kt"
      via: "exportCsv calls repository.findAll(filter)"
      pattern: "repository\\.findAll"
    - from: "src/main/kotlin/com/anjo/db/HistoryRepository.kt"
      to: "src/main/kotlin/com/anjo/model/HistoryFilter.kt"
      via: "findPaginated/findAll accept HistoryFilter"
      pattern: "HistoryFilter"
---

<objective>
Build the data and logic foundation for Phase 14: the `HistoryFilter` data class, the `HistoryValidators.sanitizeSearchTerm` wildcard-stripping sanitizer, the `kotlin-csv-jvm` dependency, the repository search/export query methods, and the service-layer `findPaginated(filter, page, size)` + `exportCsv(filter)` methods. This is the keystone plan: it changes the `findPaginated` signature (D-19), which forces all existing callers (service, tests) to be updated in this same plan.

Purpose: HIST-04 (case-insensitive text search) and HIST-05 (CSV export) both depend on `HistoryFilter` and the repository/service methods created here. Plans 02 and 03 wire these into the API and UI.
Output: New `HistoryFilter.kt`, `HistoryValidators.kt`, new test file `HistoryValidatorsTest.kt`, modified `HistoryRepository.kt`, `HistoryService.kt`, build files, and updated existing test call sites.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/14-history-enhancements/14-CONTEXT.md
@.planning/phases/14-history-enhancements/14-RESEARCH.md
@.planning/phases/14-history-enhancements/14-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 01 scope)

- `com.anjo.model.HistoryFilter` — new `data class HistoryFilter(val effect: String?, val source: String?, val zone: String?, val search: String?)` (NO `@Serializable`)
- `com.anjo.validation.HistoryValidators` — new `object` with `fun sanitizeSearchTerm(input: String): String`
- `HistoryRepository.findPaginated(filter: HistoryFilter, page: Int, size: Int): Pair<List<HistoryRecord>, Long>` — REPLACES the old 5-param signature
- `HistoryRepository.findAll(filter: HistoryFilter): List<HistoryRecord>` — new export-only query (no limit/offset)
- `HistoryService.findPaginated(filter: HistoryFilter, page: Int, size: Int): Pair<List<HistoryRecord>, Long>` — REPLACES old signature
- `HistoryService.exportCsv(filter: HistoryFilter): String` — new CSV producer
- Gradle catalog alias `kotlin-csv-jvm` (`com.jsoizo:kotlin-csv-jvm:1.10.0`) + `implementation(ktorLibs.kotlin.csv.jvm)` in build.gradle.kts
- `src/test/kotlin/com/anjo/validation/HistoryValidatorsTest.kt` — new Kotest FunSpec test file
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add kotlin-csv dependency, HistoryFilter model, and HistoryValidators sanitizer with tests</name>
  <files>gradle/ktor-libs.versions.toml, build.gradle.kts, src/main/kotlin/com/anjo/model/HistoryFilter.kt, src/main/kotlin/com/anjo/validation/HistoryValidators.kt, src/test/kotlin/com/anjo/validation/HistoryValidatorsTest.kt</files>
  <read_first>
    - gradle/ktor-libs.versions.toml (current [versions] and [libraries] blocks — add the new alias following the existing two-line module/version.ref pattern)
    - build.gradle.kts (dependencies block — add implementation line near the database deps)
    - src/main/kotlin/com/anjo/model/HistoryRecord.kt (sibling model file; HistoryFilter goes in same package com.anjo.model)
    - src/main/kotlin/com/anjo/validation/RequestValidators.kt (object-with-pure-functions pattern to mirror)
    - src/test/kotlin/com/anjo/routing/HistoryRoutesTest.kt (Kotest FunSpec + shouldBe import style to mirror for the new test)
  </read_first>
  <behavior>
    - Test 1: sanitizeSearchTerm("ab%cd") returns "abcd" (percent stripped)
    - Test 2: sanitizeSearchTerm("ab_cd") returns "abcd" (underscore stripped)
    - Test 3: sanitizeSearchTerm("%_%_") returns "" (wildcard-only input becomes empty)
    - Test 4: sanitizeSearchTerm("hello") returns "hello" (clean input unchanged)
    - Test 5: sanitizeSearchTerm("a%b_c%d") returns "abcd" (multiple mixed wildcards stripped)
  </behavior>
  <action>
    In `gradle/ktor-libs.versions.toml`: add `kotlin-csv = "1.10.0"` under `[versions]` and `kotlin-csv-jvm = { module = "com.jsoizo:kotlin-csv-jvm", version.ref = "kotlin-csv" }` under `[libraries]`. In `build.gradle.kts` dependencies block add `implementation(ktorLibs.kotlin.csv.jvm)` (the TOML alias `kotlin-csv-jvm` maps to `ktorLibs.kotlin.csv.jvm` — hyphens become dots). Do NOT upgrade to 2.0.0; 1.10.0 is the locked version per STATE.md.
    Create `src/main/kotlin/com/anjo/model/HistoryFilter.kt` per D-19: `package com.anjo.model` and `data class HistoryFilter(val effect: String?, val source: String?, val zone: String?, val search: String?)`. Do NOT annotate with `@Serializable` (internal filter type, never JSON-serialized — per PATTERNS.md ZoneStatus analog note).
    Create `src/main/kotlin/com/anjo/validation/HistoryValidators.kt` per D-18: `package com.anjo.validation`, `object HistoryValidators { fun sanitizeSearchTerm(input: String): String = input.replace("%", "").replace("_", "") }`. No `ValidationResult` import — returns plain String. This sanitizer must live here, NOT in route handlers (project rule: validation stays in validators, never in route handlers).
    Create `src/test/kotlin/com/anjo/validation/HistoryValidatorsTest.kt`: `package com.anjo.validation`, Kotest `FunSpec`, import `io.kotest.matchers.shouldBe`, no `testApplication`, no `runTest` (pure non-suspend unit tests). Cover all five behaviors above.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.anjo.validation.HistoryValidatorsTest"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "data class HistoryFilter" src/main/kotlin/com/anjo/model/HistoryFilter.kt` returns 1
    - `grep -v '^#' src/main/kotlin/com/anjo/model/HistoryFilter.kt | grep -c "@Serializable"` returns 0
    - `grep -c "object HistoryValidators" src/main/kotlin/com/anjo/validation/HistoryValidators.kt` returns 1
    - `grep -c "kotlin-csv-jvm" gradle/ktor-libs.versions.toml` returns at least 1
    - `grep -c "kotlin.csv.jvm" build.gradle.kts` returns 1
    - `./gradlew test --tests "com.anjo.validation.HistoryValidatorsTest"` exits 0 with all five tests passing
  </acceptance_criteria>
  <done>HistoryFilter data class, HistoryValidators object, the CSV dependency, and a passing HistoryValidatorsTest all exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor HistoryRepository + HistoryService to use HistoryFilter, add LIKE search, findAll, and exportCsv; fix existing test call sites</name>
  <files>src/main/kotlin/com/anjo/db/HistoryRepository.kt, src/main/kotlin/com/anjo/service/HistoryService.kt, src/test/kotlin/com/anjo/db/HistoryRepositoryTest.kt, src/test/kotlin/com/anjo/service/HistoryServiceTest.kt</files>
  <read_first>
    - src/main/kotlin/com/anjo/db/HistoryRepository.kt (current findPaginated if/else chain at lines 43-75 to replace; toHistoryRecord mapping at 77-86 stays unchanged)
    - src/main/kotlin/com/anjo/service/HistoryService.kt (current 5-param pass-through to replace)
    - src/main/kotlin/com/anjo/model/HistoryFilter.kt (the type created in Task 1)
    - src/main/kotlin/com/anjo/model/HistoryRecord.kt (field names/order for CSV columns: id, text, effect, source, scheduleId, zoneId, displayedAt, webhookStatus)
    - src/main/kotlin/com/anjo/db/HistoryTable.kt (column names: text, effect, displaySource, zoneId, scheduleId, displayedAt, webhookStatus — note displaySource not source)
    - src/test/kotlin/com/anjo/db/HistoryRepositoryTest.kt (all findPaginated call sites that must migrate to the new signature)
    - src/test/kotlin/com/anjo/service/HistoryServiceTest.kt (all findPaginated call sites that must migrate)
    - .planning/phases/14-history-enhancements/14-RESEARCH.md (Pattern 1 accumulator, Pattern 3 CSV writeAllAsString, Pitfall 3 findAll, A5 fallback if reduce does not compile)
  </read_first>
  <behavior>
    - Test 1 (repository): inserting rows "Hello" and "World", findPaginated with HistoryFilter(search="hello") returns only the "Hello" row (case-insensitive match)
    - Test 2 (repository): findPaginated with HistoryFilter(search=null) returns all rows (no search filter)
    - Test 3 (service): exportCsv with no filter returns a CSV whose first line equals the D-10 header `id,Text,Effect,Source,Zone ID,Schedule ID,Displayed At,Webhook Status`
    - Test 4 (service): exportCsv with a record whose text contains a comma quotes that field per RFC 4180
    - Test 5 (service): exportCsv with HistoryFilter(effect="SCROLL") includes only SCROLL rows in the output
  </behavior>
  <action>
    Refactor `HistoryRepository.findPaginated` to the D-19 signature `suspend fun findPaginated(filter: HistoryFilter, page: Int, size: Int): Pair<List<HistoryRecord>, Long>`. Replace the verbose if/else where/andWhere chain with the accumulator from RESEARCH.md Pattern 1: build a `List<Op<Boolean>>` via `buildList {}` adding `HistoryTable.effect eq it` for filter.effect, `HistoryTable.displaySource eq it` for filter.source, `HistoryTable.zoneId eq it` for filter.zone, and the search clause `HistoryTable.text.lowerCase() like "%${term.lowercase()}%"` for filter.search (D-20). Apply with `.where { conditions.reduce { acc, op -> acc and op } }` only when the list is non-empty. Keep `.orderBy(HistoryTable.displayedAt to SortOrder.DESC).limit(size).offset(((page - 1).toLong() * size))` exactly as today (Exposed 1.3.0 uses separate .limit(n).offset(start) calls). If `conditions.reduce` fails to compile under Exposed 1.3.0, fall back to the original if/else chain plus a 4th search branch (RESEARCH.md A5). Add `suspend fun findAll(filter: HistoryFilter): List<HistoryRecord>` (Pitfall 3) using the same `suspendTransaction {}` and same accumulator but WITHOUT `.limit().offset()` — returns all matching rows ordered by displayedAt DESC. Add import `com.anjo.model.HistoryFilter`. The 1000-row cap stays enforced by the insert guard, not by a query limit (D-08).
    Update `HistoryService`: change `findPaginated` to `suspend fun findPaginated(filter: HistoryFilter, page: Int, size: Int): Pair<List<HistoryRecord>, Long> = repository.findPaginated(filter, page, size)`. Add `suspend fun exportCsv(filter: HistoryFilter): String` per D-12: call `repository.findAll(filter)`, build header `listOf("id", "Text", "Effect", "Source", "Zone ID", "Schedule ID", "Displayed At", "Webhook Status")` (D-10), map each record to `listOf(r.id, r.text, r.effect, r.source, r.zoneId.orEmpty(), r.scheduleId.orEmpty(), r.displayedAt, r.webhookStatus.orEmpty())` (D-09 column set/order), and return `csvWriter().writeAllAsString(listOf(header) + rows)`. Keep exportCsv a `suspend fun` and call repository directly — no `runBlocking`. Add imports `com.anjo.model.HistoryFilter` and `com.github.doyaaaaaken.kotlincsv.dsl.csvWriter` (verify exact import path against the resolved jar; A3). If `writeAllAsString` is absent in 1.10.0, fall back to `ByteArrayOutputStream` + `writeAll` (RESEARCH.md A2).
    Fix ALL existing call sites broken by the signature change: in `HistoryRepositoryTest.kt` replace every `repository.findPaginated(1, 20)`, `findPaginated(1, 20, effect = "SCROLL")`, `findPaginated(1, 20, source = ...)`, `findPaginated(1, 20, zone = ...)`, `findPaginated(1, 20, effect = ..., zone = ...)`, `findPaginated(1, 1)`, `findPaginated(1, 3)`, `findPaginated(2, 3)` with the new form `findPaginated(HistoryFilter(effect=..., source=..., zone=..., search=null), page, size)`. In `HistoryServiceTest.kt` do the same for its `service.findPaginated(...)` calls. Add the search test cases (Behaviors 1-2) to HistoryRepositoryTest and exportCsv cases (Behaviors 3-5) to HistoryServiceTest. Import `com.anjo.model.HistoryFilter` in both test files.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.anjo.db.HistoryRepositoryTest" --tests "com.anjo.service.HistoryServiceTest"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fun findAll" src/main/kotlin/com/anjo/db/HistoryRepository.kt` returns 1
    - `grep -c "lowerCase() like" src/main/kotlin/com/anjo/db/HistoryRepository.kt` returns at least 1
    - `grep -c "fun exportCsv" src/main/kotlin/com/anjo/service/HistoryService.kt` returns 1
    - `grep -c "repository.findAll" src/main/kotlin/com/anjo/service/HistoryService.kt` returns 1
    - `grep -c "filter: HistoryFilter" src/main/kotlin/com/anjo/db/HistoryRepository.kt` returns at least 2
    - A HistoryServiceTest assertion checks the first CSV line equals `id,Text,Effect,Source,Zone ID,Schedule ID,Displayed At,Webhook Status`
    - `./gradlew test --tests "com.anjo.db.HistoryRepositoryTest" --tests "com.anjo.service.HistoryServiceTest"` exits 0
    - `./gradlew compileKotlin compileTestKotlin` exits 0 (no remaining old-signature call sites)
  </acceptance_criteria>
  <done>findPaginated takes HistoryFilter, findAll and exportCsv exist, CSV header matches D-10, case-insensitive search works, and all existing repository/service tests compile and pass against the new signature.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client query param → repository LIKE query | Untrusted `search` string crosses into a SQL LIKE clause |
| npm/maven install | New `com.jsoizo:kotlin-csv-jvm` artifact enters the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Tampering | `HistoryRepository.findPaginated` LIKE clause | high | mitigate | `HistoryValidators.sanitizeSearchTerm` strips `%` and `_` before the term reaches `HistoryFilter`; Exposed `like` binds the value via prepared statement. Acceptance criterion in Task 1 (Test 1-3) verifies the sanitizer; the route-level wiring is verified in Plan 02/03. |
| T-14-02 | Tampering | maven dependency `com.jsoizo:kotlin-csv-jvm:1.10.0` | low | accept | RESEARCH.md Package Legitimacy Audit verdict OK (Maven Central, ~5yrs, active GitHub repo, Apache 2.0). Version pinned to 1.10.0 — not auto-upgraded. |
| T-14-03 | Denial of Service | `findAll` unbounded query for export | low | accept | Total row count capped at 1000 by the insert-time prune guard (`MAX_ROWS`), so `findAll` returns at most 1000 rows (D-08). No additional limit needed. |

High-severity threat T-14-01 has a verifying acceptance criterion: HistoryValidatorsTest asserts `%` and `_` are stripped.
</threat_model>

<verification>
- `./gradlew test --tests "com.anjo.*History*"` passes (all history-layer tests green)
- `./gradlew compileKotlin compileTestKotlin` succeeds (no stale call sites against the old findPaginated signature)
- No `/** */` KDoc stubs introduced (project no-comments rule; STATE.md pitfall 6)
</verification>

<success_criteria>
- HistoryFilter, HistoryValidators, findAll, exportCsv, and the CSV dependency exist
- Search is case-insensitive at the repository level
- exportCsv header row matches D-10 exactly and quotes comma-containing fields
- All pre-existing repository/service tests pass against the new signature
</success_criteria>

<output>
Create `.planning/phases/14-history-enhancements/14-01-SUMMARY.md` when done
</output>
