---
phase: 14-history-enhancements
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/main/kotlin/com/anjo/routing/HistoryRoutes.kt
  - src/test/kotlin/com/anjo/routing/HistoryRoutesTest.kt
autonomous: true
requirements: [HIST-04, HIST-05]
must_haves:
  truths:
    - "GET /api/v1/history?search=foo returns only records whose text contains foo (case-insensitive)"
    - "GET /api/v1/history/export returns CSV with a Content-Disposition attachment header"
    - "The export endpoint applies the same effect/source/zone/search filters as the JSON list endpoint"
    - "A search term of % or _ returns all rows (sanitized to empty → no filter), not an injection match"
  artifacts:
    - path: "src/main/kotlin/com/anjo/routing/HistoryRoutes.kt"
      provides: "search param on /history + new /history/export endpoint"
      contains: "history/export"
  key_links:
    - from: "src/main/kotlin/com/anjo/routing/HistoryRoutes.kt"
      to: "src/main/kotlin/com/anjo/service/HistoryService.kt"
      via: "export handler calls historyService.exportCsv(filter)"
      pattern: "exportCsv"
    - from: "src/main/kotlin/com/anjo/routing/HistoryRoutes.kt"
      to: "src/main/kotlin/com/anjo/validation/HistoryValidators.kt"
      via: "search param sanitized via HistoryValidators before building HistoryFilter"
      pattern: "HistoryValidators.sanitizeSearchTerm"
---

<objective>
Wire the API layer for HIST-04 (JSON search) and HIST-05 (CSV export). Add the `search` query param to `GET /api/v1/history` and a new `GET /api/v1/history/export` endpoint that streams a downloadable CSV with the correct `Content-Disposition` header. Both routes build a `HistoryFilter` (using the sanitizer from Plan 01) and pass it to the service.

Purpose: HIST-04 requires `?search=` on the JSON API; HIST-05 requires the `/export` endpoint. Both consume the `HistoryFilter`, `exportCsv`, and `HistoryValidators` built in Plan 01.
Output: Modified `HistoryRoutes.kt` (search param + export endpoint) and new test cases in `HistoryRoutesTest.kt`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/14-history-enhancements/14-CONTEXT.md
@.planning/phases/14-history-enhancements/14-RESEARCH.md
@.planning/phases/14-history-enhancements/14-PATTERNS.md
@.planning/phases/14-history-enhancements/14-01-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 02 scope)

- `GET /api/v1/history` — extended with `zone` and `search` query params, builds `HistoryFilter`
- `GET /api/v1/history/export` — new route in `HistoryRoutes.kt` returning `text/csv` with `Content-Disposition: attachment; filename="history.csv"`
- New test cases in `HistoryRoutesTest.kt` for search filtering and CSV export
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add search param and /history/export endpoint to HistoryRoutes</name>
  <files>src/main/kotlin/com/anjo/routing/HistoryRoutes.kt, src/test/kotlin/com/anjo/routing/HistoryRoutesTest.kt</files>
  <read_first>
    - src/main/kotlin/com/anjo/routing/HistoryRoutes.kt (current query-param extraction at lines 11-14; only effect+source extracted today, no zone; respond at line 16)
    - src/main/kotlin/com/anjo/service/HistoryService.kt (Plan 01 output: findPaginated(filter, page, size) and exportCsv(filter) signatures)
    - src/main/kotlin/com/anjo/model/HistoryFilter.kt (Plan 01 output: constructor arg order effect, source, zone, search)
    - src/main/kotlin/com/anjo/validation/HistoryValidators.kt (Plan 01 output: sanitizeSearchTerm)
    - src/main/kotlin/com/anjo/routing/Routing.kt (confirms historyRoutes(historyService) is registered under route("/api/v1"), so the export path resolves to /api/v1/history/export)
    - src/test/kotlin/com/anjo/routing/HistoryRoutesTest.kt (existing testApplication + getBlocking<HistoryRepository> seeding pattern to mirror for new cases)
    - .planning/phases/14-history-enhancements/14-RESEARCH.md (Pattern 4 export response headers; Pitfall 1 Content-Disposition; A4 ContentType.Text.Plain)
  </read_first>
  <behavior>
    - Test 1: seed a "hello" record and a "world" record, GET /api/v1/history?search=hello body contains "hello" and not "world"
    - Test 2: search is case-insensitive — GET /api/v1/history?search=HELLO matches a record with text "hello"
    - Test 3: GET /api/v1/history/export returns 200 with a Content-Disposition header value containing attachment and filename="history.csv"
    - Test 4: export body first line equals the D-10 header row id,Text,Effect,Source,Zone ID,Schedule ID,Displayed At,Webhook Status
    - Test 5: GET /api/v1/history/export?effect=SCROLL excludes BLINK rows from the CSV body
  </behavior>
  <action>
    In `HistoryRoutes.kt` `get("/history")` handler: keep the existing `page`, `size`, `effect`, `source` extraction. Add `zone` extraction using the same enum pattern `call.request.queryParameters["zone"]?.uppercase()?.takeIf { it.isNotEmpty() && it != "ALL" }` — note: zone IDs are not uppercased elsewhere, so match the UI route behavior by NOT uppercasing zone; use `call.request.queryParameters["zone"]?.takeIf { it.isNotEmpty() && it != "ALL" }`. Add `search` extraction: read raw via `call.request.queryParameters["search"].orEmpty()`, then `val search = HistoryValidators.sanitizeSearchTerm(rawSearch).takeIf { it.isNotBlank() }` (D-18: sanitize in handler via the validator object, blank-after-sanitize → null → no filter). Build `val filter = HistoryFilter(effect, source, zone, search)` and call `historyService.findPaginated(filter, page, size)`. Keep the `call.respond(HistoryPageResponse(items, page, size, total))` envelope unchanged.
    Add a new `get("/history/export")` block: extract `effect`, `source`, `zone`, and sanitized `search` exactly as above (no `page`/`size` — export fetches all, D-08), build `HistoryFilter`, call `val csv = historyService.exportCsv(filter)`, set the disposition header per RESEARCH.md Pattern 4 using `call.response.headers.append(HttpHeaders.ContentDisposition, ContentDisposition.Attachment.withParameter(ContentDisposition.Parameters.FileName, "history.csv").toString())` (D-11; Pitfall 1 — must set BEFORE respondText), then `call.respondText(csv, ContentType.Text.Plain)` (A4: Ktor has no ContentType.Text.CSV; download is driven by the disposition header, not content type).
    Add imports: `io.ktor.http.ContentDisposition`, `io.ktor.http.ContentType`, `io.ktor.http.HttpHeaders`, `io.ktor.server.response.respondText`, `com.anjo.model.HistoryFilter`, `com.anjo.validation.HistoryValidators`.
    In `HistoryRoutesTest.kt` add the five test cases above following the existing `testApplication { application { module() }; client.get("/health"); getBlocking<HistoryRepository>(...) }` seeding pattern. Read the Content-Disposition header via the response headers map.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.anjo.routing.HistoryRoutesTest"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "history/export" src/main/kotlin/com/anjo/routing/HistoryRoutes.kt` returns 1
    - `grep -c "HistoryValidators.sanitizeSearchTerm" src/main/kotlin/com/anjo/routing/HistoryRoutes.kt` returns at least 1
    - `grep -c "exportCsv" src/main/kotlin/com/anjo/routing/HistoryRoutes.kt` returns 1
    - `grep -c "ContentDisposition.Attachment" src/main/kotlin/com/anjo/routing/HistoryRoutes.kt` returns 1
    - `grep -c "HistoryFilter(" src/main/kotlin/com/anjo/routing/HistoryRoutes.kt` returns 2
    - A HistoryRoutesTest case asserts the response Content-Disposition header contains `attachment` and `filename="history.csv"`
    - `./gradlew test --tests "com.anjo.routing.HistoryRoutesTest"` exits 0 with the new cases passing
  </acceptance_criteria>
  <done>The JSON history endpoint filters by search, the export endpoint returns a CSV download with the correct disposition header and respects all filters, and HistoryRoutesTest proves it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP query param → HistoryFilter → repository | Untrusted `search`/`zone`/`effect`/`source` cross into the DB query |
| server → browser (export response) | CSV bytes delivered with download semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-04 | Tampering | `search` param → LIKE clause | high | mitigate | `HistoryValidators.sanitizeSearchTerm` is called in BOTH the list and export handlers before constructing `HistoryFilter` (D-18). Acceptance criterion verifies the call exists; behavior Test 1-2 verify search matches and `%`/`_` are neutralized via Plan 01's sanitizer. |
| T-14-05 | Information Disclosure | export endpoint response rendering | medium | mitigate | `Content-Disposition: attachment` set before `respondText` (Pitfall 1, D-11) so the CSV downloads rather than rendering inline. Acceptance criterion checks the header is present and well-formed. |
| T-14-06 | Denial of Service | `/history/export` returns all matching rows | low | accept | Row total capped at 1000 by the insert prune guard (D-08); export cannot return an unbounded result set. |

High-severity threat T-14-04 has a verifying acceptance criterion: the handler invokes `HistoryValidators.sanitizeSearchTerm` (grep gate) and behavior tests confirm filtering.
</threat_model>

<verification>
- `./gradlew test --tests "com.anjo.routing.HistoryRoutesTest"` passes
- `./gradlew test` full suite stays green (JaCoCo ≥70% gate)
- Export response carries `Content-Disposition: attachment; filename="history.csv"`
</verification>

<success_criteria>
- `GET /api/v1/history?search=` filters case-insensitively
- `GET /api/v1/history/export` downloads a filtered CSV with the correct header
- Search wildcard chars are sanitized before the query (no injection)
</success_criteria>

<output>
Create `.planning/phases/14-history-enhancements/14-02-SUMMARY.md` when done
</output>
