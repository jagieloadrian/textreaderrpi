---
plan: 13-02
phase: 13
wave: 2
depends_on: [13-01]
files_modified:
  - src/main/kotlin/com/anjo/web/templates/IndexPage.kt
  - src/main/kotlin/com/anjo/web/templates/SchedulePage.kt
  - src/main/kotlin/com/anjo/web/templates/StatusPage.kt
  - src/main/kotlin/com/anjo/web/templates/HistoryPage.kt
requirements_addressed: [UI-03, UI-04, UI-05, UI-06, UI-07]
autonomous: true
---

# Plan 13-02: Page Templates — Zone Selectors, Effect Preview, Status Shell, History Filter

## Objective

Update all five page templates to reflect the Phase 13 UI decisions:
1. `IndexPage.kt` — add zone selector (SSR from ZoneStatus list), add `#effectPreview` div
2. `SchedulePage.kt` — add zone selector to create form, add zone + webhookUrl columns to SSR table
3. `StatusPage.kt` — convert to static shell (`object StatusPage`) with skeleton spans; remove 5 constructor params
4. `HistoryPage.kt` — enable zone filter select (remove `disabled`), add zone options from new `zones` param

## must_haves

```yaml
truths:
  - "IndexPage accepts zones: List<ZoneStatus> constructor param and renders a <select id=\"zoneSelect\"> with zone options"
  - "IndexPage renders <div id=\"effectPreview\"> below the effect select"
  - "SchedulePage accepts zones: List<ZoneStatus> and renders a zone select in the create form"
  - "SchedulePage SSR table has zone and webhookUrl columns"
  - "StatusPage has no constructor params and renders skeleton spans with ids: status-uptime, status-memory-used, status-memory-max, status-display, status-failures"
  - "HistoryPage zone filter select is NOT disabled and accepts zone options from zones: List<ZoneStatus> param"
```

## Tasks

<task id="13-02-T1">
<title>Update IndexPage.kt: zone selector constructor param and effect preview div</title>

<read_first>
- C:\Development\textreaderrpi\src\main\kotlin\com\anjo\web\templates\IndexPage.kt (CURRENT STATE — 60 lines, class IndexPage with no params)
- C:\Development\textreaderrpi\src\main\kotlin\com\anjo\model\ZoneStatus.kt (ZoneStatus model fields)
- C:\Development\textreaderrpi\.planning\phases\13-ui-ux-refresh\13-CONTEXT.md (D-08, D-11, D-12)
</read_first>

<action>
MODIFY src/main/kotlin/com/anjo/web/templates/IndexPage.kt:

1. Add import: import com.anjo.model.ZoneStatus
2. Change class signature from `class IndexPage` to `class IndexPage(private val zones: List<ZoneStatus> = emptyList())`

3. In the pageContent() function, after the effect select div and before the submit button, ADD:
   - A zone selector div:
     div {
       label { htmlFor = "zoneSelect"; +"Zone" }
       select {
         id = "zoneSelect"; name = "zone"
         option { value = ""; +"All zones" }
         zones.forEach { zone ->
           option {
             value = zone.id
             +"${zone.id}${if (zone.status == "OFFLINE") " (OFFLINE)" else ""}"
           }
         }
       }
     }
   - An effect preview div AFTER the zone selector (or after the effect select — place it after the zone selector div):
     div {
       id = "effectPreview"
     }

4. Ensure the submit button remains AFTER the zone selector and effectPreview div.

NOTE: The zone select must have id="zoneSelect" — this is what app.js reads in submitForm().
NOTE: The default option (value="") is the "All zones" option that sends no zone param.
NOTE: ZoneStatus.id is the zone identifier used in ?zone=X routing.
NOTE: ZoneStatus.status field — check actual field name in ZoneStatus model before using.
</action>

<acceptance_criteria>
- IndexPage.kt compiles without errors: ./gradlew compileKotlin exits 0
- IndexPage.kt class signature contains: zones: List<ZoneStatus>
- IndexPage.kt contains: id = "zoneSelect"
- IndexPage.kt contains: name = "zone"
- IndexPage.kt contains: id = "effectPreview"
- IndexPage.kt contains: import com.anjo.model.ZoneStatus
- When instantiated with empty zones list, renders HTML containing select#zoneSelect and div#effectPreview
- ./gradlew test exits 0
</acceptance_criteria>
</task>

<task id="13-02-T2">
<title>Update SchedulePage.kt: zone selector on create form, zone + webhookUrl columns in SSR table</title>

<read_first>
- C:\Development\textreaderrpi\src\main\kotlin\com\anjo\web\templates\SchedulePage.kt (CURRENT STATE — 102 lines, fun FlowContent.schedulePage(schedules: List<Schedule>))
- C:\Development\textreaderrpi\src\main\kotlin\com\anjo\model\Schedule.kt (Schedule model — verify zoneId and webhookUrl field names)
- C:\Development\textreaderrpi\src\main\kotlin\com\anjo\model\ZoneStatus.kt (ZoneStatus fields)
- C:\Development\textreaderrpi\.planning\phases\13-ui-ux-refresh\13-CONTEXT.md (D-17, D-21)
</read_first>

<action>
MODIFY src/main/kotlin/com/anjo/web/templates/SchedulePage.kt:

1. Add import: import com.anjo.model.ZoneStatus

2. Change function signature from:
   fun FlowContent.schedulePage(schedules: List<Schedule>)
   to:
   fun FlowContent.schedulePage(schedules: List<Schedule>, zones: List<ZoneStatus> = emptyList())

3. In the create form section, after the effect select div and before the createScheduleBtn button, ADD:
   div {
     label { htmlFor = "scheduleZone"; +"Zone" }
     select {
       id = "scheduleZone"; name = "zoneId"
       option { value = ""; +"Default (all zones)" }
       zones.forEach { zone ->
         option {
           value = zone.id
           +"${zone.id}${if (zone.status == "OFFLINE") " (OFFLINE)" else ""}"
         }
       }
     }
   }

4. In the schedule table thead, ADD two new <th> elements after the Status column:
   th { +"Zone" }
   th { +"Webhook" }

5. In the schedule table tbody rows, ADD two new <td> elements after the status td:
   td { +(s.zoneId ?: "—") }
   td { +(s.webhookUrl?.let { if (it.length > 30) it.take(30) + "…" else it } ?: "—") }

NOTE: Verify actual field names of Schedule model — look for zoneId and webhookUrl fields.
NOTE: The id="scheduleZone" is referenced by createSchedule() in app.js (will be updated in plan 13-03).
</action>

<acceptance_criteria>
- SchedulePage.kt compiles without errors: ./gradlew compileKotlin exits 0
- SchedulePage.kt function signature contains: zones: List<ZoneStatus>
- SchedulePage.kt contains: id = "scheduleZone"
- SchedulePage.kt contains: name = "zoneId"
- SchedulePage.kt contains: +"Zone" (as a th header)
- SchedulePage.kt contains: +"Webhook" (as a th header)
- SchedulePage.kt contains: import com.anjo.model.ZoneStatus
- ./gradlew test exits 0
</acceptance_criteria>
</task>

<task id="13-02-T3">
<title>Convert StatusPage.kt to static shell with skeleton spans</title>

<read_first>
- C:\Development\textreaderrpi\src\main\kotlin\com\anjo\web\templates\StatusPage.kt (CURRENT STATE — 35 lines, class with 5 constructor params)
- C:\Development\textreaderrpi\src\main\kotlin\com\anjo\routing\ui\WebRoutes.kt (current StatusPage usage — must update call site in plan 13-03)
- C:\Development\textreaderrpi\.planning\phases\13-ui-ux-refresh\13-CONTEXT.md (D-09, D-10, D-20)
</read_first>

<action>
REPLACE src/main/kotlin/com/anjo/web/templates/StatusPage.kt entirely with a static shell:

1. Convert class StatusPage(...) to: object StatusPage
2. Keep: fun render(): String returning BaseLayout.render(...)
3. Keep BaseLayout.render call with pageTitle="Status", activePath="/status"
4. Remove all constructor params (displayType, hardwareAvailable, isActive, currentMessage, error)
5. Replace the SSR content with a skeleton layout containing these labeled span elements:
   - A section for Service Health:
     h2 { +"System Status" }
     article {
       p { strong { +"Uptime: " }; span { id = "status-uptime"; +"Loading…" } }
       p { strong { +"Memory Used: " }; span { id = "status-memory-used"; +"Loading…" } }
       p { strong { +"Memory Max: " }; span { id = "status-memory-max"; +"Loading…" } }
       p { strong { +"Display Status: " }; span { id = "status-display"; +"Loading…" } }
       p { strong { +"Total Failures: " }; span { id = "status-failures"; +"Loading…" } }
     }
     article {
       h3 { +"Hardware Metrics" }
       div { id = "status-hardware-metrics"; +"Loading…" }
     }

6. The status page JS (which polls /health/detail and /metrics) will be added in plan 13-03 via HistoryUIRoutes (the status page route in WebRoutes.kt will be updated to use headExtra).

NOTE: StatusPage.render() takes no params. WebRoutes.kt will need to be updated in plan 13-03 to call StatusPage.render() instead of StatusPage(...).render().
NOTE: Keep the BaseLayout.render() call signature matching plan 13-01's updated signature.
</action>

<acceptance_criteria>
- StatusPage.kt compiles: ./gradlew compileKotlin exits 0 (NOTE: WebRoutes.kt still references old constructor — it WILL have compile errors until plan 13-03 updates WebRoutes.kt; this is expected)
  IMPORTANT: If the executor wants to avoid compile errors NOW, update WebRoutes.kt to call StatusPage.render() in this task too (acceptable deviation).
- StatusPage.kt does NOT contain any constructor parameters
- StatusPage.kt contains: object StatusPage (or equivalent static render pattern)
- StatusPage.kt contains: id = "status-uptime"
- StatusPage.kt contains: id = "status-memory-used"
- StatusPage.kt contains: id = "status-display"
- StatusPage.kt contains: id = "status-failures"
- StatusPage.kt contains: id = "status-hardware-metrics"
- StatusPage.kt contains: Loading…
</acceptance_criteria>
</task>

<task id="13-02-T4">
<title>Enable zone filter in HistoryPage.kt — add zones param and remove disabled attribute</title>

<read_first>
- C:\Development\textreaderrpi\src\main\kotlin\com\anjo\web\templates\HistoryPage.kt (CURRENT STATE — 132 lines, zone select has disabled="")
- C:\Development\textreaderrpi\src\main\kotlin\com\anjo\model\ZoneStatus.kt (ZoneStatus fields)
- C:\Development\textreaderrpi\.planning\phases\13-ui-ux-refresh\13-CONTEXT.md (D-22)
</read_first>

<action>
MODIFY src/main/kotlin/com/anjo/web/templates/HistoryPage.kt:

1. Add import: import com.anjo.model.ZoneStatus

2. Change function signature from:
   fun FlowContent.historyPage(items, page, rawSize, total, expandAll, effect, source)
   to:
   fun FlowContent.historyPage(items, page, rawSize, total, expandAll, effect, source,
     zone: String = "", zones: List<ZoneStatus> = emptyList())

3. In the zone filter select, make these changes:
   - REMOVE: attributes["disabled"] = ""
   - REMOVE: the single hardcoded option "Multi-zone — Phase 11"
   - CHANGE the label to: +"Zone"
   - ADD zone options inside the select:
     option { value = "ALL"; if (zone.isEmpty() || zone == "ALL") selected = true; +"ALL" }
     zones.forEach { z ->
       option { value = z.id; if (zone == z.id) selected = true; +z.id }
     }
   
4. The zone filter form already submits via GET to /history — the zone param will be read by HistoryUIRoutes.kt (updated in plan 13-03).

NOTE: The zone select uses name="zone" (this is what the route handler will read via call.request.queryParameters["zone"]).
</action>

<acceptance_criteria>
- HistoryPage.kt compiles: ./gradlew compileKotlin exits 0
- HistoryPage.kt function signature contains: zone: String = ""
- HistoryPage.kt function signature contains: zones: List<ZoneStatus>
- HistoryPage.kt does NOT contain: attributes["disabled"]
- HistoryPage.kt contains: name = "zone" (the zone select)
- HistoryPage.kt contains: import com.anjo.model.ZoneStatus
- ./gradlew test exits 0
</acceptance_criteria>
</task>

## Verification

After all 4 tasks:
```bash
./gradlew compileKotlin
```
Expected: 0 compile errors (all template changes are backwards-compatible with default params, except StatusPage which will have a WebRoutes.kt compile error — acceptable, fixed in plan 13-03).

```bash
./gradlew test
```
Expected: BUILD SUCCESSFUL — existing tests still pass. Template changes use default params so no callers break.

## Notes for Executor

- ZoneStatus model location: `src/main/kotlin/com/anjo/model/ZoneStatus.kt` — verify the exact field names (id, status) before using
- Schedule model — verify zoneId and webhookUrl field names by reading Schedule.kt before writing SchedulePage changes
- StatusPage compile error in WebRoutes.kt is expected — fixed in plan 13-03 when WebRoutes.kt is updated
- HistoryPage and SchedulePage use Kotlin default parameter values (= emptyList()) so existing callers remain valid without change until plan 13-03 passes the actual zone list

