---
plan: 13-04
phase: 13
wave: 3
depends_on: [13-02, 13-03]
files_modified:
  - src/test/kotlin/com/anjo/web/templates/BaseLayoutTest.kt
  - src/test/kotlin/com/anjo/web/templates/IndexPageTest.kt
  - src/test/kotlin/com/anjo/web/templates/SchedulePageTest.kt
  - src/test/kotlin/com/anjo/db/HistoryRepositoryTest.kt
  - src/test/kotlin/com/anjo/ApplicationTest.kt
requirements_addressed: [UI-01, UI-02, UI-03, UI-04, UI-05, UI-06, UI-07, UI-08]
autonomous: true
---

# Plan 13-04: Tests and Coverage — Template Rendering and Filter Verification

## Objective

Add targeted tests to verify the Phase 13 changes and keep JaCoCo ≥70% gate green:
1. `BaseLayoutTest.kt` — verify aside+main structure, no data-theme, headExtra hook works, custom.css link present
2. `IndexPageTest.kt` — verify zone select renders, effectPreview div renders
3. `SchedulePageTest.kt` — verify zone select in create form, zone/webhookUrl columns in table
4. `HistoryRepositoryTest.kt` extension — verify zone filter query returns correct results
5. `ApplicationTest.kt` extension — DI smoke and route integration for new signatures

## must_haves

```yaml
truths:
  - "./gradlew test exits 0 with 0 failures after all Phase 13 plans are applied"
  - "./gradlew jacocoTestCoverageVerification exits 0 (≥70% line coverage maintained)"
  - "BaseLayoutTest verifies: aside element present, no data-theme attribute, headExtra renders injected content"
  - "HistoryRepositoryTest verifies zone filter: records with matching zoneId are returned, records with non-matching zoneId are excluded"
  - "ApplicationTest DI smoke test still passes with updated route function signatures"
```

## Tasks

<task id="13-04-T1">
<title>Write BaseLayoutTest.kt verifying structural changes from plan 13-01</title>

<read_first>
- C:\Development\textreaderrpi\src\main\kotlin\com\anjo\web\templates\BaseLayout.kt (CURRENT STATE after plan 13-01)
- C:\Development\textreaderrpi\src\test\kotlin\com\anjo\ApplicationTest.kt (existing test patterns — Kotest `should` convention)
</read_first>

<action>
CREATE src/test/kotlin/com/anjo/web/templates/BaseLayoutTest.kt as a Kotest FunSpec:

Tests to include:
1. "should render aside navigation element instead of header nav":
   val html = BaseLayout.render("Test", "/") {}
   html shouldContain "<aside"
   html shouldNotContain "<header" (or verify nav is inside aside, not in header)
   
2. "should not have data-theme attribute":
   val html = BaseLayout.render("Test", "/") {}
   html shouldNotContain "data-theme"
   
3. "should load custom.css":
   val html = BaseLayout.render("Test", "/") {}
   html shouldContain "/static/custom.css"
   
4. "should render headExtra content when provided":
   val html = BaseLayout.render("Test", "/", headExtra = { link(rel="stylesheet", href="/custom.css") }) {}
   html shouldContain "/custom.css"
   
5. "should mark active navigation item with aria-current page":
   val html = BaseLayout.render("Test", "/history", {}) {}
   html shouldContain "aria-current=\"page\""
   
6. "should render main content element":
   val html = BaseLayout.render("Test", "/") { p { +"hello" } }
   html shouldContain "<p>hello</p>"
   html shouldContain "id=\"main-content\""

Use Kotest imports:
  import io.kotest.core.spec.style.FunSpec
  import io.kotest.matchers.string.shouldContain
  import io.kotest.matchers.string.shouldNotContain
  import kotlinx.html.link (for headExtra test)
</action>

<acceptance_criteria>
- BaseLayoutTest.kt compiles: ./gradlew compileTestKotlin exits 0
- ./gradlew test --tests "*.BaseLayoutTest" exits 0 with all tests passing
- Tests cover: aside present, data-theme absent, custom.css link, headExtra hook, aria-current, main-content id
</acceptance_criteria>
</task>

<task id="13-04-T2">
<title>Write IndexPageTest.kt and SchedulePageTest.kt for zone selector rendering</title>

<read_first>
- C:\Development\textreaderrpi\src\main\kotlin\com\anjo\web\templates\IndexPage.kt (CURRENT STATE after plan 13-02)
- C:\Development\textreaderrpi\src\main\kotlin\com\anjo\web\templates\SchedulePage.kt (CURRENT STATE after plan 13-02)
- C:\Development\textreaderrpi\src\main\kotlin\com\anjo\model\ZoneStatus.kt (ZoneStatus model)
</read_first>

<action>
CREATE src/test/kotlin/com/anjo/web/templates/IndexPageTest.kt as a Kotest FunSpec:

Tests:
1. "should render zone select with provided zones":
   val zones = listOf(ZoneStatus(id="zone-1", status="ONLINE", ...), ZoneStatus(id="zone-2", status="OFFLINE", ...))
   val html = IndexPage(zones).render()
   html shouldContain "id=\"zoneSelect\""
   html shouldContain "value=\"zone-1\""
   html shouldContain "(OFFLINE)"
   
2. "should render effect preview div":
   val html = IndexPage(emptyList()).render()
   html shouldContain "id=\"effectPreview\""
   
3. "should render zone select with default empty option":
   val html = IndexPage(emptyList()).render()
   html shouldContain "id=\"zoneSelect\""

CREATE src/test/kotlin/com/anjo/web/templates/SchedulePageTest.kt as a Kotest FunSpec:

Tests:
1. "should render zone select in create form":
   val html = buildString { schedulePage(emptyList(), listOf(ZoneStatus(id="z1", status="ONLINE", ...))) }
   ... OR use BaseLayout.render to produce the full page HTML, then check for scheduleZone select

Actually for top-level fun extensions, use: BaseLayout.render("Test", "/schedule") { schedulePage(emptyList(), zones) }
html shouldContain "id=\"scheduleZone\""
html shouldContain "value=\"z1\""

2. "should render zone and webhook columns in schedule table":
   val schedule = Schedule(id="test-id", text="hello", ..., zoneId="z1", webhookUrl="http://example.com/hook")
   val html = BaseLayout.render("Test", "/schedule") { schedulePage(listOf(schedule), emptyList()) }
   html shouldContain "z1"
   html shouldContain "example.com"

NOTE: When constructing ZoneStatus for tests, check the actual constructor params of ZoneStatus model.
NOTE: When constructing Schedule for tests, use the existing test factories or minimal constructors.
</action>

<acceptance_criteria>
- IndexPageTest.kt and SchedulePageTest.kt compile: ./gradlew compileTestKotlin exits 0
- ./gradlew test --tests "*.IndexPageTest" exits 0
- ./gradlew test --tests "*.SchedulePageTest" exits 0
- Tests cover: zone select present, zone options populated, effectPreview div, schedule zone column
</acceptance_criteria>
</task>

<task id="13-04-T3">
<title>Extend HistoryRepositoryTest.kt to cover zone filter</title>

<read_first>
- C:\Development\textreaderrpi\src\test\kotlin\com\anjo\db\HistoryRepositoryTest.kt (CURRENT STATE — find the file; check existing test patterns)
- C:\Development\textreaderrpi\src\main\kotlin\com\anjo\db\HistoryRepository.kt (CURRENT STATE after plan 13-03)
- C:\Development\textreaderrpi\src\main\kotlin\com\anjo\model\HistoryRecord.kt (HistoryRecord model)
</read_first>

<action>
MODIFY src/test/kotlin/com/anjo/db/HistoryRepositoryTest.kt — ADD new test cases for zone filter:

Find the existing test class (Kotest FunSpec or similar) and ADD inside it:

test("findPaginated with zone filter returns only matching records") {
  // Insert 2 records with zone-a, 1 record with zone-b
  // Call findPaginated(page=1, size=10, zone="zone-a")
  // Verify: items.size == 2 and all items have zoneId == "zone-a"
}

test("findPaginated with zone filter ALL or null returns all records") {
  // Call findPaginated(page=1, size=10, zone=null)
  // Verify: returns records from all zones
}

test("findPaginated with zone AND effect filter") {
  // Insert records with different zone + effect combinations
  // Call findPaginated(page=1, size=10, effect="SCROLL", zone="zone-a")
  // Verify: only records matching BOTH filters are returned
}

Follow the EXACT same database setup pattern as existing tests in the file (use the same in-memory H2 database setup, same suspendTransaction pattern, same HistoryRecord construction).

NOTE: Read the existing HistoryRepositoryTest.kt carefully before adding tests — match the exact framework (Kotest FunSpec or StringSpec or DescribeSpec), database init, and teardown patterns.
</action>

<acceptance_criteria>
- HistoryRepositoryTest.kt compiles: ./gradlew compileTestKotlin exits 0
- ./gradlew test --tests "*.HistoryRepositoryTest" exits 0 with all tests passing (old + new)
- New tests: zone filter returns only matching records, null zone returns all records, combined zone+effect filter
</acceptance_criteria>
</task>

<task id="13-04-T4">
<title>Extend ApplicationTest.kt — DI smoke and route integration for new signatures</title>

<read_first>
- C:\Development\textreaderrpi\src\test\kotlin\com\anjo\ApplicationTest.kt (CURRENT STATE — DI smoke test + existing route tests)
- C:\Development\textreaderrpi\src\main\kotlin\com\anjo\routing\Routing.kt (CURRENT STATE after plan 13-03)
</read_first>

<action>
MODIFY src/test/kotlin/com/anjo/ApplicationTest.kt:

1. Verify that the existing DI smoke test still passes after the signature changes.
   If the DI smoke test resolves all dependencies via configureDI(), it should still pass since
   ZoneRegistry was already a registered dependency before Phase 13.

2. ADD or UPDATE route integration tests for the new pages:

test("GET / returns HTML with zone select element") {
  // using testApplication { client.get("/") }
  response.status shouldBe HttpStatusCode.OK
  response.bodyAsText() shouldContain "id=\"zoneSelect\""  // or similar
}

test("GET /status returns HTML with status skeleton") {
  response.status shouldBe HttpStatusCode.OK
  response.bodyAsText() shouldContain "status-uptime"
}

test("GET /history returns HTML with zone filter") {
  response.status shouldBe HttpStatusCode.OK
  response.bodyAsText() shouldContain "name=\"zone\""
}

test("GET /history with zone query param returns 200") {
  val response = client.get("/history?zone=test-zone")
  response.status shouldBe HttpStatusCode.OK
  // zone filter in query does not cause a 500 error (correct handling of unknown zone)
}

test("GET /schedule returns HTML with zone column") {
  response.status shouldBe HttpStatusCode.OK
  response.bodyAsText() shouldContain "Zone"  // zone column header
}

NOTE: Use the existing testApplication {} pattern from the current ApplicationTest.kt.
NOTE: The DI smoke test that calls configureDI() should still pass unchanged. Verify it runs as part of the full test suite.
NOTE: These integration tests use the full application with H2 in-memory DB + OfflineDisplayDriver.
</action>

<acceptance_criteria>
- ApplicationTest.kt compiles: ./gradlew compileTestKotlin exits 0
- ./gradlew test --tests "*.ApplicationTest" exits 0 with all tests passing (old + new)
- New tests cover: GET / has zoneSelect, GET /status has skeleton IDs, GET /history has zone filter, GET /history?zone= returns 200, GET /schedule has Zone column header
</acceptance_criteria>
</task>

<task id="13-04-T5">
<title>Run full test suite and verify JaCoCo coverage gate</title>

<read_first>
- C:\Development\textreaderrpi\build.gradle.kts (JaCoCo configuration and coverage threshold)
</read_first>

<action>
Run the full test suite with coverage reporting:

COMMAND 1: ./gradlew test
Expected output: BUILD SUCCESSFUL, 0 test failures, ALL tests pass

COMMAND 2: ./gradlew jacocoTestReport jacocoTestCoverageVerification
Expected output: BUILD SUCCESSFUL (≥70% line coverage gate passes)

If coverage drops below 70%:
1. Check which classes are below threshold using the JaCoCo HTML report at build/reports/jacoco/test/html/index.html
2. Identify new uncovered code in template classes or repository layer
3. Add minimal targeted tests for uncovered logic (focus on HistoryRepository zone filter path and template render methods if they're included in coverage)

NOTE: Template classes in src/main/kotlin/com/anjo/web/templates/ may be excluded from JaCoCo via the jacocoTestReport excludes configuration in build.gradle.kts. If they ARE excluded, no additional template tests are needed for coverage. If NOT excluded, the tests added in tasks T1/T2 provide coverage.
</action>

<acceptance_criteria>
- ./gradlew test exits 0 (BUILD SUCCESSFUL, 0 failures)
- ./gradlew jacocoTestCoverageVerification exits 0 (line coverage ≥70%)
- Total test count is GREATER THAN the pre-Phase-13 count (new tests added)
- No existing tests have been deleted or weakened
</acceptance_criteria>
</task>

## Verification

Final verification after plan 13-04 completes:

```bash
./gradlew test jacocoTestReport jacocoTestCoverageVerification
```
Expected:
- BUILD SUCCESSFUL
- 0 test failures
- Coverage ≥70%

```bash
./gradlew shadowJar
```
Expected: BUILD SUCCESSFUL (application packages without errors)

## Notes for Executor

- Existing test patterns: use Kotest FunSpec with `test("description") {}` or `should("description") {}` per the project convention
- ZoneStatus constructor: read ZoneStatus.kt to find the exact constructor params before instantiating in tests
- H2 in-memory database setup: follow the exact same initialization pattern as existing HistoryRepositoryTest.kt (use the same test database config)
- The JaCoCo excludes in build.gradle.kts likely exclude template classes from coverage. Check the excludes list before deciding if template tests are needed for coverage purposes (they're still valuable for correctness regardless).
- If any test fails after all plans are applied, read the failure message carefully — most failures will be related to ZoneStatus constructor params or Schedule constructor params changing in Phase 11+11.2 vs what tests expect.

