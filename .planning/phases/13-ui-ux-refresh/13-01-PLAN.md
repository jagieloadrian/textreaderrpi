---
phase: 13-ui-ux-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/com/anjo/web/templates/BaseLayout.kt
  - src/main/resources/static/custom.css
autonomous: true
requirements: [UI-01, UI-02, UI-03]
must_haves:
  truths:
    - "Every page renders a persistent side nav (<aside>) with exactly 5 links: Send Text, Schedules, History, Zones, Status"
    - "No /settings/display link appears in the side nav"
    - "custom.css is served at /static/custom.css and linked from every page <head>"
    - "Dark Material 3 palette is the default; light palette applies under @media (prefers-color-scheme: light)"
    - "The <html> element has no data-theme attribute"
  artifacts:
    - path: "src/main/resources/static/custom.css"
      provides: "MD3 color tokens, PicoCSS overrides, side nav layout, effect-preview keyframes, zone card tint"
      contains: "--md-sys-color-surface"
    - path: "src/main/kotlin/com/anjo/web/templates/BaseLayout.kt"
      provides: "side-nav layout + headExtra hook + custom.css link"
      contains: "headExtra"
  key_links:
    - from: "src/main/kotlin/com/anjo/web/templates/BaseLayout.kt"
      to: "/static/custom.css"
      via: "link rel=stylesheet in <head>"
      pattern: "custom\\.css"
    - from: "src/main/resources/static/custom.css"
      to: "PicoCSS variables"
      via: ":root --pico-* override mappings"
      pattern: "--pico-background-color"
---

<objective>
Build the visual + structural foundation for the entire UI refresh: a new `custom.css` file carrying the Material 3 color system and side-nav layout, plus a rewritten `BaseLayout.kt` that replaces the top `<header>` nav with an `<aside>` side panel + mobile top bar, links `custom.css`, removes the `data-theme` attribute, removes the `/settings/display` link, and adds the `headExtra` hook (D-19).

Purpose: `BaseLayout.kt` is the single propagation point — every page (`IndexPage`, `SchedulePage`, `HistoryPage`, `ZonesPage`, `StatusPage`, `ErrorPage`) renders through it, so this plan must land in Wave 1 before any page template change (RESEARCH pitfall 4). Delivers UI-01 (MD3 layout), UI-02 (dark mode via prefers-color-scheme), UI-03 (5-link side nav).

Output: `custom.css` (new) + modified `BaseLayout.kt`. No page-template or route changes (those are Wave 2).
</objective>

<artifacts_this_phase_produces>
Symbols this plan creates (exclude from drift verification):
- `custom.css` — new file at `src/main/resources/static/custom.css`
- `BaseLayout.render(pageTitle, activePath, headExtra, content)` — new optional `headExtra: (HEAD.() -> Unit)? = null` parameter
- CSS custom properties: `--md-sys-color-surface`, `--md-sys-color-on-surface`, `--md-sys-color-primary`, `--md-sys-color-on-primary`, `--md-sys-color-secondary`, `--md-sys-color-surface-variant`, `--md-sys-color-outline`, `--md-sys-color-secondary-container`, `--md-sys-color-on-secondary-container`, `--md-sys-color-error`, `--md-sys-color-on-error`
- CSS classes: `.top-bar`, `.nav-open` (on body), `.nav-backdrop`, `.zone-card--discovered`, `.effect-preview`, `.effect-scroll`, `.effect-blink`, `.effect-reverse`, `.effect-fade`
- CSS `@keyframes`: `scroll-preview`, `blink-preview`, `reverse-preview`, `fade-preview`
- HTML element ids (referenced later by app.js in Plan 03): `navToggle`, `navBackdrop`
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-ui-ux-refresh/13-CONTEXT.md
@.planning/phases/13-ui-ux-refresh/13-UI-SPEC.md
@.planning/phases/13-ui-ux-refresh/13-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create custom.css with MD3 tokens, PicoCSS overrides, side-nav layout, animations</name>
  <files>src/main/resources/static/custom.css</files>
  <read_first>
    - .planning/phases/13-ui-ux-refresh/13-UI-SPEC.md (Color section: exact dark + light token values; Layout Contract: side nav 240px / 1200px max-width / 960px breakpoint; Component Inventory: effect-preview CSS + @keyframes definitions + status badges + zone card tint)
    - .planning/phases/13-ui-ux-refresh/13-CONTEXT.md (D-04, D-05, D-06, D-07, D-28, D-29 for color + layout decisions)
    - src/main/resources/static/app.js (lines 1-60 — confirms no existing CSS link; this is a brand-new file with no analog)
  </read_first>
  <action>
    Create `src/main/resources/static/custom.css` (no prior CSS file exists in the project — this is genuinely new). Implement per the 13-UI-SPEC.md Color and Layout contracts using these exact dependencies on prior decisions:

    1. `:root` block: declare the dark-palette MD3 tokens from UI-SPEC Color table (D-07): `--md-sys-color-surface: #1c1b1f`, `--md-sys-color-on-surface: #e6e1e5`, `--md-sys-color-primary: #d0bcff`, `--md-sys-color-on-primary: #381e72`, `--md-sys-color-secondary: #ccc2dc`, `--md-sys-color-surface-variant: #49454f`, `--md-sys-color-outline: #938f99`, `--md-sys-color-secondary-container: #4a4458`, `--md-sys-color-on-secondary-container: #e8def8`, `--md-sys-color-error: #cf6679`, `--md-sys-color-on-error: #601410`.
    2. In the same `:root` block, map PicoCSS variables to MD3 tokens (D-05): `--pico-background-color`, `--pico-color`, `--pico-primary`, `--pico-primary-foreground`, `--pico-secondary`, `--pico-border-color`, `--pico-card-background-color` — using `var(--md-sys-color-*)` per the PicoCSS mapping block in UI-SPEC.
    3. `@media (prefers-color-scheme: light)` block: override the same `--md-sys-color-*` tokens with the light values from UI-SPEC (surface `#fffbfe`, on-surface `#1c1b1f`, primary `#6750a4`, on-primary `#ffffff`, secondary `#625b71`, surface-variant `#e7e0ec`, outline `#79747e`, secondary-container `#e8def8`, on-secondary-container `#1d192b`, error `#b3261e`). Dark is the DEFAULT (in `:root`), light overrides only under the media query — matching D-04.
    4. Side-nav layout: `aside` = `width: 240px; position: fixed; top: 0; left: 0; height: 100vh; background: var(--md-sys-color-surface-variant); overflow-y: auto`. Active link rule: `aside nav a[aria-current="page"] { background-color: var(--md-sys-color-primary); color: var(--md-sys-color-on-primary); border-radius: 4px; }`. `main` = `margin-left: 240px; max-width: 1200px; margin-right: auto` on desktop.
    5. Mobile (`@media (max-width: 959px)`): hide `aside` by default (`transform: translateX(-100%); transition: transform .2s ease`), `main { margin-left: 0; }`, show `.top-bar`. `body.nav-open aside { transform: translateX(0); }` and `body.nav-open .nav-backdrop { display: block; }`. `.nav-backdrop { display: none; position: fixed; inset: 0; background: rgba(0,0,0,0.4); z-index: 1; }` and aside `z-index: 2`. Hide `.top-bar` on desktop (`@media (min-width: 960px) { .top-bar { display: none; } }`). Hamburger button min touch target 44px height.
    6. `.effect-preview` rule and `.effect-scroll`/`.effect-blink`/`.effect-reverse`/`.effect-fade` animation classes with the four `@keyframes` (`scroll-preview`, `blink-preview`, `reverse-preview`, `fade-preview`) exactly as specified in the UI-SPEC Effect Preview Animations table.
    7. `.zone-card--discovered { background-color: var(--md-sys-color-secondary-container); color: var(--md-sys-color-on-secondary-container); }` (D-28).
    8. Status badge helper classes are optional; the UI-SPEC inline-style badges already exist in ZonesPage. Spacing values must be multiples of 4 per UI-SPEC Spacing Scale.

    Do NOT add a CDN change — PicoCSS stays as the base (D-05). Do NOT introduce any JS framework styling. CSS only.
  </action>
  <verify>
    <automated>test -f src/main/resources/static/custom.css && grep -q -- '--md-sys-color-surface' src/main/resources/static/custom.css && grep -q -- '--pico-background-color' src/main/resources/static/custom.css && grep -q 'prefers-color-scheme: light' src/main/resources/static/custom.css && grep -q 'scroll-preview' src/main/resources/static/custom.css && grep -q 'zone-card--discovered' src/main/resources/static/custom.css && echo CSS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/resources/static/custom.css` exists
    - File contains `--md-sys-color-surface` and 10 other `--md-sys-color-*` tokens defined in `:root`
    - File contains a `@media (prefers-color-scheme: light)` block redefining `--md-sys-color-surface`
    - File maps `--pico-background-color` to `var(--md-sys-color-surface)`
    - File defines `@keyframes scroll-preview`, `blink-preview`, `reverse-preview`, `fade-preview`
    - File defines `.zone-card--discovered` using `var(--md-sys-color-secondary-container)`
    - File defines `aside` width 240px and `main` margin-left 240px under a min-width:960px context, with mobile `.nav-open` rules under max-width:959px
  </acceptance_criteria>
  <done>custom.css renders the MD3 dark palette by default, light under prefers-color-scheme:light, side nav layout for desktop + mobile overlay, effect-preview keyframes, and the discovered-zone tint class.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite BaseLayout.kt — aside side nav + mobile top bar + custom.css link + headExtra hook + remove data-theme and /settings/display</name>
  <files>src/main/kotlin/com/anjo/web/templates/BaseLayout.kt</files>
  <read_first>
    - src/main/kotlin/com/anjo/web/templates/BaseLayout.kt (current top-nav structure, lines 21-52 — the file being modified)
    - .planning/phases/13-ui-ux-refresh/13-PATTERNS.md (BaseLayout.kt section: new render() signature with headExtra, required new imports, active-link aria-current pattern to preserve, 5 side nav links)
    - .planning/phases/13-ui-ux-refresh/13-CONTEXT.md (D-01, D-02, D-03, D-06, D-19, D-25 — side nav structure, hamburger, headExtra, settings removal)
    - .planning/phases/13-ui-ux-refresh/13-UI-SPEC.md (Layout Contract: side nav links + active pill + mobile top bar + backdrop)
    - src/main/kotlin/com/anjo/web/templates/IndexPage.kt (lines 14-19 — confirms callers use `BaseLayout.render(pageTitle, activePath) { ... }` form; new headExtra param must default to null so all callers compile unchanged)
  </read_first>
  <action>
    Rewrite `BaseLayout.render()` to the new signature: `fun render(pageTitle: String, activePath: String, headExtra: (HEAD.() -> Unit)? = null, content: FlowContent.() -> Unit): String`. `headExtra` defaults to `null` so all six existing callers (IndexPage, SchedulePage via scheduleUIRoutes, HistoryPage via historyUIRoutes, ZonesPage via zonesUIRoutes, StatusPage, ErrorPage) compile unchanged.

    In `<html>`: REMOVE `attributes["data-theme"] = "dark"` (D-06 — theme is now fully controlled by prefers-color-scheme). Keep `attributes["lang"] = "en"`.

    In `<head>`: keep title/meta charset/meta viewport/PicoCSS CDN link. ADD `link(rel = "stylesheet", href = "/static/custom.css")` AFTER the PicoCSS link (D-06). ADD `headExtra?.invoke(this)` as the last statement inside `head { }` (the `head {}` receiver is `HEAD`).

    In `<body>`: REPLACE the `header(classes = "container") { h1 + nav }` block with:
    - A mobile top bar: `div { attributes["class"] = "top-bar" }` containing the app name text "TextReaderRpi" and a `button { id = "navToggle"; +"☰" }` (use `<button>`, agent's discretion per CONTEXT). Import `kotlinx.html.button`.
    - An `aside { }` (import `kotlinx.html.aside`) containing `h1 { +"TextReaderRpi" }` then `nav { ul { ... } }` with EXACTLY 5 links, each preserving the `attributes["aria-current"] = if (activePath == "<path>") "page" else ""` pattern:
      - Send Text → `/`
      - Schedules → `/schedule`
      - History → `/history`
      - Zones → `/zones`
      - Status → `/status`
      REMOVE the `/settings/display` link entirely (D-25). REMOVE the old "Home" label — first link is now labeled "Send Text".
    - A `div { id = "navBackdrop"; attributes["class"] = "nav-backdrop" }` for the mobile overlay backdrop (D-03).
    - Keep `main(classes = "container") { content() }`, `footer(classes = "container") { +"TextReaderRpi" }`, and `script(src = "/static/app.js") {}`.

    Imports: ADD `import kotlinx.html.aside`, `import kotlinx.html.button`, `import kotlinx.html.HEAD`, `import kotlinx.html.div`, `import kotlinx.html.id`. REMOVE `import kotlinx.html.header` (no longer used). Keep existing imports still referenced.

    Do NOT touch any page template in this plan. Do NOT change WebRoutes.kt (that is Plan 03's job). Do NOT delete SettingsPage.kt here (Plan 03 deletes it alongside the route).
  </action>
  <verify>
    <automated>./gradlew compileKotlin -q 2>&1 | tail -5; grep -q 'headExtra' src/main/kotlin/com/anjo/web/templates/BaseLayout.kt && grep -q 'custom.css' src/main/kotlin/com/anjo/web/templates/BaseLayout.kt && grep -q 'aside' src/main/kotlin/com/anjo/web/templates/BaseLayout.kt && ! grep -q 'data-theme' src/main/kotlin/com/anjo/web/templates/BaseLayout.kt && ! grep -q 'settings/display' src/main/kotlin/com/anjo/web/templates/BaseLayout.kt && echo BASELAYOUT_OK</automated>
  </verify>
  <acceptance_criteria>
    - `BaseLayout.kt` compiles (`./gradlew compileKotlin` exits 0)
    - `render()` signature contains `headExtra: (HEAD.() -> Unit)? = null`
    - File contains `link(rel = "stylesheet", href = "/static/custom.css")`
    - File contains `aside {` and `id = "navToggle"` and `id = "navBackdrop"`
    - File does NOT contain `data-theme`
    - File does NOT contain `settings/display`
    - Side nav contains exactly 5 `a(href=...)` link targets: `/`, `/schedule`, `/history`, `/zones`, `/status`
    - All existing callers compile unchanged (`./gradlew compileKotlin` succeeds without touching IndexPage/StatusPage/etc.)
  </acceptance_criteria>
  <done>BaseLayout renders an aside side nav with 5 links + mobile top bar + backdrop, links custom.css, exposes a headExtra hook, has no data-theme attr, and no settings link. All existing page callers still compile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → static asset server | `/static/custom.css` is served by Ktor `staticResources`; no user input crosses here |
| browser → BaseLayout-rendered HTML | `activePath` is a server-supplied constant per route (not user input); page titles are constants |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure | custom.css served at /static | accept | CSS contains only color tokens + layout; no secrets. Already-public static dir (app.js served identically). |
| T-13-02 | Tampering | BaseLayout activePath reflected into aria-current | accept | activePath is a hardcoded route constant per call site, never user-controlled; no reflection of request input. |
| T-13-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — zero new dependencies (RESEARCH confirms). No legitimacy gate needed. |
</threat_model>

<verification>
- `./gradlew compileKotlin` exits 0 (BaseLayout signature change compiles; all callers default headExtra=null)
- `test -f src/main/resources/static/custom.css` succeeds
- `grep` gates in each task pass
</verification>

<success_criteria>
- custom.css exists with MD3 dark+light palettes, PicoCSS overrides, side-nav layout, effect keyframes, discovered-zone tint
- BaseLayout renders an aside side nav (5 links, no settings), mobile top bar + backdrop, custom.css link, headExtra hook, no data-theme attr
- All six existing page callers compile unchanged
</success_criteria>

<output>
Create `.planning/phases/13-ui-ux-refresh/13-01-SUMMARY.md` when done
</output>
