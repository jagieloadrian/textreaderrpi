---
phase: 11.2-code-quality-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/com/anjo/validation/IpValidation.kt
  - src/main/kotlin/com/anjo/model/AddZoneRequest.kt
  - src/main/kotlin/com/anjo/validation/RequestValidators.kt
  - src/main/kotlin/com/anjo/validation/RequestValidationConfig.kt
  - src/main/kotlin/com/anjo/routing/ZoneRoutes.kt
  - src/main/kotlin/com/anjo/routing/DisplayRoutes.kt
  - src/main/kotlin/com/anjo/config/model/DisplayConfig.kt
  - src/main/kotlin/com/anjo/config/model/ZonesConfig.kt
  - src/main/kotlin/com/anjo/config/loader/ConfigLoader.kt
  - src/main/kotlin/com/anjo/service/ZoneRegistry.kt
  - src/test/kotlin/com/anjo/routing/ZoneRoutesTest.kt
  - src/test/kotlin/com/anjo/routing/WebAndDisplayRoutesTest.kt
autonomous: true
requirements: [REF-11.2]
must_haves:
  truths:
    - "POST /api/v1/zones with an invalid/public IP in the body returns 422 (validation plugin)"
    - "POST /api/v1/zones with a valid RFC1918 IP in the body returns 201"
    - "POST /api/v1/display/select with an unknown type returns 422 (validation plugin)"
    - "No route handler contains IP or display-type if-chain validation logic"
    - "DisplayConfig.type and ZoneConfig.type are DisplayType, not String"
    - "ZoneRegistry.initLocalZone dispatches on DisplayType enum cases, not string literals"
  artifacts:
    - path: "src/main/kotlin/com/anjo/model/AddZoneRequest.kt"
      provides: "Serializable request body model carrying the IP for manual zone add"
      contains: "data class AddZoneRequest"
    - path: "src/main/kotlin/com/anjo/validation/RequestValidators.kt"
      provides: "validateAddZoneRequest + validateDisplaySelectRequest"
      contains: "fun validateAddZoneRequest"
    - path: "src/main/kotlin/com/anjo/validation/RequestValidationConfig.kt"
      provides: "validate<AddZoneRequest> and validate<DisplaySelectRequest> plugin blocks"
      contains: "validate<AddZoneRequest>"
  key_links:
    - from: "src/main/kotlin/com/anjo/routing/ZoneRoutes.kt"
      to: "AddZoneRequest"
      via: "call.receive<AddZoneRequest>()"
      pattern: "receive<AddZoneRequest>"
    - from: "src/main/kotlin/com/anjo/service/ZoneRegistry.kt"
      to: "DisplayType"
      via: "when (zoneConfig.type)"
      pattern: "DisplayType\\.MAX7219"
---

<objective>
Migrate IP validation and display-type validation out of route handlers into the Ktor `RequestValidation` plugin, and enforce the `DisplayType` enum across config models. Implements D-01 through D-10 and D-22.

Purpose: Eliminate "ifologii" (if-chains) from route handlers and string-literal dispatch from config-driven code, per REF-11.2 and the user's explicit requests.
Output: `IpValidation` deleted; `AddZoneRequest` model + two plugin validators added; `ZoneRoutes` POST becomes body-based; `DisplayRoutes` POST /select if-chain removed; `DisplayConfig.type`/`ZoneConfig.type` become `DisplayType`; `ConfigLoader` converts via `fromString`; `ZoneRegistry.initLocalZone` dispatches on enum; affected tests updated to expect 422 and body-based POST.
</objective>

<artifacts_this_phase_produces>
Symbols and files created or changed by THIS plan (exclude from drift verification):
- `com.anjo.model.AddZoneRequest` — new `@Serializable data class AddZoneRequest(val ip: String)`
- `com.anjo.validation.RequestValidators.validateAddZoneRequest(req: AddZoneRequest): ValidationResult` — new function
- `com.anjo.validation.RequestValidators.validateDisplaySelectRequest(req: DisplaySelectRequest): ValidationResult` — new function
- New plugin blocks in `RequestValidationConfig.kt`: `validate<AddZoneRequest>`, `validate<DisplaySelectRequest>`
- `DisplayConfig.type` field type changed `String` → `DisplayType` (default `DisplayType.MAX7219`)
- `ZoneConfig.type` field type changed `String` → `DisplayType` (default `DisplayType.MAX7219`)
- `ZoneRoutes` route shape changed: `post("/{ip}")` → `post("/")` (body-based)
- DELETED file: `src/main/kotlin/com/anjo/validation/IpValidation.kt` (object `IpValidation` removed)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.2-code-quality-cleanup/11.2-CONTEXT.md
@.planning/phases/11.2-code-quality-cleanup/11.2-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: IP validation migration — delete IpValidation, add AddZoneRequest + plugin validator, flip ZoneRoutes to body</name>
  <files>src/main/kotlin/com/anjo/validation/IpValidation.kt, src/main/kotlin/com/anjo/model/AddZoneRequest.kt, src/main/kotlin/com/anjo/validation/RequestValidators.kt, src/main/kotlin/com/anjo/validation/RequestValidationConfig.kt, src/main/kotlin/com/anjo/routing/ZoneRoutes.kt, src/test/kotlin/com/anjo/routing/ZoneRoutesTest.kt</files>
  <read_first>
    - src/main/kotlin/com/anjo/validation/IpValidation.kt (RFC1918 logic to migrate verbatim — octet split, 0..255 bounds, 10/172.16-31/192.168 ranges)
    - src/main/kotlin/com/anjo/validation/RequestValidators.kt (object pattern; ValidationResult.Valid/Invalid usage; remove its 3 inline comments)
    - src/main/kotlin/com/anjo/validation/RequestValidationConfig.kt (existing validate<TextRequest> / validate<Schedule> blocks; add new blocks inside same install lambda)
    - src/main/kotlin/com/anjo/routing/ZoneRoutes.kt (current post("/{ip}") handler — containsIp/findById 409 logic, NetworkZone construction with type=DisplayType.MAX7219.name)
    - src/test/kotlin/com/anjo/routing/ZoneRoutesTest.kt (5 call sites use post("/api/v1/zones/$ip"))
    - src/main/kotlin/com/anjo/di/ErrorHandling.kt (RequestValidationException maps to HttpStatusCode.UnprocessableEntity = 422, code VAL_001)
  </read_first>
  <action>
    Delete `src/main/kotlin/com/anjo/validation/IpValidation.kt` entirely (D-01).
    Create `src/main/kotlin/com/anjo/model/AddZoneRequest.kt` (D-02): package `com.anjo.model`, a `@Serializable data class AddZoneRequest(val ip: String)` (import `kotlinx.serialization.Serializable`).
    In `RequestValidators.kt` (D-03): add object function `validateAddZoneRequest(req: AddZoneRequest): ValidationResult` whose body replicates `IpValidation.isValidPrivateIpv4` logic exactly — split ip on ".", require 4 parts else Invalid, map parts to Int catching NumberFormatException → Invalid, require every octet in 0..255 else Invalid, then RFC1918 `when` (octet0==10 || octet0==172 && octet1 in 16..31 || octet0==192 && octet1==168) → Valid else Invalid. Use the single Invalid message string "IP must be a valid RFC1918 private address" for every failure branch. Import `com.anjo.model.AddZoneRequest`. Also REMOVE the three existing inline comments in this file (`// Check for blank text`, `// Check length against configured max`, `// All validations passed`) per D-19 (Pitfall 4).
    In `RequestValidationConfig.kt` (D-03): inside the existing `install(RequestValidation) {}` lambda add `validate<AddZoneRequest> { req -> RequestValidators.validateAddZoneRequest(req) }`. Add import `com.anjo.model.AddZoneRequest`.
    In `ZoneRoutes.kt` (D-04, D-21): change `post("/{ip}")` to `post("/")`. Body: `val req = call.receive<AddZoneRequest>()`; then the existing duplicate checks `if (zoneRegistry.containsIp(req.ip))` → 409 Conflict, `if (zoneRepository.findById(req.ip) != null)` → 409 Conflict; build `NetworkZone(id=req.ip, name=req.ip, ip=req.ip, type=DisplayType.MAX7219.name, discoveryMethod="MANUAL", createdAt=Instant.now().toString(), lastSeenAt=null)`; `zoneRepository.upsert(zone)`; `zoneRegistry.addNetworkZone(zone)`; `call.respond(HttpStatusCode.Created, zone)`. Remove the `if (!IpValidation.isValidPrivateIpv4(ip))` block and the `import com.anjo.validation.IpValidation` line. Add `import com.anjo.model.AddZoneRequest` and `import io.ktor.server.request.receive`. Keep the GET, DELETE /{id}, and POST /discover handlers unchanged.
    In `ZoneRoutesTest.kt`: rewrite the 4 POST-to-add call sites from path form to body form. For each, use `client.post("/api/v1/zones") { header(HttpHeaders.ContentType, ContentType.Application.Json.toString()); setBody("""{"ip":"<IP>"}""") }`. Change the two tests that assert `HttpStatusCode.BadRequest` for invalid IP (8.8.8.8 public, not-an-ip) to assert `HttpStatusCode.UnprocessableEntity` (Pitfall 1). Keep the 201 test (192.168.1.50) and the 409 duplicate test (192.168.1.99 twice, same testApplication block). Add imports `io.ktor.client.request.setBody`, `io.ktor.client.request.header`, `io.ktor.http.ContentType`, `io.ktor.http.HttpHeaders`. Leave the GET zones and POST /discover tests unchanged.
    Add NO comments to any edited file (D-19).
  </action>
  <verify>
    <automated>cd /home/diether18/IdeaProjects/TextReaderRpi && ./gradlew test --tests "com.anjo.routing.ZoneRoutesTest" -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/kotlin/com/anjo/validation/IpValidation.kt` does not exist: `test ! -f src/main/kotlin/com/anjo/validation/IpValidation.kt`
    - `src/main/kotlin/com/anjo/model/AddZoneRequest.kt` contains `data class AddZoneRequest`
    - `RequestValidators.kt` contains `fun validateAddZoneRequest`
    - `RequestValidators.kt` has zero `//` comment lines: `grep -c '//' src/main/kotlin/com/anjo/validation/RequestValidators.kt` returns 0
    - `RequestValidationConfig.kt` contains `validate<AddZoneRequest>`
    - `ZoneRoutes.kt` contains `receive<AddZoneRequest>` and does NOT contain `IpValidation`
    - `ZoneRoutes.kt` contains `post("/")` and does NOT contain `post("/{ip}")`
    - `ZoneRoutesTest.kt` contains no occurrence of `zones/8.8.8.8` or `zones/192.168` (grep returns nothing): `grep -c 'zones/192.168\|zones/8.8.8.8' src/test/kotlin/com/anjo/routing/ZoneRoutesTest.kt` returns 0
    - `./gradlew test --tests "com.anjo.routing.ZoneRoutesTest"` exits 0 with all tests green
  </acceptance_criteria>
  <done>IpValidation deleted; AddZoneRequest model and validator wired through the plugin; ZoneRoutes POST is body-based; ZoneRoutesTest green with 422 assertions and JSON-body POSTs.</done>
</task>

<task type="auto">
  <name>Task 2: DisplayType enum enforcement — config models, ConfigLoader, ZoneRegistry, DisplayRoutes validation</name>
  <files>src/main/kotlin/com/anjo/config/model/DisplayConfig.kt, src/main/kotlin/com/anjo/config/model/ZonesConfig.kt, src/main/kotlin/com/anjo/config/loader/ConfigLoader.kt, src/main/kotlin/com/anjo/service/ZoneRegistry.kt, src/main/kotlin/com/anjo/routing/DisplayRoutes.kt, src/main/kotlin/com/anjo/validation/RequestValidators.kt, src/main/kotlin/com/anjo/validation/RequestValidationConfig.kt, src/test/kotlin/com/anjo/routing/WebAndDisplayRoutesTest.kt</files>
  <read_first>
    - src/main/kotlin/com/anjo/model/DisplayType.kt (enum MAX7219, LCD, OLED, UNKNOWN; fromString returns UNKNOWN for unknown input)
    - src/main/kotlin/com/anjo/config/model/DisplayConfig.kt (type: String = "MAX7219")
    - src/main/kotlin/com/anjo/config/model/ZonesConfig.kt (ZoneConfig.type: String = "MAX7219")
    - src/main/kotlin/com/anjo/config/loader/ConfigLoader.kt (THREE type sites: display.type in loadConfig line ~25, display.zones.$index.type line ~103, fallback display.type in loadZonesConfig line ~111)
    - src/main/kotlin/com/anjo/service/ZoneRegistry.kt (initLocalZone when(zoneConfig.type.uppercase()) string-literal dispatch; LocalZoneDriver constructor takes type: String)
    - src/main/kotlin/com/anjo/routing/DisplayRoutes.kt (post("/select") allowed-set if-chain to remove)
    - src/main/kotlin/com/anjo/model/DisplayModels.kt (DisplaySelectRequest.type stays String per D-08)
    - src/main/kotlin/com/anjo/zone/LocalZoneDriver.kt (type: String constructor param — receives DisplayType.name)
    - src/test/kotlin/com/anjo/routing/WebAndDisplayRoutesTest.kt (the "should reject invalid driver type with 400" test asserting BadRequest + "Unsupported driver type")
  </read_first>
  <action>
    `DisplayConfig.kt` (D-05): change `val type: String = "MAX7219"` to `val type: DisplayType = DisplayType.MAX7219`. Add `import com.anjo.model.DisplayType`.
    `ZonesConfig.kt` (D-07): change `ZoneConfig.val type: String = "MAX7219"` to `val type: DisplayType = DisplayType.MAX7219`. Add `import com.anjo.model.DisplayType`.
    `ConfigLoader.kt` (D-22, Pitfall 6): at all THREE sites, wrap the parsed string in `DisplayType.fromString(...)`, defaulting the raw string to "MAX7219" before conversion, then if result is `DisplayType.UNKNOWN` log a warn ("Unknown display type ... defaulting to MAX7219") and substitute `DisplayType.MAX7219`. Site 1: `displayConfig.type` in loadConfig. Site 2: `type` local in loadZonesConfig per-zone loop. Site 3: `fallbackType` in loadZonesConfig empty-zones branch. Add `import com.anjo.model.DisplayType`. Extract a single private helper `parseDisplayType(raw: String?): DisplayType` if it reduces duplication across the three sites (Claude's discretion — follow existing private-fun style in this object).
    `ZoneRegistry.kt` (D-06, Pitfall 5): in `initLocalZone`, change `when (zoneConfig.type.uppercase())` to `when (zoneConfig.type)` with enum cases `DisplayType.MAX7219 -> Max7219Matrix(...)`, `DisplayType.LCD -> LcdDisplay(ctx)`, `DisplayType.OLED -> OledDisplay(ctx)`, `DisplayType.UNKNOWN -> { log.warn(...); OfflineDisplayDriver }` (the old `else` becomes the explicit UNKNOWN case — Kotlin enforces exhaustiveness). At the `LocalZoneDriver(id = ..., type = zoneConfig.type, driver = driver)` call, change to `type = zoneConfig.type.name` because LocalZoneDriver.type is String. `DisplayType` is already imported in this file.
    `DisplayRoutes.kt` (D-09, D-21): remove the `val normalized = request.type.lowercase()`, `val allowed = setOf(...)`, and `if (normalized !in allowed) { ... return@post }` block. Handler body becomes: `val request = call.receive<DisplaySelectRequest>()`; `val queued = screenDriverService.queueDisplaySwitch(request.type)`; if not queued respond BadRequest DisplaySelectResponse(accepted=false, message="Driver switch rejected"); else respond DisplaySelectResponse(accepted=true, message="Driver switch queued: ${request.type}"). Keep the GET /status handler unchanged. The type-validity check now lives in the plugin (next edit).
    `RequestValidators.kt` (D-08): add object function `validateDisplaySelectRequest(req: DisplaySelectRequest): ValidationResult` returning `Invalid("Unsupported driver type: ${req.type}")` when `DisplayType.fromString(req.type) == DisplayType.UNKNOWN`, else `Valid`. Import `com.anjo.model.DisplaySelectRequest` and `com.anjo.model.DisplayType`. (Message keeps the substring "Unsupported driver type" so the existing test body assertion still matches — Assumption A2.)
    `RequestValidationConfig.kt` (D-08): add `validate<DisplaySelectRequest> { req -> RequestValidators.validateDisplaySelectRequest(req) }` in the install lambda. Add import `com.anjo.model.DisplaySelectRequest`.
    `WebAndDisplayRoutesTest.kt` (Pitfall 1, A2): in the "should reject invalid driver type" test change `response.status shouldBe HttpStatusCode.BadRequest` to `HttpStatusCode.UnprocessableEntity`. Keep `response.bodyAsText() shouldContain "Unsupported driver type"`. Rename the test string from "with 400" to "with 422" if present. Leave all other tests unchanged.
    Add NO comments to any edited file (D-19).
  </action>
  <verify>
    <automated>cd /home/diether18/IdeaProjects/TextReaderRpi && ./gradlew test --tests "com.anjo.routing.WebAndDisplayRoutesTest" --tests "com.anjo.service.ZoneRegistryTest" -q</automated>
  </verify>
  <acceptance_criteria>
    - `DisplayConfig.kt` contains `val type: DisplayType = DisplayType.MAX7219`
    - `ZonesConfig.kt` contains `val type: DisplayType = DisplayType.MAX7219`
    - `ConfigLoader.kt` contains `DisplayType.fromString` and has zero remaining bare `?: "MAX7219"` String assignments to a type field: `grep -c 'DisplayType.fromString' src/main/kotlin/com/anjo/config/loader/ConfigLoader.kt` is at least 1
    - `ZoneRegistry.kt` contains `DisplayType.MAX7219 ->` and `zoneConfig.type.name`; does NOT contain `.uppercase()` in initLocalZone
    - `DisplayRoutes.kt` does NOT contain `setOf(` and does NOT contain `!in allowed`
    - `RequestValidators.kt` contains `fun validateDisplaySelectRequest` and still has zero `//` comment lines
    - `RequestValidationConfig.kt` contains `validate<DisplaySelectRequest>`
    - `WebAndDisplayRoutesTest.kt` invalid-driver test asserts `HttpStatusCode.UnprocessableEntity`
    - `./gradlew test --tests "com.anjo.routing.WebAndDisplayRoutesTest" --tests "com.anjo.service.ZoneRegistryTest"` exits 0
  </acceptance_criteria>
  <done>Config models use DisplayType; ConfigLoader converts all three sites with UNKNOWN→MAX7219 fallback; ZoneRegistry dispatches on enum and passes .name to LocalZoneDriver; DisplayRoutes if-chain removed with validation in the plugin returning 422; affected tests green.</done>
</task>

</tasks>

<verification>
- `./gradlew test --tests "com.anjo.routing.*"` passes (route-layer suite, includes ZoneRoutesTest + WebAndDisplayRoutesTest).
- `./gradlew test --tests "com.anjo.service.ZoneRegistryTest"` passes.
- No route handler in `ZoneRoutes.kt` or `DisplayRoutes.kt` contains validation if-chains.
- `grep -rn "IpValidation" src/` returns no results.
</verification>

<success_criteria>
- D-01 through D-10 and D-22 implemented exactly.
- IP and display-type validation occur in the `RequestValidation` plugin, producing 422 on failure.
- `DisplayConfig.type` and `ZoneConfig.type` are `DisplayType`; `ConfigLoader` converts all three sites with UNKNOWN→MAX7219 fallback.
- `ZoneRegistry.initLocalZone` uses exhaustive enum `when`.
- No comments added (D-19).
</success_criteria>

<output>
Create `.planning/phases/11.2-code-quality-cleanup/11.2-01-SUMMARY.md` when done.
</output>
