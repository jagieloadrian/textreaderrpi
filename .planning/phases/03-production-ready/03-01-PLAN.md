---
phase: 03-production-ready
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/com/anjo/di/Monitoring.kt
  - src/main/kotlin/com/anjo/routing/Routing.kt
  - src/main/kotlin/com/anjo/service/HealthService.kt
  - src/test/kotlin/com/anjo/service/HealthServiceTest.kt
  - src/test/kotlin/com/anjo/routing/HealthRoutesTest.kt
autonomous: true
requirements:
  - R3-HEALTH-ENDPOINTS
must_haves:
  truths:
    - "Health endpoints return deterministic status for uptime checks."
    - "Health behavior follows locked endpoint policy (per D-01)."
    - "No /metrics endpoint is introduced (deferred decision)."
  artifacts:
    - "src/main/kotlin/com/anjo/service/HealthService.kt"
    - "src/main/kotlin/com/anjo/routing/Routing.kt"
    - "src/test/kotlin/com/anjo/routing/HealthRoutesTest.kt"
  key_links:
    - "Routing.kt -> HealthService via DI"
    - "Health routes -> response schema consumed by probes"
---

<objective>
Implement production health endpoints with policy-locked behavior for probes and orchestration checks.

Purpose: Make service health observable without adding deferred metrics scope.
Output: Health service + route wiring + tests.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-production-ready/03-CONTEXT.md
@.planning/phases/03-production-ready/03-RESEARCH.md
@src/main/kotlin/com/anjo/di/Monitoring.kt
@src/main/kotlin/com/anjo/routing/Routing.kt
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add health status service and tests</name>
  <read_first>
    src/main/kotlin/com/anjo/di/Monitoring.kt
    src/main/resources/application.yaml
    .planning/phases/03-production-ready/03-CONTEXT.md
  </read_first>
  <action>Create HealthService with policy-aligned health checks and explicit status payload per D-01. Add unit tests for healthy and degraded states. Do not add metrics collectors or /metrics handlers (deferred scope).</action>
  <verify>
    <automated>./gradlew test --tests "*HealthServiceTest"</automated>
  </verify>
  <acceptance_criteria>HealthService returns expected HTTP status and JSON fields for all policy states defined in CONTEXT.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire health endpoints in Routing.kt</name>
  <read_first>
    src/main/kotlin/com/anjo/routing/Routing.kt
    src/main/kotlin/com/anjo/Application.kt
    src/main/kotlin/com/anjo/di/Monitoring.kt
  </read_first>
  <action>Register health route(s) exactly as locked in D-01 and route through HealthService via DI. Add route tests validating status codes, payload shape, and endpoint paths. Keep route composition in Routing.kt and avoid business logic there.</action>
  <verify>
    <automated>./gradlew test --tests "*HealthRoutesTest"</automated>
  </verify>
  <acceptance_criteria>Configured endpoint paths respond as specified by D-01 and tests confirm no /metrics route exists.</acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| probe/client -> HTTP routes | Untrusted request input reaches health handlers |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-01 | DoS | health routes | mitigate | Constant-time lightweight checks and no blocking IO in route handlers |
| T-03-02 | Information Disclosure | health payload | mitigate | Return minimal operational fields only; exclude secrets/internal stack traces |
| T-03-SC | Tampering | dependency installs | accept | No new packages in this plan |
</threat_model>

<verification>
Run `./gradlew test --tests "*Health*"` and `./gradlew test`.
</verification>

<success_criteria>
Health endpoints are test-covered, policy-compliant, and /metrics remains deferred.
</success_criteria>

<output>
Create `.planning/phases/03-production-ready/03-01-SUMMARY.md` when done.
</output>

