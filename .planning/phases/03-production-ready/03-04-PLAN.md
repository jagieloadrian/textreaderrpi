---
phase: 03-production-ready
plan: 04
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/main/kotlin/com/anjo/di/RateLimiting.kt
  - src/main/kotlin/com/anjo/routing/Routing.kt
  - src/main/resources/application.yaml
  - src/test/kotlin/com/anjo/routing/RateLimitRoutesTest.kt
autonomous: true
requirements:
  - R3-RATE-LIMITING
must_haves:
  truths:
    - "Requests above the threshold are throttled with configured behavior."
    - "Rate-limit policy matches locked decision values and headers."
  artifacts:
    - "src/main/kotlin/com/anjo/di/RateLimiting.kt"
    - "src/test/kotlin/com/anjo/routing/RateLimitRoutesTest.kt"
  key_links:
    - "Routing.kt -> rate limiter plugin from DI"
    - "application.yaml -> rate limiting config consumed at startup"
---

<objective>
Implement policy-locked request rate limiting for production abuse protection.

Purpose: Protect service availability and enforce fair usage.
Output: Rate limiter config, route integration, tests.
</objective>

<context>
@.planning/phases/03-production-ready/03-CONTEXT.md
@src/main/kotlin/com/anjo/routing/Routing.kt
@src/main/resources/application.yaml
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Replace custom limiter with Flaxoos plugin</name>
  <read_first>
    src/main/resources/application.yaml
    src/main/kotlin/com/anjo/di/Monitoring.kt
    .planning/phases/03-production-ready/03-CONTEXT.md
  </read_first>
  <action>Replace custom `RateLimitPlugin`/`RateLimiter` implementation with `io.github.flaxoos:ktor-server-rate-limiting`. Keep config-driven capacity and enforce HTTP 429 + `Retry-After` policy.</action>
  <verify>
    <automated>./gradlew test --tests "*RateLimit*"</automated>
  </verify>
  <acceptance_criteria>Flaxoos-based limiter initializes from config and all previous behavior checks pass with no custom limiter classes required in runtime path.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply limiter to selected routes in Routing.kt</name>
  <read_first>
    src/main/kotlin/com/anjo/routing/Routing.kt
    src/main/kotlin/com/anjo/di/RateLimiting.kt
  </read_first>
  <action>Integrate rate limiting in route composition for protected endpoints and add route tests verifying allow/deny transitions, status code, and required headers.</action>
  <verify>
    <automated>./gradlew test --tests "*RateLimitRoutesTest"</automated>
  </verify>
  <acceptance_criteria>Requests over limit receive policy-defined response and compliant headers; under-limit requests remain unaffected.</acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| external clients -> API routes | Untrusted clients can flood endpoints |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-07 | DoS | route handlers | mitigate | Enforced request quotas and retry windows |
| T-03-08 | Spoofing | client identity key | mitigate | Use trusted remote address parsing strategy in server context |
| T-03-SC | Tampering | dependency installs | accept | No new packages in this plan |
</threat_model>

<verification>
Run `./gradlew test --tests "*RateLimit*"` and targeted route tests.
</verification>

<success_criteria>
Rate limiting is active and validated against locked decision behavior.
</success_criteria>

<output>
Create `.planning/phases/03-production-ready/03-04-SUMMARY.md` when done.
</output>

