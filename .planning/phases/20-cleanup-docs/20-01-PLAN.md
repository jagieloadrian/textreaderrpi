---
phase: 20-cleanup-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: [DOCS-01]
user_setup: []

must_haves:
  truths:
    - "README has a Table of Contents linking every top-level section (D-11)"
    - "README API tables document GET /api/v1/live, GET /api/v1/history/export, and WS /ws/zone/{id} with a usage example each (D-12)"
    - "README History section lists the search query param; Zones section shows FIRMWARE-type registration"
    - "Configuration table GPIO SPI defaults match application.yaml (24/19/23), and 4 previously-missing vars are documented"
    - "No dangling docs/ links remain in the Configuration or Deployment sections (D-04)"
  artifacts:
    - "README.md '## Table of Contents' section"
    - "README.md '### Live Feed' API subsection"
    - "README.md updated '### History', '### Zones', '### Display' subsections"
    - "README.md extended Configuration table + Helm defaults"
  key_links:
    - "README endpoint rows must match the exact mount paths in src/main/kotlin/com/anjo/routing/*.kt (source of truth, not memory)"
    - "Config-table defaults must match src/main/resources/application.yaml"
---

<objective>
Bring the existing README.md reference sections current for v1.2: add a Table of Contents (D-11), extend the API tables with the three new endpoints and the changed History/Zones/Display behavior (D-12), fix the Configuration table against application.yaml, extend the Helm deployment defaults, and remove the two dangling docs/ links that this phase's cleanup makes dead (D-04).

Purpose: SC3/SC4 — README lists all v1.2 endpoints and accurately reflects current config/deployment state. This plan handles every README section EXCEPT the new Firmware walkthrough (Plan 02) and the final accuracy sweep (Plan 03).
Output: An updated README.md whose API, Configuration, and Deployment sections are accurate against code, with a TOC and zero dead docs/ links in the edited sections.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-cleanup-docs/20-RESEARCH.md
@.planning/phases/20-cleanup-docs/20-UI-SPEC.md
@.planning/phases/20-cleanup-docs/20-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Table of Contents and extend API tables (D-11, D-12)</name>
  <files>README.md</files>
  <read_first>
    - README.md (current state — the '## API' section and its '### Text' subsection are the format to copy)
    - .planning/phases/20-cleanup-docs/20-RESEARCH.md (§Authoritative Endpoint Inventory — verified endpoint facts and curl/wscat examples; do not re-derive)
    - .planning/phases/20-cleanup-docs/20-UI-SPEC.md (§Documentation Structure Contract — section ordering, table conventions, TOC format)
    - .planning/phases/20-cleanup-docs/20-PATTERNS.md (§README.md — API table extension — the exact table+curl shape to apply)
  </read_first>
  <action>
    Insert a `## Table of Contents` section immediately after the one-line description and before the first `---`, as a flat GitHub-anchor bullet list in this order: API, Firmware, Configuration, Build and Run, Deployment, Code Layout, Notes. Include the `[Firmware](#firmware)` bullet now even though Plan 02 authors that section.

    Extend the `## API` section using the existing `### Text` subsection's exact `| Method | Path | Description |` table + one fenced bash curl block convention (D-12) — do NOT create a "New in v1.2" section:
    - Add a new `### Live Feed` subsection for `GET /api/v1/live`: SSE stream emitting `event: display` frames (DisplayEvent JSON), 30s heartbeat, no auth. Usage example: `curl -N http://localhost:8080/api/v1/live`. State explicitly that this SSE live feed is distinct from the firmware WebSocket below.
    - Extend `### History`: add a `GET /api/v1/history/export` row (CSV export, `text/csv`, `Content-Disposition: attachment; filename="history.csv"`, same filters as `/history`). Add `search` to the query-parameters prose line (currently `page`, `size`, `effect`, `source`). Example: `curl -o history.csv "http://localhost:8080/api/v1/history/export?effect=SCROLL&search=hello"`.
    - Extend `### Zones`: add a FIRMWARE-type registration example `curl -X POST http://localhost:8080/api/v1/zones -H 'Content-Type: application/json' -d '{"name":"pico-01","type":"FIRMWARE"}'` returning `201 Created`, alongside the existing IP-based example. Add a `WS` `/ws/zone/{id}` row (inbound firmware WebSocket; server closes with `1003 CANNOT_ACCEPT` if the zone is not a registered FIRMWARE zone) with a wscat example `wscat -c ws://localhost:8080/ws/zone/pico-01`. Keep the firmware WS clearly labeled as firmware-inbound, NOT a browser live feed.
    - Update the `### Display` row for `POST /api/v1/display/select` to note it is reserved and returns `501 Not Implemented`.

    All endpoint facts are already verified in 20-RESEARCH.md §Authoritative Endpoint Inventory — copy those values; do not invent request/response shapes.
  </action>
  <verify>
    <automated>grep -q '## Table of Contents' README.md && grep -q '/api/v1/live' README.md && grep -q '/api/v1/history/export' README.md && grep -q '/ws/zone/{id}' README.md && grep -q '### Live Feed' README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `## Table of Contents` with bullets for API, Firmware, Configuration, Build and Run, Deployment, Code Layout, Notes
    - README.md contains `### Live Feed` and the string `/api/v1/live` with a `curl -N` example
    - README.md `### History` contains `/api/v1/history/export` and the `search` param is listed in the History query-parameters line
    - README.md `### Zones` contains a `"type":"FIRMWARE"` curl example and a `/ws/zone/{id}` row with a wscat example
    - README.md `/api/v1/display/select` description mentions `501`
    - Each new endpoint row is followed by a fenced bash example (curl or wscat), matching the `### Text` convention
  </acceptance_criteria>
  <done>All three new v1.2 endpoints and the History/Zones/Display changes are documented in the API tables with examples, and a TOC links every top-level section.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Configuration table and extend Deployment/Helm; remove dead docs/ links (D-04)</name>
  <files>README.md</files>
  <read_first>
    - README.md (current Configuration and Deployment sections)
    - src/main/resources/application.yaml (single source of truth for env-var defaults)
    - .devops/helm/textreaderrpi/values.yaml (Helm defaults)
    - .devops/helm/textreaderrpi/templates/deployment.yaml (probe paths and timings)
    - .planning/phases/20-cleanup-docs/20-RESEARCH.md (§Existing README Accuracy Issues, §Helm Deployment Facts)
  </read_first>
  <action>
    In the Configuration table, correct the three wrong GPIO SPI defaults to match `application.yaml`: `GPIO_SPI_CE` default `24`, `GPIO_SPI_MOSI` default `19`, `GPIO_SPI_SCK` default `23` (`GPIO_SPI_MISO` stays `9`, correct). Add the four verified-live variables absent from the table, using their defaults/descriptions from `application.yaml:43-59`: `SPI_TIMEOUT_MS`, `GPIO_TIMEOUT_MS`, `API_METRICS_RATE_LIMIT`, `METRICS_PREFIX`. Do NOT add `API_QUEUE_SIZE` — it does not exist in current config and must be dropped per D-02, not fixed.

    Remove the dead docs/ link line at the end of the Configuration section (the `→ Full variable reference in docs/deployment/production-guide.md` line) — its content now lives inline in this table (D-04).

    In the Deployment `### Helm (Kubernetes / K3s)` subsection, extend the "Key chart defaults" line with two verified facts from 20-RESEARCH.md §Helm Deployment Facts: health probes (`GET /health` liveness and `GET /health/ready` readiness, `initialDelaySeconds: 20`, `periodSeconds: 30`) and database password auto-generation (`database.password` is auto-generated as a 16-char random string at install when unset). Remove the two dead docs/ links at the end of the Deployment section (production-guide and monitoring-alerting).

    Optional per D-03 (Claude's discretion, keep to ≤10 lines): add a brief Monitoring subsection under Deployment listing only the three condensed alerting options (cron health check, systemd watchdog, external monitor) from the salvaged monitoring content. Include only if it stays concise; otherwise omit and note the decision in the SUMMARY.
  </action>
  <verify>
    <automated>grep -q 'SPI_TIMEOUT_MS' README.md && grep -q 'METRICS_PREFIX' README.md && grep -q 'API_METRICS_RATE_LIMIT' README.md && grep -q 'GPIO_TIMEOUT_MS' README.md && ! grep -q 'API_QUEUE_SIZE' README.md && ! grep -q 'docs/deployment/production-guide.md' README.md && ! grep -q 'docs/operations/monitoring-alerting.md' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `GPIO_SPI_CE` row shows default `24`, `GPIO_SPI_MOSI` shows `19`, `GPIO_SPI_SCK` shows `23`
    - Configuration table includes `SPI_TIMEOUT_MS`, `GPIO_TIMEOUT_MS`, `API_METRICS_RATE_LIMIT`, `METRICS_PREFIX`
    - `API_QUEUE_SIZE` does not appear anywhere in README.md
    - No `docs/deployment/production-guide.md` or `docs/operations/monitoring-alerting.md` link remains in README.md
    - Helm "Key chart defaults" text mentions `/health` and `/health/ready` probes and password auto-generation
  </acceptance_criteria>
  <done>Configuration table matches application.yaml, Helm defaults are extended with probe/password facts, and the Configuration + Deployment sections contain zero dead docs/ links.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New symbols/sections created by this plan (exclude from drift verification):
- README.md `## Table of Contents` section
- README.md `### Live Feed` API subsection
- README.md new API table rows: `/api/v1/live`, `/api/v1/history/export`, `/ws/zone/{id}`
- README.md new Configuration table rows: `SPI_TIMEOUT_MS`, `GPIO_TIMEOUT_MS`, `API_METRICS_RATE_LIMIT`, `METRICS_PREFIX`
- README.md optional `### Monitoring` subsection under Deployment (discretionary, D-03)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → reader | A reader copies commands/config from README into a live deployment; inaccurate or insecure examples propagate into their environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Information Disclosure | Configuration table / curl examples | low | mitigate | Use placeholder/default values only; never document a real `DATABASE_PASSWORD`; document that Helm auto-generates the DB password rather than suggesting a hardcoded one |
| T-20-02 | Tampering | Zones registration curl example | low | mitigate | The FIRMWARE/IP zone examples use RFC1918 addresses and do not suggest disabling the RFC1918 validation guard (per 20-RESEARCH.md §Security Domain) |

No package installs occur in this plan (no npm/pip/cargo) — no supply-chain (T-20-SC) threat applies.
</threat_model>

<verification>
- API section documents all three v1.2 endpoints with examples (grep in Task 1 verify)
- Configuration table matches application.yaml; no `API_QUEUE_SIZE`; no dead docs/ links (grep in Task 2 verify)
- Human read: TOC anchors resolve and section ordering matches 20-UI-SPEC (deferred to Plan 03 accuracy sweep / /gsd-verify-work)
</verification>

<success_criteria>
- README API, Configuration, and Deployment sections are accurate against code and contain all v1.2 endpoint references except the Firmware walkthrough (Plan 02)
- Zero dead docs/ links in the sections this plan edits
</success_criteria>

<output>
Create `.planning/phases/20-cleanup-docs/20-01-SUMMARY.md` when done. Note whether the optional Monitoring subsection (D-03) was included or omitted.
</output>
