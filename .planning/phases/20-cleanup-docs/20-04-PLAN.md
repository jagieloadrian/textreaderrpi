---
phase: 20-cleanup-docs
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/STATE.md
  - .planning/MILESTONES.md
  - .planning/phases/14-history-enhancements/
  - .planning/phases/15-sse-live-feed/
  - .planning/phases/16-zone-management/
  - .planning/phases/17-firmware-skeletons/
  - .planning/phases/18-kubernetes-helm/
  - .planning/phases/19-dry-yagni-refactoring/
autonomous: true
requirements: [CLEAN-01]
user_setup: []

must_haves:
  truths:
    - "STATE.md and MILESTONES.md contain only decisions, summaries, and conventions — no session logs or per-plan performance metrics (SC2, D-05)"
    - "The four must-survive decisions are retained in STATE.md (D-05, 20-RESEARCH.md Key Pitfalls)"
    - "Phase directories 14-19 are deleted; phase 20's own directory is preserved (D-06, D-07)"
    - "STATE.md documents the post-verification deletion of phase 20's directory as a Next-steps item (D-07)"
  artifacts:
    - "Rewritten .planning/STATE.md (essentials only)"
    - "Rewritten .planning/MILESTONES.md (one compact block per milestone)"
  key_links:
    - "The compression must not drop the two security-relevant decisions: parseDiscoveryReply SSRF guard and LIKE-wildcard injection strip"
    - ".planning/phases/20-cleanup-docs/ must remain on disk — its deletion is a separate post-verification commit, never a task here"
---

<objective>
Compress the GSD planning artifacts to essentials (D-05): rewrite STATE.md to keep current position, accumulated decisions, and locked conventions while dropping session logs and per-plan performance metrics; verify MILESTONES.md is one compact block per milestone; and delete completed phase directories 14-19 (D-06). Phase 20's own directory is explicitly preserved — its deletion is a documented post-verification step, not a task here (D-07).

Purpose: SC2 (CLEAN-01) — .planning/ compressed to decisions/summaries/conventions with no informational noise. Independent of the README work (different files), so this runs in Wave 1 in parallel with Plan 01.
Output: Compressed STATE.md and MILESTONES.md; phase dirs 14-19 removed; phase 20 dir retained.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-cleanup-docs/20-RESEARCH.md
@.planning/phases/20-cleanup-docs/20-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite STATE.md to essentials (D-05)</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (current 284-line file — the rewrite input; note which sections to keep vs drop)
    - .planning/phases/20-cleanup-docs/20-RESEARCH.md (§.planning/ Compression Facts — the four must-survive decisions and the exact drop list)
    - .planning/phases/20-cleanup-docs/20-PATTERNS.md (§.planning/STATE.md — compression rewrite: keep/delete lists)
  </read_first>
  <action>
    Rewrite .planning/STATE.md in place, keeping the frontmatter unchanged and retaining only these sections: Current Position, Project Context, Codebase Status, Existing Features, the Accumulated Context → Key Decisions table (~40 rows — the locked conventions), Key Pitfalls for v1.2 (all 7, which include the four must-survive decisions: displaySource rename, parseDiscoveryReply SSRF guard, Material 3 dark-default CSS, testApplication first-HTTP-call guard), Architecture Summary, DevOps, Deferred Items, and Milestone Archive.

    DELETE entirely: the `## Session` section (session log), the `## Performance Metrics` table (per-plan minute counts — pure noise per 20-RESEARCH.md), and the bottom `## Decisions` list (it duplicates/supersedes the Key Decisions table with low-value entries such as the `[Phase ?]: test summary` line and other `[Phase ?]` fragments).

    Before deleting the bottom `## Decisions` list, migrate any genuinely-unique decision it holds that is NOT already in the Key Decisions table into that table — in particular preserve the two security-relevant decisions (parseDiscoveryReply kernel-verified senderIp SSRF guard, and the LIKE-wildcard `%`/`_` strip / zone-name XSS regex) if they are not already represented. Do not lose a security decision to compression.

    In the Current Position section, add a Next-steps note documenting D-07: after Phase 20 verification passes and 20-VERIFICATION.md is written, a final milestone-closing commit will delete `.planning/phases/20-cleanup-docs/` (git history preserves it). This must be recorded so the final step is not lost.
  </action>
  <verify>
    <automated>! grep -q '## Session' .planning/STATE.md && ! grep -q '## Performance Metrics' .planning/STATE.md && grep -q 'parseDiscoveryReply' .planning/STATE.md && grep -q 'testApplication' .planning/STATE.md && grep -q 'displaySource' .planning/STATE.md && grep -q '20-cleanup-docs' .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/STATE.md` has no `## Session` and no `## Performance Metrics` section
    - The bottom `## Decisions` list (with `[Phase ?]` fragments) is removed; any unique security decision from it survives in the Key Decisions table
    - The four must-survive markers are present: `displaySource`, `parseDiscoveryReply`, Material 3 dark-default CSS, `testApplication`
    - The Current Position / Next-steps text references the post-verification deletion of `.planning/phases/20-cleanup-docs/` (D-07)
  </acceptance_criteria>
  <done>STATE.md is compressed to essentials, retains all locked/security decisions, and documents the D-07 final step.</done>
</task>

<task type="auto">
  <name>Task 2: Compress MILESTONES.md and delete phase dirs 14-19 (D-05, D-06)</name>
  <files>.planning/MILESTONES.md, .planning/phases/14-history-enhancements/, .planning/phases/15-sse-live-feed/, .planning/phases/16-zone-management/, .planning/phases/17-firmware-skeletons/, .planning/phases/18-kubernetes-helm/, .planning/phases/19-dry-yagni-refactoring/</files>
  <read_first>
    - .planning/MILESTONES.md (current 78-line file — already close to target shape; trim only overgrown subsections)
    - .planning/phases/20-cleanup-docs/20-PATTERNS.md (§.planning/MILESTONES.md — compression rewrite: lightest touch)
    - .planning/phases/20-cleanup-docs/20-RESEARCH.md (§.planning/ Compression Facts — phase-dir deletion scope and the D-07 sequencing caution)
  </read_first>
  <action>
    Verify .planning/MILESTONES.md already fits the D-05 target shape (one compact block per milestone: goal, outcome, key decisions) and trim only where a block has grown beyond that — e.g. condense the v1.0 `### Known Gaps` table and `### Previously Deferred Items` down to a one-line pointer to the archived audit rather than a full table. Do NOT add a v1.2 milestone block (v1.2 is not yet closed — that is a milestone-close activity, out of scope for this phase). Keep the v1.1 and v1.0 blocks.

    Then delete completed phase directories 14 through 19 ONLY, using git: `git rm -r .planning/phases/14-history-enhancements .planning/phases/15-sse-live-feed .planning/phases/16-zone-management .planning/phases/17-firmware-skeletons .planning/phases/18-kubernetes-helm .planning/phases/19-dry-yagni-refactoring`. Git history preserves everything (D-06).

    CRITICAL (D-07, 20-RESEARCH.md Pitfall 4): do NOT delete `.planning/phases/20-cleanup-docs/`. Phase 20's own directory must survive this phase — the verifier needs its PLAN/SUMMARY files and writes 20-VERIFICATION.md into it. Its deletion is a separate, later, documented final commit, never part of this task.
  </action>
  <verify>
    <automated>for d in 14 15 16 17 18 19; do if ls -d .planning/phases/${d}-* >/dev/null 2>&1; then echo "FAIL: phase ${d} still present"; exit 1; fi; done; test -d .planning/phases/20-cleanup-docs && echo "PHASE20-PRESERVED"</automated>
  </verify>
  <acceptance_criteria>
    - Phase directories 14-19 are absent (`ls -d .planning/phases/1[4-9]-*` finds nothing)
    - `.planning/phases/20-cleanup-docs/` is still present on disk
    - `.planning/MILESTONES.md` retains the v1.1 and v1.0 blocks in compact one-block-per-milestone form; no v1.2 block was added
  </acceptance_criteria>
  <done>MILESTONES.md is compact, phase dirs 14-19 are deleted, and phase 20's directory is preserved for verification.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan rewrites and deletes planning artifacts; no new code symbols are created.
- Rewritten `.planning/STATE.md` (compressed structure; Next-steps documents D-07)
- Rewritten `.planning/MILESTONES.md` (compact blocks)
- Deletions: `.planning/phases/14-*` through `.planning/phases/19-*`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning artifact → future planner | Compression that drops a security decision removes a guardrail that future phases rely on; over-eager deletion could destroy needed history |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-07 | Repudiation / knowledge loss | STATE.md compression | medium | mitigate | Explicitly preserve the four must-survive decisions and the two security decisions (parseDiscoveryReply SSRF guard, LIKE-wildcard strip) into the Key Decisions table before deleting the low-value `## Decisions` list |
| T-20-08 | Repudiation | phase-dir 14-19 deletion | low | accept | Git history preserves all deleted planning artifacts (`git log --all -- .planning/phases/17-firmware-skeletons/`) per D-06 — accepted, recoverable |
| T-20-09 | Denial of Service (verification) | premature phase-20 dir deletion | medium | mitigate | Verify guards that `.planning/phases/20-cleanup-docs/` is preserved; deleting it now would break the verifier (D-07, Pitfall 4) — deletion is deferred to a documented post-verification commit |

No package installs occur in this plan — no supply-chain (T-20-SC) threat applies.
</threat_model>

<verification>
- STATE.md has no Session/Performance-Metrics noise and retains all must-survive/security decisions (Task 1 verify)
- Phase dirs 14-19 deleted; phase 20 preserved (Task 2 verify)
- MILESTONES.md is compact (human-check / /gsd-verify-work)
</verification>

<success_criteria>
- STATE.md and MILESTONES.md contain only decisions, summaries, and conventions (SC2)
- Phase dirs 14-19 removed; phase 20 dir retained for verification; D-07 final step documented
</success_criteria>

<output>
Create `.planning/phases/20-cleanup-docs/20-04-SUMMARY.md` when done. Explicitly restate the D-07 final step (post-verification deletion of `.planning/phases/20-cleanup-docs/`) so it is not lost.
</output>
