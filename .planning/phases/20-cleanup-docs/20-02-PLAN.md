---
phase: 20-cleanup-docs
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - README.md
autonomous: true
requirements: [DOCS-01]
user_setup: []

must_haves:
  truths:
    - "README has a '## Firmware' section with a complete Linux flash walkthrough for both Pico and ESP32 (D-08)"
    - "Each platform documents toolchain install, wiring, config values, build, and flash (D-10)"
    - "The ESP32 walkthrough is written from ESP-IDF / CMake facts, never the superseded build tool (20-RESEARCH.md Pitfall 1)"
    - "A shared firmware troubleshooting table and a one-line non-Linux pointer are present (D-09, D-10)"
  artifacts:
    - "README.md '## Firmware' section"
    - "README.md '### Pico (RP2040/RP2350)' subsection"
    - "README.md '### ESP32' subsection"
    - "README.md firmware troubleshooting table"
  key_links:
    - "Firmware facts must mirror firmware/pico/README.md and firmware/esp32/README.md (the ground-truth walkthroughs), not REQUIREMENTS.md/CONTEXT.md prose"
    - "ESP32 build commands must match .github/workflows/firmware-ci.yml (idf.py / cmake --build), confirming the ESP-IDF toolchain"
---

<objective>
Add the new `## Firmware` top-level section to README.md: a complete beginner-path flash walkthrough on Linux for both the RP2040/RP2350 Pico (pico-sdk + CMake) and the ESP32 (ESP-IDF 5.2.1 native CMake), covering toolchain, wiring, configuration, build, and flash, plus a shared troubleshooting table and a one-line non-Linux pointer (D-08, D-09, D-10, D-11). Single README, no FIRMWARE.md split (D-11).

Purpose: SC3 — README includes a firmware flash quick-start for Pico and ESP32. This is a transposition/compression of the existing firmware/*/README.md ground-truth walkthroughs into the root README, verified against code (D-02), not new authorship.
Output: A `## Firmware` section placed after the API section and before Configuration (per 20-UI-SPEC ordering).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-cleanup-docs/20-RESEARCH.md
@.planning/phases/20-cleanup-docs/20-UI-SPEC.md
@.planning/phases/20-cleanup-docs/20-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Firmware section intro + Pico (RP2040/RP2350) walkthrough (D-08, D-10)</name>
  <files>README.md</files>
  <read_first>
    - README.md (current state after Plan 01 — find the insertion point after the '## API' section, before '## Configuration', per 20-UI-SPEC ordering)
    - firmware/pico/README.md (ground-truth Pico walkthrough — transpose, do not re-derive)
    - firmware/pico/config.h (verified config field values)
    - firmware/pico/CMakeLists.txt (build system: pico-sdk + CMake)
    - .planning/phases/20-cleanup-docs/20-RESEARCH.md (§Firmware Walkthrough Facts → Pico — all values pre-verified)
    - .planning/phases/20-cleanup-docs/20-UI-SPEC.md (§Documentation Structure Contract → Firmware ordering: Toolchain → Wiring → Configuration → Build → Flash)
  </read_first>
  <action>
    Insert a new `## Firmware` top-level section after the `## API` section and before `## Configuration`. Open with one sentence framing it as a complete Linux flash path (install toolchain → wire the display → configure → build → flash → zone appears on server).

    Add a `### Pico (RP2040/RP2350)` subsection built on pico-sdk 2.1.1 + CMake, in this order:
    - Toolchain table (`| Tool | Version | Install |`): CMake 3.13+ (`apt install cmake`); arm-none-eabi-gcc 10.3+ (`apt install gcc-arm-none-eabi`); pico-sdk 2.1.1 (`git clone --branch 2.1.1 https://github.com/raspberrypi/pico-sdk`, then set `PICO_SDK_PATH`); picotool (for `--target flash`); picowota (git submodule, `git submodule update --init --recursive`); ninja-build.
    - Wiring (MAX7219 default, Pico W): GP18 (SPI0 SCK)→CLK, GP19 (SPI0 TX)→DIN, GP17 (SPI0 CSn)→LOAD/CS, 3V3 (pin 36)→VCC, GND (pin 38)→GND.
    - Configuration: a fenced `c` block of the `firmware/pico/config.h` fields — `WIFI_SSID`, `WIFI_PASS`, `SERVER_HOST`, `SERVER_PORT` (`8080`), `ZONE_ID` (`"pico-01"`), `RECONNECT_INTERVAL_MS` (`5000`), `NUM_DEVICES` (`1`). Note the display driver is chosen at configure time via `-DDISPLAY_DRIVER=<NAME>` (default `MAX7219`; others: `SSD1306`, `SSD1309`, `SSD1327`, `SH1106`, `HT16K33`, `ST7735`, `ST7789`, `ILI9225`, `PCD8544`, `SSD1680`). Note first-boot WiFi provisioning: joins `TextReader-Setup` open AP → `http://192.168.4.1` captive portal → saved to flash → reboots into STA mode.
    - Build (fenced `bash`): export `PICO_SDK_PATH`; `cmake -S firmware/pico -B firmware/pico/build -DPICO_BOARD=pico_w` (or `pico2_w` for Pico 2W); `cmake --build firmware/pico/build -j$(nproc)`. Outputs: `textreader.uf2` (OTA payload) and `textreader_combined.uf2` (bootloader+app, first flash).
    - Flash (fenced `bash`): `cmake --build firmware/pico/build --target flash` (requires picotool + BOOTSEL mode); manual alternative — hold BOOTSEL while plugging USB, drag `textreader_combined.uf2` onto the mounted drive.
    - OTA note: mention picowota OTA scaffolding exists but is NOT presented as a finished v1.2 feature (OTA is deferred to v1.3 per REQUIREMENTS.md Future Requirements — match the firmware README's hedged wording).

    All values are pre-verified in 20-RESEARCH.md §Firmware Walkthrough Facts → Pico; do not invent pins, versions, or commands.
  </action>
  <verify>
    <automated>grep -q '## Firmware' README.md && grep -q '### Pico' README.md && grep -q 'PICO_SDK_PATH' README.md && grep -q 'pico2_w' README.md && grep -q 'textreader_combined.uf2' README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `## Firmware` and `### Pico (RP2040/RP2350)`
    - Pico subsection contains a toolchain table, wiring facts, a `c` config block with `ZONE_ID` and `NUM_DEVICES`, a build block referencing `PICO_SDK_PATH` and `-DPICO_BOARD=pico_w`, and a flash block referencing `--target flash`
    - `textreader.uf2` and `textreader_combined.uf2` are both named
    - OTA is mentioned as scaffolding, not a shipped feature
  </acceptance_criteria>
  <done>The Firmware section exists with a complete, code-verified Pico walkthrough (toolchain → wiring → config → build → flash).</done>
</task>

<task type="auto">
  <name>Task 2: ESP32 walkthrough (ESP-IDF), shared troubleshooting table, non-Linux pointer (D-08, D-09, D-10)</name>
  <files>README.md</files>
  <read_first>
    - README.md (current state — append after the Pico subsection within the Firmware section)
    - firmware/esp32/README.md (ground-truth ESP32 walkthrough)
    - firmware/esp32/main/config.h (verified config field values)
    - firmware/esp32/CMakeLists.txt (build system — confirms ESP-IDF native CMake)
    - .github/workflows/firmware-ci.yml (confirms CI builds ESP32 via ESP-IDF idf.py / cmake --build)
    - .planning/phases/20-cleanup-docs/20-RESEARCH.md (§Firmware Walkthrough Facts → ESP32, the shared troubleshooting list, and the non-Linux pointer; §Common Pitfalls Pitfall 1)
    - .planning/phases/20-cleanup-docs/20-PATTERNS.md (critical accuracy note for the ESP32 section)
  </read_first>
  <action>
    Add a `### ESP32` subsection built on ESP-IDF 5.2.1 native CMake. CRITICAL: the ESP32 build system must be documented strictly from the current ESP-IDF/CMake facts in firmware/esp32/README.md, firmware/esp32/CMakeLists.txt (`idf_component_register`), and .github/workflows/firmware-ci.yml — REQUIREMENTS.md/CONTEXT.md prose about the old ESP32 build tool is stale and must NOT be reproduced (see 20-RESEARCH.md Pitfall 1 "Warning signs" and 20-PATTERNS.md critical accuracy note). Order:
    - Toolchain table (`| Tool | Version | Install |`): ESP-IDF 5.2.1 (`git clone --branch v5.2.1 https://github.com/espressif/esp-idf`, then `./install.sh esp32`, then `. ./export.sh` per shell session); CMake 3.16+ (bundled with ESP-IDF); Python 3.8+. Note supported chips: ESP32, S2, S3, C2, C3, C6, H2 (H2 has no WiFi — provisioning will not work on H2).
    - Wiring (MAX7219 default, classic ESP32): GPIO23→DIN, GPIO18→CLK, GPIO5→LOAD/CS, 3.3V→VCC, GND→GND. Note some MAX7219 modules need 5V VCC and a level shifter if the module does not tolerate 3.3V logic.
    - Configuration: a fenced `c` block of `firmware/esp32/main/config.h` fields — `WIFI_SSID` (`""`), `WIFI_PASS` (`""`), `SERVER_HOST` (`""`), `SERVER_PORT` (`8080`), `ZONE_ID` (`"esp32"`), `NUM_DEVICES` (`4`), `RECONNECT_INTERVAL_MS` (`5000`). Note SPI/I2C pins vary per chip family (classic ESP32 defaults: `SPI_MOSI_PIN=23`, `SPI_CLK_PIN=18`, `SPI_CS_PIN=5`, `I2C_SDA_PIN=21`, `I2C_SCL_PIN=22`). Note captive-portal provisioning is the primary path (config.h WiFi fields are fallback only): `TextReader-Setup` AP → `http://192.168.4.1` → NVS storage → reboot into STA.
    - Build (fenced `bash`): `. $IDF_PATH/export.sh`; `idf.py set-target esp32` (once per build dir; or `esp32s3`, `esp32c3`, etc.); `idf.py build` (default driver MAX7219); explicit driver alternative: `cmake -S firmware/esp32 -B firmware/esp32/build -DDISPLAY_DRIVER=SSD1306` then `cmake --build firmware/esp32/build`.
    - Flash (fenced `bash`): `idf.py flash` (or `idf.py -p /dev/ttyUSB0 flash`); `idf.py monitor` for the serial monitor.
    - OTA note: ESP-IDF OTA partition scaffolding exists but full OTA push is planned for a future phase (deferred to v1.3) — match the ESP32 README's own hedged wording, do not present it as shipped.

    Then add ONE shared firmware troubleshooting table at the end of the Firmware section (`| Symptom | Cause / check |`) with the four verified rows from 20-RESEARCH.md (board not detected over USB; WiFi provisioning fails / no captive portal; zone does not appear on server; display shows nothing after connecting) — do not duplicate per platform.

    Finally add a single one-line non-Linux pointer (D-09) at the end of the Firmware section: building on macOS or Windows follows the official pico-sdk (`https://www.raspberrypi.com/documentation/pico-sdk/getting-started.html`) or ESP-IDF (`https://docs.espressif.com/projects/esp-idf/en/stable/esp32/get-started/`) install guides, with otherwise-identical build/flash commands.
  </action>
  <verify>
    <!-- planner-discipline-allow: platformio -->
    <automated>grep -q '### ESP32' README.md && grep -qi 'esp-idf' README.md && grep -q 'idf.py' README.md && grep -q 'set-target esp32' README.md && grep -q 'Symptom' README.md && ! grep -qi 'platformio' README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `### ESP32` documenting ESP-IDF 5.2.1, `idf.py set-target`, `idf.py build`, and `idf.py flash`
    - The lowercase string `platformio` does NOT appear anywhere in README.md (`grep -i` returns nothing) — the superseded ESP32 build tool must be absent (20-RESEARCH.md Pitfall 1)
    - ESP32 subsection contains a toolchain table, wiring facts, a `c` config block with `ZONE_ID` `"esp32"` and `NUM_DEVICES` `4`, build and flash blocks
    - A single shared troubleshooting table (`| Symptom | Cause / check |`) with the four verified rows is present once
    - A one-line non-Linux pointer with the pico-sdk and ESP-IDF getting-started URLs is present
  </acceptance_criteria>
  <done>The Firmware section has a complete ESP-IDF-based ESP32 walkthrough, a shared troubleshooting table, and a non-Linux pointer; the obsolete ESP32 build-tool name is absent.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New symbols/sections created by this plan (exclude from drift verification):
- README.md `## Firmware` section
- README.md `### Pico (RP2040/RP2350)` subsection
- README.md `### ESP32` subsection
- README.md firmware troubleshooting table (`| Symptom | Cause / check |`)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → firmware builder | A reader flashes a device using the README walkthrough; wrong wiring/config/toolchain facts can damage hardware or leak credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-03 | Information Disclosure | config.h example blocks | low | mitigate | WiFi/server config examples use placeholder values (`"YourSSID"`, empty strings); do not embed real credentials; captive-portal provisioning is documented as the primary path so secrets are not committed to flashed binaries |
| T-20-04 | Spoofing / accuracy | ESP32 toolchain instructions | low | mitigate | Document only code-verified ESP-IDF facts (firmware/esp32 + firmware-ci.yml); an inaccurate toolchain step would send builders down a broken/obsolete path (20-RESEARCH.md Pitfall 1) — negative-grep guards the obsolete tool name |

No package installs occur in this plan — no supply-chain (T-20-SC) threat applies.
</threat_model>

<verification>
- Firmware section present with both platform walkthroughs (Task 1 + Task 2 grep verifies)
- Obsolete ESP32 build-tool name absent (Task 2 negative grep)
- Human read of the full firmware walkthrough for completeness is deferred to Plan 03 accuracy sweep / /gsd-verify-work
</verification>

<success_criteria>
- README `## Firmware` section provides a complete, code-verified Linux flash walkthrough for Pico and ESP32, plus troubleshooting and a non-Linux pointer
- No stale firmware facts (obsolete ESP32 build tool) appear in the README
</success_criteria>

<output>
Create `.planning/phases/20-cleanup-docs/20-02-SUMMARY.md` when done.
</output>
