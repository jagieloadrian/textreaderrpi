---
phase: 20-cleanup-docs
plan: 03
type: execute
wave: 3
depends_on: ["20-02"]
files_modified:
  - README.md
  - docs/
autonomous: true
requirements: [CLEAN-01, DOCS-01]
user_setup: []

must_haves:
  truths:
    - "The docs/ directory no longer exists in the working tree (SC1, CLEAN-01)"
    - "Zero references to the deleted docs/ path remain outside .planning/ (D-04)"
    - "Every README endpoint/config/command claim matches code, not memory (D-13, SC4)"
    - "No deferred v1.3 feature is presented as a shipped v1.2 feature (D-13, SC4)"
  artifacts:
    - "docs/ directory removed (deletion, not creation)"
    - "README.md accuracy sweep applied (drift fixes if any)"
  key_links:
    - "The D-04 grep must return zero matches outside .planning/ — this is the gate proving no dead docs/ links remain"
    - "README endpoint list must match src/main/kotlin/com/anjo/routing/*.kt exactly"
---

<objective>
Finalize the repository cleanup: skim the three docs/ files not fully read during research for any unique salvageable fact, then delete the docs/ directory (D-01), and run the systematic accuracy sweep (D-13) — cross-check every remaining README claim against code, confirm zero dead docs/ references repo-wide (D-04), and confirm no deferred v1.3 feature is presented as current (SC4).

Purpose: SC1 (docs/ deleted), SC4 (README accurate, no deferred/removed features referenced). This plan runs last (Wave 3) because the accuracy sweep needs the complete README from Plans 01 and 02.
Output: docs/ removed; a verified-accurate README with zero dead docs/ links and no misrepresented deferred features.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-cleanup-docs/20-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Skim remaining docs/ files, salvage any unique fact, delete docs/ (D-01, D-03)</name>
  <files>README.md, docs/</files>
  <read_first>
    - docs/architecture/overview.md (not fully read during research — skim for unique facts)
    - docs/configuration/overview.md (not fully read during research)
    - docs/testing/overview.md (not fully read during research)
    - README.md (current state after Plans 01-02 — the target if any fact is salvaged)
    - .planning/phases/20-cleanup-docs/20-RESEARCH.md (§docs/ Salvage Assessment, §Open Questions — expected near-zero net salvage)
  </read_first>
  <action>
    Skim the three docs files not fully read during research (architecture/overview.md, configuration/overview.md, testing/overview.md) for any single unique, code-verifiable fact that fills a required README gap (endpoints, deployment, quick-start per D-01) and is not already present. Research expects near-zero net salvage — if a candidate fact is found, verify it against current code first (D-02) before adding it to the appropriate README section; if it does not verify, drop it (do not fix it). The five higher-value docs files were already salvaged into Plans 01-02, so no re-read of those is needed. Per D-03, docs/guides/development.md's content (build-command duplication + internal conventions) does NOT earn a place in README — do not migrate it.

    Then delete the entire docs/ directory with `git rm -r docs/`. Delete docs/ ONLY — do not touch any other directory. Git history is the archive (D-01).

    Record in the SUMMARY exactly what (if anything) was salvaged from the three skimmed files and confirm development.md was intentionally dropped.
  </action>
  <verify>
    <automated>test ! -d docs && echo "DOCS-DELETED"</automated>
  </verify>
  <acceptance_criteria>
    - The docs/ directory is absent from the working tree (`test ! -d docs` succeeds)
    - Any fact salvaged from the three skimmed files is code-verified and present in the correct README section; if none, the SUMMARY states "no net salvage"
    - No directory other than docs/ was deleted by this task
  </acceptance_criteria>
  <done>docs/ is deleted, salvage decision is documented, and only docs/ was removed.</done>
</task>

<task type="auto">
  <name>Task 2: D-13 accuracy sweep + D-04 repo-wide grep + deferred-v1.3 check (D-04, D-13)</name>
  <files>README.md</files>
  <read_first>
    - README.md (full current state — the sweep target)
    - src/main/kotlin/com/anjo/routing/ (Routing.kt + all route files — authoritative endpoint list)
    - src/main/resources/application.yaml (env-var ground truth)
    - .devops/helm/textreaderrpi/values.yaml (Helm claims ground truth)
    - .planning/REQUIREMENTS.md (§Future Requirements — the deferred-v1.3 negative checklist)
    - .planning/phases/20-cleanup-docs/20-RESEARCH.md (§Repo-Wide docs/ Reference Sweep — exact D-04 grep; §Deferred v1.3 Items)
  </read_first>
  <action>
    Systematic accuracy sweep (D-13): walk every README endpoint, env-var, and command claim and confirm it matches the code source of truth (routing/*.kt for endpoints, application.yaml for env vars, values.yaml for Helm) — the endpoint list is generated from code, not memory. Fix any drift found (e.g., a description that no longer matches a handler).

    Run the D-04 repo-wide docs/ reference grep exactly as specified in 20-RESEARCH.md §Repo-Wide docs/ Reference Sweep (searching *.md/*.yml/*.yaml/*.kt/*.kts, excluding docs/ itself, .git/, and .planning/). Confirm it returns zero matches — any remaining reference to the deleted docs/ path (outside the .planning/ archive) must be updated or removed.

    Run the deferred-v1.3 negative check against REQUIREMENTS.md §Future Requirements — confirm no deferred item is worded as a shipped v1.2 feature: (a) the obsolete ESP32 build-tool name must be absent (automated negative grep); (b) firmware OTA must read as scaffolding/not-finished, never a supported v1.2 feature; (c) the Helm ingress must remain framed as disabled-by-default (`ingress.enabled: false`), not a ready feature; (d) PostgreSQL GIN full-text search and multi-replica Postgres must not appear as current features; (e) the SSE live feed (`/api/v1/live`) must remain clearly distinct from the firmware WebSocket (`/ws/zone/{id}`) — no "WebSocket live feed" wording that conflates them. Fix any violation.
  </action>
  <verify>
    <!-- planner-discipline-allow: platformio -->
    <automated>test -z "$(grep -rn 'docs/' --include='*.md' --include='*.yml' --include='*.yaml' --include='*.kt' --include='*.kts' . | grep -v '^\./docs/' | grep -v '/\.git/' | grep -v '^\./\.planning/')" && ! grep -qi 'platformio' README.md && echo "SWEEP-CLEAN"</automated>
    <human-check>Read the full README against 20-RESEARCH.md §Deferred v1.3 Items: confirm OTA is hedged, ingress is disabled-by-default framing, no GIN/multi-replica-Postgres as current, and the SSE live feed vs firmware WebSocket are not conflated.</human-check>
  </verify>
  <acceptance_criteria>
    - The D-04 grep (excluding docs/, .git/, .planning/) returns zero lines
    - README endpoint rows match the mount paths in src/main/kotlin/com/anjo/routing/*.kt
    - `grep -i 'platformio' README.md` returns nothing
    - No deferred v1.3 item from REQUIREMENTS.md §Future Requirements is presented as a shipped v1.2 feature (human-check for the nuanced OTA/ingress/live-feed items)
  </acceptance_criteria>
  <done>README claims are verified against code, zero dead docs/ references remain repo-wide, and no deferred feature is misrepresented as current.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan primarily deletes and verifies; no new code symbols are created.
- docs/ directory removed (deletion)
- README.md drift fixes (if any) applied during the accuracy sweep
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → reader | Inaccurate or deferred-feature-as-shipped claims mislead a reader into relying on capabilities that do not exist |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-05 | Spoofing / accuracy | README feature claims | low | mitigate | D-13 sweep verifies every claim against code; deferred-v1.3 negative check prevents presenting unshipped features (OTA, GIN search, multi-replica Postgres, ingress/TLS) as available |
| T-20-06 | Repudiation | docs/ deletion | low | accept | docs/ content is preserved in git history (D-01); deletion is recoverable via `git log --all -- docs/` — accepted rather than mitigated further |

No package installs occur in this plan — no supply-chain (T-20-SC) threat applies.
</threat_model>

<verification>
- docs/ absent (Task 1 verify)
- D-04 grep returns zero matches outside .planning/ (Task 2 verify)
- Obsolete ESP32 build-tool name absent from README (Task 2 negative grep)
- Deferred-v1.3 items not misrepresented (Task 2 human-check + /gsd-verify-work)
</verification>

<success_criteria>
- docs/ directory deleted (SC1)
- README accurately reflects current project state with zero dead docs/ links and no deferred/removed features referenced as current (SC4)
</success_criteria>

<output>
Create `.planning/phases/20-cleanup-docs/20-03-SUMMARY.md` when done. Record the salvage decision from Task 1 and any drift fixed in Task 2.
</output>
