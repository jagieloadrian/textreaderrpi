---
phase: 15-sse-live-feed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gradle/ktor-libs.versions.toml
  - build.gradle.kts
  - src/main/kotlin/com/anjo/model/DisplayEvent.kt
  - src/main/kotlin/com/anjo/service/DisplayEventBus.kt
  - src/main/kotlin/com/anjo/service/ScreenDriverService.kt
  - src/main/kotlin/com/anjo/di/DependencyInjection.kt
  - src/test/kotlin/com/anjo/service/DisplayEventBusTest.kt
  - src/test/kotlin/com/anjo/routing/LiveRoutesTest.kt
autonomous: true
requirements:
  - LIVE-01
nyquist_compliant: true

must_haves:
  truths:
    - "A DisplayEventBus singleton holds a SharedFlow(replay=5) of DisplayEvent"
    - "Every successful display (immediate single-zone, broadcast, scheduled) emits exactly one DisplayEvent after the history insert persists"
    - "A new collector of DisplayEventBus.events immediately receives the last 5 emitted events"
    - "DisplayEventBus is resolvable from the DI container as a singleton"
  artifacts:
    - path: "src/main/kotlin/com/anjo/model/DisplayEvent.kt"
      provides: "DisplayEvent data class (id, text, effect, zoneId, displayedAt)"
      contains: "data class DisplayEvent"
    - path: "src/main/kotlin/com/anjo/service/DisplayEventBus.kt"
      provides: "SharedFlow(replay=5) event bus with suspend emit"
      contains: "MutableSharedFlow"
    - path: "src/test/kotlin/com/anjo/service/DisplayEventBusTest.kt"
      provides: "Wave-0 unit tests for emit/collect and replay=5"
      contains: "class DisplayEventBusTest"
    - path: "src/test/kotlin/com/anjo/routing/LiveRoutesTest.kt"
      provides: "Wave-0 SSE route test stub (used by Plan 02)"
      contains: "class LiveRoutesTest"
  key_links:
    - from: "src/main/kotlin/com/anjo/service/ScreenDriverService.kt"
      to: "src/main/kotlin/com/anjo/service/DisplayEventBus.kt"
      via: "displayEventBus?.emit(DisplayEvent(...)) after historyRepository.insert() in tryInsertHistory"
      pattern: "displayEventBus\\?\\.emit"
    - from: "src/main/kotlin/com/anjo/di/DependencyInjection.kt"
      to: "src/main/kotlin/com/anjo/service/DisplayEventBus.kt"
      via: "construct DisplayEventBus(), pass to ScreenDriverService, provide { displayEventBus }"
      pattern: "DisplayEventBus\\(\\)"
---

<objective>
Create the backend event infrastructure for the SSE live feed (LIVE-01): a `DisplayEvent` model, a `DisplayEventBus` singleton wrapping `MutableSharedFlow(replay=5)`, the emit wiring inside `ScreenDriverService` so every successful display produces an event after its history insert, and DI registration. Also create the two Wave-0 test files so downstream tasks have failing-then-passing automated verification.

Purpose: This is the producer side of LIVE-01. Without a populated `SharedFlow`, the SSE route in Plan 02 has nothing to stream. The replay=5 buffer satisfies LIVE-04 (last 5 events on connect) with zero custom ring-buffer code (per D-02).
Output: `DisplayEvent.kt`, `DisplayEventBus.kt`, modified `ScreenDriverService.kt`, modified `DependencyInjection.kt`, build dependency for `ktor-server-sse`, plus `DisplayEventBusTest.kt` and `LiveRoutesTest.kt` (Wave-0 stubs).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-sse-live-feed/15-CONTEXT.md
@.planning/phases/15-sse-live-feed/15-RESEARCH.md
@.planning/phases/15-sse-live-feed/15-VALIDATION.md
</context>

<artifacts_this_phase_produces>
Symbols this plan introduces (exclude from drift/source-grounding checks — they are NEW):
- `com.anjo.model.DisplayEvent` (data class: `id: String`, `text: String`, `effect: String`, `zoneId: String?`, `displayedAt: String`)
- `com.anjo.service.DisplayEventBus` (class with `events: SharedFlow<DisplayEvent>` and `suspend fun emit(event: DisplayEvent)`)
- `ScreenDriverService` new constructor parameter `displayEventBus: DisplayEventBus? = null`
- Gradle version-catalog alias `ktor-server-sse` and `build.gradle.kts` dependency `ktorLibs.ktor.server.sse`
- Test classes `com.anjo.service.DisplayEventBusTest`, `com.anjo.routing.LiveRoutesTest`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add ktor-server-sse dependency and create Wave-0 test files</name>
  <files>gradle/ktor-libs.versions.toml, build.gradle.kts, src/test/kotlin/com/anjo/service/DisplayEventBusTest.kt, src/test/kotlin/com/anjo/routing/LiveRoutesTest.kt</files>
  <read_first>
    - gradle/ktor-libs.versions.toml (existing `[libraries]` alias style — every Ktor alias uses `version.ref = "ktor"`; add the new alias under the "# Ktor server" group)
    - build.gradle.kts (existing `dependencies {}` block — Ktor server deps use `implementation(ktorLibs.ktor.server.*)`; add under "// Core Ktor")
    - src/test/kotlin/com/anjo/ApplicationTest.kt (Kotest FunSpec + testApplication + module() wiring pattern; DI resolution via getBlocking)
    - src/test/kotlin/com/anjo/routing/WebRoutesTest.kt (testApplication HTTP request + bodyAsText assertion pattern)
    - .planning/phases/15-sse-live-feed/15-VALIDATION.md (Wave 0 requirements table for LiveRoutesTest and DisplayEventBusTest)
  </read_first>
  <action>
    Add catalog alias `ktor-server-sse = { module = "io.ktor:ktor-server-sse", version.ref = "ktor" }` to the `[libraries]` section of gradle/ktor-libs.versions.toml under the "# Ktor server" group. Add `implementation(ktorLibs.ktor.server.sse)` to build.gradle.kts in the "// Core Ktor" group of the `dependencies {}` block.
    Create `DisplayEventBusTest.kt` (package `com.anjo.service`) as a Kotest `FunSpec` using `kotlinx.coroutines.test.runTest`. Write three tests against the NOT-YET-CREATED `DisplayEventBus`: (a) emit then a fresh collector receives the emitted `DisplayEvent`; (b) emit 6 events, then a fresh collector's first 5 replayed values equal the LAST 5 emitted (proves `replay = 5` and drop-oldest); (c) `bus.events` is typed `SharedFlow<DisplayEvent>` and is the same instance across reads. Collect from the replay cache using `bus.events.take(n).toList()` after emitting, since replay is synchronous on a `MutableSharedFlow`. These tests MUST fail to compile until Task 2 exists — that is the intended RED state.
    Create `LiveRoutesTest.kt` (package `com.anjo.routing`) as a Kotest `FunSpec` Wave-0 STUB for Plan 02: a single test `GET /api/v1/live returns text/event-stream content type` using `testApplication { application { module() } ... }`. Mark its body with `// implemented in Plan 02` and assert `true shouldBe true` for now (placeholder green) OR leave a failing assertion guarded behind a comment — choose placeholder-green so the suite stays runnable. The real assertions land in Plan 02.
  </action>
  <verify>
    <automated>./gradlew compileTestKotlin 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - gradle/ktor-libs.versions.toml contains `ktor-server-sse = { module = "io.ktor:ktor-server-sse"`
    - build.gradle.kts contains `implementation(ktorLibs.ktor.server.sse)`
    - File `src/test/kotlin/com/anjo/service/DisplayEventBusTest.kt` exists and declares `class DisplayEventBusTest`
    - File `src/test/kotlin/com/anjo/routing/LiveRoutesTest.kt` exists and declares `class LiveRoutesTest`
    - `DisplayEventBusTest.kt` references `replay` behavior by emitting 6 events and asserting the replayed values are the last 5
  </acceptance_criteria>
  <done>SSE dependency declared in both catalog and build script; both Wave-0 test files exist. DisplayEventBusTest references DisplayEventBus (compile fails until Task 2 — expected). LiveRoutesTest is a runnable placeholder for Plan 02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create DisplayEvent model and DisplayEventBus service</name>
  <files>src/main/kotlin/com/anjo/model/DisplayEvent.kt, src/main/kotlin/com/anjo/service/DisplayEventBus.kt</files>
  <behavior>
    - DisplayEventBus.emit(e) makes e available to any collector that subscribes afterwards (within replay window)
    - After 6 emits, a new collector's replay cache is exactly the last 5 events in emission order
    - DisplayEvent serializes to JSON via kotlinx.serialization (annotated @Serializable) so the SSE route in Plan 02 can call Json.encodeToString
  </behavior>
  <read_first>
    - src/main/kotlin/com/anjo/model/HistoryRecord.kt (existing @Serializable data class style and package com.anjo.model; source of field names — DisplayEvent is a lean subset)
    - src/test/kotlin/com/anjo/service/DisplayEventBusTest.kt (the tests written in Task 1 define the exact contract)
    - .planning/phases/15-sse-live-feed/15-CONTEXT.md (D-08 field list, D-02 SharedFlow config, D-09 excluded fields)
  </read_first>
  <action>
    Create `src/main/kotlin/com/anjo/model/DisplayEvent.kt`, package `com.anjo.model`, annotated `@Serializable`: `data class DisplayEvent(val id: String, val text: String, val effect: String, val zoneId: String?, val displayedAt: String)`. NOTE the deliberate resolution of the D-08 type mismatch: D-08 wrote `id: Long`, but `HistoryRecord.id` is a UUID `String`; use `id: String` to match the persisted record exactly (per RESEARCH Open Question #1 — String is unambiguous, lossless, no conversion). Exclude `webhookStatus` and `scheduleId` per D-09.
    Create `src/main/kotlin/com/anjo/service/DisplayEventBus.kt`, package `com.anjo.service`: a class holding `private val _events = MutableSharedFlow<DisplayEvent>(replay = 5, extraBufferCapacity = 0)`, exposing `val events: SharedFlow<DisplayEvent> = _events`, and `suspend fun emit(event: DisplayEvent) = _events.emit(event)`. Imports: `kotlinx.coroutines.flow.MutableSharedFlow`, `kotlinx.coroutines.flow.SharedFlow`. No comments in the file (project no-comments rule).
  </action>
  <verify>
    <automated>./gradlew test --tests "com.anjo.service.DisplayEventBusTest"</automated>
  </verify>
  <acceptance_criteria>
    - src/main/kotlin/com/anjo/model/DisplayEvent.kt contains `data class DisplayEvent(` and `val id: String`
    - DisplayEvent.kt is annotated `@Serializable`
    - src/main/kotlin/com/anjo/service/DisplayEventBus.kt contains `MutableSharedFlow<DisplayEvent>(replay = 5`
    - DisplayEventBus.kt exposes `val events: SharedFlow<DisplayEvent>`
    - `./gradlew test --tests "com.anjo.service.DisplayEventBusTest"` exits 0 (all three Task 1 tests pass)
  </acceptance_criteria>
  <done>DisplayEvent and DisplayEventBus exist; DisplayEventBusTest passes (GREEN), proving emit/collect and replay=5 drop-oldest behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire emit into ScreenDriverService and register DisplayEventBus in DI</name>
  <files>src/main/kotlin/com/anjo/service/ScreenDriverService.kt, src/main/kotlin/com/anjo/di/DependencyInjection.kt</files>
  <behavior>
    - A successful displayImmediate to a single zone emits one DisplayEvent whose text/effect/zoneId/displayedAt match the persisted HistoryRecord
    - A successful broadcast (zoneId == null) emits one DisplayEvent per successful zone
    - A successful displayScheduled emits one DisplayEvent
    - A history insert that throws emits NO event (emit only after insert returns a record)
    - Existing ScreenDriverService tests that construct it without a bus still compile and pass (nullable default param)
  </behavior>
  <read_first>
    - src/main/kotlin/com/anjo/service/ScreenDriverService.kt (constructor parameter list; tryInsertHistory currently returns Unit and discards the insert result; it is the SINGLE shared sink called from broadcastImmediate, renderImmediate, and runScheduledRender)
    - src/main/kotlin/com/anjo/db/HistoryRepository.kt (insert() returns the persisted HistoryRecord with id and displayedAt populated — this is the authoritative record to map into DisplayEvent)
    - src/main/kotlin/com/anjo/di/DependencyInjection.kt (singleton construction order; ScreenDriverService construction call; dependencies {} provide block — mirror the ZoneRegistry/historyRepository pattern)
    - src/test/kotlin/com/anjo/service/HistoryRecordingTest.kt (last test constructs ScreenDriverService directly with keyword args and NO bus — must keep compiling; this is why the new param is nullable with default null)
    - .planning/phases/15-sse-live-feed/15-CONTEXT.md (D-03 DI singleton, D-04 emit after insert, D-07 lifecycle)
  </read_first>
  <action>
    In `ScreenDriverService`, add constructor parameter `private val displayEventBus: DisplayEventBus? = null` (nullable, default `null`) as the last parameter after `historyRepository` (per RESEARCH Pitfall 5 — keeps existing direct-construction tests compiling). Change `tryInsertHistory(...)` to capture the return value of `historyRepository?.insert(...)` into a local `record`, and after the insert succeeds (record != null), call `displayEventBus?.emit(DisplayEvent(id = record.id, text = record.text, effect = record.effect, zoneId = record.zoneId, displayedAt = record.displayedAt))`. Keep the emit INSIDE the existing try/catch so a failed insert (which already logs and swallows) results in NO emit. Because all three display paths funnel through `tryInsertHistory`, this single emit point covers immediate single-zone (D-04), broadcast (the per-zone loop in broadcastImmediate), and scheduled — satisfying the intent of D-04 ("after historyRepository.insert() completes, authoritative record with id") for every display trigger. Import `com.anjo.model.DisplayEvent`.
    In `DependencyInjection.kt`: construct `val displayEventBus = DisplayEventBus()` before the `ScreenDriverService(...)` constructor call; pass `displayEventBus = displayEventBus` as a named argument in that call; add `provide { displayEventBus }` inside the `dependencies {}` block alongside the existing singletons. Import `com.anjo.service.DisplayEventBus`.
    Add no code comments (project no-comments rule). If an IDE generates KDoc stubs during edits, delete them.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.anjo.service.HistoryRecordingTest" --tests "com.anjo.ApplicationTest"</automated>
  </verify>
  <acceptance_criteria>
    - ScreenDriverService.kt constructor contains `displayEventBus: DisplayEventBus? = null`
    - ScreenDriverService.kt tryInsertHistory contains `displayEventBus?.emit(`
    - The emit references `record.id`, `record.text`, `record.effect`, `record.zoneId`, `record.displayedAt` from the insert return value (not the raw input args)
    - DependencyInjection.kt contains `val displayEventBus = DisplayEventBus()`
    - DependencyInjection.kt `dependencies {}` block contains `provide { displayEventBus }`
    - DependencyInjection.kt passes `displayEventBus = displayEventBus` into the ScreenDriverService constructor
    - `./gradlew test --tests "com.anjo.service.HistoryRecordingTest" --tests "com.anjo.ApplicationTest"` exits 0 (existing direct-construction test still compiles and passes; DI still resolves)
  </acceptance_criteria>
  <done>ScreenDriverService emits a DisplayEvent after every successful history insert across all three display paths; DisplayEventBus is a DI singleton; pre-existing ScreenDriverService and DI tests remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| display coroutine → SharedFlow | In-process emit; same JVM, no external input crosses here |
| (no client boundary in this plan) | The client-facing SSE endpoint is added in Plan 02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | DisplayEvent payload constructed from persisted HistoryRecord | accept | Fields originate from the same validated text already accepted by POST /api/v1/text; no new untrusted input enters at the emit point |
| T-15-02 | Denial of Service | MutableSharedFlow(replay=5, extraBufferCapacity=0) | accept | replay buffer is bounded at 5; emit suspends only if a slow collector blocks — acceptable for home-lab scale (D-06, no connection limit) |
| T-15-SC | Tampering | Maven dependency `io.ktor:ktor-server-sse` | mitigate | First-party Ktor artifact, same group/version as all existing Ktor deps; verified on Maven Central in RESEARCH Package Legitimacy Audit — no [ASSUMED]/[SUS] packages, no checkpoint required |
</threat_model>

<verification>
- `./gradlew compileKotlin compileTestKotlin` succeeds (new model, service, and wiring compile)
- `./gradlew test --tests "com.anjo.service.DisplayEventBusTest"` exits 0 (LIVE-01 bus emit/collect/replay)
- `./gradlew test --tests "com.anjo.service.HistoryRecordingTest" --tests "com.anjo.ApplicationTest"` exits 0 (no regression; DI resolves DisplayEventBus singleton)
- Full suite green after the wave: `./gradlew test`
</verification>

<success_criteria>
- DisplayEvent model and DisplayEventBus service exist with replay=5 SharedFlow
- ScreenDriverService emits a DisplayEvent after each successful history insert (all three display paths)
- DisplayEventBus is registered and resolvable as a DI singleton
- Wave-0 test files (DisplayEventBusTest, LiveRoutesTest) exist
- DisplayEventBusTest passes; no existing test regresses
</success_criteria>

<output>
Create `.planning/phases/15-sse-live-feed/15-01-SUMMARY.md` when done
</output>
