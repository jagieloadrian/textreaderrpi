---
phase: 15-sse-live-feed
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - src/main/kotlin/com/anjo/routing/LiveRoutes.kt
  - src/main/kotlin/com/anjo/di/HTTP.kt
  - src/main/kotlin/com/anjo/routing/Routing.kt
  - src/test/kotlin/com/anjo/routing/LiveRoutesTest.kt
autonomous: true
requirements:
  - LIVE-01
  - LIVE-03
nyquist_compliant: true

must_haves:
  truths:
    - "GET /api/v1/live responds with Content-Type text/event-stream"
    - "The SSE route streams every DisplayEvent from DisplayEventBus as a named 'display' event with JSON data"
    - "The SSE route declares a 30-second heartbeat that emits a keep-alive comment frame"
    - "The SSE route is registered OUTSIDE the rate-limited /api/v1 block so heartbeats do not trip the rate limiter"
    - "install(SSE) runs before routing is configured"
  artifacts:
    - path: "src/main/kotlin/com/anjo/routing/LiveRoutes.kt"
      provides: "sse(\"/live\") route with heartbeat + DisplayEventBus collect"
      contains: "sse(\"/live\")"
    - path: "src/test/kotlin/com/anjo/routing/LiveRoutesTest.kt"
      provides: "Content-Type assertion for the SSE endpoint"
      contains: "text/event-stream"
  key_links:
    - from: "src/main/kotlin/com/anjo/routing/Routing.kt"
      to: "src/main/kotlin/com/anjo/routing/LiveRoutes.kt"
      via: "route(\"/api/v1\") { liveRoutes(displayEventBus) } outside the rate-limited block"
      pattern: "liveRoutes\\(displayEventBus\\)"
    - from: "src/main/kotlin/com/anjo/routing/LiveRoutes.kt"
      to: "src/main/kotlin/com/anjo/service/DisplayEventBus.kt"
      via: "displayEventBus.events.collect { send(ServerSentEvent(...)) }"
      pattern: "displayEventBus\\.events\\.collect"
    - from: "src/main/kotlin/com/anjo/di/HTTP.kt"
      to: "io.ktor.server.sse.SSE"
      via: "install(SSE) in configureHTTP()"
      pattern: "install\\(SSE\\)"
---

<objective>
Expose the live feed over HTTP (LIVE-01 endpoint side + LIVE-03 heartbeat): create `LiveRoutes.kt` with an `sse("/live")` handler that declares a 30-second keep-alive heartbeat and then collects `DisplayEventBus.events`, sending each as a named `display` SSE frame with JSON `data`. Install the SSE plugin in `configureHTTP()` and register the route under `/api/v1` OUTSIDE the rate-limited block.

Purpose: This is the consumer/transport side of LIVE-01 and the entirety of LIVE-03. Plan 01 populates the bus; this plan streams it to browsers and `curl`. Heartbeat lives in the same `sse {}` block (it cannot be a separate plan touching the same file).
Output: `LiveRoutes.kt` (new), modified `HTTP.kt` (install SSE), modified `Routing.kt` (register route), and the real assertions in `LiveRoutesTest.kt` (replacing the Plan 01 placeholder).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-sse-live-feed/15-CONTEXT.md
@.planning/phases/15-sse-live-feed/15-RESEARCH.md
@.planning/phases/15-sse-live-feed/15-VALIDATION.md
@.planning/phases/15-sse-live-feed/15-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols this plan introduces (exclude from drift/source-grounding checks — they are NEW):
- `com.anjo.routing.LiveRoutes` file with `fun Route.liveRoutes(displayEventBus: DisplayEventBus)`
- New endpoint `GET /api/v1/live` (SSE, Content-Type `text/event-stream`)
- `install(SSE)` call added to `com.anjo.di.HTTP.configureHTTP`
- New route registration block `route("/api/v1") { liveRoutes(displayEventBus) }` in `Routing.configureRouting`
Consumes symbols created in Plan 01: `com.anjo.service.DisplayEventBus`, `com.anjo.model.DisplayEvent`, catalog alias `ktor-server-sse`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create LiveRoutes.kt with heartbeat and DisplayEventBus collect</name>
  <files>src/main/kotlin/com/anjo/routing/LiveRoutes.kt</files>
  <read_first>
    - src/main/kotlin/com/anjo/routing/HistoryRoutes.kt (thin route-handler pattern: a top-level fun Route.xxxRoutes(service) extension function in package com.anjo.routing)
    - src/main/kotlin/com/anjo/service/DisplayEventBus.kt (the events: SharedFlow<DisplayEvent> property to collect; created in Plan 01)
    - src/main/kotlin/com/anjo/model/DisplayEvent.kt (the @Serializable type to JSON-encode; created in Plan 01)
    - .planning/phases/15-sse-live-feed/15-RESEARCH.md (Pattern 1 SSE route; Pitfall 4 heartbeat-before-collect ordering; Code Examples "Complete SSE Route Handler")
    - .planning/phases/15-sse-live-feed/15-CONTEXT.md (D-10 named 'display' event, D-16 30-second keep-alive comment frame)
  </read_first>
  <action>
    Create `src/main/kotlin/com/anjo/routing/LiveRoutes.kt`, package `com.anjo.routing`, with a top-level extension `fun Route.liveRoutes(displayEventBus: DisplayEventBus)`. Inside, call `sse("/live") { ... }`. As the FIRST statement in the sse block declare `heartbeat { period = 30.seconds; eventProvider = { ServerSentEvent(comments = "keep-alive") } }` — it must come before collect because collect suspends forever (RESEARCH Pitfall 4). Then `displayEventBus.events.collect { event -> send(ServerSentEvent(event = "display", data = Json.encodeToString(event))) }`.
    Imports: `io.ktor.server.sse.sse`, `io.ktor.server.sse.heartbeat`, `io.ktor.sse.ServerSentEvent`, `io.ktor.server.routing.Route`, `com.anjo.service.DisplayEventBus`, `com.anjo.model.DisplayEvent`, `kotlinx.coroutines.flow.collect`, `kotlinx.serialization.encodeToString`, `kotlinx.serialization.json.Json`, `kotlin.time.Duration.Companion.seconds`.
    Use the named event string `display` (D-10) and the comment value `keep-alive` (D-16). No query parameters on this route (D-11 all-zone stream) — therefore no validator needed, consistent with the project rule that validation lives only in validator objects. No code comments in the file.
  </action>
  <verify>
    <automated>./gradlew compileKotlin 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/main/kotlin/com/anjo/routing/LiveRoutes.kt exists and declares `fun Route.liveRoutes(displayEventBus: DisplayEventBus)`
    - LiveRoutes.kt contains `sse("/live")`
    - LiveRoutes.kt contains `heartbeat` and `period = 30.seconds`
    - LiveRoutes.kt contains `ServerSentEvent(comments = "keep-alive")`
    - LiveRoutes.kt contains `displayEventBus.events.collect`
    - LiveRoutes.kt contains `ServerSentEvent(` with `event = "display"` and `data = Json.encodeToString(event)`
    - `./gradlew compileKotlin` exits 0
  </acceptance_criteria>
  <done>LiveRoutes.kt compiles; the sse route declares the 30-second heartbeat before collecting and sends named 'display' frames with JSON-encoded DisplayEvent.</done>
</task>

<task type="auto">
  <name>Task 2: Install SSE plugin and register liveRoutes outside the rate-limited block</name>
  <files>src/main/kotlin/com/anjo/di/HTTP.kt, src/main/kotlin/com/anjo/routing/Routing.kt</files>
  <read_first>
    - src/main/kotlin/com/anjo/di/HTTP.kt (configureHTTP installs CORS + DefaultHeaders; runs before configureRouting per Application.module() — add install(SSE) here so the plugin exists before routes register, RESEARCH Pitfall 1)
    - src/main/kotlin/com/anjo/routing/Routing.kt (the routing {} block; the `route("/api/v1") { installApiRateLimiting(...) ... }` rate-limited block; the `by dependencies` injection pattern at the top of configureRouting)
    - src/main/kotlin/com/anjo/Application.kt (confirms configureHTTP() runs before configureRouting())
    - .planning/phases/15-sse-live-feed/15-RESEARCH.md (Pitfall 1 install ordering; Pitfall 2 SSE must be outside rate-limited block; Code Examples "Routing Registration")
  </read_first>
  <action>
    In `HTTP.kt` `configureHTTP()`: add `install(SSE)` after the existing `install(DefaultHeaders) { ... }` call. Import `io.ktor.server.sse.SSE`. SSE needs no configuration lambda — it is a bare `install(SSE)`.
    In `Routing.kt` `configureRouting()`: inject the bus with `val displayEventBus: DisplayEventBus by dependencies` alongside the other `by dependencies` declarations at the top. Then, INSIDE `routing {}` but OUTSIDE the existing `route("/api/v1") { installApiRateLimiting(...) }` block, add a SEPARATE block `route("/api/v1") { liveRoutes(displayEventBus) }`. Do NOT place liveRoutes inside the rate-limited block (RESEARCH Pitfall 2 — long-lived SSE + heartbeats would eventually trip the 60 req/min limiter and drop the connection). Imports: `com.anjo.service.DisplayEventBus`. The `liveRoutes` extension is in the same package (`com.anjo.routing`) so no import is needed for it. No code comments in either file.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.anjo.ApplicationTest"</automated>
  </verify>
  <acceptance_criteria>
    - HTTP.kt contains `install(SSE)`
    - HTTP.kt imports `io.ktor.server.sse.SSE`
    - Routing.kt contains `val displayEventBus: DisplayEventBus by dependencies`
    - Routing.kt contains a `route("/api/v1")` block containing `liveRoutes(displayEventBus)`
    - The `liveRoutes(displayEventBus)` call is NOT inside the block that contains `installApiRateLimiting`
    - `./gradlew test --tests "com.anjo.ApplicationTest"` exits 0 (app still boots, all DI bindings resolve, routes register)
  </acceptance_criteria>
  <done>SSE plugin installed in configureHTTP; liveRoutes registered under /api/v1 outside the rate-limited block; application boots and existing route/DI tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Replace LiveRoutesTest placeholder with real SSE endpoint assertions</name>
  <files>src/test/kotlin/com/anjo/routing/LiveRoutesTest.kt</files>
  <read_first>
    - src/test/kotlin/com/anjo/routing/LiveRoutesTest.kt (the Plan 01 placeholder to replace)
    - src/test/kotlin/com/anjo/routing/WebRoutesTest.kt (testApplication + module() + client.get + header assertion pattern)
    - .planning/phases/15-sse-live-feed/15-RESEARCH.md (Validation Architecture "Testing note on streaming": assert Content-Type header only; do not attempt to read frames in unit tests; reading SSE frames needs responseTimeout=0 and byte-channel handling)
    - .planning/phases/15-sse-live-feed/15-VALIDATION.md (15-02 LIVE-01 unit HTTP; 15-03-01 LIVE-03 Content-Type assertion)
  </read_first>
  <action>
    Replace the placeholder body of `LiveRoutesTest.kt` with real Kotest `FunSpec` tests using `testApplication { application { module() } ... }`. Because the SSE handler holds the connection open via `collect` (it never completes), do NOT `client.get("/api/v1/live")` with a default client (it would hang). Instead create a test client with a short response timeout — `val sseClient = createClient { install(io.ktor.client.plugins.HttpTimeout) { requestTimeoutMillis = 1500 } }` — issue `sseClient.get("/api/v1/live")`, and read only the response headers, asserting the response `Content-Type` starts with `text/event-stream`. Wrap the call so a timeout after headers are received is treated as success (headers arrive before the stream blocks). If the chosen Ktor test-host API surfaces headers before the body without a hang, prefer that and drop the timeout shim. Add two tests: (a) `GET /api/v1/live returns text/event-stream content type` (LIVE-01 endpoint exists + correct content type); (b) `GET /api/v1/live keeps stream open with event-stream content type` referencing LIVE-03 keep-alive transport (same Content-Type assertion is the unit-testable proxy; actual 30s heartbeat is manual per VALIDATION.md). No code comments in the file.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.anjo.routing.LiveRoutesTest"</automated>
  </verify>
  <acceptance_criteria>
    - LiveRoutesTest.kt no longer contains the `// implemented in Plan 02` placeholder marker
    - LiveRoutesTest.kt contains the string `text/event-stream`
    - LiveRoutesTest.kt issues a request to `/api/v1/live` via testApplication
    - LiveRoutesTest.kt uses a bounded client timeout (or a header-only read) so the test does not hang on the open stream
    - `./gradlew test --tests "com.anjo.routing.LiveRoutesTest"` exits 0
  </acceptance_criteria>
  <done>LiveRoutesTest asserts GET /api/v1/live returns Content-Type text/event-stream without hanging on the open SSE connection; the Plan 01 placeholder is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/curl → SSE endpoint | Untrusted clients connect to GET /api/v1/live; one-directional server→client push, no request body parsed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Information Disclosure | GET /api/v1/live unauthenticated stream | accept | Project-wide no-auth scope (home LAN only); the stream exposes the same display text already returned by /api/v1/history without auth |
| T-15-04 | Denial of Service | Unbounded concurrent SSE connections | accept | D-06 home-lab scale (1-5 subscribers); SharedFlow collectors are lightweight; no connection cap by decision |
| T-15-05 | Denial of Service | SSE registered under rate-limited /api/v1 | mitigate | liveRoutes registered OUTSIDE the installApiRateLimiting block so long-lived heartbeat frames never count against the 60 req/min limit (RESEARCH Pitfall 2) |
| T-15-06 | Tampering | SSE frame data field | mitigate | data is produced via Json.encodeToString of a typed @Serializable DisplayEvent — no string concatenation, no injection of raw client input into the frame |
</threat_model>

<verification>
- `./gradlew compileKotlin` succeeds (LiveRoutes + install(SSE) + Routing wiring compile)
- `./gradlew test --tests "com.anjo.routing.LiveRoutesTest"` exits 0 (LIVE-01 endpoint Content-Type, LIVE-03 transport)
- `./gradlew test --tests "com.anjo.ApplicationTest"` exits 0 (no boot/DI/routing regression)
- Full suite green after the wave: `./gradlew test`
- Manual (per VALIDATION.md): `curl --no-buffer http://localhost:8080/api/v1/live`, POST to /api/v1/text, observe `event: display` frame < 1s (LIVE-01); wait 30s idle, observe `: keep-alive` comment (LIVE-03)
</verification>

<success_criteria>
- GET /api/v1/live returns Content-Type text/event-stream
- The route streams DisplayEventBus events as named 'display' JSON frames
- A 30-second keep-alive heartbeat comment frame is declared in the route
- The route is outside the rate-limited /api/v1 block
- install(SSE) is present in configureHTTP
- LiveRoutesTest passes; no existing test regresses
</success_criteria>

<output>
Create `.planning/phases/15-sse-live-feed/15-02-SUMMARY.md` when done
</output>
