---
phase: 08-refactor-dead-code-analysis
plan: 05
type: execute
wave: 3
depends_on: ["08-02", "08-03", "08-04"]
files_modified:
  - src/main/kotlin/com/anjo/config/loader/ConfigLoader.kt
  - src/main/kotlin/com/anjo/config/model/ApplicationConfig.kt
  - src/main/kotlin/com/anjo/config/model/ApiConfig.kt
  - src/main/kotlin/com/anjo/service/DisplaySelectionService.kt
  - src/main/kotlin/com/anjo/service/ScreenDriverService.kt
  - src/main/kotlin/com/anjo/utils/Font.kt
  - src/main/kotlin/com/anjo/driver/Max7219Matrix.kt
  - .planning/phases/08-refactor-dead-code-analysis/08-05-SUMMARY.md
autonomous: true
requirements: [REF-01, REF-02, REF-04]
must_haves:
  truths:
    - "./gradlew build emits no unused-symbol warnings for any symbol not already removed by Phase 8"
    - "Any additional dead symbols surfaced by the compiler and confirmed by grep are removed in this phase"
    - "Unused imports are gone from every production file touched in Phase 8"
    - "The JaCoCo line-coverage gate of 70% passes on the cleaned codebase"
  artifacts:
    - path: ".planning/phases/08-refactor-dead-code-analysis/08-05-SUMMARY.md"
      provides: "Record of the build-warning sweep, any extra removals, and the final coverage figure"
      contains: "Coverage"
  key_links:
    - from: "build.gradle.kts"
      to: "jacocoTestCoverageVerification"
      via: "minimum = 0.70 line-coverage gate"
      pattern: "jacocoTestCoverageVerification"
---

<objective>
Final cleanup and gate pass. Run `./gradlew build` to surface Kotlin unused-symbol warnings, cross-reference each candidate with grep across both `src/main` and `src/test` for real callers (D-11, D-12), remove any confirmed-dead symbols and unused imports left across the files touched in Waves 1–2 (D-16), then enforce the JaCoCo ≥70% line-coverage gate and add Kotest tests for any Phase-8-touched path that drops below it (D-17).

Purpose: Close out REF-01/REF-02 by removing residual dead symbols and stale imports the targeted removals could not predict, and prove REF-04 coverage holds.
Output: Cleaned imports/symbols (if any found) and a green `./gradlew test jacocoTestCoverageVerification`; a SUMMARY recording the build-warning findings and final coverage.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-refactor-dead-code-analysis/08-CONTEXT.md
@.planning/phases/08-refactor-dead-code-analysis/08-RESEARCH.md
@.planning/phases/08-refactor-dead-code-analysis/08-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols created by THIS plan (exclude from drift verification):
- None created. This plan removes residual unused imports / dead symbols and may add narrowly-scoped Kotest tests only if the coverage gate requires them. Any such test file is created under `src/test/kotlin/com/anjo/` following the existing FunSpec `should` convention.

Note on `files_modified`: the seven production files listed in frontmatter (ConfigLoader.kt, ApplicationConfig.kt, ApiConfig.kt, DisplaySelectionService.kt, ScreenDriverService.kt, Font.kt, Max7219Matrix.kt) are the Phase-8-touched files this sweep re-inspects for residual unused imports/symbols (D-16). They may be edited (import removals only) or left untouched if `./gradlew build` reports no warnings — either outcome is valid and must be recorded in the SUMMARY.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Build-warning dead-symbol sweep and unused-import cleanup</name>
  <files>src/main/kotlin/com/anjo/config/loader/ConfigLoader.kt, src/main/kotlin/com/anjo/config/model/ApplicationConfig.kt, src/main/kotlin/com/anjo/config/model/ApiConfig.kt, src/main/kotlin/com/anjo/service/DisplaySelectionService.kt, src/main/kotlin/com/anjo/service/ScreenDriverService.kt, src/main/kotlin/com/anjo/utils/Font.kt, src/main/kotlin/com/anjo/driver/Max7219Matrix.kt</files>
  <read_first>
    - .planning/phases/08-refactor-dead-code-analysis/08-CONTEXT.md (D-11 — scan src/main AND src/test; D-12 — build first, grep callers, remove confirmed dead symbols in same phase; D-16 — unused imports)
    - .planning/phases/08-refactor-dead-code-analysis/08-RESEARCH.md (§Don't Hand-Roll — use `./gradlew build` warnings + grep, not a custom scanner)
    - .planning/phases/08-refactor-dead-code-analysis/08-PATTERNS.md (No comments in code rule — do not add comments while editing)
  </read_first>
  <action>
    Run `./gradlew build` and capture every Kotlin warning of the form "is never used", "is never used, and could be removed", "parameter ... is never used", and unused-import warnings. For each warned symbol, confirm with `grep -rn "<symbol>" src/main src/test` (D-11 scope: both source trees, including dead test helpers/methods) that it has no caller anywhere; if and only if it has zero callers (excluding its own declaration), remove it. Scope removals to symbols within `com/anjo/` production and test code; do NOT remove public API surface referenced by tests or routing (e.g. route handlers, DI-provided types, `displayImmediate`, `selectDisplay`, `getCurrentDisplayType`). Remove any unused imports flagged in the files touched during Phase 8 (the files listed in `files_modified`). If `./gradlew build` reports zero actionable unused-symbol/import warnings, make no source edits and record "no residual dead symbols found" in the SUMMARY. Per the no-comments project rule, add no comments. Do not introduce new abstractions (D-13).
  </action>
  <verify>
    <automated>./gradlew build</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew build` exits 0
    - For every symbol removed (if any), `grep -rn "<symbol>" src/main src/test` returns no matches post-removal
    - No new `import` lines added; no `//` or `/* */` comments added to any touched file (grep the diff)
    - SUMMARY lists each unused-symbol/import warning found and its disposition (removed / kept-with-reason), or states "no residual dead symbols found"
  </acceptance_criteria>
  <done>The build is warning-clean for unused symbols/imports within Phase 8 scope, every removal is grep-confirmed dead across src/main and src/test, and the disposition is recorded in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce JaCoCo 70% coverage gate and backfill tests if needed</name>
  <files>.planning/phases/08-refactor-dead-code-analysis/08-05-SUMMARY.md</files>
  <read_first>
    - .planning/phases/08-refactor-dead-code-analysis/08-CONTEXT.md (D-17 — run JaCoCo, add Kotest `should` tests for any Phase-8-touched path below 70%)
    - .planning/phases/08-refactor-dead-code-analysis/08-RESEARCH.md (§JaCoCo Exclusions Already in Place — utils/ and config/model/ excluded; coverage enforced for routing/service/db/di/validation)
    - .planning/phases/08-refactor-dead-code-analysis/08-PATTERNS.md (Kotest FunSpec `should` convention for any backfill test)
  </read_first>
  <action>
    Run `./gradlew test jacocoTestCoverageVerification`. The gate is `minimum = 0.70` line coverage (build.gradle.kts). If it passes, record the final coverage figure in the SUMMARY and stop. If it fails, identify which Phase-8-touched class under `routing/`, `service/`, `db/`, `di/`, or `validation/` dropped below the gate (note: `utils/` including `Font.kt` and `config/model/` are JaCoCo-excluded, so the Font fix and config deletions cannot move the gate). For each such class add a Kotest `FunSpec` test using the `should` convention that exercises the uncovered branch introduced or exposed by Phase 8 — do NOT call hardware methods or add new abstractions. Re-run the gate until green. Record the final coverage percentage and any tests added in the SUMMARY.
  </action>
  <verify>
    <automated>./gradlew test jacocoTestCoverageVerification</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test jacocoTestCoverageVerification` exits 0
    - SUMMARY records the final line-coverage percentage and confirms it is ≥ 70%
    - Any backfill test added uses Kotest `FunSpec` with a `should`-style description and calls no hardware methods
  </acceptance_criteria>
  <done>The full suite plus JaCoCo verification is green at ≥70% line coverage, and the final figure is recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| Build-warning sweep over-removes a symbol that tests/routing actually use (accidental API breakage) | Each removal is grep-confirmed zero-caller across `src/main` AND `src/test` (D-11); public API surface explicitly excluded; `./gradlew build` gates compilation |
| Coverage degradation from cumulative Phase 8 removals (REF-04) | Final `jacocoTestCoverageVerification` gate enforces ≥70%; backfill tests added for any below-gate Phase-8-touched class |
| False sense that coverage moved when the changed file is JaCoCo-excluded | Action documents that `utils/` and `config/model/` are excluded; only routing/service/db/di/validation classes can affect the gate |
| Dead-code removal exposing a hidden caller at runtime (not compile time) | Sweep limited to compiler-flagged unused symbols with zero static callers; behavior-bearing public methods retained |
| Backfill test introduces a new abstraction, violating D-13 | Action forbids new abstractions; tests follow the existing FunSpec `should` convention only |
</threat_model>

<verification>
- `./gradlew build` exits 0 (warning-clean within Phase 8 scope)
- `./gradlew test jacocoTestCoverageVerification` exits 0 at ≥70% line coverage
</verification>

<success_criteria>
- Residual dead symbols and unused imports removed across src/main and src/test, each grep-confirmed (REF-01, REF-02 / D-11, D-12, D-16)
- JaCoCo ≥70% line-coverage gate passes on the cleaned codebase (REF-04 / D-17)
- Final coverage figure recorded in the SUMMARY
</success_criteria>

<output>
Create `.planning/phases/08-refactor-dead-code-analysis/08-05-SUMMARY.md` when done
</output>
