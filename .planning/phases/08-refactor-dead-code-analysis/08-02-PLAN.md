---
phase: 08-refactor-dead-code-analysis
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/main/kotlin/com/anjo/config/model/HardwareConfig.kt
  - src/main/kotlin/com/anjo/config/model/TimingConfig.kt
  - src/main/kotlin/com/anjo/config/model/LoggingConfig.kt
  - src/main/kotlin/com/anjo/config/model/ApplicationConfig.kt
  - src/main/kotlin/com/anjo/config/model/ApiConfig.kt
  - src/main/kotlin/com/anjo/config/loader/ConfigLoader.kt
autonomous: true
requirements: [REF-01]
must_haves:
  truths:
    - "HardwareConfig, TimingConfig, LoggingConfig classes no longer exist in the codebase"
    - "ApplicationConfig has no hardware, timing, or logging fields"
    - "ApiConfig has no queueSize field"
    - "The application compiles and the full test suite passes after removal"
  artifacts:
    - path: "src/main/kotlin/com/anjo/config/model/ApplicationConfig.kt"
      provides: "Slimmed config record without dead nested configs"
      contains: "data class ApplicationConfig"
    - path: "src/main/kotlin/com/anjo/config/model/ApiConfig.kt"
      provides: "ApiConfig without queueSize"
      contains: "data class ApiConfig"
    - path: "src/main/kotlin/com/anjo/config/loader/ConfigLoader.kt"
      provides: "Config loader that no longer constructs dead configs"
      contains: "fun loadConfig"
  key_links:
    - from: "src/main/kotlin/com/anjo/config/loader/ConfigLoader.kt"
      to: "src/main/kotlin/com/anjo/config/model/ApplicationConfig.kt"
      via: "ApplicationConfig(...) constructor call"
      pattern: "ApplicationConfig\\("
---

<objective>
Remove the confirmed-dead configuration code: delete the `HardwareConfig`, `TimingConfig`, and `LoggingConfig` classes, remove their fields from `ApplicationConfig`, remove the `queueSize` field from `ApiConfig`, and strip all corresponding construction blocks and imports from `ConfigLoader`. All three nested configs are loaded but never consumed downstream; `queueSize` has zero references anywhere.

Purpose: Eliminate dead configuration that inflates the config record and reads three unused YAML sections on every startup (REF-01 / D-06, D-07).
Output: Three deleted files; three slimmed/edited config files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-refactor-dead-code-analysis/08-CONTEXT.md
@.planning/phases/08-refactor-dead-code-analysis/08-RESEARCH.md
@.planning/phases/08-refactor-dead-code-analysis/08-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols created by THIS plan (exclude from drift verification):
- None created. This plan only deletes symbols: classes `HardwareConfig`, `TimingConfig`, `LoggingConfig`; fields `ApplicationConfig.hardware`, `ApplicationConfig.timing`, `ApplicationConfig.logging`, `ApiConfig.queueSize`; and their construction/import sites in `ConfigLoader.kt`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead config classes and remove their fields/wiring</name>
  <files>src/main/kotlin/com/anjo/config/model/HardwareConfig.kt, src/main/kotlin/com/anjo/config/model/TimingConfig.kt, src/main/kotlin/com/anjo/config/model/LoggingConfig.kt, src/main/kotlin/com/anjo/config/model/ApplicationConfig.kt, src/main/kotlin/com/anjo/config/model/ApiConfig.kt, src/main/kotlin/com/anjo/config/loader/ConfigLoader.kt</files>
  <read_first>
    - src/main/kotlin/com/anjo/config/model/ApplicationConfig.kt (current 8-field record; remove hardware/timing/logging)
    - src/main/kotlin/com/anjo/config/model/ApiConfig.kt (current 4-field record; remove queueSize)
    - src/main/kotlin/com/anjo/config/loader/ConfigLoader.kt (construction blocks + imports + ApplicationConfig(...) and ApiConfig(...) calls)
    - .planning/phases/08-refactor-dead-code-analysis/08-PATTERNS.md (ApplicationConfig / ConfigLoader / ApiConfig sections — exact lines and target states)
    - .planning/phases/08-refactor-dead-code-analysis/08-RESEARCH.md (§Common Pitfalls §5 and §6 — both constructor call sites must change in the same edit)
  </read_first>
  <action>
    Delete the three files `HardwareConfig.kt`, `TimingConfig.kt`, `LoggingConfig.kt` from `src/main/kotlin/com/anjo/config/model/`.
    In `ApplicationConfig.kt` remove the three fields `val hardware: HardwareConfig`, `val timing: TimingConfig`, `val logging: LoggingConfig`; the resulting primary constructor is exactly `display: DisplayConfig`, `api: ApiConfig`, `metrics: MetricsConfig`, `retryConfig: RetryConfig`, `databaseConfig: DatabaseConfig`. Remove any now-unused imports of the deleted classes.
    In `ApiConfig.kt` remove the field `val queueSize: Int`; the resulting record is `maxTextLength: Int`, `rateLimitPerMinute: Int`, `metricsRateLimitPerMinute: Int = 120`.
    In `ConfigLoader.kt` remove: the imports `com.anjo.config.model.HardwareConfig`, `com.anjo.config.model.TimingConfig`, `com.anjo.config.model.LoggingConfig`; the `val hardwareConfig = HardwareConfig(...)`, `val timingConfig = TimingConfig(...)`, `val loggingConfig = LoggingConfig(...)` construction blocks; the `queueSize = config.propertyOrNull("api.queueSize")...` named argument inside the `ApiConfig(...)` call; and the `hardware = hardwareConfig,`, `timing = timingConfig,`, `logging = loggingConfig,` named arguments inside the `ApplicationConfig(...)` return. The final return is `ApplicationConfig(display = displayConfig, api = apiConfig, metrics = metricsConfig, retryConfig = retryConfig, databaseConfig)`. Per D-16 remove any unused imports left behind. Per the no-comments project rule, do not add explanatory comments.
  </action>
  <verify>
    <automated>./gradlew test</automated>
  </verify>
  <acceptance_criteria>
    - Files `HardwareConfig.kt`, `TimingConfig.kt`, `LoggingConfig.kt` do not exist under `src/main/kotlin/com/anjo/config/model/`
    - `grep -rn "HardwareConfig\|TimingConfig\|LoggingConfig" src/main src/test` returns no matches
    - `grep -rn "queueSize" src/main src/test` returns no matches
    - `ApplicationConfig.kt` primary constructor lists exactly: display, api, metrics, retryConfig, databaseConfig (grep -v '^//' for the field names)
    - `./gradlew test` exits 0
  </acceptance_criteria>
  <done>The three dead config classes are gone, ApplicationConfig and ApiConfig are slimmed, ConfigLoader constructs both correctly, and the full suite is green.</done>
</task>

</tasks>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| ApplicationConfig(...) or ApiConfig(...) call site left referencing a removed field → compile error (Pitfall 5, 6) | Both record and both ConfigLoader call sites edited in the same task; `./gradlew test` gates compilation |
| A hidden consumer of `.hardware`/`.timing`/`.logging`/`.queueSize` exists outside inspected files (accidental API breakage) | Acceptance criteria grep `src/main` AND `src/test` for zero references before commit |
| Dead-code removal exposes a stale YAML key read elsewhere | Removal is field-level; YAML keys become inert; full suite (including ConfigLoader tests) gates regression |
| Unused import left after deletion fails strict build | D-16 removes unused imports; `./gradlew test` compiles cleanly |
</threat_model>

<verification>
- `./gradlew test` exits 0
- `grep -rn "HardwareConfig\|TimingConfig\|LoggingConfig\|queueSize" src/main src/test` returns nothing
</verification>

<success_criteria>
- Three dead config classes deleted; ApplicationConfig and ApiConfig fields removed (REF-01 / D-06, D-07)
- Build compiles cleanly with both constructor call sites updated
- Full test suite green
</success_criteria>

<output>
Create `.planning/phases/08-refactor-dead-code-analysis/08-02-SUMMARY.md` when done
</output>
