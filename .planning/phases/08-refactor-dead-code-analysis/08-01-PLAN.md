---
phase: 08-refactor-dead-code-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/test/kotlin/com/anjo/ApplicationTest.kt
autonomous: true
requirements: [REF-03]
must_haves:
  truths:
    - "A DI smoke test resolves all 11 configureDI() bindings and fails fast if one is missing"
    - "The smoke test runs inside testApplication with a real H2 in-memory DB"
    - "The smoke test calls no methods on resolved instances (no hardware side effects)"
  artifacts:
    - path: "src/test/kotlin/com/anjo/ApplicationTest.kt"
      provides: "DI binding-resolution smoke test"
      contains: "should resolve all configureDI bindings without error"
  key_links:
    - from: "src/test/kotlin/com/anjo/ApplicationTest.kt"
      to: "src/main/kotlin/com/anjo/di/DependencyInjection.kt"
      via: "application.dependencies.getBlocking<T>(DependencyKey<T>())"
      pattern: "getBlocking.*DependencyKey"
---

<objective>
Add a DI smoke test to `ApplicationTest.kt` that resolves every binding registered in `configureDI()` and asserts each is non-null. This is the FIRST task of Phase 8 (D-05) — it must exist as a green baseline before any dead-code removal begins, so that later refactors cannot silently break DI wiring. Per D-02 all 11 bindings are covered; per D-03 a real H2 in-memory DB is used so Exposed/Flyway wiring failures surface.

Purpose: Establish a fast-failing guard over the 11-binding DI graph so subsequent cleanup waves are safe.
Output: A new test `should resolve all configureDI bindings without error` inside the existing `ApplicationTest.kt`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-refactor-dead-code-analysis/08-CONTEXT.md
@.planning/phases/08-refactor-dead-code-analysis/08-RESEARCH.md
@.planning/phases/08-refactor-dead-code-analysis/08-PATTERNS.md
@src/main/kotlin/com/anjo/di/DependencyInjection.kt
@src/test/kotlin/com/anjo/ApplicationTest.kt
</context>

<artifacts_this_phase_produces>
Symbols created by THIS plan (exclude from drift verification):
- Test case `should resolve all configureDI bindings without error` in `ApplicationTest.kt`
- New imports in `ApplicationTest.kt`: `DependencyKey`, `dependencies`, `shouldNotBeNull`, `CoroutineDispatcher`, and the 11 resolved binding types
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DI binding-resolution smoke test to ApplicationTest.kt</name>
  <files>src/test/kotlin/com/anjo/ApplicationTest.kt</files>
  <read_first>
    - src/test/kotlin/com/anjo/ApplicationTest.kt (existing testApplication tests — append the new test before the closing `})`)
    - src/main/kotlin/com/anjo/di/DependencyInjection.kt (authoritative list of `provide {}` bindings — the 11 types to resolve)
    - .planning/phases/08-refactor-dead-code-analysis/08-RESEARCH.md (§Code Examples §DI Smoke Test Pattern; §Common Pitfalls §1)
    - .planning/phases/08-refactor-dead-code-analysis/08-PATTERNS.md (ApplicationTest.kt section — exact imports and test body)
  </read_first>
  <behavior>
    - Per D-02 all 11 configureDI() bindings are covered; per D-03 the test runs against a real H2 in-memory DB (via testApplication) so Exposed/Flyway wiring failures surface
    - Test resolves ApplicationConfig (provided by `provide { appConfig }`) → non-null
    - Test resolves ApiConfig (`provide { appConfig.api }`) → non-null
    - Test resolves DisplayConfig (`provide { appConfig.display }`) → non-null
    - Test resolves CoroutineDispatcher (`provide { Dispatchers.IO }`) → non-null
    - Test resolves MetricRegistry, DisplaySelectionService, ScreenDriverService, MetricsCollector, ScheduleRepository, EffectRendererFactory, SchedulerService → each non-null
    - If any binding is absent, getBlocking throws MissingDependencyException and the test fails
  </behavior>
  <action>
    Append a new `test("should resolve all configureDI bindings without error") { ... }` block inside the existing `ApplicationTest` FunSpec, before its closing `})`. Inside a `testApplication { application { module() } }` block (real H2 in-memory DB per D-03), read `val deps = application.dependencies`, then call `deps.getBlocking<T>(DependencyKey<T>())` followed by `shouldNotBeNull {}` for each of these 11 types in order (the full D-02 binding set): `ApplicationConfig`, `ApiConfig`, `DisplayConfig`, `CoroutineDispatcher`, `MetricRegistry`, `DisplaySelectionService`, `ScreenDriverService`, `MetricsCollector`, `ScheduleRepository`, `EffectRendererFactory`, `SchedulerService`. Use the exact fully-qualified types: config types from `com.anjo.config.model.*`, services from `com.anjo.service.*` (note `MetricsCollector`, `EffectRendererFactory`, `SchedulerService`, `ScreenDriverService`, `DisplaySelectionService` are all in `com.anjo.service`), `ScheduleRepository` from `com.anjo.db`, `MetricRegistry` from `com.codahale.metrics`, `CoroutineDispatcher` from `kotlinx.coroutines`. Add imports: `io.ktor.server.plugins.di.DependencyKey`, `io.ktor.server.plugins.di.dependencies`, `io.kotest.matchers.nulls.shouldNotBeNull`, plus the 11 binding type imports. Per D-01 do NOT invoke any method on the resolved instances — only `shouldNotBeNull {}`. Per D-04 add the test to this existing file; create no new test file. The `Dispatchers.IO` binding resolves under key type `CoroutineDispatcher` (per A3); if `getBlocking<CoroutineDispatcher>()` fails at runtime, fall back to `CloseableCoroutineDispatcher` and note the change in the SUMMARY.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.anjo.ApplicationTest"</automated>
  </verify>
  <acceptance_criteria>
    - `ApplicationTest.kt` contains the string `should resolve all configureDI bindings without error`
    - `ApplicationTest.kt` contains `getBlocking` and `DependencyKey` at least 11 times each
    - `ApplicationTest.kt` contains no method invocation on resolved instances beyond `shouldNotBeNull` (grep: no `.start(`, `.displayImmediate(`, `.currentDriver(` inside the new test)
    - `./gradlew test --tests "com.anjo.ApplicationTest"` exits 0
  </acceptance_criteria>
  <done>The DI smoke test compiles, runs green, resolves all 11 bindings, and invokes no methods on resolved instances.</done>
</task>

</tasks>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| Smoke test calls a method on a resolved service → triggers Pi4J/hardware side effect and flakes in CI (Pitfall 1) | Assertions limited to `shouldNotBeNull {}`; acceptance criteria greps for forbidden method calls inside the new test |
| Wrong DependencyKey type for `Dispatchers.IO` → false negative (binding "missing") | Use `CoroutineDispatcher` per A3; documented runtime fallback to `CloseableCoroutineDispatcher` |
| Smoke test gives false confidence by resolving a subset of bindings | All 11 `provide {}` entries enumerated from `DependencyInjection.kt` (D-02); acceptance criterion counts ≥11 `getBlocking` calls |
| Coverage degradation from adding a test | Test-only addition; raises coverage, cannot lower it |
</threat_model>

<verification>
- `./gradlew test --tests "com.anjo.ApplicationTest"` exits 0
- Full suite still green: `./gradlew test`
</verification>

<success_criteria>
- DI smoke test resolves all 11 configureDI() bindings and fails fast on a missing provider (REF-03 / D-02)
- Real H2 in-memory DB used (D-03)
- No method calls on resolved instances (D-01)
- Test lives in existing `ApplicationTest.kt` (D-04)
</success_criteria>

<output>
Create `.planning/phases/08-refactor-dead-code-analysis/08-01-SUMMARY.md` when done
</output>
