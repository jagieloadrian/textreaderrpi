---
phase: 4
plan: "04-02"
title: "DevOps Reorganization + Model Relocation"
wave: 1
depends_on: []
autonomous: true
requirements: [D4-10, D4-11, D4-12, D4-21]
files_modified:
  - .devops/host/textreaderrpi.service
  - .devops/host/install-systemd.sh
  - .devops/host/build-docker-image.sh
  - .devops/containers/Dockerfile
  - src/main/kotlin/com/anjo/model/DisplayStatusResponse.kt
  - src/main/kotlin/com/anjo/model/DisplaySelectRequest.kt
  - src/main/kotlin/com/anjo/model/DisplaySelectResponse.kt
  - src/main/kotlin/com/anjo/api/DisplayApi.kt
  - deployment/
  - Dockerfile
  - README.md
---

<objective>
Reorganize DevOps artifacts into `.devops/` with environment-based layout (host/, containers/) and relocate DisplayApi DTOs from the `api` package into the `model` package where they belong with other data transfer objects.
</objective>

<context>
Current `deployment/` folder contains `scripts/` (build-docker-image.sh, install-systemd.sh) and `systemd/` (textreaderrpi.service). The Dockerfile sits at project root. D4-21 specifies `.devops/containers/*` and `.devops/host/*` layout. DisplayApi.kt in `com.anjo.api` holds three DTOs (DisplayStatusResponse, DisplaySelectRequest, DisplaySelectResponse) that belong in `com.anjo.model` per D4-11.
</context>

<tasks>

<task id="1" name="Create .devops directory structure and move host artifacts">
<read_first>
- deployment/systemd/textreaderrpi.service
- deployment/scripts/install-systemd.sh
- deployment/scripts/build-docker-image.sh
</read_first>
<action>
Create `.devops/host/` directory. Move:
- `deployment/systemd/textreaderrpi.service` → `.devops/host/textreaderrpi.service`
- `deployment/scripts/install-systemd.sh` → `.devops/host/install-systemd.sh`
- `deployment/scripts/build-docker-image.sh` → `.devops/host/build-docker-image.sh`

Update any internal path references within these scripts (e.g., if install-systemd.sh references `../systemd/textreaderrpi.service`, update to use the new location). Keep the scripts executable. After moving, remove the empty `deployment/` directory.
</action>
<acceptance_criteria>
- `.devops/host/textreaderrpi.service` exists with same content as original
- `.devops/host/install-systemd.sh` exists and is executable
- `.devops/host/build-docker-image.sh` exists and is executable
- `deployment/` directory no longer exists
- Internal path references in scripts are updated to new layout
</acceptance_criteria>
</task>

<task id="2" name="Move Dockerfile to .devops/containers/">
<read_first>
- Dockerfile
</read_first>
<action>
Create `.devops/containers/` directory. Move `Dockerfile` from project root to `.devops/containers/Dockerfile`. Update the build context references in `build-docker-image.sh` (in `.devops/host/`) to point to the new Dockerfile location (likely `docker build -f .devops/containers/Dockerfile .` from project root). If there's a `.dockerignore`, keep it at project root (Docker requires it there).
</action>
<acceptance_criteria>
- `.devops/containers/Dockerfile` exists with same content as original
- Project root no longer has `Dockerfile`
- `.devops/host/build-docker-image.sh` references `.devops/containers/Dockerfile`
- `docker build -f .devops/containers/Dockerfile .` syntax is valid
</acceptance_criteria>
</task>

<task id="3" name="Relocate DisplayApi DTOs to model package">
<read_first>
- src/main/kotlin/com/anjo/api/DisplayApi.kt
- src/main/kotlin/com/anjo/model/TextRequest.kt
- src/main/kotlin/com/anjo/web/routes/ApiRoutes.kt
- src/main/kotlin/com/anjo/routing/Routing.kt
</read_first>
<action>
Move the three `@Serializable` data classes from `com.anjo.api.DisplayApi` to `com.anjo.model`:
1. Create `src/main/kotlin/com/anjo/model/DisplayModels.kt` with `package com.anjo.model` containing `DisplayStatusResponse`, `DisplaySelectRequest`, and `DisplaySelectResponse` (same fields, same annotations)
2. Update all import sites from `com.anjo.api.DisplayStatusResponse` (etc.) to `com.anjo.model.DisplayStatusResponse` — search across: `web/routes/ApiRoutes.kt`, any test files referencing these classes
3. Delete `src/main/kotlin/com/anjo/api/DisplayApi.kt` (or keep the `api` package empty if other files exist there)
4. If `com.anjo.api` package is now empty, remove the directory
</action>
<acceptance_criteria>
- `src/main/kotlin/com/anjo/model/DisplayModels.kt` exists with all three DTOs
- Package declaration is `package com.anjo.model`
- All `@Serializable` annotations preserved
- No file in the project imports from `com.anjo.api.DisplayStatusResponse` (or the other two)
- `./gradlew compileKotlin` passes
- `./gradlew test` passes (import updates don't break tests)
</acceptance_criteria>
</task>

<task id="4" name="Update README and documentation references">
<read_first>
- README.md
- docs/deployment/production-guide.md
</read_first>
<action>
Update paths in documentation to reflect new locations:
1. In README.md, replace any references to `deployment/` with `.devops/host/` or `.devops/containers/` as appropriate
2. In `docs/deployment/production-guide.md`, update references to systemd file location and Dockerfile path
3. Add a brief note in README about the `.devops/` structure: "DevOps artifacts are organized under `.devops/` — `host/` for bare-metal deployment (systemd, scripts) and `containers/` for containerized deployment (Dockerfile)."
</action>
<acceptance_criteria>
- README.md contains no references to the old `deployment/` path
- README.md mentions `.devops/host/` and `.devops/containers/`
- `docs/deployment/production-guide.md` references updated paths
- No dead path references remain in documentation
</acceptance_criteria>
</task>

</tasks>

<verification>
- `./gradlew compileKotlin` — project compiles
- `./gradlew test` — all tests pass (no broken imports)
- `.devops/host/` contains service file and scripts
- `.devops/containers/` contains Dockerfile
- `deployment/` directory removed
- No imports from `com.anjo.api` in the codebase
</verification>

<must_haves>
- DevOps artifacts under `.devops/` with environment-based layout (D4-10, D4-21)
- DisplayApi DTOs relocated to model package (D4-11)
- All existing tests pass without import changes leaking
</must_haves>

