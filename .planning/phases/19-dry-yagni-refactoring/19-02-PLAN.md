---
phase: 19-dry-yagni-refactoring
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - src/test/kotlin/com/anjo/TestSupport.kt
  - src/test/kotlin/com/anjo/ApplicationTest.kt
  - src/test/kotlin/com/anjo/routing/HistoryRoutesTest.kt
  - src/test/kotlin/com/anjo/routing/HistoryUIRoutesTest.kt
  - src/test/kotlin/com/anjo/service/HistoryRecordingTest.kt
  - src/test/kotlin/com/anjo/routing/ZonesUIRoutesTest.kt
  - src/test/kotlin/com/anjo/routing/FirmwareZoneRoutesTest.kt
autonomous: true
requirements:
  - REF-05
must_haves:
  truths:
    - "A shared src/test/kotlin/com/anjo/TestSupport.kt provides appTest{}, dep<T>(), and historyRecord() and is consumed by multiple test classes (SC2, D-04, D-05)"
    - "The getBlocking(DependencyKey) incantation is gone from all six migrated test files, replaced by dep<T>()"
    - "The two repeat(25) history insertion loops use the historyRecord() builder"
    - "Full test suite is green and JaCoCo line coverage stays >=70% (SC3, D-10)"
    - "No new compiler warnings introduced by the migration — unused test imports removed (SC4, D-09)"
  artifacts:
    - "src/test/kotlin/com/anjo/TestSupport.kt (new: appTest, dep, historyRecord)"
    - "Migrated: ApplicationTest.kt, HistoryRoutesTest.kt, HistoryUIRoutesTest.kt, HistoryRecordingTest.kt, ZonesUIRoutesTest.kt, FirmwareZoneRoutesTest.kt"
  key_links:
    - "appTest{} wraps testApplication { application { module() }; client.get(\"/health\") }"
    - "dep<T>() resolves application.dependencies.getBlocking<T>(DependencyKey<T>())"
---

<objective>
Extract the repeated Kotest bootstrap into a shared TestSupport.kt (appTest{}, dep<T>(), historyRecord())
and migrate the six test files that carry the genuine >5-line copy-paste / getBlocking-incantation duplication
(SC2). Then run the full phase gate — complete test suite, JaCoCo coverage, and warning-diff — over the
combined refactor.

Purpose: REF-05 — the test suite has no significant duplication. The `dependencies.getBlocking<T>(DependencyKey<T>())`
call and the `testApplication { application { module() }; client.get("/health") }` bootstrap are the largest
copy-paste blocks (heaviest: ApplicationTest's 15-binding resolution test).
Output: one shared helper file, six migrated test files, a documented below-threshold list, and green suite/coverage/warning gates.

Scope decision (recorded for reviewers): SC2's own gate is "no copy-paste duplication longer than 5 lines
across test classes." Only the six getBlocking-using files carry >5-line blocks (getBlocking chains + fixture loops).
The nine getBlocking-free files (TextApiRouteTest, ScheduleRoutesTest, ZoneRoutesTest, WebAndDisplayRoutesTest,
RateLimitRoutesTest, WebRoutesTest, MetricsRoutesTest, HealthRoutesTest, ScheduleUIRoutesTest) carry only the
3-line `testApplication { application { module() }; client.get("/health") }` bootstrap, which is BELOW the 5-line
threshold — they are listed, not churned (D-07 spirit; RESEARCH Pitfall 5). LiveRoutesTest and FirmwareZoneDriverTest
are exempt (D-06 — SSE/WS need embeddedServer).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-dry-yagni-refactoring/19-CONTEXT.md
@.planning/phases/19-dry-yagni-refactoring/19-RESEARCH.md
@.planning/phases/19-dry-yagni-refactoring/19-PATTERNS.md
</context>

## Artifacts this phase produces

Symbols/paths newly created by Phase 19 (source-grounding: exclude these from drift verification):

- `com.anjo.appTest(block: suspend ApplicationTestBuilder.() -> Unit)` — new test bootstrap helper
- `com.anjo.dep<T>(): T` — new reified `ApplicationTestBuilder` DI accessor extension
- `com.anjo.historyRecord(i: Int, effect, source, scheduleId): HistoryRecord` — new fixture builder
- `src/test/kotlin/com/anjo/TestSupport.kt` — new file holding the three helpers above
- `HistoryValidators.parseFilter(...)` — created in plan 19-01 (referenced only)

<tasks>

<task type="auto">
  <name>Task 1: Create TestSupport.kt with appTest{}, dep<T>(), and historyRecord()</name>
  <files>src/test/kotlin/com/anjo/TestSupport.kt</files>
  <read_first>
    - src/test/kotlin/com/anjo/routing/HistoryRoutesTest.kt (exact bootstrap block being extracted — lines 22-25; the two repeat(25) fixture loops at lines 26-30 and 46-47)
    - src/test/kotlin/com/anjo/ApplicationTest.kt (imports and the getBlocking chain — the heaviest dep<T>() payoff)
    - src/test/kotlin/com/anjo/ProjectConfig.kt (coroutineTestScope stays false — do NOT change)
    - src/main/kotlin/com/anjo/model/HistoryRecord.kt (fixture return type and field names)
    - .planning/phases/19-dry-yagni-refactoring/19-PATTERNS.md (TestSupport.kt section — verified helper bodies)
    - .planning/phases/19-dry-yagni-refactoring/19-RESEARCH.md (Pattern 2/3 — signatures verified against pinned Ktor 3.5.0 source jars)
  </read_first>
  <action>
    Create `src/test/kotlin/com/anjo/TestSupport.kt` in `package com.anjo` with three top-level members (no class, no Kotest base spec — D-04 composition):

    1. `fun appTest(block: suspend ApplicationTestBuilder.() -> Unit) = testApplication { application { module() }; client.get("/health"); block() }` — the shared bootstrap + `/health` warm-up wrapper. The warm-up is always included (idempotent, required before any dep<T>() call since testApplication defers module() until the first HTTP interaction).

    2. `inline fun <reified T> ApplicationTestBuilder.dep(): T = application.dependencies.getBlocking<T>(DependencyKey<T>())` — the reified DI accessor. Note the `application` here is the ApplicationTestBuilder.application PROPERTY (not the `application { }` DSL block used inside appTest); isolate the distinction in this one place.

    3. `fun historyRecord(i: Int, effect: String = "SCROLL", source: String = "IMMEDIATE", scheduleId: String? = null) = HistoryRecord(text = "text $i", effect = effect, source = source, scheduleId = scheduleId)` — fixture builder matching the current `text = "text $i"` insertion loops.

    Imports: `io.ktor.client.request.get`, `io.ktor.server.plugins.di.DependencyKey`, `io.ktor.server.plugins.di.dependencies`, `io.ktor.server.plugins.di.getBlocking`, `io.ktor.server.testing.ApplicationTestBuilder`, `io.ktor.server.testing.testApplication`, `com.anjo.module`, `com.anjo.model.HistoryRecord`. No comments/KDoc.
  </action>
  <verify>
    <automated>./gradlew compileTestKotlin</automated>
  </verify>
  <acceptance_criteria>
    - src/test/kotlin/com/anjo/TestSupport.kt exists in `package com.anjo`
    - `grep -c "fun appTest" src/test/kotlin/com/anjo/TestSupport.kt` returns 1
    - `grep -c "fun .*dep()" src/test/kotlin/com/anjo/TestSupport.kt` returns 1 (the reified `dep()` extension)
    - `grep -c "fun historyRecord" src/test/kotlin/com/anjo/TestSupport.kt` returns 1
    - `./gradlew compileTestKotlin` succeeds
    - ProjectConfig.kt `coroutineTestScope` is still `false`
  </acceptance_criteria>
  <done>TestSupport.kt compiles and exposes appTest{}, the reified dep<T>() accessor, and historyRecord().</done>
</task>

<task type="auto">
  <name>Task 2: Migrate the three heaviest files (ApplicationTest, HistoryRoutesTest, HistoryUIRoutesTest)</name>
  <files>src/test/kotlin/com/anjo/ApplicationTest.kt, src/test/kotlin/com/anjo/routing/HistoryRoutesTest.kt, src/test/kotlin/com/anjo/routing/HistoryUIRoutesTest.kt</files>
  <read_first>
    - src/test/kotlin/com/anjo/TestSupport.kt (the helpers created in Task 1)
    - src/test/kotlin/com/anjo/ApplicationTest.kt (10 testApplication blocks, 18 getBlocking calls including the 15-binding resolution test using a `val deps` local)
    - src/test/kotlin/com/anjo/routing/HistoryRoutesTest.kt (8 getBlocking calls; two repeat(25) loops at lines 26-30 and 46-47; two repeat(5) loops at lines 60-61 and 73-74 with distinct literal text)
    - src/test/kotlin/com/anjo/routing/HistoryUIRoutesTest.kt (4 getBlocking calls)
    - .planning/phases/19-dry-yagni-refactoring/19-PATTERNS.md (migrated call-site examples)
  </read_first>
  <action>
    In each of the three files, apply the mechanical migration:
    - Replace every `testApplication { application { module() }; client.get("/health") }` opening with `appTest {` (drop the inner `application { module() }` line and the `client.get("/health")` warm-up line — appTest now supplies both).
    - Replace every `application.dependencies.getBlocking<T>(DependencyKey<T>())` and `deps.getBlocking<T>(DependencyKey<T>())` with `dep<T>()`. Where a `val deps = application.dependencies` local existed only to feed those calls (ApplicationTest's binding-resolution test — its highest-payoff case, ~15 chained calls), delete that local.
    - In HistoryRoutesTest ONLY: replace the two `repeat(25) { i -> historyRepository.insert(HistoryRecord(text = "text $i", ...)) }` loops (lines 26-30 and 46-47) with the `historyRecord(i, ...)` builder, preserving each loop's per-i effect/source/scheduleId arguments. Leave the two `repeat(5)` loops (lines 60-61, 73-74) as direct `HistoryRecord(...)` calls — their literal text (`scroll-text`, `blink-text`, `imm-text`, `sched-text`) differs and does not match the builder's `"text $i"` default (D-05 scope note / RESEARCH Assumption A2).
    - Add imports `com.anjo.appTest`, `com.anjo.dep`, and (HistoryRoutesTest only) `com.anjo.historyRecord`. Remove the now-unused imports that the migration orphans — `io.ktor.server.plugins.di.DependencyKey`, `io.ktor.server.plugins.di.dependencies`, `io.ktor.server.plugins.di.getBlocking`, `io.ktor.server.testing.testApplication` — so no new unused-import warning is emitted.
    - Do not touch ProjectConfig.kt. Add no comments; delete any IDE-generated KDoc stubs.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.anjo.ApplicationTest" --tests "com.anjo.routing.HistoryRoutesTest" --tests "com.anjo.routing.HistoryUIRoutesTest"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "getBlocking" src/test/kotlin/com/anjo/ApplicationTest.kt src/test/kotlin/com/anjo/routing/HistoryRoutesTest.kt src/test/kotlin/com/anjo/routing/HistoryUIRoutesTest.kt` returns 0 for all three files
    - `grep -c "appTest" src/test/kotlin/com/anjo/routing/HistoryRoutesTest.kt` is >= 8 (all bootstrap blocks migrated)
    - `grep -c "historyRecord(" src/test/kotlin/com/anjo/routing/HistoryRoutesTest.kt` returns 2 (the two repeat(25) loops)
    - HistoryRoutesTest still contains the `repeat(5)` loops with literal `scroll-text`/`blink-text` (left inline, not forced through the builder)
    - `./gradlew test --tests "com.anjo.ApplicationTest" --tests "com.anjo.routing.HistoryRoutesTest" --tests "com.anjo.routing.HistoryUIRoutesTest"` — all tests pass
  </acceptance_criteria>
  <done>The three heaviest files use appTest{}/dep<T>() with zero remaining getBlocking calls; the two repeat(25) loops use historyRecord(); their tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate remaining getBlocking files, then run the full phase gate</name>
  <files>src/test/kotlin/com/anjo/service/HistoryRecordingTest.kt, src/test/kotlin/com/anjo/routing/ZonesUIRoutesTest.kt, src/test/kotlin/com/anjo/routing/FirmwareZoneRoutesTest.kt</files>
  <read_first>
    - src/test/kotlin/com/anjo/TestSupport.kt (helpers)
    - src/test/kotlin/com/anjo/service/HistoryRecordingTest.kt (7 getBlocking calls)
    - src/test/kotlin/com/anjo/routing/ZonesUIRoutesTest.kt (3 getBlocking calls)
    - src/test/kotlin/com/anjo/routing/FirmwareZoneRoutesTest.kt (3 getBlocking calls)
    - .planning/phases/19-dry-yagni-refactoring/19-RESEARCH.md (Code Examples — warning-baseline command; Common Pitfall 4 — use jacocoTestCoverageVerification, not the report HTML %)
    - .planning/phases/19-dry-yagni-refactoring/19-CONTEXT.md (D-06 exemptions; D-09/D-10 gates)
  </read_first>
  <action>
    Apply the same mechanical migration as Task 2 to HistoryRecordingTest.kt, ZonesUIRoutesTest.kt, and FirmwareZoneRoutesTest.kt: `appTest {` for the bootstrap, `dep<T>()` for every getBlocking call, remove orphaned `DependencyKey`/`dependencies`/`getBlocking`/`testApplication` imports, add `com.anjo.appTest` / `com.anjo.dep` imports. Apply `historyRecord()` only where an insertion loop uses the `text = "text $i"` pattern; otherwise leave records inline. Do NOT touch LiveRoutesTest.kt or FirmwareZoneDriverTest.kt (D-06 — SSE/WS stay on embeddedServer). Add no comments.

    Then run the full phase gate over the complete Phase 19 refactor (this plan depends on 19-01, so main-code and test changes are both present):
    - `./gradlew test` (auto-runs jacocoTestReport + jacocoTestCoverageVerification via finalizedBy) — full suite must be green.
    - `./gradlew jacocoTestCoverageVerification` — confirm BUILD SUCCESSFUL (coverage >=70%; do not read the report HTML percentage — it excludes the driver/model filters and understates the gated metric).
    - `./gradlew compileKotlin compileTestKotlin --rerun-tasks --warning-mode=all` — confirm the only `w:` lines are the 2 pre-existing Max7219Matrix.kt SpiChipSelect warnings; no new warning from any migrated test file or Task-1 helper.

    In the SUMMARY, record: the six migrated files, the nine below-threshold getBlocking-free files left as-is (with the SC2 5-line-threshold rationale), the two D-06-exempt files, and the final gate results.
  </action>
  <verify>
    <automated>./gradlew test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "getBlocking" src/test/kotlin/com/anjo/service/HistoryRecordingTest.kt src/test/kotlin/com/anjo/routing/ZonesUIRoutesTest.kt src/test/kotlin/com/anjo/routing/FirmwareZoneRoutesTest.kt` returns 0 for all three files
    - `grep -rl "getBlocking" src/test/kotlin/com/anjo/` returns exactly three files: TestSupport.kt (the shared helper — legitimate home of the getBlocking call) plus the two D-06-exempt files (LiveRoutesTest.kt, FirmwareZoneDriverTest.kt); no individual migrated route/service test file appears
    - `./gradlew test` prints BUILD SUCCESSFUL (full suite green, all 27 test classes)
    - `./gradlew jacocoTestCoverageVerification` prints BUILD SUCCESSFUL (SC3, coverage >=70%)
    - `./gradlew compileKotlin compileTestKotlin --rerun-tasks --warning-mode=all` emits only the 2 pre-existing Max7219Matrix.kt warnings — no new warning from any touched file (SC4)
    - SUMMARY lists the six migrated files, the nine below-threshold files (not churned), and the two exempt files
  </acceptance_criteria>
  <done>All non-exempt getBlocking usage is gone (only the two SSE/WS-exempt files retain it); full suite, coverage gate, and warning-diff are all green over the complete Phase 19 refactor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test tier → (none) | Test-only code (Kotest helpers + fixtures). No production request path, no runtime attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-03 | Tampering | TestSupport.kt / migrated test files | low | accept | Test-tier only; changes do not alter production behavior. The coverage gate (>=70%) and full-suite green guarantee the migration preserved every existing assertion (no test silently weakened or deleted). |
| T-19-04 | Tampering | Package installs (npm/pip/cargo) | low | accept | No new external packages introduced — Package Legitimacy Gate not applicable; no install task exists. |

ASVS level 1; block-on threshold: high. No production trust boundary is crossed by this plan; highest severity is low (accepted). No blocking human checkpoint required.
</threat_model>

<verification>
- `./gradlew test` — full suite green (all 27 test classes)
- `./gradlew jacocoTestCoverageVerification` — BUILD SUCCESSFUL (coverage >=70%)
- `./gradlew compileKotlin compileTestKotlin --rerun-tasks --warning-mode=all` — only the 2 pre-existing Max7219Matrix warnings
- `grep -rl "getBlocking" src/test/kotlin/com/anjo/` — only TestSupport.kt (shared helper) plus LiveRoutesTest.kt and FirmwareZoneDriverTest.kt (the D-06 exemptions)
</verification>

<success_criteria>
- SC2: A shared TestSupport.kt (appTest{}, dep<T>(), historyRecord()) is consumed by six test classes; no >5-line copy-paste block remains across test classes
- SC3: JaCoCo line coverage gate stays green (>=70%)
- SC4: no new compiler warnings introduced by the migration
- The nine below-threshold and two exempt files are documented, not churned
</success_criteria>

<output>
Create `.planning/phases/19-dry-yagni-refactoring/19-02-SUMMARY.md` when done.
</output>
