---
phase: 19-dry-yagni-refactoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/com/anjo/validation/HistoryValidators.kt
  - src/main/kotlin/com/anjo/routing/HistoryRoutes.kt
  - src/main/kotlin/com/anjo/routing/ui/HistoryUIRoutes.kt
autonomous: true
requirements:
  - REF-05
must_haves:
  truths:
    - "Query-param to HistoryFilter parsing exists only in HistoryValidators.parseFilter; the three route handlers call it instead of constructing HistoryFilter inline (D-01, D-02)"
    - "parseFilter preserves the sanitize-before-blank-check ordering so LIKE-wildcard stripping stays intact (V5 input validation)"
    - "HistoryRoutesTest and HistoryUIRoutesTest pass unchanged — behavior preserved, no new capabilities"
    - "The compiler-warning list is unchanged from the 2-warning baseline; no new w: line originates from a touched file (D-09)"
    - "JaCoCo line-coverage gate stays >=70% (D-10)"
  artifacts:
    - "src/main/kotlin/com/anjo/validation/HistoryValidators.kt (parseFilter added)"
    - "src/main/kotlin/com/anjo/routing/HistoryRoutes.kt (both handlers call parseFilter)"
    - "src/main/kotlin/com/anjo/routing/ui/HistoryUIRoutes.kt (filter construction replaced; raw redisplay reads kept)"
  key_links:
    - "HistoryValidators.parseFilter(call.request.queryParameters) invoked from both HistoryRoutes handlers and the HistoryUIRoutes handler"
    - "sanitizeSearchTerm invoked inside parseFilter before takeIf { isNotBlank() }"
---

<objective>
Consolidate the duplicated history query-param extraction (SC1) by adding a single
HistoryValidators.parseFilter(parameters) and routing all three call sites through it,
then run a bounded main-code duplication / dead-code audit (D-07/D-08) and confirm the
compiler-warning baseline is unchanged (D-09) and JaCoCo stays green (D-10).

Purpose: REF-05 — main code has no significant duplication. The 4-line effect/source/zone/search
extraction block is currently copy-pasted 3× across HistoryRoutes.kt (twice) and HistoryUIRoutes.kt.
Output: parseFilter in HistoryValidators, three simplified call sites, an audit result recorded in
the SUMMARY, and green tests / coverage / warning-diff gates.
Behavior-preserving only — no new endpoints, no signature changes to HistoryFilter/HistoryRepository/HistoryService.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-dry-yagni-refactoring/19-CONTEXT.md
@.planning/phases/19-dry-yagni-refactoring/19-RESEARCH.md
@.planning/phases/19-dry-yagni-refactoring/19-PATTERNS.md
</context>

## Artifacts this phase produces

Symbols/paths newly created or extended by Phase 19 (source-grounding: exclude these from drift verification):

- `HistoryValidators.parseFilter(parameters: io.ktor.http.Parameters): HistoryFilter` — new function in the existing `object HistoryValidators`
- `com.anjo.appTest(block)` — new test helper (created in plan 19-02)
- `com.anjo.dep<T>()` — new reified `ApplicationTestBuilder` extension (created in plan 19-02)
- `com.anjo.historyRecord(i, effect, source, scheduleId)` — new test fixture builder (created in plan 19-02)
- `src/test/kotlin/com/anjo/TestSupport.kt` — new file (created in plan 19-02)

<tasks>

<task type="auto">
  <name>Task 1: Add HistoryValidators.parseFilter and route all three call sites through it</name>
  <files>src/main/kotlin/com/anjo/validation/HistoryValidators.kt, src/main/kotlin/com/anjo/routing/HistoryRoutes.kt, src/main/kotlin/com/anjo/routing/ui/HistoryUIRoutes.kt</files>
  <read_first>
    - src/main/kotlin/com/anjo/validation/HistoryValidators.kt (current object — only sanitizeSearchTerm exists; parseFilter is added here)
    - src/main/kotlin/com/anjo/routing/HistoryRoutes.kt (both handlers repeat the extraction block at lines 19-23 and 29-33)
    - src/main/kotlin/com/anjo/routing/ui/HistoryUIRoutes.kt (filter construction at lines 28-32; raw redisplay reads at lines 23-27 and sanitizedSearch at line 34)
    - src/main/kotlin/com/anjo/model/HistoryFilter.kt (return type — do NOT move or add Ktor imports to this file, D-01)
    - src/main/kotlin/com/anjo/validation/ZoneValidators.kt (convention analog — validator objects import Ktor framework types directly)
    - src/test/kotlin/com/anjo/routing/HistoryRoutesTest.kt (regression net — must stay green)
    - src/test/kotlin/com/anjo/routing/HistoryUIRoutesTest.kt (regression net — must stay green)
    - .planning/phases/19-dry-yagni-refactoring/19-PATTERNS.md (Pattern Assignments — exact parseFilter body and call-site replacements)
  </read_first>
  <action>
    Add `fun parseFilter(parameters: io.ktor.http.Parameters): HistoryFilter` to the existing `object HistoryValidators`. Add imports `io.ktor.http.Parameters` and `com.anjo.model.HistoryFilter` to HistoryValidators.kt only (this validator object is already the framework-aware layer, consistent with ZoneValidators importing ValidationResult — D-01). The body must reproduce the current extraction verbatim: effect = `parameters["effect"]?.uppercase()?.takeIf { it.isNotEmpty() && it != "ALL" }`; source = same transform on `parameters["source"]`; zone = `parameters["zone"]?.takeIf { it.isNotEmpty() && it != "ALL" }` (no uppercase); search = `sanitizeSearchTerm(parameters["search"].orEmpty()).takeIf { it.isNotBlank() }`. Preserve the sanitize-BEFORE-takeIf ordering exactly (security-relevant LIKE-wildcard stripping). Return `HistoryFilter(effect = effect, source = source, zone = zone, search = search)`.

    In HistoryRoutes.kt, replace the four extraction lines plus the HistoryFilter construction line in BOTH handlers (the `/history` handler lines 19-23 and the `/history/export` handler lines 29-33) with a single `val filter = HistoryValidators.parseFilter(call.request.queryParameters)`. Keep the page/size parsing (lines 17-18) inline and untouched — the API cap is coerceIn(1, 200) and must not be folded into parseFilter. Remove the now-unused `import com.anjo.model.HistoryFilter` (line 3) since HistoryFilter is no longer referenced directly.

    In HistoryUIRoutes.kt, replace only the filter-construction lines 28-32 (effectFilter/sourceFilter/zoneFilter/searchFilter locals and the HistoryFilter constructor) with `val filter = HistoryValidators.parseFilter(call.request.queryParameters)`. KEEP lines 23-27 exactly as-is (the raw `.orEmpty()` reads of effect/source/zone, expandAll, rawSearch that feed form redisplay — D-03), and KEEP line 34 `sanitizedSearch` and line 35 `exportHref` unchanged (the export link needs original casing, not the uppercased filter value). Remove the now-unused `import com.anjo.model.HistoryFilter` (line 3). Note: the UI's parsed `effect`/`source` now become uppercased where they previously were not — this is a no-op for the real dropdown values (already uppercase enum names) and the redisplay reads are unaffected; confirm HistoryUIRoutesTest stays green to prove behavior is preserved.

    Do not add any comments or KDoc; if an IDE extract action generates a `/**` stub, delete it (no-comments-in-code project rule).
  </action>
  <verify>
    <automated>./gradlew test --tests "com.anjo.routing.HistoryRoutesTest" --tests "com.anjo.routing.HistoryUIRoutesTest"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "HistoryValidators.parseFilter" src/main/kotlin/com/anjo/routing/HistoryRoutes.kt` returns 2
    - `grep -c "HistoryValidators.parseFilter" src/main/kotlin/com/anjo/routing/ui/HistoryUIRoutes.kt` returns 1
    - `grep -c "fun parseFilter" src/main/kotlin/com/anjo/validation/HistoryValidators.kt` returns 1
    - HistoryValidators.kt imports `io.ktor.http.Parameters`; model/HistoryFilter.kt contains no `io.ktor` import (D-01 framework-free model)
    - `import com.anjo.model.HistoryFilter` no longer present in either route file (removed as unused)
    - HistoryUIRoutes.kt still contains the raw `rawSearch` read and the `exportHref` line (redisplay concern kept — D-03)
    - `./gradlew test --tests "com.anjo.routing.HistoryRoutesTest" --tests "com.anjo.routing.HistoryUIRoutesTest"` reports all existing tests pass (behavior preserved)
  </acceptance_criteria>
  <done>parseFilter is the single parsing source, called 2× from HistoryRoutes and 1× from HistoryUIRoutes; sanitize ordering preserved; the two history test files pass unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Bounded main-code dedup/dead-code audit + warning-baseline and coverage gate</name>
  <files>src/main/kotlin/com/anjo/routing/, src/main/kotlin/com/anjo/routing/ui/, src/main/kotlin/com/anjo/validation/, src/main/kotlin/com/anjo/web/templates/</files>
  <read_first>
    - .planning/phases/19-dry-yagni-refactoring/19-CONTEXT.md (D-07 clear >5-line wins only + list borderline; D-08 delete only obviously-unused code; D-09 warning diff; D-10 coverage gate)
    - .planning/phases/19-dry-yagni-refactoring/19-RESEARCH.md (Code Examples — exact warning-baseline command and the 2-warning baseline; Common Pitfalls 3 and 4)
    - build.gradle.kts (JaCoCo gate lines ~102-151 — do NOT weaken; excludes are com/anjo/driver/**, com/anjo/utils/**, com/anjo/config/model/**, com/anjo/model/**)
    - src/main/kotlin/com/anjo/routing/ (route error-handling repetition candidates)
    - src/main/kotlin/com/anjo/web/templates/ (SSR template repetition candidates)
  </read_first>
  <action>
    First capture the warning baseline by running `./gradlew compileKotlin compileTestKotlin --rerun-tasks --warning-mode=all` and confirm it emits exactly 2 `w:` lines, both in src/main/kotlin/com/anjo/driver/Max7219Matrix.kt (SpiChipSelect deprecation) and none from any file touched in Task 1 (HistoryValidators.kt, HistoryRoutes.kt, HistoryUIRoutes.kt) — the Max7219Matrix warnings are pre-existing and out of scope.

    Then audit main code for duplication strictly within D-07 bounds: scan routing/, routing/ui/, validation/, and web/templates/ for a CLEAR copy-paste block longer than 5 lines (candidates: repeated route error-handling/respond patterns, repeated HTML-template scaffolding, repeated validator patterns). Fix ONLY unambiguous >5-line wins, keeping behavior identical, and run the affected package's tests after each fix. Do NOT churn a working v1.2 codebase — for anything borderline (near the threshold, or where extraction would need as many params as call sites), record it in the SUMMARY as "listed, not fixed" with a one-line rationale rather than editing it.

    Per D-08 (YAGNI half), delete obviously-unused code encountered during the audit only — unused private helpers, orphaned model fields, dead config flags — with no dedicated dead-code hunt. Rely on the compiler and the JaCoCo gate to backstop over-deletion; if a deletion drops coverage below 70% or breaks compilation, revert it. Add no comments; delete any IDE-generated KDoc stubs.

    Record in the SUMMARY: the confirmed warning baseline, every fix applied, every deletion made, and the listed-not-fixed borderline set.
  </action>
  <verify>
    <automated>./gradlew jacocoTestCoverageVerification</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew compileKotlin compileTestKotlin --rerun-tasks --warning-mode=all` emits only the 2 pre-existing Max7219Matrix.kt SpiChipSelect warnings — no `w:` line references HistoryValidators.kt, HistoryRoutes.kt, HistoryUIRoutes.kt, or any file edited in this plan (SC4 / D-09)
    - `./gradlew jacocoTestCoverageVerification` prints BUILD SUCCESSFUL (line coverage >=70% — SC3 / D-10)
    - build.gradle.kts JaCoCo `minimum` remains `"0.70".toBigDecimal()` (gate not weakened)
    - SUMMARY records the warning baseline, applied fixes, deletions, and the listed-not-fixed borderline cases
  </acceptance_criteria>
  <done>Warning list unchanged from the 2-warning baseline; coverage gate green; audit result (fixes + deletions + listed borderline cases) documented in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser/API client → history routes | Untrusted query-string parameters (`search`, `effect`, `source`, `zone`) cross into filter parsing and reach the Exposed LIKE query |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Tampering | HistoryValidators.parseFilter (search param → LIKE query) | medium | mitigate | Preserve `sanitizeSearchTerm` (strips `%` and `_`) invoked BEFORE `takeIf { isNotBlank() }` inside parseFilter, verbatim from the current route code — do not simplify the wildcard stripping while consolidating. Existing HistoryRoutesTest search cases + `./gradlew test` are the regression guard. |
| T-19-02 | Tampering | Package installs (npm/pip/cargo) | low | accept | No new external packages are introduced this phase (pure internal refactor) — Package Legitimacy Gate not applicable; no install task exists. |

ASVS level 1; block-on threshold: high. Highest severity here is medium (mitigated), so no blocking human checkpoint is required. No auth/session/access-control/crypto surface is touched (history search is unauthenticated by project design).
</threat_model>

<verification>
- `./gradlew test --tests "com.anjo.routing.HistoryRoutesTest" --tests "com.anjo.routing.HistoryUIRoutesTest"` — all existing history tests green (behavior preserved)
- `./gradlew jacocoTestCoverageVerification` — BUILD SUCCESSFUL (coverage >=70%)
- `./gradlew compileKotlin compileTestKotlin --rerun-tasks --warning-mode=all` — warning list identical to the 2-warning Max7219Matrix baseline
- parseFilter is the sole parsing source: 2 calls in HistoryRoutes, 1 in HistoryUIRoutes; no inline HistoryFilter construction remains in the route files
</verification>

<success_criteria>
- SC1: A single HistoryValidators.parseFilter consolidates the search/zone/effect/source extraction previously duplicated across the two route files
- The `model` package remains framework-free (no Ktor import in HistoryFilter.kt)
- SC3: JaCoCo line coverage gate stays green (>=70%)
- SC4: no new compiler warnings originate from any touched file
- Main-code audit complete: clear >5-line duplications fixed, obviously-unused code removed, borderline cases listed in SUMMARY
</success_criteria>

<output>
Create `.planning/phases/19-dry-yagni-refactoring/19-01-SUMMARY.md` when done.
</output>
