---
phase: 12-observability-gap-closures
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - src/main/kotlin/com/anjo/routing/Routing.kt
  - src/test/kotlin/com/anjo/ApplicationTest.kt
autonomous: false
requirements: [OBS-03]
must_haves:
  truths:
    - "A browser GET to an unknown path returns the HTML 404 error page, not a SwaggerUI JSON response"
    - "GET /does-not-exist with Accept: text/html returns content-type text/html and a body containing 'Error 404' and 'Page not found'"
    - "API paths (/api/...) continue to receive JSON error responses"
  artifacts:
    - path: "src/test/kotlin/com/anjo/ApplicationTest.kt"
      provides: "Automated assertion that browser-Accept unknown route returns HTML 404"
      contains: "text/html"
  key_links:
    - from: "src/main/kotlin/com/anjo/di/ErrorHandling.kt"
      to: "src/main/kotlin/com/anjo/web/templates/ErrorPage.kt"
      via: "status(NotFound) + prefersHtml() → ErrorPage(404).render()"
      pattern: "ErrorPage\\(404"
    - from: "src/main/kotlin/com/anjo/routing/Routing.kt"
      to: "swaggerUI catch-all ordering"
      via: "swaggerUI registered last so StatusPages NotFound fires for unknown routes"
      pattern: "swaggerUI\\(path = \"openapi\"\\)"
---

<objective>
Verify and lock in HTML 404/500 error pages for browser navigation (OBS-03). The handler in `ErrorHandling.kt` already renders HTML via `prefersHtml()`; this plan adds an automated content-type assertion, confirms `swaggerUI()` ordering does not intercept unknown routes, applies the single-line reorder fix only if needed, and confirms the browser experience via a human-verify checkpoint (SC-3).

Purpose: Close the v1.0 audit gap where a browser hitting an unknown path could receive a SwaggerUI JSON payload instead of the friendly HTML 404 page. Depends on 12-02 because both plans edit `Routing.kt` and `ApplicationTest.kt`; serialising avoids file conflicts.
Output: Strengthened `ApplicationTest` 404 assertion (content-type + body), confirmed/locked `swaggerUI()` ordering in `Routing.kt`, human-verified browser behavior.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-observability-gap-closures/12-CONTEXT.md
@.planning/phases/12-observability-gap-closures/12-RESEARCH.md
@.planning/phases/12-observability-gap-closures/12-UI-SPEC.md
@.planning/phases/12-observability-gap-closures/12-02-SUMMARY.md
@src/main/kotlin/com/anjo/di/ErrorHandling.kt
@src/main/kotlin/com/anjo/web/templates/ErrorPage.kt
</context>

<artifacts_this_phase_produces>
Symbols created/modified by THIS plan (exclude from drift verification):

- New/strengthened test case in `ApplicationTest` asserting HTML 404 content-type and body for a browser-Accept unknown route
- Possible (conditional) reordering of the existing `swaggerUI(path = "openapi")` call in `Routing.kt` — no new symbols, only ordering

No new production files or symbols are created. ErrorPage.kt, ErrorHandling.kt, and prefersHtml() already exist (D-11, D-13) and must not be rewritten.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add automated HTML-404 content-type assertion + confirm swaggerUI ordering</name>
  <files>
    src/test/kotlin/com/anjo/ApplicationTest.kt,
    src/main/kotlin/com/anjo/routing/Routing.kt
  </files>
  <read_first>
    - src/test/kotlin/com/anjo/ApplicationTest.kt (existing "should return 404 for unknown routes" test lines 55-61; get/header request patterns; imports)
    - src/main/kotlin/com/anjo/di/ErrorHandling.kt (status(NotFound) + prefersHtml() → ErrorPage(404, "Page not found").render(); prefersHtml = !path.startsWith("/api") && accept contains text/html)
    - src/main/kotlin/com/anjo/web/templates/ErrorPage.kt (render() output — what strings the HTML body contains for assertion: "Error 404", "Page not found")
    - src/main/kotlin/com/anjo/routing/Routing.kt (routing block order; swaggerUI must be last)
    - .planning/phases/12-observability-gap-closures/12-RESEARCH.md (Pitfall 1 + A1 — SwaggerUI catch-all ordering)
    - .planning/phases/12-observability-gap-closures/12-UI-SPEC.md (OBS-03 interaction contract — exact copy strings)
  </read_first>
  <action>
    First confirm the current behavior is correct (12-RESEARCH A1 says `swaggerUI(path = "openapi")` is already last, so OBS-03 likely needs zero production change). Inspect `Routing.kt`: verify `swaggerUI(path = "openapi")` is the last call inside `routing { }`. If it is already last, make NO change to Routing.kt. If (and only if) it is not last, move the entire `swaggerUI(path = "openapi") { ... }` block to be the final statement inside `routing { }` — do not change its contents.

    Add a new test to `ApplicationTest.kt` titled "should return HTML 404 page for browser navigation to unknown route": use `client.get("/does-not-exist") { header(HttpHeaders.Accept, "text/html") }`, assert `response.status shouldBe HttpStatusCode.NotFound`, assert `response.contentType()?.match(ContentType.Text.Html) shouldBe true` (or assert the `Content-Type` header `shouldContain "text/html"`), and assert `response.bodyAsText() shouldContain "Error 404"` and `shouldContain "Page not found"`. Read ErrorPage.kt first to confirm the exact heading/body strings the template emits and match them.

    Add a second test "should return JSON error for API path unknown route" confirming `prefersHtml()` discrimination is intact: `client.get("/api/v1/does-not-exist")` returns 404 and the body does NOT contain "Error 404" (it returns the JSON ErrorResponse path). Add the required imports (`io.ktor.client.request.header`, `io.ktor.http.HttpHeaders`, `io.ktor.http.ContentType`, `io.ktor.http.contentType`, `io.ktor.client.statement.bodyAsText`, `io.kotest.matchers.string.shouldContain`) — only those not already present. Keep the existing "should return 404 for unknown routes" test unchanged. No comments in any Kotlin file.
  </action>
  <verify>
    <automated>cd /home/diether18/IdeaProjects/TextReaderRpi && ./gradlew test --tests "com.anjo.ApplicationTest"</automated>
  </verify>
  <acceptance_criteria>
    - `swaggerUI(path = "openapi")` is the last call inside the `routing {` block in Routing.kt (confirmed; reordered only if it was not)
    - ApplicationTest.kt contains a test asserting `/does-not-exist` with `Accept: text/html` returns `HttpStatusCode.NotFound` AND content-type text/html
    - ApplicationTest.kt asserts that HTML 404 body `shouldContain "Error 404"` and `shouldContain "Page not found"`
    - ApplicationTest.kt contains a test asserting `/api/v1/does-not-exist` returns 404 with a body NOT containing "Error 404"
    - `./gradlew test --tests "com.anjo.ApplicationTest"` exits 0
    - No `//`, `/*`, or `/**` comments added to any modified Kotlin file
  </acceptance_criteria>
  <done>Automated tests prove a browser-Accept unknown route returns the HTML 404 page while API paths keep JSON errors; swaggerUI ordering is confirmed last (or fixed); the full ApplicationTest suite passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The HTML 404 error page for browser navigation (OBS-03 / SC-3). Automated tests already confirm the content-type and body via `Accept: text/html`, and `swaggerUI()` is registered last so its catch-all does not intercept unknown routes. This checkpoint confirms the real browser experience.
  </what-built>
  <how-to-verify>
    1. Start the app: `cd /home/diether18/IdeaProjects/TextReaderRpi && ./gradlew run` (wait for "Application started" / Netty listening on the configured port, default 8080).
    2. In a real browser, navigate to `http://localhost:8080/does-not-exist`.
    3. Confirm the page renders the HTML error page (PicoCSS dark theme) with heading "Error 404", body text "Page not found", and a "Back to home" link — NOT a raw JSON payload and NOT a SwaggerUI page.
    4. Click "Back to home" and confirm it returns to the home page.
    5. (Optional) Navigate to `http://localhost:8080/openapi` and confirm the SwaggerUI page still loads correctly (ordering change, if any, did not break Swagger).
    6. Stop the app (Ctrl+C).
  </how-to-verify>
  <resume-signal>Type "approved" if the HTML 404 page renders correctly in the browser, or describe what appeared instead (e.g., JSON body, Swagger page, blank page).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → unknown GET route | Untrusted path triggers StatusPages NotFound; HTML rendered server-side via prefersHtml() |
| browser → /openapi (SwaggerUI) | Static Swagger asset serving; registered last to avoid catch-all interception |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Information Disclosure | HTML 404 page content | accept | ErrorPage(404) renders only static copy ("Error 404"/"Page not found"); no request path echo, no stack trace, no internal detail |
| T-12-08 | Information Disclosure | 500 error page | mitigate | Existing exception<Throwable> handler renders generic "An internal error occurred" via prefersHtml(); no raw exception text leaked to browser (D-11) |
| T-12-09 | Spoofing/Tampering | SwaggerUI catch-all intercepting routes | mitigate | swaggerUI() confirmed last inside routing{}; StatusPages NotFound resolves unknown routes first (Pitfall 1) |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced (12-RESEARCH Package Legitimacy Audit: none); no install task in this plan |
</threat_model>

<verification>
- `./gradlew test --tests "com.anjo.ApplicationTest"` exits 0
- `GET /does-not-exist` with `Accept: text/html` → 404, content-type text/html, body contains "Error 404" and "Page not found"
- `GET /api/v1/does-not-exist` → 404, JSON body (not HTML)
- Human-verify checkpoint approved: browser renders the HTML 404 page (SC-3)
- No Kotlin file modified in this plan contains `//`, `/*`, or `/**` comments (project no-comments rule)
</verification>

<success_criteria>
- OBS-03 satisfied: a browser navigating to an unknown path renders the HTML 404 page instead of a SwaggerUI JSON response
- API paths retain JSON error responses (prefersHtml discrimination intact)
- swaggerUI() ordering confirmed/locked; human-verify checkpoint approved
</success_criteria>

<output>
Create `.planning/phases/12-observability-gap-closures/12-03-SUMMARY.md` when done
</output>
